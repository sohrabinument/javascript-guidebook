import { Callout } from "nextra/components";

<img src="https://upload.wikimedia.org/wikipedia/commons/a/a7/React-icon.svg" alt="React Logo" width="70" style={{marginBottom: '1rem'}} />

# ⚡ রিঅ্যাক্টে রেন্ডারিং কীভাবে কাজ করে

> **রেন্ডারিং বোঝা কেন জরুরি?**
>
> রিঅ্যাক্ট কীভাবে আপনার UI রেন্ডার করে, তা জানলে আপনি আরও দ্রুত, নির্ভরযোগ্য ও বাগ-ফ্রি অ্যাপ বানাতে পারবেন। চলুন সহজ ভাষা, উদাহরণ ও ডায়াগ্রাম দিয়ে পুরো প্রক্রিয়া দেখি!

---

## রেস্টুরেন্ট অ্যানালজি 🍽️

ভাবুন, আপনার রিঅ্যাক্ট কম্পোনেন্টগুলো হচ্ছে কিচেনের শেফ, যারা সুস্বাদু UI তৈরি করছে। React হচ্ছে ওয়েটার, যে অর্ডার (ইউজার অ্যাকশন) নিয়ে কিচেনে যায় এবং প্রস্তুত খাবার (UI) কাস্টমার (ইউজার)-এর কাছে পৌঁছে দেয়।

- **User** = কাস্টমার (অর্ডার দেয়)
- **React** = ওয়েটার (অর্ডার ডেলিভারি দেয়)
- **Component** = শেফ (UI তৈরি করে)
- **DOM** = টেবিল (খাবার/ UI পরিবেশন হয়)

---

## রেন্ডারিংয়ের তিনটি ধাপ 🪄

React-এর UI আপডেট প্রক্রিয়া **তিনটি প্রধান ধাপে** হয়:

1. **Trigger:** কিছু ঘটে (ইউজার ক্লিক, টাইপ, বা অ্যাপ লোড) যা React-কে UI আপডেট করতে বলে।
2. **Render:** React আপনার কম্পোনেন্টগুলো কল করে, কী UI দেখাবে তা নির্ধারণ করে (নতুন virtual DOM তৈরি করে)।
3. **Commit:** React আসল DOM-কে virtual DOM-এর সাথে মিলিয়ে আপডেট করে, যাতে ইউজার পরিবর্তন দেখতে পায়।

<Callout type="info">React-এ যেকোনো স্ক্রিন আপডেট এই তিন ধাপে হয়: <b>Trigger → Render → Commit</b></Callout>

---

## ধাপ ১: রেন্ডার ট্রিগার 🚦

রেন্ডার ট্রিগার হয় যখন:
- অ্যাপ প্রথমবার লোড হয় (initial render)
- কোনো কম্পোনেন্টের state বা props চেঞ্জ হয়

**অ্যানালজি:**
- কাস্টমার (ইউজার) অর্ডার দেয় (ক্লিক, টাইপ, বা পেজ লোড)।
- ওয়েটার (React) অর্ডার কিচেনে (কম্পোনেন্ট) নিয়ে যায়।

```jsx
// Initial render
root.render(<App />);

// State update triggers re-render
setCount(count + 1);
```

---

## ধাপ ২: রেন্ডার ফেজ 🎨

React আপনার কম্পোনেন্ট (ফাংশন) কল করে, কী দেখাবে তা নির্ধারণ করে। এটি একটি pure হিসাব—একই ইনপুট দিলে, একই আউটপুট পাবেন।

- Initial render-এ, React root কম্পোনেন্ট কল করে।
- আপডেটে, React সেই কম্পোনেন্ট কল করে যার state বা props চেঞ্জ হয়েছে।
- যদি কোনো কম্পোনেন্ট অন্য কম্পোনেন্ট রিটার্ন করে, React সেগুলোও রেন্ডার করে (recursively)।

**অ্যানালজি:**
- শেফ (কম্পোনেন্ট) অর্ডার (state/props) অনুযায়ী খাবার (UI) তৈরি করে।
- ওয়েটার (React) শেফের কাছ থেকে নতুন খাবার নেয়।

<Callout type="warning">রেন্ডারিং অবশ্যই pure হতে হবে! রেন্ডার চলাকালীন বাইরের কোনো ভ্যারিয়েবল বা অবজেক্ট চেঞ্জ করবেন না। একই ইনপুটে সবসময় একই আউটপুট আসা উচিত।</Callout>

---

## ধাপ ৩: কমিট ফেজ 📝

রেন্ডার শেষে, React DOM আপডেট করে:
- Initial render-এ, React সব DOM node তৈরি ও অ্যাপেন্ড করে।
- আপডেটে, React শুধুমাত্র প্রয়োজনীয় পরিবর্তনগুলো করে (virtual DOM-এর সাথে মিলিয়ে)।

**অ্যানালজি:**
- ওয়েটার (React) কিচেন (কম্পোনেন্ট) থেকে প্রস্তুত খাবার (UI) নিয়ে টেবিলে (DOM) পরিবেশন করে।

---

## উদাহরণ: সিম্পল কাউন্টার 🔢

```jsx
import { useState } from 'react';

export default function Counter() {
  const [count, setCount] = useState(0);
  return (
    <div style={{textAlign: 'center'}}>
      <h1>{count}</h1>
      <button onClick={() => setCount(count + 1)}>Increment</button>
    </div>
  );
}
```

- বাটনে ক্লিক করলে, এটি রেন্ডার ট্রিগার করে।
- React Counter কম্পোনেন্ট কল করে (render phase)।
- React নতুন count দিয়ে DOM আপডেট করে (commit phase)।

---

## রিক্যাপ ও ভিজ্যুয়াল সামারি 🚀

| ধাপ      | কী ঘটে?                                         | অ্যানালজি (রেস্টুরেন্ট)      |
|----------|--------------------------------------------------|-----------------------------|
| Trigger  | ইউজার অ্যাকশন বা state/props চেঞ্জ               | কাস্টমার অর্ডার দেয়         |
| Render   | React কম্পোনেন্ট কল করে, virtual DOM তৈরি করে   | শেফ খাবার তৈরি করে          |
| Commit   | React DOM আপডেট করে, ইউজার চেঞ্জ দেখে           | ওয়েটার খাবার পরিবেশন করে   |

- React-এ রেন্ডারিংয়ের তিনটি ধাপ: <b>Trigger → Render → Commit</b>
- শুধু state/props চেঞ্জ বা initial load-এ রেন্ডার হয়
- রেন্ডার ফাংশন সবসময় pure রাখুন
- কিছু না বদলালে React DOM আপডেট করে না

<Callout type="success">আরও জানতে দেখুন <a href="https://react.dev/learn/render-and-commit" target="_blank">React অফিসিয়াল ডকস</a>। নিজের ইন্টারঅ্যাকটিভ কম্পোনেন্ট বানিয়ে প্র্যাকটিস করুন! 🎉</Callout>
