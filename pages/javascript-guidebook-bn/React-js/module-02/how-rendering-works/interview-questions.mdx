import { Callout } from "nextra/components";

<img src="https://upload.wikimedia.org/wikipedia/commons/a/a7/React-icon.svg" alt="React Logo" width="70" style={{marginBottom: '1rem'}} />

# রিঅ্যাক্টে রেন্ডারিং – ইন্টারভিউ প্রশ্ন ও উত্তর 🎯

এই প্রশ্নগুলো ব্যবহার করুন ইন্টারভিউ প্রস্তুতির জন্য বা রিঅ্যাক্টের রেন্ডারিং প্রসেস সম্পর্কে আপনার বোঝা যাচাই করতে।

> **💡 প্রো টিপ:** উত্তর দেখার আগে নিজে চেষ্টা করুন। এতে কনসেপ্টগুলো ভালোভাবে মনে থাকবে!

---

## ১.১ – রেন্ডারিং বেসিক ⚡

<div style={{ background: 'rgba(0, 100, 200, 0.10)', padding: '15px', borderRadius: '5px', marginBottom: '20px', border: '1px solid rgba(0, 100, 200, 0.15)', color: 'inherit' }}>

**প্রশ্ন ১: রিঅ্যাক্টের রেন্ডারিং প্রসেসের তিনটি প্রধান ধাপ কী?**  
উত্তর: Trigger, Render, Commit।

**প্রশ্ন ২: রিঅ্যাক্টে কী কী কারণে রেন্ডার ট্রিগার হয়?**  
উত্তর: অ্যাপের initial load, অথবা কোনো কম্পোনেন্টের state বা props চেঞ্জ হলে।

**প্রশ্ন ৩: রেস্টুরেন্ট অ্যানালজিতে React কী ভূমিকা পালন করে?**  
উত্তর: React হচ্ছে ওয়েটার, যে ইউজার ও কম্পোনেন্ট কিচেনের মধ্যে অর্ডার (UI আপডেট) ডেলিভারি করে।

</div>

---

## ১.২ – গভীর বোঝাপড়া ও সাধারণ ভুল ⚠️

<div style={{ background: 'rgba(230, 150, 70, 0.10)', padding: '15px', borderRadius: '5px', marginBottom: '20px', border: '1px solid rgba(230, 150, 70, 0.15)', color: 'inherit' }}>

**প্রশ্ন ৪: রেন্ডারিং "pure" হওয়া মানে কী?**  
উত্তর: একই ইনপুট দিলে, কম্পোনেন্ট সবসময় একই আউটপুট (JSX) রিটার্ন করবে। রেন্ডার চলাকালীন বাইরের কোনো অবজেক্ট বা ভ্যারিয়েবল চেঞ্জ করা যাবে না।

**প্রশ্ন ৫: প্রতিবার রেন্ডারের পর কি React DOM আপডেট করে?**  
উত্তর: না, React শুধু তখনই DOM আপডেট করে যখন আগের ও বর্তমান রেন্ডারের মধ্যে পার্থক্য থাকে।

**প্রশ্ন ৬: Commit ফেজে কী হয়?**  
উত্তর: React আসল DOM-কে virtual DOM-এর সাথে মিলিয়ে আপডেট করে, যাতে ইউজার পরিবর্তন দেখতে পায়।

</div>

---

## ১.৩ – সিনারিও ও প্র্যাকটিক্যাল উদাহরণ 🏆

<div style={{ background: 'rgba(20, 120, 220, 0.10)', padding: '20px', borderRadius: '5px', marginBottom: '20px', border: '1px solid rgba(20, 120, 220, 0.15)', color: 'inherit' }}>

### সিনারিও-ভিত্তিক প্রশ্ন:

ধরুন, আপনার একটি কাউন্টার কম্পোনেন্ট আছে। আপনি ইনক্রিমেন্ট বাটনে ক্লিক করলে, তিনটি রেন্ডারিং ধাপে কী ঘটে, ব্যাখ্যা করুন।

উত্তর দেখার আগে একটু ভাবুন।

<details>
<summary>**উত্তর দেখতে ক্লিক করুন**</summary>

**উত্তর:**
- **Trigger:** বাটনে ক্লিক করলে state আপডেট হয়।
- **Render:** React কম্পোনেন্ট কল করে, নতুন UI (আপডেটেড count) হিসাব করে।
- **Commit:** React DOM আপডেট করে, নতুন count দেখায়।

</details>
</div>

---

## ১.৪ – আরও সিনারিও ও অ্যাডভান্স প্রশ্ন 🚀

<div style={{ background: 'rgba(100, 200, 100, 0.10)', padding: '20px', borderRadius: '5px', marginBottom: '20px', border: '1px solid rgba(100, 200, 100, 0.15)', color: 'inherit' }}>

### সিনারিও-ভিত্তিক প্রশ্ন:

ধরুন, আপনার একটি parent কম্পোনেন্ট আছে, যেটি child কম্পোনেন্টের একটি লিস্ট রেন্ডার করে। parent-এর state আপডেট করলে child-দের কী হয়?

<details>
<summary>**উত্তর দেখতে ক্লিক করুন**</summary>

**উত্তর:**
- parent-এর state আপডেট হলে, React parent-কে রি-রেন্ডার করে।
- সব child-ও রি-রেন্ডার হয়, যদি না তারা memoized (React.memo) হয় এবং তাদের props না বদলায়।

</details>

---

### সিনারিও-ভিত্তিক প্রশ্ন:

একটি কম্পোনেন্ট API থেকে ডেটা ফেচ করে state-এ রাখে। আপনি যদি আগের মতোই ডেটা দিয়ে setState কল করেন, React কি রি-রেন্ডার করবে?

<details>
<summary>**উত্তর দেখতে ক্লিক করুন**</summary>

**উত্তর:**
- যদি setState-এ আগের state-এর মতোই exact value পাঠান, React রি-রেন্ডার স্কিপ করবে (পারফরম্যান্সের জন্য)।
- অবজেক্ট বা অ্যারে হলে, reference equality (===) দেখে; মান একই হলেও নতুন reference দিলে রি-রেন্ডার হবে।

</details>

---

### অ্যাডভান্স প্রশ্ন:

React.StrictMode কী এবং এটি রেন্ডারিং-এ কী প্রভাব ফেলে?

<details>
<summary>**উত্তর দেখতে ক্লিক করুন**</summary>

**উত্তর:**
- React.StrictMode অ্যাপের সম্ভাব্য সমস্যা ধরার জন্য ব্যবহৃত হয়।
- ডেভেলপমেন্টে, এটি কিছু lifecycle method ও render function ইচ্ছাকৃতভাবে দুইবার কল করে, যাতে side effect ও বাগ ধরা যায়।
- প্রোডাকশনে কোনো প্রভাব ফেলে না।

</details>

---

### অ্যাডভান্স প্রশ্ন:

রেন্ডারিং pure হওয়া কেন গুরুত্বপূর্ণ? রেন্ডার চলাকালীন state mutate বা side effect করলে কী সমস্যা হতে পারে?

<details>
<summary>**উত্তর দেখতে ক্লিক করুন**</summary>

**উত্তর:**
- pure rendering মানে একই ইনপুটে সবসময় একই আউটপুট, ফলে UI predictable ও debug-friendly হয়।
- রেন্ডার চলাকালীন state mutate বা side effect করলে বাগ, অপ্রত্যাশিত UI, ও React-এর অপ্টিমাইজেশন (যেমন render skip) নষ্ট হতে পারে।
- React একাধিকবার render function কল করতে পারে (বিশেষত StrictMode-এ), ফলে side effect থাকলে ডুপ্লিকেট অ্যাকশন বা inconsistent state হতে পারে।

</details>

---

### অ্যাডভান্স প্রশ্ন:

বড় React অ্যাপে রেন্ডারিং পারফরম্যান্স কীভাবে অপ্টিমাইজ করবেন?

<details>
<summary>**উত্তর দেখতে ক্লিক করুন**</summary>

**উত্তর:**
- React.memo ব্যবহার করুন, যাতে child কম্পোনেন্ট অপ্রয়োজনীয় রি-রেন্ডার না হয়।
- useMemo ও useCallback দিয়ে খরচবহুল হিসাব ও ফাংশন মেমোাইজ করুন।
- বড় কম্পোনেন্ট ছোট ছোট কম্পোনেন্টে ভাগ করুন।
- লিস্টে key সঠিকভাবে ব্যবহার করুন।
- অপ্রয়োজনীয় state উপরের কম্পোনেন্টে রাখবেন না।
- বড় লিস্টের জন্য virtualization (যেমন react-window) ব্যবহার করুন।

</details>
</div>

---

<Callout type="info">এই প্রশ্নগুলো রিভিউ করুন এবং নিজের ভাষায় কনসেপ্টগুলো ব্যাখ্যা করার চেষ্টা করুন—এটাই ইন্টারভিউ প্রস্তুতির সবচেয়ে ভালো উপায়!</Callout>

---

> **🚀 শেষ পরামর্শ:** ইন্টারভিউতে শুধু রেন্ডারিং সম্পর্কে জ্ঞান দেখানো নয়, বরং ক্লিন, মেইনটেইনেবল ও ইউজার-ফ্রেন্ডলি কোড লেখার দক্ষতাও দেখান।

<img src="https://upload.wikimedia.org/wikipedia/commons/a/a7/React-icon.svg" alt="React Interview Success" width="100" style={{borderRadius: '8px', marginTop: '2rem'}} />
