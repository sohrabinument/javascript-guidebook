import { Callout } from "nextra/components";

<img src="https://upload.wikimedia.org/wikipedia/commons/a/a7/React-icon.svg" alt="React Logo" width="70" style={{marginBottom: '1rem'}} />

# State Update Queue – ইন্টারভিউ প্রশ্ন ও উত্তর 🎯

এই প্রশ্নগুলো ব্যবহার করুন ইন্টারভিউ প্রস্তুতির জন্য বা React-এ state queue ও batching কিভাবে কাজ করে তা বোঝার জন্য।

> **💡 প্রো টিপ:** প্রতিটি প্রশ্নের উত্তর নিজে দেওয়ার চেষ্টা করুন, তারপর উত্তর দেখুন। এতে আপনার মনে রাখার ক্ষমতা বাড়বে!

---

## ১.১ – Batching ও State Queue মৌলিক ধারণা 🌱

<div style={{ background: 'rgba(0, 100, 200, 0.10)', padding: '15px', borderRadius: '5px', marginBottom: '20px', border: '1px solid rgba(0, 100, 200, 0.15)', color: 'inherit' }}>

**প্রশ্ন ১: React "batch" মানে কী?**  
উত্তর: React একাধিক state update event handler-এর মধ্যে জমা রাখে এবং ফাংশন শেষ হলে একসাথে প্রসেস করে, প্রতিটি কলের পরপর UI আপডেট না করে।

```jsx
setCount(count + 1);
setName('Alice');
// React ফাংশন শেষ হলে একসাথে আপডেট করে।
```

**প্রশ্ন ২: React কেন batching করে?**  
উত্তর: পারফরম্যান্স বাড়াতে এবং UI-তে অপ্রয়োজনীয় রেন্ডার এড়াতে batching করা হয়।

```jsx
setA(a + 1);
setB(b + 1);
// দুইটা একসাথে আপডেট হয়, UI সবসময় সিঙ্ক থাকে।
```

**প্রশ্ন ৩: State updater function কী?**  
উত্তর: Updater function হলো এমন একটি ফাংশন, যেটা state setter-এ (যেমন setCount(c => c + 1)) পাঠানো হয় এবং সর্বশেষ state মান পায়, যাতে আপডেট সবসময় সর্বশেষ মানের উপর ভিত্তি করে হয়।

```jsx
setCount(c => c + 1); // c সর্বশেষ মান
```

**প্রশ্ন ৪: এক event handler-এ সরাসরি মান দিয়ে setState একাধিকবার কল করলে কী হয়?**  
উত্তর: প্রতিটি কল ফাংশনের শুরুতে state-এর snapshot ব্যবহার করে, তাই শুধু শেষ আপডেটটাই কার্যকর হয়।

```jsx
setCount(count + 1);
setCount(count + 1);
setCount(count + 1);
// শুধু ১ বাড়ে, ৩ নয়!
```

</div>

---

## ১.২ – মধ্যবর্তী ও সাধারণ ভুল 💡

<div style={{ background: 'rgba(230, 150, 70, 0.10)', padding: '15px', borderRadius: '5px', marginBottom: '20px', border: '1px solid rgba(230, 150, 70, 0.15)', color: 'inherit' }}>

**প্রশ্ন ৫: কীভাবে নিশ্চিত করবেন, একাধিক state update সঠিকভাবে হবে?**  
উত্তর: প্রতিটি আপডেটের জন্য updater function (যেমন setCount(c => c + 1)) ব্যবহার করুন। React সেগুলো সর্বশেষ মান দিয়ে প্রসেস করে।

```jsx
setCount(c => c + 1);
setCount(c => c + 1);
setCount(c => c + 1);
// ৩ বাড়ে
```

**প্রশ্ন ৬: লুপে বা একাধিকবার state আপডেটের সাধারণ ভুল কী?**  
উত্তর: setState(value + 1) ব্যবহার করা, যেখানে setState(prev => prev + 1) ব্যবহার করা উচিত। এতে সব আপডেট পুরনো মান ব্যবহার করে।

```jsx
// ভুল:
for (let i = 0; i < 3; i++) setCount(count + 1); // শুধু ১ বাড়ে
// সঠিক:
for (let i = 0; i < 3; i++) setCount(c => c + 1); // ৩ বাড়ে
```

**প্রশ্ন ৭: সরাসরি মান ও updater function একসাথে ব্যবহার করলে কী হয়?**  
উত্তর: হ্যাঁ, কিন্তু শেষের সরাসরি মান সব আপডেট ও updater function ওভাররাইট করে।

```jsx
setCount(count + 5); // ৫ সেট করে
setCount(c => c + 1); // ১ যোগ করে
setCount(42);         // ৪২ সেট করে (চূড়ান্ত মান)
```

**প্রশ্ন ৮: Updater function কেন pure হওয়া উচিত?**  
উত্তর: React Strict Mode-এ একাধিকবার কল করতে পারে, তাই শুধু নতুন state return করা উচিত, কোনো side effect নয়।

```jsx
// ভালো:
setCount(c => c + 1);
// খারাপ:
setCount(c => { alert('hi'); return c + 1; }); // Side effect! এড়ান।
```

**প্রশ্ন ৯: একই event handler-এ একাধিক state variable আপডেট করলে কী হয়?**  
উত্তর: React সব আপডেট batch করে, তাই সব পরিবর্তন একসাথে হয়।

```jsx
setA(a + 1);
setB(b + 1);
// একসাথে আপডেট হয়
```

</div>

---

## ১.৩ – উন্নত ও বাস্তব জীবনের পরিস্থিতি 🧠

<div style={{ background: 'rgba(220, 50, 50, 0.10)', padding: '15px', borderRadius: '5px', marginBottom: '20px', border: '1px solid rgba(220, 50, 50, 0.15)', color: 'inherit' }}>

**প্রশ্ন ১০: আপনি চান, একটি বাটনে ক্লিক করলে counter-এ ৫ বাড়বে। event handler কীভাবে লিখবেন?**  
উত্তর: setCount(c => c + 1) পাঁচবার কল করুন। প্রতিটি কল সর্বশেষ মান ব্যবহার করবে।

```jsx
for (let i = 0; i < 5; i++) setCount(c => c + 1);
```

**প্রশ্ন ১১: setCount(count + 1) পাঁচবার কল করলে কী হবে?**  
উত্তর: শুধু ১ বাড়বে, কারণ প্রতিটি কল একই snapshot ব্যবহার করে।

```jsx
setCount(count + 1);
setCount(count + 1);
setCount(count + 1);
setCount(count + 1);
setCount(count + 1);
// শুধু ১ বাড়ে
```

**প্রশ্ন ১২: দুইটি counter এক event-এ আপডেট করতে চান। batching কীভাবে সাহায্য করে?**  
উত্তর: দুইটি একসাথে এক render-এ আপডেট হয়, UI সিঙ্ক থাকে।

```jsx
setA(a + 1);
setB(b + 1);
// একসাথে আপডেট হয়
```

**প্রশ্ন ১৩: setState-এর পর state সাথে সাথে আপডেট হবে ধরে নিলে কী সমস্যা হতে পারে?**  
উত্তর: পুরনো মান পড়া বা বাগ হতে পারে, কারণ state handler শেষ না হওয়া পর্যন্ত আপডেট হয় না।

```jsx
setCount(count + 1);
console.log(count); // এখনো পুরনো মান!
```

**প্রশ্ন ১৪: async callback (যেমন setTimeout)-এ সর্বশেষ মানের উপর ভিত্তি করে state আপডেট করতে চাইলে কী করবেন?**  
উত্তর: async callback-এ updater function ব্যবহার করুন।

```jsx
setTimeout(() => {
  setCount(c => c + 1);
}, 1000);
```

**প্রশ্ন ১৫: সরাসরি মান ও updater function মিশিয়ে দিলে React কীভাবে প্রসেস করে?**  
উত্তর: React প্রতিটি আপডেট ক্রমানুসারে প্রসেস করে। updater function সর্বশেষ মান ব্যবহার করে, কিন্তু সরাসরি মান সব ওভাররাইট করে।

```jsx
setCount(5);
setCount(c => c + 1);
setCount(10);
// চূড়ান্ত মান ১০
```

**প্রশ্ন ১৬: ফর্ম reset করে কনফার্মেশন মেসেজ দেখাতে চান। মাঝে মাঝে পুরনো ডেটা দেখায় কেন?**  
উত্তর: State batching-এর কারণে সাথে সাথে আপডেট হয় না, তাই পুরনো মান দেখা যায়। useEffect ব্যবহার করুন।

```jsx
setMessage('');
setShowConfirmation(true);
// useEffect(() => { ... }, [message]);
```

**প্রশ্ন ১৭: updater function-এ side effect থাকলে কী ঝুঁকি?**  
উত্তর: Strict Mode-এ একাধিকবার বা অপ্রত্যাশিত সময়ে side effect হতে পারে, বাগ হতে পারে।

```jsx
setCount(c => { fetch('/api'); return c + 1; }); // এটা করবেন না!
```

**প্রশ্ন ১৮: নেটওয়ার্ক রিকোয়েস্ট শেষ হলে মান বাড়াতে চান, একাধিক রিকোয়েস্ট একসাথে শেষ হলে কীভাবে সঠিক আপডেট নিশ্চিত করবেন?**  
উত্তর: updater function ব্যবহার করুন, যাতে প্রতিটি আপডেট সর্বশেষ মানের উপর ভিত্তি করে হয়।

```jsx
fetch('/api').then(() => setCount(c => c + 1));
```

</div>

---

<Callout type="info">এই প্রশ্নগুলো পড়ে নিজে উত্তর দেয়ার চেষ্টা করুন, এতে ইন্টারভিউ প্রস্তুতি আরও ভালো হবে!</Callout>

---

## অনুশীলনী প্রশ্ন 🏋️‍♂️

<div style={{ background: 'rgba(20, 120, 220, 0.10)', padding: '20px', borderRadius: '5px', marginBottom: '20px', border: '1px solid rgba(20, 120, 220, 0.15)', color: 'inherit' }}>

### পরিস্থিতি-ভিত্তিক প্রশ্ন:

আপনি একটি কুইজ অ্যাপ বানাচ্ছেন। ইউজার উত্তর দিলে স্কোর ও প্রশ্নের সংখ্যা বাড়াতে চান। মাঝে মাঝে দ্রুত উত্তর দিলে স্কোর বা সংখ্যা ভুল হয়।

**প্রশ্ন:** কীভাবে নিশ্চিত করবেন, ইউজার দ্রুত ক্লিক করলেও দুই state আপডেট সবসময় সঠিক হবে?

উত্তর নিজে ভাবুন, তারপর নিচের উত্তর দেখুন।

<details>
<summary>**উত্তরের নমুনা দেখুন**</summary>

**সমাধান:**
- দুই state আপডেটের জন্য updater function ব্যবহার করুন:
```jsx
setScore(s => s + 1);
setQuestionsAnswered(q => q + 1);
```
- এতে প্রতিটি আপডেট সর্বশেষ মান ব্যবহার করবে, দ্রুত queue হলেও।

</details>
</div>

---

**টিপ:** এই প্রশ্ন-উত্তরগুলো পড়ে নিজে বোঝার চেষ্টা করুন, এতে ইন্টারভিউতে আরও ভালো করবেন!

> **🚀 চূড়ান্ত পরামর্শ:** ইন্টারভিউতে শুধু batching ও queueing-এর জ্ঞান নয়, বাস্তব ও async পরিস্থিতিতে কিভাবে প্রয়োগ করবেন সেটাও দেখান।
