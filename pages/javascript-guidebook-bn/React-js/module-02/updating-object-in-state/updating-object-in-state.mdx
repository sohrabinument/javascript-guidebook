import { Callout } from 'nextra/components';

<img src="https://upload.wikimedia.org/wikipedia/commons/a/a7/React-icon.svg" alt="React Logo" width="70" style={{marginBottom: '1rem'}} />

# স্টেটে অবজেক্ট আপডেট – সহজ ও মজার উপায়! 🚀

স্বাগতম! যদি কখনো React স্টেটে অবজেক্ট আপডেট নিয়ে বিভ্রান্ত হন, আপনি একা নন। এই গাইডটি আপনার জন্য—বিশেষ করে যদি মনে করেন আপনি "খারাপ ছাত্র"। চলুন, এত সহজ ও মজার করি যেন বারবার পড়তে ইচ্ছা করে!

---

## 🎯 আপনি যা শিখবেন
- কেন স্টেটে অবজেক্ট সরাসরি পরিবর্তন করা উচিত নয়
- React-এর সঠিক উপায়ে অবজেক্ট আপডেট
- নেস্টেড অবজেক্ট আপডেট
- "ইমিউটেবিলিটি" মানে কী (সহজ ভাষায়!)
- প্রো টিপস ও সাধারণ ভুল

---

<Callout type="info">**উপমা:** React স্টেটকে রেস্টুরেন্টের অর্ডারের মতো ভাবুন। একবার অর্ডার দিলে, আপনি সেটা আর পরিবর্তন করতে পারবেন না। নতুন কিছু চাইলে নতুন অর্ডার দিতে হবে!</Callout>

---

## ১. কেন সরাসরি অবজেক্ট পরিবর্তন করা যাবে না? 🤔

ধরা যাক আপনার আছে:

```jsx
const [position, setPosition] = useState({ x: 0, y: 0 });
```

আপনি ভাবতে পারেন:

```jsx
position.x = 5; // ❌
```

কিন্তু React বুঝবে না! এটা যেন নিজের কাছে ফিসফিস করে অর্ডার বদলানো—শেফ (React) শুনতেই পায় না, তাই আপনার প্লেটে (UI) কিছুই বদলায় না।

---

## ২. সঠিক উপায়: কপি করুন, তারপর আপডেট! 📝

**সবসময় নতুন অবজেক্ট তৈরি করুন এবং setter ফাংশন ব্যবহার করুন।**

```jsx
setPosition({ x: 5, y: 10 }); // ✅
```

অথবা, পুরনো মান রেখে শুধু একটি পরিবর্তন করতে চাইলে:

```jsx
setPosition({ ...position, x: 5 });
```

<Callout type="tip">**প্রো টিপ:** <b>স্প্রেড সিনট্যাক্স</b> (`...`) পুরনো অবজেক্টের সব প্রপার্টি কপি করে। এরপর আপনি যেটা চাইবেন সেটি ওভাররাইড করতে পারবেন!</Callout>

---

## ৩. ধাপে ধাপে: ডট মুভ করার উদাহরণ 🎯

আসুন একটি বাস্তব উদাহরণ দেখি:

```jsx
import { useState } from 'react';

export default function MovingDot() {
  const [position, setPosition] = useState({ x: 0, y: 0 });
  return (
    <div
      onPointerMove={e => {
        setPosition({ x: e.clientX, y: e.clientY });
      }}
      style={{ position: 'relative', width: 300, height: 300, background: '#f0f0f0' }}
    >
      <div style={{
        position: 'absolute',
        backgroundColor: 'red',
        borderRadius: '50%',
        transform: `translate(${position.x}px, ${position.y}px)`,
        left: -10,
        top: -10,
        width: 20,
        height: 20,
      }} />
    </div>
  );
}
```

মাউস ঘোরান—লাল ডটটি আপনাকে অনুসরণ করবে! যদি অবজেক্ট সরাসরি পরিবর্তন করতেন, ডট নড়ত না।

---

## ৪. কেন মিউটেট নয়? (একটি গল্প) 📚

ধরা যাক, আপনি কলমে হোমওয়ার্ক লিখছেন। ভুল করলে কেটে দেন, শিক্ষক (React) বুঝতে পারেন না কী বদলেছে। কিন্তু প্রতিবার নতুন কাগজে লিখলে, শিক্ষক সব পরিবর্তন দেখতে পান!

- **স্টেট মিউটেট** = একই কাগজে কাটাকাটি (React বুঝে না)
- **কপি করা** = নতুন কাগজ (React নতুনটা দেখে)

---

## ৫. নেস্টেড অবজেক্ট আপডেট 🏗️

যদি আপনার state এমন হয়:

```jsx
const [person, setPerson] = useState({
  name: 'Niki',
  artwork: {
    title: 'Blue Nana',
    city: 'Hamburg',
    image: 'https://i.imgur.com/Sd1AgUOm.jpg',
  }
});
```

শুধু city আপডেট করতে:

```jsx
setPerson({
  ...person,
  artwork: {
    ...person.artwork,
    city: 'Lagos'
  }
});
```

<Callout type="warning">**সাধারণ ভুল:** শুধু উপরের অবজেক্ট কপি করলে, নেস্টেড অবজেক্ট একই থাকে! যেটা পরিবর্তন করতে চান, প্রতিটি লেভেল কপি করুন।</Callout>

---

## ৬. প্রো টিপস ও ট্রিকস 💡

- **কখনোই স্টেট সরাসরি মিউটেট করবেন না!**
- অবজেক্ট কপি করতে স্প্রেড সিনট্যাক্স (`...`) ব্যবহার করুন
- ডিপ আপডেটের জন্য প্রতিটি লেভেল কপি করুন
- খুব জটিল স্টেটের জন্য [Immer](https://immerjs.github.io/immer/) ব্যবহার করুন

---

## ৭. রিক্যাপ ও চিটশিট 📝

- স্টেটকে **read-only** ভাবুন
- আপডেট করতে **নতুন কপি** তৈরি করুন এবং সেট করুন
- `{ ...oldObject, changedProp: newValue }` ব্যবহার করুন
- নেস্টেড অবজেক্টের জন্য প্রতিটি লেভেল কপি করুন

---

## ৮. FAQ 🤓

**প্রশ্ন: সরাসরি স্টেট মিউটেট করলে কী হয়?**
উত্তর: React রি-রেন্ডার করবে না, অদ্ভুত বাগ হতে পারে!

**প্রশ্ন: React কেন ইমিউটেবিলিটি চায়?**
উত্তর: এতে React সহজে UI আপডেট করতে পারে, ডিবাগ সহজ হয়, টাইম ট্রাভেল/আনডু-এর মতো ফিচার সম্ভব হয়।

**প্রশ্ন: নতুন অবজেক্ট মিউটেট করে তারপর সেট করলে সমস্যা?**
উত্তর: না! যতক্ষণ না সেটা আগের state-এর রেফারেন্স, ততক্ষণ ঠিক আছে।

---

<Callout type="info">
  <b>নিজে চেষ্টা করুন:</b>
  <ul>
    <li>নেস্টেড অবজেক্টসহ একটি ফর্ম তৈরি করুন</li>
    <li>প্রথমে ভুলভাবে (ডাইরেক্ট মিউটেট) আপডেট করুন</li>
    <li>তারপর সঠিকভাবে (কপি ও আপডেট) করুন</li>
    <li>ফারাক লক্ষ্য করুন!</li>
  </ul>
</Callout>

---

> **🚀 চূড়ান্ত পরামর্শ:** স্টেটে অবজেক্ট নিয়ে ভয় পাবেন না! শুধু মনে রাখুন: কপি করুন, তারপর আপডেট করুন। আপনি পারবেন!
