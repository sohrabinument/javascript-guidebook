import { Callout } from 'nextra/components';

# স্টেটে অবজেক্ট আপডেট – ইন্টারভিউ প্রশ্ন ও উত্তর 🎯

<img src="https://upload.wikimedia.org/wikipedia/commons/a/a7/React-icon.svg" alt="React Logo" width="80" style={{marginBottom: '1rem'}} />

এই প্রশ্নগুলো ব্যবহার করুন ইন্টারভিউ প্রস্তুতির জন্য বা React-এ কিভাবে অবজেক্ট স্টেটে আপডেট করতে হয় তা বোঝার জন্য।

> **💡 প্রো টিপ:** প্রতিটি প্রশ্নের উত্তর নিজে দেওয়ার চেষ্টা করুন, তারপর উত্তর দেখুন। এতে আপনার মনে রাখার ক্ষমতা বাড়বে!

---

## ১.১ – স্টেট অবজেক্ট আপডেট মৌলিক ধারণা 🌱

<div style={{ background: 'rgba(0, 100, 200, 0.10)', padding: '15px', borderRadius: '5px', marginBottom: '20px', border: '1px solid rgba(0, 100, 200, 0.15)', color: 'inherit' }}>

**প্রশ্ন ১: কেন React স্টেটে অবজেক্ট সরাসরি পরিবর্তন করা উচিত নয়?**  
উত্তর: সরাসরি পরিবর্তন করলে React বুঝতে পারে না, তাই UI আপডেট হয় না। সবসময় setter ফাংশন ব্যবহার করে অবজেক্ট রিপ্লেস করুন।

```jsx
// ❌ ভুল উপায়
position.x = 5;
// React জানে না state পরিবর্তন হয়েছে!

// ✅ সঠিক উপায়
setPosition({ ...position, x: 5 });
```

**প্রশ্ন ২: স্টেটে রাখা অবজেক্টের একটি প্রপার্টি কীভাবে আপডেট করবেন?**  
উত্তর: পুরনো অবজেক্ট কপি করতে spread syntax ব্যবহার করুন, তারপর যেটা পরিবর্তন করতে চান সেটি ওভাররাইড করুন।

```jsx
setPerson({ ...person, name: 'Alex' });
```

**প্রশ্ন ৩: React স্টেটে 'immutability' বলতে কী বোঝায়?**  
উত্তর: মানে আপনি state কে read-only হিসেবে দেখবেন এবং সরাসরি পরিবর্তন করবেন না। বরং, নতুন অবজেক্ট বা অ্যারে তৈরি করে সেটাকে state হিসেবে সেট করবেন।

</div>

---

## ১.২ – নেস্টেড অবজেক্ট ও সাধারণ ভুল 💡

<div style={{ background: 'rgba(230, 150, 70, 0.10)', padding: '15px', borderRadius: '5px', marginBottom: '20px', border: '1px solid rgba(230, 150, 70, 0.15)', color: 'inherit' }}>

**প্রশ্ন ৪: স্টেটে রাখা নেস্টেড অবজেক্টের প্রপার্টি কীভাবে আপডেট করবেন?**  
উত্তর: প্রতিটি লেভেল কপি করতে হবে, প্রতিটি লেভেলে spread syntax ব্যবহার করুন।

```jsx
setPerson({
  ...person,
  artwork: {
    ...person.artwork,
    city: 'Lagos'
  }
});
```

**প্রশ্ন ৫: শুধু টপ-লেভেল অবজেক্ট কপি করলে কিন্তু নেস্টেড অবজেক্ট কপি না করলে কী হয়?**  
উত্তর: নেস্টেড অবজেক্টের রেফারেন্স একই থাকে, তাই পরিবর্তন করলে রি-রেন্ডার নাও হতে পারে এবং বাগ হতে পারে।

**প্রশ্ন ৬: ডিপ আপডেট সহজ করতে কোনো লাইব্রেরি ব্যবহার করা যায়?**  
উত্তর: হ্যাঁ! [Immer](https://immerjs.github.io/immer/) লাইব্রেরি ব্যবহার করলে কোডে মিউটেশন লিখে আসলে নতুন কপি তৈরি করা যায়।

```jsx
import { useImmer } from 'use-immer';

const [person, updatePerson] = useImmer({
  name: 'Niki',
  artwork: { city: 'Hamburg' }
});

updatePerson(draft => {
  draft.artwork.city = 'Lagos';
});
```

</div>

---

## ১.৩ – উন্নত ও পরিস্থিতি-ভিত্তিক প্রশ্ন 🧠

<div style={{ background: 'rgba(220, 50, 50, 0.10)', padding: '15px', borderRadius: '5px', marginBottom: '20px', border: '1px solid rgba(220, 50, 50, 0.15)', color: 'inherit' }}>

**প্রশ্ন ৭: পরিস্থিতি: আপনার state অবজেক্টে counter এবং nested user অবজেক্ট আছে। কিভাবে একসাথে counter বাড়াবেন এবং user-এর নাম আপডেট করবেন?**  
উত্তর:
```jsx
setState(prev => ({
  ...prev,
  counter: prev.counter + 1,
  user: {
    ...prev.user,
    name: 'Sam'
  }
}));
```

**প্রশ্ন ৮: কলাবোরেটিভ বা async পরিস্থিতিতে state সরাসরি পরিবর্তন করলে কী ঝুঁকি আছে?**  
উত্তর: রেস কন্ডিশন, পুরনো ডেটা, বা বাগ হতে পারে কারণ React জানে না state পরিবর্তন হয়েছে। সবসময় setter ব্যবহার করুন এবং state কে read-only ধরে নিন।

**প্রশ্ন ৯: স্টেটে রাখা অবজেক্টের অ্যারে, যেখানে শুধু একটি অবজেক্ট id দিয়ে পরিবর্তন করতে চান, কীভাবে করবেন?**  
উত্তর:
```jsx
setItems(items => items.map(item =>
  item.id === targetId ? { ...item, value: 'new' } : item
));
```

**প্রশ্ন ১০: পরিস্থিতি: একটি ফর্মে nested state আছে। সাবমিট করার পর শুধু nested address অবজেক্ট রিসেট করতে চান। কীভাবে করবেন?**  
উত্তর:
```jsx
setForm(form => ({
  ...form,
  address: { street: '', city: '', zip: '' }
}));
```

</div>

---

## ১.৪ – আরও পরিস্থিতি ও ডিপ ডাইভ 🚦

<div style={{ background: 'rgba(100, 200, 100, 0.10)', padding: '15px', borderRadius: '5px', marginBottom: '20px', border: '1px solid rgba(100, 200, 100, 0.15)', color: 'inherit' }}>

**প্রশ্ন ১১: পরিস্থিতি: আপনার state অবজেক্টে todos-এর লিস্ট আছে। নির্দিষ্ট id-র todo কমপ্লিটেড করতে চান।**  
উত্তর:
```jsx
setTodos(todos => todos.map(todo =>
  todo.id === targetId ? { ...todo, completed: true } : todo
));
```

**প্রশ্ন ১২: পরিস্থিতি: ডিপলি নেস্টেড প্রপার্টি (যেমন user.profile.settings.theme) আপডেট করতে চান। সেরা উপায় কী?**  
উত্তর: প্রতিটি লেভেলে spread syntax ব্যবহার করুন, অথবা Immer লাইব্রেরি ব্যবহার করুন।

```jsx
// Spread syntax দিয়ে
setUser(user => ({
  ...user,
  profile: {
    ...user.profile,
    settings: {
      ...user.profile.settings,
      theme: 'dark'
    }
  }
}));

// Immer দিয়ে
updateUser(draft => {
  draft.profile.settings.theme = 'dark';
});
```

**প্রশ্ন ১৩: পরিস্থিতি: আপনার shopping cart state অবজেক্ট আছে। নির্দিষ্ট id-র item রিমুভ করতে চান।**  
উত্তর:
```jsx
setCart(cart => cart.filter(item => item.id !== removeId));
```

**প্রশ্ন ১৪: আগের state মানের উপর ভিত্তি করে state আপডেট করতে হলে কী করবেন?**  
উত্তর: সবসময় setter-এর updater ফাংশন ফর্ম ব্যবহার করুন যাতে সর্বশেষ state পান।

```jsx
setCount(count => count + 1);
```

**প্রশ্ন ১৫: পরিস্থিতি: nested array আছে state অবজেক্টে। নতুন item যোগ করতে চান।**  
উত্তর:
```jsx
setData(data => ({
  ...data,
  items: [...data.items, newItem]
}));
```

**প্রশ্ন ১৬: state-এর কিছু অংশ initial value-তে রিসেট করতে চান, বাকি অংশ অপরিবর্তিত রাখতে চান।**  
উত্তর:
```jsx
setState(state => ({
  ...state,
  partToReset: initialPartValue
}));
```

**প্রশ্ন ১৭: state অবজেক্টে loading ফ্ল্যাগ ও data আছে। loading true এবং data null করতে চান।**  
উত্তর:
```jsx
setState(state => ({
  ...state,
  loading: true,
  data: null
}));
```

**প্রশ্ন ১৮: nested অবজেক্টে boolean প্রপার্টি টগল করতে চান।**  
উত্তর:
```jsx
setSettings(settings => ({
  ...settings,
  preferences: {
    ...settings.preferences,
    notifications: !settings.preferences.notifications
  }
}));
```

**প্রশ্ন ১৯: nested অবজেক্টে একাধিক প্রপার্টি একসাথে আপডেট করতে চান।**  
উত্তর:
```jsx
setProfile(profile => ({
  ...profile,
  details: {
    ...profile.details,
    firstName: 'Jane',
    lastName: 'Doe',
    age: 30
  }
}));
```

**প্রশ্ন ২০: state অবজেক্টে নতুন প্রপার্টি মার্জ করতে চান, পুরনো সব প্রপার্টি রেখে।**  
উত্তর:
```jsx
setConfig(config => ({
  ...config,
  ...newProperties
}));
```

</div>

---

<Callout type="info">আরও পরিস্থিতি নিয়ে নিজে নিজে চর্চা করুন! spread syntax বা Immer ব্যবহার করে নিজের state update চ্যালেঞ্জ তৈরি করুন ও সমাধান করুন।</Callout>

---

<Callout type="info">এই প্রশ্নগুলো রিভিউ করুন এবং ইন্টারভিউ প্রস্তুতির জন্য নিজের ভাষায় কনসেপ্টগুলো ব্যাখ্যা করার চেষ্টা করুন!</Callout>

---

## অনুশীলনী প্রশ্ন 🏋️‍♂️

<div style={{ background: 'rgba(20, 120, 220, 0.10)', padding: '20px', borderRadius: '5px', marginBottom: '20px', border: '1px solid rgba(20, 120, 220, 0.15)', color: 'inherit' }}>

### পরিস্থিতি-ভিত্তিক প্রশ্ন:

আপনার state অবজেক্টে একটি ইউজার প্রোফাইল আছে, যার মধ্যে nested preferences অবজেক্ট আছে। ইউজার dark mode টগল করলে, preferences-এ শুধু darkMode প্রপার্টি কীভাবে আপডেট করবেন?

উত্তর নিজে ভাবুন, তারপর নিচের উত্তর দেখুন।

<details>
<summary>**উত্তরের নমুনা দেখুন**</summary>

**সমাধান:**
```jsx
setProfile(profile => ({
  ...profile,
  preferences: {
    ...profile.preferences,
    darkMode: !profile.preferences.darkMode
  }
}));
```

এইভাবে প্রতিটি লেভেল কপি করে শুধু যেটা পরিবর্তন করতে চান সেটি আপডেট করুন, এতে React আপডেট বুঝতে পারে এবং UI রি-রেন্ডার হয়।

</details>
</div>

---

**টিপ:** এই প্রশ্ন-উত্তরগুলো রিভিউ করুন এবং ইন্টারভিউ প্রস্তুতির জন্য নিজের ভাষায় প্রতিটি কনসেপ্ট ব্যাখ্যা করার চেষ্টা করুন!

> **🚀 চূড়ান্ত পরামর্শ:** ইন্টারভিউতে শুধু state অবজেক্ট আপডেটের জ্ঞান নয়, বাস্তব জীবন, async, এবং কলাবোরেটিভ পরিস্থিতিতে এই বোধগম্যতা প্রয়োগ করার ক্ষমতাও দেখান।
