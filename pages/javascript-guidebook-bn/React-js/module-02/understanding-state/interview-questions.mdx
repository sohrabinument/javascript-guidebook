import { Callout } from "nextra/components";

<img src="https://upload.wikimedia.org/wikipedia/commons/a/a7/React-icon.svg" alt="React Logo" width="70" style={{marginBottom: '1rem'}} />

# রিঅ্যাক্টে স্টেট – ইন্টারভিউ প্রশ্ন ও উত্তর 🎯

এই প্রশ্নগুলো ব্যবহার করুন ইন্টারভিউ প্রস্তুতির জন্য বা রিঅ্যাক্টের state ও useState সম্পর্কে আপনার বোঝা যাচাই করতে।

> **💡 প্রো টিপ:** উত্তর দেখার আগে নিজে চেষ্টা করুন। এতে কনসেপ্টগুলো ভালোভাবে মনে থাকবে!

---

## ১.১ – স্টেটের বেসিক 🧠

<div style={{ background: 'rgba(0, 100, 200, 0.10)', padding: '15px', borderRadius: '5px', marginBottom: '20px', border: '1px solid rgba(0, 100, 200, 0.15)', color: 'inherit' }}>

**প্রশ্ন ১: রিঅ্যাক্টে state কী?**  
উত্তর: state হলো এমন একটি উপায়, যার মাধ্যমে রিঅ্যাক্ট কম্পোনেন্ট রেন্ডারের মাঝে তথ্য মনে রাখতে পারে। এটি কম্পোনেন্টকে ডাইনামিক ডেটা আপডেট ও দেখাতে দেয়।

**প্রশ্ন ২: ফাংশন কম্পোনেন্টে state ভ্যারিয়েবল কীভাবে ডিক্লেয়ার করবেন?**  
উত্তর: `useState` হুক ব্যবহার করে, যেমন: `const [count, setCount] = useState(0);`

**প্রশ্ন ৩: useState থেকে পাওয়া setter ফাংশনের কাজ কী?**  
উত্তর: এটি state ভ্যারিয়েবল আপডেট করে এবং কম্পোনেন্টকে রি-রেন্ডার করে।

</div>

---

## ১.২ – রুলস ও সাধারণ ভুল ⚠️

<div style={{ background: 'rgba(230, 150, 70, 0.10)', padding: '15px', borderRadius: '5px', marginBottom: '20px', border: '1px solid rgba(230, 150, 70, 0.15)', color: 'inherit' }}>

**প্রশ্ন ৪: useState হুক কোথায় কল করা যায়?**  
উত্তর: শুধুমাত্র রিঅ্যাক্ট ফাংশন কম্পোনেন্ট বা কাস্টম হুকের টপ-লেভেলে—লুপ, কন্ডিশন, বা নেস্টেড ফাংশনের ভিতরে নয়।

**প্রশ্ন ৫: যদি useState লুপ বা কন্ডিশনের ভিতরে কল করেন কী হবে?**  
উত্তর: এটি হুকের নিয়ম ভেঙে ফেলবে, ফলে বাগ ও অপ্রত্যাশিত আচরণ হতে পারে।

**প্রশ্ন ৬: একাধিক কম্পোনেন্ট ইনস্ট্যান্সের মধ্যে state কি শেয়ার হয়?**  
উত্তর: না, প্রতিটি কম্পোনেন্ট ইনস্ট্যান্সের state আলাদা ও স্বাধীন।

</div>

---

## ১.৩ – সিনারিও ও প্র্যাকটিক্যাল উদাহরণ 🏆

<div style={{ background: 'rgba(20, 120, 220, 0.10)', padding: '20px', borderRadius: '5px', marginBottom: '20px', border: '1px solid rgba(20, 120, 220, 0.15)', color: 'inherit' }}>

### সিনারিও-ভিত্তিক প্রশ্ন:

ধরুন, আপনার একটি কাউন্টার কম্পোনেন্ট আছে: `const [count, setCount] = useState(0);`। যদি একই ফাংশনের ভিতরে দুইবার `setCount(count + 1)` কল করেন, কী হবে?

উত্তর দেখার আগে একটু ভাবুন।

<details>
<summary>**উত্তর দেখতে ক্লিক করুন**</summary>

**উত্তর:**  
- দুইবার কল করলেও, দুইবারই একই মান (কারেন্ট state) ব্যবহার হবে, তাই কাউন্টার শুধু ১ বাড়বে, ২ নয়।
- ২ বাড়াতে চাইলে, আপডেটার ফাংশন ব্যবহার করুন: `setCount(c => c + 1); setCount(c => c + 1);`

</details>
</div>

---

<Callout type="info">এই প্রশ্নগুলো রিভিউ করুন এবং নিজের ভাষায় কনসেপ্টগুলো ব্যাখ্যা করার চেষ্টা করুন—এটাই ইন্টারভিউ প্রস্তুতির সবচেয়ে ভালো উপায়!</Callout>

---

> **🚀 শেষ পরামর্শ:** ইন্টারভিউতে শুধু state সম্পর্কে জ্ঞান দেখানো নয়, বরং ক্লিন, মেইনটেইনেবল ও ইউজার-ফ্রেন্ডলি কোড লেখার দক্ষতাও দেখান।

<img src="https://upload.wikimedia.org/wikipedia/commons/a/a7/React-icon.svg" alt="React Interview Success" width="100" style={{borderRadius: '8px', marginTop: '2rem'}} />
