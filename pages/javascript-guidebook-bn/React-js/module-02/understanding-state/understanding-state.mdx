import { Callout } from "nextra/components";

<img src="https://upload.wikimedia.org/wikipedia/commons/a/a7/React-icon.svg" alt="React Logo" width="70" style={{marginBottom: '1rem'}} />

# 🧠 রিঅ্যাক্টে স্টেট বোঝা

> **স্টেট কেন গুরুত্বপূর্ণ?**
>
> স্টেট আপনার কম্পোনেন্টকে "মনে রাখতে" সাহায্য করে—যেমন ইউজার ইনপুট, কারেন্ট ইমেজ, বা কোনো মডাল ওপেন আছে কিনা। ইন্টারঅ্যাকটিভ UI বানাতে স্টেট মাস্টার করা জরুরি!

---

## স্টেট কী? 🤔

স্টেট হলো রিঅ্যাক্টের এমন একটি উপায়, যার মাধ্যমে কম্পোনেন্ট রেন্ডার হওয়ার পরও তথ্য মনে রাখতে পারে। যেমন, আপনি ফর্মে টাইপ করলে, বাটনে ক্লিক করলে, বা মডাল টগল করলে—স্টেট রিঅ্যাক্টকে বলে দেয়, স্ক্রিনে কী দেখাতে হবে।

<Callout type="info">স্টেট <b>লোকাল</b> প্রতিটি কম্পোনেন্ট ইনস্ট্যান্সের জন্য। একই কম্পোনেন্ট দুইবার রেন্ডার করলে, প্রতিটির স্টেট আলাদা থাকবে!</Callout>

---

## সাধারণ ভ্যারিয়েবল যথেষ্ট নয় কেন?

ধরুন, আপনি চান প্রতিবার বাটনে ক্লিক করলে ভিন্ন ইমেজ দেখাক। যদি সাধারণ ভ্যারিয়েবল ব্যবহার করেন, রিঅ্যাক্ট রেন্ডারের মাঝে তার মান মনে রাখবে না, আর UI আপডেটও হবে না।

```jsx
// এটা কাজ করবে না:
let index = 0;
function handleClick() {
  index = index + 1;
}
```

- সাধারণ ভ্যারিয়েবল রেন্ডারের মাঝে টিকে থাকে না।
- ভ্যারিয়েবল চেঞ্জ করলে রি-রেন্ডার হয় না।

---

## পরিচিত হোন useState: আপনার প্রথম হুক 🪝

রিঅ্যাক্টে এই সমস্যার সমাধান দেয় `useState` হুক। এটি দেয়:
- একটি স্টেট ভ্যারিয়েবল, যা রেন্ডারের মাঝে মান মনে রাখে
- একটি setter ফাংশন, যা মান আপডেট করে এবং রি-রেন্ডার ট্রিগার করে

```jsx
import { useState } from 'react';

function Gallery() {
  const [index, setIndex] = useState(0);
  // ...
}
```

> `[index, setIndex]` কে বলে array destructuring। `useState` সবসময় একটি জোড়া রিটার্ন করে: কারেন্ট মান ও সেট করার ফাংশন।

---

## useState কিভাবে কাজ করে (ভিজ্যুয়াল)

<img src="/useState.svg" alt="React useState Hook Diagram" width="650" style={{display:'block',margin:'2.5rem auto'}} />

1. আপনার কম্পোনেন্ট প্রথমবার রেন্ডার হলে, `useState(0)` রিটার্ন করে `[0, setIndex]`।
2. আপনি `setIndex(1)` কল করলে, রিঅ্যাক্ট নতুন মান মনে রাখে এবং কম্পোনেন্ট রি-রেন্ডার করে।
3. পরের রেন্ডারে, `useState(0)` রিটার্ন করে `[1, setIndex]`।

---

## উদাহরণ: ইমেজ গ্যালারি 🖼️

```jsx
import { useState } from 'react';

const images = [
  'https://images.unsplash.com/photo-1506744038136-46273834b3fb',
  'https://images.unsplash.com/photo-1465101046530-73398c7f28ca',
  'https://images.unsplash.com/photo-1519125323398-675f0ddb6308'
];

export default function Gallery() {
  const [index, setIndex] = useState(0);
  function handleNext() {
    setIndex((prev) => (prev + 1) % images.length);
  }
  return (
    <div style={{textAlign: 'center'}}>
      <img src={images[index]} alt="Gallery" width="400" style={{borderRadius: '10px'}} />
      <br />
      <button onClick={handleNext} style={{marginTop: '1rem'}}>Next</button>
      <p>{index + 1} / {images.length}</p>
    </div>
  );
}
```

---

## স্টেট আলাদা ও প্রাইভেট 🔒

প্রতিটি কম্পোনেন্ট ইনস্ট্যান্সের স্টেট আলাদা। আপনি যদি দুইটি `<Gallery />` রেন্ডার করেন, তাদের স্টেট একে অপরের থেকে স্বাধীন:

```jsx
export default function Page() {
  return (
    <>
      <Gallery />
      <Gallery />
    </>
  );
}
```

<Callout type="info">স্টেট <b>প্রাইভেট</b> কম্পোনেন্টের জন্য। প্যারেন্ট সরাসরি চেঞ্জ করতে পারে না।</Callout>

---

## রিক্যাপ ও বেস্ট প্র্যাকটিস 🚀

- যখন কম্পোনেন্টকে রেন্ডারের মাঝে কিছু "মনে রাখতে" হয়, তখন স্টেট ব্যবহার করুন।
- `useState` হুক দিয়ে স্টেট ভ্যারিয়েবল ডিক্লেয়ার করুন।
- হুক সবসময় কম্পোনেন্টের টপ-লেভেলে কল করুন (লুপ, কন্ডিশন, বা নেস্টেড ফাংশনে নয়)।
- প্রতিটি কম্পোনেন্ট ইনস্ট্যান্সের স্টেট আলাদা।
- setter ফাংশন মান আপডেট করে এবং রি-রেন্ডার ট্রিগার করে।

<Callout type="success">নিজের কম্পোনেন্টে <code>useState</code> ব্যবহার করে প্র্যাকটিস করুন! আরও জানতে দেখুন <a href="https://react.dev/learn/state-a-components-memory" target="_blank">React অফিসিয়াল ডকস</a>। 🎉</Callout>
