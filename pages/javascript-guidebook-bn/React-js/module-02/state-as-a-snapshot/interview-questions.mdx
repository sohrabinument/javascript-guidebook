import { Callout } from "nextra/components";

<img src="https://upload.wikimedia.org/wikipedia/commons/a/a7/React-icon.svg" alt="React Logo" width="70" style={{marginBottom: '1rem'}} />

# স্টেট স্ন্যাপশট – ইন্টারভিউ প্রশ্ন ও উত্তর 🎯

ইন্টারভিউ প্রস্তুতির জন্য বা React-এ স্টেট কিভাবে স্ন্যাপশট হিসেবে কাজ করে তা বোঝার জন্য এই প্রশ্নগুলো ব্যবহার করুন।

> **💡 প্রো টিপ:** প্রতিটি প্রশ্নের উত্তর নিজে দেওয়ার চেষ্টা করুন, তারপর উত্তর দেখুন। এতে আপনার মনে রাখার ক্ষমতা বাড়বে!

---

## ২.১ – স্টেট স্ন্যাপশট মৌলিক ধারণা 🌱

<div style={{ background: 'rgba(0, 100, 200, 0.10)', padding: '15px', borderRadius: '5px', marginBottom: '20px', border: '1px solid rgba(0, 100, 200, 0.15)', color: 'inherit' }}>

**প্রশ্ন ১: React-এ স্টেট "স্ন্যাপশট" বলতে কী বোঝায়?**  
উত্তর: এর মানে হল প্রতিটি কম্পোনেন্ট রেন্ডারে স্টেটের মান সেই রেন্ডারের সময়ের মতো দেখা যায়। স্টেট আপডেট নতুন রেন্ডার শিডিউল করে, কিন্তু বর্তমান রেন্ডার পরবর্তী রেন্ডার না হওয়া পর্যন্ত পুরনো মান "দেখে"।

```jsx
function Counter() {
  const [count, setCount] = useState(0);
  
  // This will always show the count from this render
  console.log('Current count:', count);
  
  return (
    <button onClick={() => {
      setCount(count + 1);
      // This still shows the old count
      console.log('After setCount:', count);
    }}>
      Count: {count}
    </button>
  );
}
```

**প্রশ্ন ২: setState কল করার পর স্টেট সাথে সাথে আপডেট হয় না কেন?**  
উত্তর: পারফরম্যান্সের জন্য React স্টেট আপডেটগুলো ব্যাচ করে। নতুন মান পরবর্তী রেন্ডারে পাওয়া যায়, setState কল করার সাথে সাথে নয়।

```jsx
function Example() {
  const [value, setValue] = useState(0);
  
  const handleClick = () => {
    setValue(42);
    console.log(value); // Still 0, not 42!
  };
}
```

**প্রশ্ন ৩: React কিভাবে ঠিক করে কখন কম্পোনেন্ট রি-রেন্ডার করবে?**  
উত্তর: যখন আপনি স্টেট আপডেটার ফাংশন (যেমন setState) কল করেন, React সেই কম্পোনেন্টের জন্য নতুন স্টেট মান দিয়ে রি-রেন্ডার শিডিউল করে।

```jsx
function Component() {
  const [state, setState] = useState(0);
  
  // This schedules a re-render
  setState(1);
  
  // The component will re-render with state = 1
  return <div>{state}</div>;
}
```

</div>

---

## ২.২ – মধ্যবর্তী ও সাধারণ ভুল 💡

<div style={{ background: 'rgba(230, 150, 70, 0.10)', padding: '15px', borderRadius: '5px', marginBottom: '20px', border: '1px solid rgba(230, 150, 70, 0.15)', color: 'inherit' }}>

**প্রশ্ন ৪: বর্তমান স্টেট মান ব্যবহার করে একাধিকবার setState কল করলে কী হয়?**  
উত্তর: প্রতিটি কল বর্তমান রেন্ডারের স্টেট স্ন্যাপশট ব্যবহার করে, তাই শুধু শেষ আপডেটটি কার্যকর হয়। সর্বশেষ মানের উপর ভিত্তি করে আপডেট করতে, updater ফাংশন ফর্ম ব্যবহার করুন: `setState(prev => prev + 1)`।

```jsx
// ❌ Wrong way - only increases by 1
setCount(count + 1);
setCount(count + 1);
setCount(count + 1);

// ✅ Correct way - increases by 3
setCount(c => c + 1);
setCount(c => c + 1);
setCount(c => c + 1);
```

**প্রশ্ন ৫: async কোডে (যেমন setTimeout) স্টেট সাথে সাথে আপডেট হবে ধরে নিতে পারবেন?**  
উত্তর: না। async কোড callback তৈরি হওয়ার সময়ের স্টেট মান "দেখবে", সর্বশেষ মান নয়। সর্বশেষ মানের জন্য updater ফাংশন বা refs ব্যবহার করুন।

```jsx
// ❌ Wrong way
setTimeout(() => {
  console.log(count); // Shows old value
}, 1000);

// ✅ Correct way
setTimeout(() => {
  setCount(c => {
    console.log(c); // Shows latest value
    return c + 1;
  });
}, 1000);
```

**প্রশ্ন ৬: ইভেন্ট হ্যান্ডলার লেখার সময় স্টেট স্ন্যাপশট বোঝা কেন গুরুত্বপূর্ণ?**  
উত্তর: কারণ ইভেন্ট হ্যান্ডলার তাদের রেন্ডারের সময়ের স্টেট ক্যাপচার করে। হ্যান্ডলার তৈরি হওয়ার পর স্টেট পরিবর্তন হলেও, পরবর্তী রেন্ডার না হওয়া পর্যন্ত হ্যান্ডলার পুরনো মান "মনে রাখে"।

```jsx
function Counter() {
  const [count, setCount] = useState(0);
  
  const handleClick = () => {
    // This handler "remembers" count from when it was created
    console.log('Count when handler was created:', count);
    setCount(count + 1);
  };
  
  return <button onClick={handleClick}>Increment</button>;
}
```

</div>

---

## ২.৩ – উন্নত ও বাস্তব জীবনের পরিস্থিতি 🧠

<div style={{ background: 'rgba(220, 50, 50, 0.10)', padding: '15px', borderRadius: '5px', marginBottom: '20px', border: '1px solid rgba(220, 50, 50, 0.15)', color: 'inherit' }}>

**প্রশ্ন ৭: পরিস্থিতি: আপনার একটি কাউন্টার আছে যার "+3" বাটনে setCount(count + 1) তিনবার কল করা হয়। বাটনে একবার ক্লিক করলে চূড়ান্ত মান কত হবে এবং কেন?**  
উত্তর: মান শুধু ১ বাড়বে, ৩ নয়, কারণ প্রতিটি setCount বর্তমান রেন্ডারের count স্ন্যাপশট ব্যবহার করে।

```jsx
function Counter() {
  const [count, setCount] = useState(0);
  
  const handleClick = () => {
    // ❌ Only increases by 1
    setCount(count + 1);
    setCount(count + 1);
    setCount(count + 1);
  };
  
  return <button onClick={handleClick}>+3</button>;
}
```

**প্রশ্ন ৮: উপরের পরিস্থিতিতে কাউন্টার ৩ বাড়ানোর জন্য কী করবেন?**  
উত্তর: updater ফাংশন ব্যবহার করুন: `setCount(c => c + 1)` তিনবার। প্রতিটি কল সর্বশেষ মান ব্যবহার করবে।

```jsx
function Counter() {
  const [count, setCount] = useState(0);
  
  const handleClick = () => {
    // ✅ Increases by 3
    setCount(c => c + 1);
    setCount(c => c + 1);
    setCount(c => c + 1);
  };
  
  return <button onClick={handleClick}>+3</button>;
}
```

**প্রশ্ন ৯: বাস্তব জীবন: আপনার একটি চ্যাট অ্যাপ আছে। ইউজার মেসেজ পাঠালে, আপনি ইনপুট খালি করে "মেসেজ পাঠানো হয়েছে!" নোটিফিকেশন দেখাতে চান। setState-এর পর সাথে সাথে স্টেট পড়লে কেন পুরনো মেসেজ নোটিফিকেশনে দেখা যেতে পারে?**  
উত্তর: কারণ স্টেট আপডেট সাথে সাথে হয় না; নোটিফিকেশন বর্তমান রেন্ডারের পুরনো মান দেখে। নতুন স্টেটের প্রতিক্রিয়া জানার জন্য useEffect বা updater ফাংশন ব্যবহার করুন।

```jsx
function ChatApp() {
  const [message, setMessage] = useState('');
  const [notification, setNotification] = useState('');
  
  const handleSend = () => {
    // ❌ Wrong way
    setMessage('');
    setNotification(`Sent: ${message}`); // Shows old message
    
    // ✅ Correct way
    setMessage('');
    setNotification(`Sent: ${message}`);
    // Or use useEffect
    useEffect(() => {
      if (message === '') {
        setNotification('Message sent!');
      }
    }, [message]);
  };
}
```

**প্রশ্ন ১০: উন্নত: স্টেট স্ন্যাপশট ভুল বোঝার ফলে কলাবোরেটিভ বা রিয়েল-টাইম অ্যাপে কী ধরনের বাগ হতে পারে?**  
উত্তর: স্টেট আপডেট সাথে সাথে হবে ধরে নিলে, আপনি পরিবর্তন ওভাররাইট করতে পারেন, পুরনো ডেটা দেখাতে পারেন, বা রেস কন্ডিশন তৈরি করতে পারেন। async পরিস্থিতিতে সবসময় updater ফাংশন ব্যবহার করুন বা স্টেট সিঙ্ক্রোনাইজ করুন।

```jsx
function CollaborativeEditor() {
  const [content, setContent] = useState('');
  
  // ❌ Wrong way - might lose updates
  socket.on('update', (newContent) => {
    setContent(newContent);
  });
  
  // ✅ Correct way - preserves updates
  socket.on('update', (newContent) => {
    setContent(prev => {
      // Merge changes safely
      return mergeChanges(prev, newContent);
    });
  });
}
```

**প্রশ্ন ১১: পরিস্থিতি: আপনি একটি মাল্টি-স্টেপ ফর্ম বানাচ্ছেন। ইউজার "Next" ক্লিক করলে, আপনি বর্তমান স্টেপ ভ্যালিডেট করে ফর্ম স্টেট আপডেট করতে চান। কখনও কখনও ভ্যালিডেশন পুরনো ডেটা ব্যবহার করে। এটা কেন হয় এবং কীভাবে ঠিক করবেন?**  
উত্তর: এটা হয় কারণ ভ্যালিডেশন ফাংশন বর্তমান রেন্ডারের স্টেট স্ন্যাপশট ব্যবহার করে, সর্বশেষ স্টেট নয়। এটা ঠিক করতে, updater ফাংশন ব্যবহার করুন বা ভ্যালিডেশন লজিক useEffect-এ সরান যা স্টেট আপডেটের পর চলে।

```jsx
function MultiStepForm() {
  const [formData, setFormData] = useState({});
  const [currentStep, setCurrentStep] = useState(1);
  
  const handleNext = () => {
    // ❌ Wrong way - uses stale data
    if (validateStep(currentStep, formData)) {
      setCurrentStep(currentStep + 1);
    }
    
    // ✅ Correct way
    setFormData(prev => {
      if (validateStep(currentStep, prev)) {
        setCurrentStep(currentStep + 1);
      }
      return prev;
    });
  };
}
```

**প্রশ্ন ১২: উন্নত: একটি কলাবোরেটিভ ডকুমেন্ট এডিটরে, একাধিক ইউজার একই ডকুমেন্ট এডিট করতে পারে। React-এর স্ন্যাপশট আচরণ বিবেচনা করে, কীভাবে নিশ্চিত করবেন যে লোকাল স্টেট আপডেট এবং অন্য ইউজারদের রিমোট আপডেট সঠিকভাবে মার্জ হবে?**  
উত্তর: লোকাল এবং রিমোট পরিবর্তন মার্জ করার জন্য updater ফাংশন ব্যবহার করুন, এবং কনকারেন্ট আপডেট নিরাপদে হ্যান্ডেল করার জন্য স্টেট ম্যানেজমেন্ট লাইব্রেরি বা CRDTs (Conflict-free Replicated Data Types) ব্যবহার করুন।

```jsx
function DocumentEditor() {
  const [document, setDocument] = useState('');
  
  // Handle local changes
  const handleLocalChange = (change) => {
    setDocument(prev => mergeChanges(prev, change));
  };
  
  // Handle remote changes
  socket.on('remoteChange', (change) => {
    setDocument(prev => mergeChanges(prev, change));
  });
}
```

**প্রশ্ন ১৩: পরিস্থিতি: আপনার একটি টাইমার কম্পোনেন্ট আছে যা প্রতি সেকেন্ডে স্টেট আপডেট করে। কখনও কখনও টাইমার এক সেকেন্ড স্কিপ করে বা ভুলভাবে আপডেট হয়। এটা কেন হতে পারে এবং কীভাবে ডিবাগ করবেন?**  
উত্তর: এটা হতে পারে যদি টাইমার callback স্টেটের পুরনো স্ন্যাপশট ব্যবহার করে। ইন্টারভাল বা টাইমআউটের ভিতরে setState-এ সবসময় updater ফাংশন ব্যবহার করুন যাতে সর্বশেষ মান পাবেন। callback-এর ভিতরে স্টেট লগ করে ডিবাগ করুন এবং প্রত্যাশিত মানের সাথে মিলছে কিনা দেখুন।

```jsx
function Timer() {
  const [seconds, setSeconds] = useState(0);
  
  useEffect(() => {
    const interval = setInterval(() => {
      // ❌ Wrong way
      setSeconds(seconds + 1);
      
      // ✅ Correct way
      setSeconds(s => s + 1);
    }, 1000);
    
    return () => clearInterval(interval);
  }, []);
}
```

**প্রশ্ন ১৪: উন্নত: এমন পরিস্থিতি হ্যান্ডেল করবেন কীভাবে যেখানে স্টেট আপডেট এবং কম্পোনেন্ট রি-রেন্ডারের পর সাথে সাথে একটি অ্যাকশন করতে হবে?**  
উত্তর: স্টেট ভেরিয়েবল ডিপেন্ডেন্সি হিসেবে ব্যবহার করে useEffect হুক ব্যবহার করুন। এতে নিশ্চিত হবে যে আপনার ইফেক্ট স্টেট আপডেট এবং রি-রেন্ডারের পর চলে।

```jsx
function Component() {
  const [data, setData] = useState(null);
  
  // This runs after data updates and re-render
  useEffect(() => {
    if (data) {
      // Perform action with new data
      processData(data);
    }
  }, [data]);
  
  return <div>{/* ... */}</div>;
}
```

</div>

---

<Callout type="info">এই প্রশ্নগুলো রিভিউ করুন এবং ইন্টারভিউ প্রস্তুতির জন্য নিজের ভাষায় কনসেপ্টগুলো ব্যাখ্যা করার চেষ্টা করুন!</Callout>

---

## অনুশীলনী প্রশ্ন 🏋️‍♂️

<div style={{ background: 'rgba(20, 120, 220, 0.10)', padding: '20px', borderRadius: '5px', marginBottom: '20px', border: '1px solid rgba(20, 120, 220, 0.15)', color: 'inherit' }}>

### পরিস্থিতি-ভিত্তিক প্রশ্ন:

আপনি একটি টু-ডু অ্যাপ বানাচ্ছেন। ইউজার নতুন টাস্ক যোগ করলে, আপনি ইনপুট খালি করে কনফার্মেশন মেসেজ দেখাতে চান। কখনও কখনও কনফার্মেশন মেসেজে নতুন টাস্কের পরিবর্তে পুরনো টাস্ক দেখা যায়।

**প্রশ্ন:** এটা কেন হয় এবং স্টেট স্ন্যাপশটের জ্ঞান ব্যবহার করে কীভাবে ঠিক করবেন?

উত্তর নিজে ভাবুন, তারপর নিচের উত্তর দেখুন।

<details>
<summary>**উত্তরের নমুনা দেখুন**</summary>

**সমাধান:**
```jsx
function TodoApp() {
  const [task, setTask] = useState('');
  const [confirmation, setConfirmation] = useState('');
  
  const handleAddTask = () => {
    // ❌ Wrong way
    setTask('');
    setConfirmation(`Added: ${task}`); // Shows old task
    
    // ✅ Correct way
    setTask('');
    setConfirmation(`Added: ${task}`);
    
    // Or use useEffect
    useEffect(() => {
      if (task === '') {
        setConfirmation('Task added successfully!');
      }
    }, [task]);
  };
  
  return (
    <div>
      <input value={task} onChange={e => setTask(e.target.value)} />
      <button onClick={handleAddTask}>Add Task</button>
      {confirmation && <p>{confirmation}</p>}
    </div>
  );
}
```

কনফার্মেশন মেসেজ পুরনো টাস্ক দেখায় কারণ স্টেট আপডেট সাথে সাথে হয় না; রেন্ডার এখনও পুরনো মান "দেখে"। এটা ঠিক করতে, স্টেট পরিবর্তনের প্রতিক্রিয়া জানার জন্য useEffect ব্যবহার করুন, বা সর্বশেষ মান নিশ্চিত করার জন্য updater ফাংশন ব্যবহার করুন।

</details>
</div>

---

**টিপ:** এই প্রশ্ন-উত্তরগুলো রিভিউ করুন এবং ইন্টারভিউ প্রস্তুতির জন্য নিজের ভাষায় প্রতিটি কনসেপ্ট ব্যাখ্যা করার চেষ্টা করুন!

> **🚀 চূড়ান্ত পরামর্শ:** ইন্টারভিউতে শুধু স্টেট স্ন্যাপশটের জ্ঞান নয়, বাস্তব জীবন, async, এবং কলাবোরেটিভ পরিস্থিতিতে এই বোধগম্যতা প্রয়োগ করার ক্ষমতাও দেখান।
