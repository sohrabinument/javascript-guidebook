import { Callout } from "nextra/components";

<img src="https://upload.wikimedia.org/wikipedia/commons/a/a7/React-icon.svg" alt="React Logo" width="70" style={{marginBottom: '1rem'}} />

# স্টেটে অ্যারে আপডেট – ইন্টারভিউ প্রশ্ন ও উত্তর 🎯

এই প্রশ্নগুলো ব্যবহার করুন ইন্টারভিউ প্রস্তুতির জন্য বা React স্টেটে অ্যারে আপডেট সম্পর্কে আপনার বোঝা যাচাই করতে।

> **💡 প্রো টিপ:** প্রতিটি প্রশ্নের উত্তর নিজে দেয়ার চেষ্টা করুন, তারপর উত্তর দেখুন। এতে মনে রাখতে সুবিধা হবে!

---

## ২.১ – অ্যারে স্টেটের বেসিক 🧩

<div style={{ background: 'rgba(0, 100, 200, 0.10)', padding: '15px', borderRadius: '5px', marginBottom: '20px', border: '1px solid rgba(0, 100, 200, 0.15)', color: 'inherit' }}>

**প্রশ্ন ১: কেন আপনি React স্টেটের অ্যারেতে আইটেম যোগ করতে `push()` ব্যবহার করবেন না?**  
উত্তর: কারণ `push()` মূল অ্যারে মিউটেট করে, React পরিবর্তন ধরতে পারে না। সবসময় নতুন অ্যারে তৈরি করুন।

**প্রশ্ন ২: কীভাবে স্টেটের অ্যারেতে আইটেম যোগ করবেন?**  
উত্তর:
```jsx
setItems([
  ...items,
  { id: nextId++, name: name }
]);
```

**প্রশ্ন ৩: কীভাবে স্টেটের অ্যারে থেকে আইটেম বাদ দেবেন?**  
উত্তর:
```jsx
setItems(items.filter(item => item.id !== removeId));
```

**প্রশ্ন ৪: কীভাবে স্টেটের অ্যারেতে কোনো আইটেম আপডেট করবেন?**  
উত্তর:
```jsx
setItems(items.map(item =>
  item.id === updateId ? { ...item, name: 'New Name' } : item
));
```

</div>

---

## ২.২ – অ্যারে মেথড ও সতর্কতা ⚠️

<div style={{ background: 'rgba(230, 150, 70, 0.10)', padding: '15px', borderRadius: '5px', marginBottom: '20px', border: '1px solid rgba(230, 150, 70, 0.15)', color: 'inherit' }}>

**প্রশ্ন ৫: `slice` ও `splice`-এর মধ্যে পার্থক্য কী?**  
উত্তর: `slice` নতুন অ্যারে দেয় এবং মূল অ্যারে বদলায় না। `splice` মূল অ্যারে বদলায়। React স্টেটের জন্য `slice` ব্যবহার করুন!

**প্রশ্ন ৬: কীভাবে স্টেটের অ্যারেতে নির্দিষ্ট পজিশনে আইটেম ইনসার্ট করবেন?**  
উত্তর:
```jsx
const insertAt = 2;
setItems([
  ...items.slice(0, insertAt),
  newItem,
  ...items.slice(insertAt)
]);
```

**প্রশ্ন ৭: কীভাবে স্টেটের অ্যারে মিউটেশন ছাড়া sort করবেন?**  
উত্তর:
```jsx
setItems([...items].sort((a, b) => a.value - b.value));
```

**প্রশ্ন ৮: স্টেটের অ্যারে সরাসরি মিউটেট করলে কী ঝুঁকি আছে?**  
উত্তর: React রি-রেন্ডার নাও করতে পারে, বাগ ও অপ্রত্যাশিত UI আসতে পারে।

</div>

---

## ২.৩ – নেস্টেড ও অ্যাডভান্সড আপডেট 🏗️

<div style={{ background: 'rgba(20, 120, 220, 0.10)', padding: '20px', borderRadius: '5px', marginBottom: '20px', border: '1px solid rgba(20, 120, 220, 0.15)', color: 'inherit' }}>

**প্রশ্ন ৯: কীভাবে স্টেটের অবজেক্টের ভিতরের নেস্টেড অ্যারে আপডেট করবেন?**  
উত্তর:
```jsx
setTodos(todos.map(todo =>
  todo.id === 1
    ? {
        ...todo,
        tasks: todo.tasks.map(task =>
          task.id === 1 ? { ...task, text: 'Updated' } : task
        )
      }
    : todo
));
```

**প্রশ্ন ১০: কীভাবে একসাথে একাধিক আইটেম আপডেট করবেন?**  
উত্তর:
```jsx
setItems(items.map(item =>
  item.category === 'Dairy' ? { ...item, checked: true } : item
));
```

**প্রশ্ন ১১: খুব জটিল বা ডিপলি নেস্টেড স্টেট আপডেট কীভাবে করবেন?**  
উত্তর: [Immer](https://immerjs.github.io/immer/) লাইব্রেরি ব্যবহার করুন বা স্টেট ছোট ছোট অংশে ভাগ করুন।

</div>

---

## ২.৪ – সিনারিও ও প্র্যাকটিক্যাল উদাহরণ 🏆

<div style={{ background: 'rgba(0, 180, 120, 0.10)', padding: '20px', borderRadius: '5px', marginBottom: '20px', border: '1px solid rgba(0, 180, 120, 0.15)', color: 'inherit' }}>

### সিনারিও-ভিত্তিক প্রশ্ন:

ধরা যাক, আপনার স্টেটে একটি shopping cart অ্যারে আছে। কীভাবে নির্দিষ্ট আইটেমের quantity বাড়াবেন?

উত্তর দেখার আগে নিজে ভাবুন।

<details>
<summary>**উত্তর দেখতে ক্লিক করুন**</summary>

**উত্তর:**
```jsx
setCart(cart.map(item =>
  item.id === targetId ? { ...item, quantity: item.quantity + 1 } : item
));
```
</details>

---

ধরা যাক, আপনি cart থেকে quantity 0 এমন সব আইটেম বাদ দিতে চান। কীভাবে করবেন?

<details>
<summary>**উত্তর দেখতে ক্লিক করুন**</summary>

**উত্তর:**
```jsx
setCart(cart.filter(item => item.quantity > 0));
```
</details>

---

কীভাবে অ্যারেতে নির্দিষ্ট আইটেমের জন্য কোনো boolean property (যেমন `selected`) টগল করবেন?

<details>
<summary>**উত্তর দেখতে ক্লিক করুন**</summary>

**উত্তর:**
```jsx
setItems(items.map(item =>
  item.id === targetId ? { ...item, selected: !item.selected } : item
));
```
</details>

</div>

---

<Callout type="info">এই প্রশ্নগুলো পড়ে নিজে নিজের ভাষায় ব্যাখ্যা দেয়ার চেষ্টা করুন—ইন্টারভিউ প্রস্তুতির জন্য দারুণ অনুশীলন!</Callout>

---

> **🚀 চূড়ান্ত পরামর্শ:** ইন্টারভিউতে শুধু অ্যারে আপডেটের জ্ঞান নয়, পরিষ্কার, রক্ষণাবেক্ষণযোগ্য ও ব্যবহারবান্ধব কোড লেখার দক্ষতাও দেখান।

<img src="https://upload.wikimedia.org/wikipedia/commons/a/a7/React-icon.svg" alt="React Interview Success" width="100" style={{borderRadius: '8px', marginTop: '2rem'}} />
