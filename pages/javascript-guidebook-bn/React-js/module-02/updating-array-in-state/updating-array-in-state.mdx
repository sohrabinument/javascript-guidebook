import { Callout } from 'nextra/components';

<img src="https://upload.wikimedia.org/wikipedia/commons/a/a7/React-icon.svg" alt="React Logo" width="70" style={{marginBottom: '1rem'}} />

# স্টেটে অ্যারে আপডেট করা – একদম সহজভাবে! 🥳

স্বাগতম! যদি কখনো React স্টেটে অ্যারে নিয়ে দ্বিধায় পড়ে থাকেন, তাহলে আপনি সঠিক জায়গায় এসেছেন। এই গাইডটি সবার জন্য—বিশেষ করে যদি আপনি নিজেকে "খারাপ ছাত্র" মনে করেন। চলুন এত সহজ ও মজার করে শিখি, বারবার পড়তে ইচ্ছা করবেন!

---

## 🎯 আপনি যা শিখবেন
- কেন স্টেটের অ্যারে সরাসরি পরিবর্তন করা উচিত নয়
- কীভাবে React-এর নিয়মে অ্যারেতে আইটেম যোগ, বাদ, ও আপডেট করবেন
- কীভাবে অ্যারের ভিতরের অবজেক্ট আপডেট করবেন
- কীভাবে নেস্টেড অ্যারে ও জটিল আপডেট করবেন
- প্রো টিপস, সাধারণ ভুল, ও আরও অনেক কিছু!

---

<Callout type="info">**উপমা:** আপনার React স্টেটকে একটি ফটো অ্যালবামের মতো ভাবুন। আপনি যদি নতুন ছবি যোগ বা মুছতে চান, তাহলে পরিবর্তনসহ অ্যালবামের নতুন কপি বানান। পুরনো অ্যালবামে কখনো আঁকিবুকি করবেন না!</Callout>

---

## ১. শুধু অ্যারে পরিবর্তন করলেই হবে না কেন? 🤔

ধরা যাক আপনার কোড এমন:

```jsx
const [artists, setArtists] = useState([]);
```

আপনি হয়তো ভাবছেন, এমন করতে পারেন:

```jsx
artists.push({ id: 1, name: 'Picasso' }); // ❌
```

কিন্তু React বুঝবেই না! এটা যেন গোপনে অ্যালবামে ছবি যোগ করা—কেউ জানবেই না।

---

## ২. সঠিক উপায়: নতুন অ্যারে তৈরি করুন! 📝

**সবসময় নতুন অ্যারে তৈরি করুন এবং সেটার মাধ্যমে স্টেট আপডেট করুন।**

### আইটেম যোগ করা:

```jsx
setArtists([
  ...artists,
  { id: nextId++, name: name }
]);
```

### আইটেম বাদ দেয়া:

```jsx
setArtists(artists.filter(a => a.id !== removeId));
```

### আইটেম আপডেট করা:

```jsx
setArtists(artists.map(a =>
  a.id === updateId ? { ...a, name: 'New Name' } : a
));
```

<Callout type="tip">**প্রো টিপ:** <b>স্প্রেড সিনট্যাক্স</b> (`...`) ব্যবহার করে অ্যারে ও অবজেক্ট কপি করুন। এটাই React-এর নিয়ম!</Callout>

---

## ৩. ধাপে ধাপে: যোগ, বাদ, ও আপডেট 🎯

### অ্যারেতে যোগ করা

```jsx
setArtists([
  ...artists,
  { id: nextId++, name: name }
]);
```
- `...artists` পুরনো সব আইটেম কপি করে
- `{ id: nextId++, name: name }` নতুন আইটেম
- নতুন অ্যারে স্টেটে সেট হয়

### অ্যারে থেকে বাদ দেয়া

```jsx
setArtists(artists.filter(a => a.id !== removeId));
```
- `filter` নতুন অ্যারে বানায়, যেটাতে বাদ দেয়া আইটেম নেই

### অ্যারেতে আইটেম আপডেট

```jsx
setArtists(artists.map(a =>
  a.id === updateId ? { ...a, name: 'New Name' } : a
));
```
- `map` নতুন অ্যারে বানায়
- id মিলে গেলে নতুন অবজেক্ট ফেরত দেয়
- না মিললে আগেরটাই ফেরত দেয়

---

## ৪. সাধারণ ভুল ও সতর্কতা ⚠️

<Callout type="warning">**`push()`, `pop()`, `splice()`, বা সরাসরি অ্যাসাইনমেন্ট (`arr[0] = ...`) ব্যবহার করবেন না।** সবসময় `map`, `filter`, `concat`, বা স্প্রেড সিনট্যাক্স ব্যবহার করুন।</Callout>

- `slice` নিরাপদ (কপি করে), কিন্তু `splice` অ্যারে বদলে দেয়! মনে রাখুন: **slice = নিরাপদ, splice = বদলায়**

---

## ৫. অ্যারের ভিতরের অবজেক্ট আপডেট 🏗️

যদি অ্যারের মধ্যে অবজেক্ট থাকে এবং শুধু একটি অবজেক্ট আপডেট করতে চান:

```jsx
setList(list.map(item =>
  item.id === targetId ? { ...item, seen: true } : item
));
```
- এটা নতুন অ্যারে বানায়
- শুধু মিল থাকা অবজেক্ট কপি ও আপডেট হয়
- বাকি সব অপরিবর্তিত থাকে

---

## ৬. উন্নত উদাহরণ 🚀

### নেস্টেড অ্যারে আপডেট

```jsx
const [todos, setTodos] = useState([
  {
    id: 1,
    title: "Learn React",
    tasks: [
      { id: 1, text: "Read docs" },
      { id: 2, text: "Build app" }
    ]
  }
]);

// নেস্টেড টাস্ক আপডেট
setTodos(todos.map(todo => 
  todo.id === 1 
    ? {
        ...todo,
        tasks: todo.tasks.map(task =>
          task.id === 1 ? { ...task, text: "Read docs carefully" } : task
        )
      }
    : todo
));
```

### একাধিক স্টেট আপডেট

```jsx
const [items, setItems] = useState([]);
const [selectedId, setSelectedId] = useState(null);

// একসাথে একাধিক স্টেট আপডেট
function handleItemSelect(id) {
  setSelectedId(id);
  setItems(items.map(item =>
    item.id === id ? { ...item, selected: true } : { ...item, selected: false }
  ));
}
```

### জটিল অ্যারে ট্রান্সফরমেশন

```jsx
const [shoppingList, setShoppingList] = useState([
  { id: 1, name: "Milk", quantity: 2, category: "Dairy" },
  { id: 2, name: "Bread", quantity: 1, category: "Bakery" }
]);

// ক্যাটাগরি অনুযায়ী গ্রুপ
const groupedItems = shoppingList.reduce((acc, item) => {
  if (!acc[item.category]) {
    acc[item.category] = [];
  }
  acc[item.category].push(item);
  return acc;
}, {});

// একাধিক আইটেম একসাথে আপডেট
setShoppingList(shoppingList.map(item =>
  item.category === "Dairy" 
    ? { ...item, quantity: item.quantity + 1 }
    : item
));
```

### অ্যারে মেথড নিয়ে কাজ

```jsx
const [numbers, setNumbers] = useState([1, 2, 3, 4, 5]);

// একসাথে filter ও map
setNumbers(numbers
  .filter(n => n > 2)
  .map(n => n * 2)
);

// মিউটেশন ছাড়া sort
setNumbers([...numbers].sort((a, b) => a - b));

// খুঁজে আপডেট
setNumbers(numbers.map(n =>
  n === 3 ? 6 : n
));
```

---

## ৭. প্রো টিপস ও ট্রিক্স 💡

- স্টেটের অ্যারে সবসময় **read-only** ভাবুন
- আপডেটের সময় নতুন অ্যারে বানান
- আপডেটের জন্য `map`, বাদ দেয়ার জন্য `filter`, যোগের জন্য স্প্রেড (`...`) ব্যবহার করুন
- ডিপ আপডেটের জন্য [Immer](https://immerjs.github.io/immer/) ব্যবহার করুন
- নতুন অ্যারে ফেরত দেয় এমন মেথড ব্যবহার করুন: `map`, `filter`, `slice`, `concat`
- মিউটেট করে এমন মেথড এড়িয়ে চলুন: `push`, `pop`, `splice`, `sort`, `reverse`
- জটিল স্টেট আপডেটের জন্য রিডিউসার ব্যবহার করতে পারেন

---

## ৮. রিক্যাপ ও চিটশিট 📝

- কখনোই স্টেটের অ্যারে মিউটেট করবেন না
- যোগ করতে `[...arr, newItem]` ব্যবহার করুন
- বাদ দিতে `filter` ব্যবহার করুন
- আপডেট করতে `map` ব্যবহার করুন
- জটিল আপডেটের জন্য Immer ব্যবহার করুন
- মনে রাখুন: slice = নিরাপদ, splice = বদলায়
- নেস্টেড আপডেটের জন্য nested map/filter ব্যবহার করুন
- একাধিক আপডেটের জন্য রিডিউসার ব্যবহার করুন

---

## ৯. FAQ 🤓

**প্রশ্ন: যদি আমি স্টেটের অ্যারে মিউটেট করি কী হবে?**
উত্তর: React রি-রেন্ডার করবে না, এবং অদ্ভুত বাগ আসতে পারে!

**প্রশ্ন: React কেন ইমিউটেবিলিটি নিয়ে এত গুরুত্ব দেয়?**
উত্তর: এতে React সহজে UI আপডেট করতে পারে, ডিবাগ সহজ হয়, এবং undo/redo-এর মতো ফিচার সম্ভব হয়।

**প্রশ্ন: আমি কি কপি করার পর মিউটেটিং মেথড ব্যবহার করতে পারি?**
উত্তর: হ্যাঁ, যতক্ষণ না আপনি মূল স্টেট অ্যারে মিউটেট করছেন।

**প্রশ্ন: খুব জটিল নেস্টেড অ্যারে কীভাবে হ্যান্ডেল করব?**
উত্তর: Immer ব্যবহার করুন বা স্টেট ছোট ছোট অংশে ভাগ করুন।

**প্রশ্ন: একসাথে একাধিক আইটেম আপডেটের সেরা উপায় কী?**
উত্তর: `map`-এ কন্ডিশন ব্যবহার করুন, অথবা জটিল হলে রিডিউসার ব্যবহার করুন।

---

<Callout type="info">
  <b>নিজে চেষ্টা করুন:</b>
  <ul>
    <li>একটা লিস্ট বানিয়ে React-এর নিয়মে আইটেম যোগ, বাদ, ও আপডেট করুন</li>
    <li>`push()` বা `splice()` ব্যবহার করে দেখুন কী হয়</li>
    <li>তারপর `map`, `filter`, ও স্প্রেড সিনট্যাক্স দিয়ে চেষ্টা করুন</li>
    <li>ফারাকটা লক্ষ্য করুন!</li>
  </ul>
</Callout>

---

> **🚀 চূড়ান্ত পরামর্শ:** স্টেটের অ্যারে নিয়ে ভয় পাবেন না! শুধু মনে রাখুন: কপি করুন, তারপর আপডেট করুন। আপনি পারবেন!
