import { Callout } from "nextra/components";

<img src="https://upload.wikimedia.org/wikipedia/commons/a/a7/React-icon.svg" alt="React Logo" width="70" style={{marginBottom: '1rem'}} />

# 🔄 রিঅ্যাক্টে ইভেন্ট প্রোপাগেশন

> **👋 সহজ পরিচিতি:**
>
> আপনার অ্যাপকে ইন্টারঅ্যাকটিভ করতে চান? রিঅ্যাক্টে ইভেন্ট কীভাবে প্রবাহিত হয়, তা জানা খুবই গুরুত্বপূর্ণ! এই গাইডে আপনি ইভেন্ট প্রোপাগেশন, ডেলিগেশন, ক্যাপচারিং, এবং ইভেন্ট থামানোর কৌশল শিখবেন—সহজ ভাষায়, ডায়াগ্রাম ও প্র্যাকটিক্যাল টিপসসহ। অনুপ্রেরণা: [React অফিসিয়াল ডকুমেন্টেশন](https://react.dev/learn/responding-to-events#event-propagation)।

---

## ইভেন্ট প্রোপাগেশন কী? 🤔

ইভেন্ট প্রোপাগেশন হলো এমন একটি প্রক্রিয়া, যেখানে ক্লিক, কী-প্রেস ইত্যাদি ইভেন্ট DOM ট্রির মধ্যে একাধিক ধাপে প্রবাহিত হয়। এই প্রক্রিয়ায় **তিনটি ধাপ** থাকে:

1. **ক্যাপচারিং ফেজ:** ইভেন্টটি DOM-এর রুট থেকে শুরু হয়ে টার্গেট এলিমেন্ট পর্যন্ত _নিচে_ নামে। রিঅ্যাক্টে `onClickCapture`-এর মতো প্রপ দিয়ে এই ফেজে ইভেন্ট শোনা যায়।
2. **টার্গেট ফেজ:** ইভেন্টটি টার্গেট এলিমেন্টে পৌঁছে (যেটিতে আপনি ক্লিক করেছেন)।
3. **বাবলিং ফেজ:** এরপর ইভেন্টটি টার্গেট থেকে রুট পর্যন্ত _উপরে_ উঠে যায়। রিঅ্যাক্টে বেশিরভাগ ইভেন্ট হ্যান্ডলার (যেমন `onClick`) ডিফল্টভাবে এই ফেজে কাজ করে।

<img src="/dom-event-flow.svg" alt="DOM Event Flow Diagram" width="600" style={{display:'block',margin:'2rem auto'}} />

<Callout type="info">রিঅ্যাক্টে বেশিরভাগ ইভেন্ট হ্যান্ডলার (<code>onClick</code> এর মতো) <b>বাবলিং ফেজে</b> কাজ করে। তবে আপনি <code>onClickCapture</code> ব্যবহার করে ক্যাপচারিং ফেজেও ইভেন্ট শুনতে পারেন।</Callout>

**বাস্তব উদাহরণ:**
ধরুন, আপনি কনসার্টে আছেন। ভিড়ের মধ্যে কেউ (টার্গেট) চিৎকার করলে, সেই শব্দ প্রথমে আশেপাশের লোকদের কাছে পৌঁছায় (বাবলিং—প্যারেন্টদের কাছে), তারপর নিরাপত্তারক্ষীদের (রুট) কাছে যায়। নিরাপত্তারক্ষীরা যদি চিৎকার শোনার জন্য আগে থেকেই প্রস্তুত থাকেন, সেটাই ক্যাপচারিং ফেজ!

**কেন প্রোপাগেশন গুরুত্বপূর্ণ?**
- আপনার UI-এর বিভিন্ন লেভেলে ইভেন্ট হ্যান্ডলিং করতে পারবেন।
- `e.stopPropagation()` দিয়ে ইভেন্ট প্যারেন্টে যেতে বাধা দিতে পারবেন।
- ক্যাপচারিং ব্যবহার করে ইভেন্ট টার্গেটে পৌঁছানোর আগেই ধরতে পারবেন।

---

## ইভেন্ট ডেলিগেশন 🏷️ (ডিটেইলসে)

ইভেন্ট ডেলিগেশন হলো এমন একটি কৌশল, যেখানে আপনি একটি প্যারেন্ট এলিমেন্টে একটি ইভেন্ট হ্যান্ডলার অ্যাটাচ করেন এবং সেটি দিয়ে সব চাইল্ডের ইভেন্ট ম্যানেজ করেন—even যদি চাইল্ড ডাইনামিকভাবে যোগ হয়।

**কীভাবে কাজ করে?**
- বাবলিংয়ের কারণে, চাইল্ডে ইন্টারঅ্যাকশন হলে ইভেন্টটি প্যারেন্ট পর্যন্ত পৌঁছে যায়। প্যারেন্টের হ্যান্ডলার চেক করতে পারে, কোন চাইল্ডে ইভেন্ট হয়েছে এবং সেই অনুযায়ী রেসপন্ড করতে পারে।

**কেন দরকার?**
- **পারফরম্যান্স:** প্রতিটি চাইল্ডে আলাদা হ্যান্ডলার লাগাতে হয় না। বড় বা ডাইনামিক লিস্টের জন্য খুবই উপকারী।
- **ডাইনামিক UI:** চাইল্ড এলিমেন্ট যোগ বা বাদ দিলে হ্যান্ডলার নিয়ে ভাবতে হয় না।
- **কোড পরিষ্কার:** ইভেন্ট লজিক এক জায়গায় থাকে।

**React প্র্যাকটিক্যাল উদাহরণ:**
```jsx
function TodoList({ todos, onToggle }) {
  const handleClick = (e) => {
    if (e.target.tagName === 'LI') {
      const id = e.target.getAttribute('data-id');
      onToggle(id);
    }
  };
  return (
    <ul onClick={handleClick}>
      {todos.map(todo => (
        <li key={todo.id} data-id={todo.id} style={{ cursor: 'pointer' }}>
          {todo.text}
        </li>
      ))}
    </ul>
  );
}
```

<Callout type="tip">ইভেন্ট ডেলিগেশন দিয়ে, সব <code>&lt;li&gt;</code> আইটেমের ক্লিক হ্যান্ডল করা যায়—লিস্ট চেঞ্জ হলেও—শুধু <code>&lt;ul&gt;</code>-এ একটি হ্যান্ডলার দিয়েই!</Callout>

**ভিজ্যুয়াল ডায়াগ্রাম:**

<img src="/event-delegation.svg" alt="Event Delegation Diagram" width="600" style={{display:'block',margin:'2rem auto'}} />

**সারাংশ:**
- ইভেন্ট প্রোপাগেশন ইভেন্টকে আপনার UI-তে প্রবাহিত হতে দেয়, ফলে আপনি বিভিন্নভাবে হ্যান্ডল করতে পারেন।
- ইভেন্ট ডেলিগেশন প্রোপাগেশনকে কাজে লাগিয়ে কোডকে আরও কার্যকর ও মেইনটেইনেবল করে তোলে, বিশেষ করে ডাইনামিক লিস্টের জন্য।

---

## বাবলিং বনাম ক্যাপচারিং: ভিজ্যুয়াল ডায়াগ্রাম

<img src="/bubbling-vs-capturing.svg" alt="Bubbling vs Capturing in React" width="700" style={{display:'block',margin:'2rem auto'}} />

---

## রিঅ্যাক্টে ইভেন্ট হ্যান্ডলার যোগ করা 🖱️

1. **একটি ফাংশন ডিফাইন করুন** আপনার কম্পোনেন্টে (যেমন `handleClick`)।
2. **প্রপ হিসেবে দিন** এলিমেন্টে (যেমন `onClick={handleClick}`)।

**উদাহরণ:**
```jsx
export default function Button() {
  function handleClick() {
    alert('You clicked me!');
  }
  return (
    <button onClick={handleClick}>Click me</button>
  );
}
```

<Callout type="tip">
সবসময় ফাংশন <b>পাস</b> করুন (যেমন, `onClick={handleClick}`), <b>কল</b> করবেন না (যেমন, `onClick={handleClick()}`), না হলে এটি সঙ্গে সঙ্গে রান করবে!
</Callout>

---

## ইভেন্ট ডেলিগেশন 🏷️

ইভেন্ট ডেলিগেশন মানে একটি প্যারেন্ট এলিমেন্টে একটি ইভেন্ট হ্যান্ডলার অ্যাটাচ করে সব চাইল্ডের ইভেন্ট হ্যান্ডল করা। এটা সম্ভব হয় বাবলিংয়ের জন্য।

**কেন ব্যবহার করবেন?**
- কম ইভেন্ট লিসেনার = ভালো পারফরম্যান্স
- ডাইনামিক লিস্ট ম্যানেজ করা সহজ

**উদাহরণ:**
```jsx
function List() {
  const handleClick = (e) => {
    if (e.target.tagName === 'LI') {
      alert('Clicked: ' + e.target.textContent);
    }
  };
  return (
    <ul onClick={handleClick}>
      <li>Apple</li>
      <li>Banana</li>
      <li>Cherry</li>
    </ul>
  );
}
```

<Callout type="tip">ইভেন্ট ডেলিগেশন দিয়ে, সব <code>&lt;li&gt;</code> আইটেমের ক্লিক হ্যান্ডল করা যায়—শুধু <code>&lt;ul&gt;</code>-এ একটি হ্যান্ডলার দিয়েই!</Callout>

---

## ইভেন্ট ক্যাপচারিং (ক্যাপচার ফেজ) 🕵️‍♂️

রিঅ্যাক্টে <code>onClickCapture</code> এর মতো প্রপ দিয়ে ক্যাপচার ফেজে ইভেন্ট শোনা যায়।

**উদাহরণ:**
```jsx
function Parent() {
  const handleCapture = () => {
    alert('Captured on parent!');
  };
  return (
    <div onClickCapture={handleCapture}>
      <button>Click Me</button>
    </div>
  );
}
```

<Callout type="info"><code>onClickCapture</code> <b>চাইল্ডের</b> <code>onClick</code> হ্যান্ডলারের আগেই ফায়ার হয়, এমনকি চাইল্ড <code>e.stopPropagation()</code> দিলেও।</Callout>

---

## প্রোপাগেশন থামানো 🚦

ইভেন্ট প্যারেন্টে যেতে না দিলে চাইলে, হ্যান্ডলারে <code>e.stopPropagation()</code> ব্যবহার করুন।

**উদাহরণ:**
```jsx
function Child({ children }) {
  const handleClick = (e) => {
    e.stopPropagation();
    alert('Child clicked!');
  };
  return <button onClick={handleClick}>{children}</button>;
}
```

<Callout type="warning"><code>e.stopPropagation()</code> ইভেন্টকে প্যারেন্টের <code>onClick</code> হ্যান্ডলারে যেতে বাধা দেয়, কিন্তু <b>onClickCapture</b> হ্যান্ডলারে নয়!</Callout>

---

## ডিফল্ট বিহেভিয়ার বন্ধ করা 🛑

কিছু ইভেন্টের ডিফল্ট ব্রাউজার অ্যাকশন থাকে (যেমন ফর্ম সাবমিশন)। <code>e.preventDefault()</code> দিয়ে তা বন্ধ করতে পারেন।

**উদাহরণ:**
```jsx
export default function Signup() {
  return (
    <form onSubmit={e => {
      e.preventDefault();
      alert('Submitting!');
    }}>
      <input />
      <button>Send</button>
    </form>
  );
}
```

<Callout type="info"><code>e.preventDefault()</code> ব্রাউজারের ডিফল্ট অ্যাকশন (যেমন ফর্ম সাবমিটে পেজ রিলোড) বন্ধ করে। এটি <b>ইভেন্ট প্রোপাগেশন বন্ধ করে না</b>।</Callout>

---

## সাধারণ ভুল ও বেস্ট প্র্যাকটিস ⚠️

- **JSX-এ ইভেন্ট হ্যান্ডলার সরাসরি কল করবেন না:**
  - ❌ `onClick={handleClick()}` (সঙ্গে সঙ্গে রান হয়)
  - ✅ `onClick={handleClick}` (ক্লিক করলে রান হয়)
- **`e.stopPropagation()` আর `e.preventDefault()` গুলিয়ে ফেলবেন না:**
  - `e.stopPropagation()` ইভেন্ট বাবলিং বন্ধ করে।
  - `e.preventDefault()` ডিফল্ট ব্রাউজার অ্যাকশন বন্ধ করে।
- **লিস্টে ইভেন্ট ডেলিগেশন ব্যবহার করুন:** প্রতিটি চাইল্ডে না দিয়ে প্যারেন্টে একটি হ্যান্ডলার দিন।
- **অ্যানালিটিক্স বা লগিংয়ের জন্য `onClickCapture` ব্যবহার করুন:** এটি বাবলিংয়ের আগেই ফায়ার হয় এবং চাইল্ড থামাতে পারে না।

---

## রিক্যাপ ও পরবর্তী ধাপ 🚀

- ইভেন্ট দুই ফেজে প্রবাহিত হয়: ক্যাপচার ও বাবল।
- ইভেন্ট হ্যান্ডলার দিয়ে ইউজার অ্যাকশনে রেসপন্ড করুন।
- `e.stopPropagation()` দিয়ে বাবলিং বন্ধ করুন, `e.preventDefault()` দিয়ে ডিফল্ট অ্যাকশন বন্ধ করুন।
- অ্যাডভান্সড কেসে ইভেন্ট ডেলিগেশন ও `onClickCapture` ব্যবহার করুন।

<Callout type="success">নিজের রিঅ্যাক্ট কম্পোনেন্টে এগুলো প্র্যাকটিস করুন! আরও জানতে দেখুন <a href="https://react.dev/learn/responding-to-events#event-propagation" target="_blank">React অফিসিয়াল ডকুমেন্টেশন</a>। 🎉</Callout>
