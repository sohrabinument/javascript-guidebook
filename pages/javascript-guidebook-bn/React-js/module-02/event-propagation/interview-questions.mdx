import { Callout } from "nextra/components";

<img src="https://upload.wikimedia.org/wikipedia/commons/a/a7/React-icon.svg" alt="React Logo" width="70" style={{marginBottom: '1rem'}} />

# ইভেন্ট প্রোপাগেশন – ইন্টারভিউ প্রশ্ন ও উত্তর 🎯

রিঅ্যাক্টে ইভেন্ট প্রোপাগেশন সম্পর্কে আপনার জ্ঞান যাচাই করতে বা ইন্টারভিউ প্রস্তুতির জন্য এই প্রশ্নগুলো ব্যবহার করুন।

> **💡 প্রো টিপ:** উত্তর দেখার আগে নিজে চেষ্টা করুন। এতে কনসেপ্টগুলো ভালোভাবে মনে থাকবে!

---

## ১.১ – ইভেন্ট প্রোপাগেশন বেসিক 🔄

<div style={{ background: 'rgba(0, 100, 200, 0.10)', padding: '15px', borderRadius: '5px', marginBottom: '20px', border: '1px solid rgba(0, 100, 200, 0.15)', color: 'inherit' }}>

**প্রশ্ন ১: রিঅ্যাক্টে ইভেন্ট প্রোপাগেশন কী?**  
উত্তর: ইভেন্ট প্রোপাগেশন হলো এমন একটি প্রক্রিয়া, যেখানে ইভেন্ট DOM ট্রির মধ্যে দুইটি ফেজে প্রবাহিত হয়: ক্যাপচারিং (রুট থেকে টার্গেট) এবং বাবলিং (টার্গেট থেকে রুট)।

**প্রশ্ন ২: রিঅ্যাক্টের `onClick` হ্যান্ডলার ডিফল্টভাবে কোন ফেজে কাজ করে?**  
উত্তর: রিঅ্যাক্টের `onClick` হ্যান্ডলার ডিফল্টভাবে বাবলিং ফেজে কাজ করে।

**প্রশ্ন ৩: রিঅ্যাক্টে ক্যাপচারিং ফেজে ইভেন্ট শোনার জন্য কী করতে হয়?**  
উত্তর: `onClick` এর পরিবর্তে `onClickCapture` এর মতো প্রপ ব্যবহার করতে হয়।

</div>

---

## ১.২ – প্রোপাগেশন থামানো ও ডিফল্ট অ্যাকশন বন্ধ করা 🚦

<div style={{ background: 'rgba(230, 150, 70, 0.10)', padding: '15px', borderRadius: '5px', marginBottom: '20px', border: '1px solid rgba(230, 150, 70, 0.15)', color: 'inherit' }}>

**প্রশ্ন ৪: ইভেন্ট প্যারেন্ট এলিমেন্টে যেতে না দিতে কী করতে হয়?**  
উত্তর: ইভেন্ট হ্যান্ডলারের ভিতরে `e.stopPropagation()` কল করতে হয়।

**প্রশ্ন ৫: `e.stopPropagation()` এবং `e.preventDefault()` এর মধ্যে পার্থক্য কী?**  
উত্তর: `e.stopPropagation()` DOM-এ ইভেন্ট বাবলিং বন্ধ করে, আর `e.preventDefault()` ব্রাউজারের ডিফল্ট অ্যাকশন (যেমন ফর্ম সাবমিশন) বন্ধ করে।

**প্রশ্ন ৬: `e.stopPropagation()` কি প্যারেন্টের `onClickCapture` ফায়ার হওয়া বন্ধ করতে পারে?**  
উত্তর: না, `onClickCapture` ক্যাপচার ফেজে ফায়ার হয়, বাবলিং এবং `stopPropagation()` এর আগে।

</div>

---

## ১.৩ – সিনারিও ও প্র্যাকটিক্যাল উদাহরণ 🏆

<div style={{ background: 'rgba(20, 120, 220, 0.10)', padding: '20px', borderRadius: '5px', marginBottom: '20px', border: '1px solid rgba(20, 120, 220, 0.15)', color: 'inherit' }}>

### সিনারিও-ভিত্তিক প্রশ্ন:

ধরুন, আপনার একটি প্যারেন্ট `<div>` আছে যার একটি `onClick` হ্যান্ডলার আছে, এবং একটি চাইল্ড `<button>` আছে যার নিজের `onClick` হ্যান্ডলার আছে। বাটনে ক্লিক করলে কী হবে? শুধু বাটনের হ্যান্ডলার রান করার জন্য কী করতে হবে?

উত্তর দেখার আগে একটু ভাবুন।

<details>
<summary>**উত্তর দেখতে ক্লিক করুন**</summary>

**উত্তর:**
- ডিফল্টভাবে, বাটন এবং প্যারেন্ট উভয়ের `onClick` হ্যান্ডলার রান হবে (প্রথমে বাটন, তারপর প্যারেন্ট) বাবলিংয়ের কারণে।
- শুধু বাটনের হ্যান্ডলার রান করার জন্য, বাটনের হ্যান্ডলারে `e.stopPropagation()` কল করতে হবে।

</details>
</div>

---

<Callout type="info">এই প্রশ্নগুলো রিভিউ করুন এবং নিজের ভাষায় কনসেপ্টগুলো ব্যাখ্যা করার চেষ্টা করুন—এটাই ইন্টারভিউ প্রস্তুতির সবচেয়ে ভালো উপায়!</Callout>

---

> **🚀 শেষ পরামর্শ:** ইন্টারভিউতে শুধু ইভেন্ট প্রোপাগেশন সম্পর্কে জ্ঞান দেখানোর পাশাপাশি, রিয়েল-ওয়ার্ল্ড সিনারিওতে ক্লিন, মেইনটেইনেবল এবং ইউজার-ফ্রেন্ডলি কোড লিখতে পারার দক্ষতাও দেখান।

<img src="https://upload.wikimedia.org/wikipedia/commons/a/a7/React-icon.svg" alt="React Interview Success" width="100" style={{borderRadius: '8px', marginTop: '2rem'}} />
