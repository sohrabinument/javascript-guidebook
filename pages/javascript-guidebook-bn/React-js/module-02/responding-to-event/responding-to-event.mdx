import { Callout } from "nextra/components";

<img src="https://upload.wikimedia.org/wikipedia/commons/a/a7/React-icon.svg" alt="React Logo" width="70" style={{marginBottom: '1rem'}} />

# 🖱️ রিঅ্যাক্টে ইভেন্টের প্রতিক্রিয়া

> **👋 বন্ধুত্বপূর্ণ পরিচিতি:**
>
> আপনার অ্যাপকে ইন্টারঅ্যাকটিভ করা মানে সেটিকে প্রাণবন্ত করে তোলা! রিঅ্যাক্টে, আপনি ইউজারের বিভিন্ন অ্যাকশনের (যেমন ক্লিক, টাইপিং, বা মাউস মুভমেন্ট) প্রতিক্রিয়া জানাতে ইভেন্ট হ্যান্ডল করতে পারেন। চলুন দেখি, রিঅ্যাক্টে কীভাবে সহজে ইভেন্ট হ্যান্ডল করা যায়! ⚡

---

## ইভেন্ট হ্যান্ডলার কী? 🤔

একটি **ইভেন্ট হ্যান্ডলার** হলো এমন একটি ফাংশন, যা আপনার UI-তে কিছু ঘটলে (যেমন, বাটন ক্লিক) রান হয়। রিঅ্যাক্টে, আপনি ইভেন্ট হ্যান্ডলারগুলোকে এলিমেন্টের প্রপ হিসেবে যোগ করেন, যেমন `onClick`, `onChange`, বা `onMouseEnter`।

---

## বাটনে ইভেন্ট হ্যান্ডলার যোগ করা 🛎️

এখানে দেখুন, কীভাবে একটি বাটনে ক্লিক ইভেন্ট হ্যান্ডল করা যায়:

```jsx
export default function Button() {
  // হ্যান্ডলার ফাংশন কম্পোনেন্টের ভিতরে ডিফাইন করুন
  const handleClick = () => {
    alert('You clicked me!');
  };

  return (
    <button onClick={handleClick}>
      Click me
    </button>
  );
}
```

<Callout type="tip">
**ফাংশন রেফারেন্স** (যেমন, `onClick={handleClick}`) প্রপ হিসেবে দিন, ফাংশন কল (যেমন, `onClick={handleClick()}`) নয়—নইলে এটি সাথে সাথে রান হয়ে যাবে!
</Callout>

---

## ইনলাইন ইভেন্ট হ্যান্ডলার 📝

দ্রুত কোনো অ্যাকশন নিতে চাইলে ইনলাইন ফাংশনও ব্যবহার করতে পারেন:

```jsx
export default function InlineButton() {
  return (
    <button onClick={() => alert('Hello!')}>Say Hello</button>
  );
}
```

<Callout type="info">ছোট কাজের জন্য ইনলাইন হ্যান্ডলার ভালো। বড় বা জটিল লজিকের জন্য আলাদা ফাংশন ব্যবহার করুন।</Callout>

---

## রিঅ্যাক্টে কমন ইভেন্ট হ্যান্ডলার প্রপ

- `onClick`: কোনো এলিমেন্ট ক্লিক হলে ফায়ার হয়
- `onChange`: ইনপুটের ভ্যালু বদলালে ফায়ার হয়
- `onSubmit`: ফর্ম সাবমিট হলে ফায়ার হয়
- `onMouseEnter`: মাউস এলিমেন্টে ঢুকলে ফায়ার হয়
- `onKeyDown`: কোনো কী প্রেস হলে ফায়ার হয়

---

## বেস্ট প্র্যাকটিস ও সাধারণ ভুল 🚦

- ✅ **ফাংশন রেফারেন্স** দিন, ফাংশন কল নয়।
- ✅ ইনলাইন লজিকের জন্য অ্যারো ফাংশন ব্যবহার করুন।
- 🚫 **প্রপ-এ সরাসরি ফাংশন কল করবেন না** (যেমন, `onClick={myFunc()}`)।
- ✅ হ্যান্ডলার ফাংশনের নাম পরিষ্কারভাবে দিন (যেমন, `handleClick`)।

<Callout type="warning">যদি সরাসরি ফাংশন কল করেন, তাহলে এটি প্রতিবার কম্পোনেন্ট রেন্ডার হলে রান হবে—শুধু ইউজার ইন্টারঅ্যাকশনে নয়!</Callout>

---

## উদাহরণ: ফর্ম সাবমিশন হ্যান্ডলিং 📝

```jsx
export default function MyForm() {
  const handleSubmit = (e) => {
    e.preventDefault(); // পেজ রিলোড আটকায়
    alert('Form submitted!');
  };

  return (
    <form onSubmit={handleSubmit}>
      <input type="text" />
      <button type="submit">Submit</button>
    </form>
  );
}
```

<Callout type="info">`e.preventDefault()` ব্যবহার করুন, যাতে ফর্ম সাবমিটে ডিফল্ট ব্রাউজার বিহেভিয়ার (যেমন, পেজ রিলোড) বন্ধ থাকে।</Callout>

---

## রিক্যাপ ও পরবর্তী ধাপ 🚀

- রিঅ্যাক্টে ইভেন্ট হ্যান্ডল করে ইন্টারঅ্যাকটিভিটি যোগ করুন।
- ইভেন্ট প্রপ-এ সবসময় ফাংশন রেফারেন্স দিন।
- দ্রুত কাজের জন্য ইনলাইন ফাংশন, আর জটিল লজিকের জন্য আলাদা ফাংশন ব্যবহার করুন।
- নিজে নিজে বাটন, ফর্ম, ইনপুট ইন্টারঅ্যাকটিভ করে প্র্যাকটিস করুন!

<Callout type="success">নিজের কম্পোনেন্টে ইভেন্ট হ্যান্ডলার যোগ করে দেখুন, কীভাবে আপনার অ্যাপ প্রাণবন্ত হয়ে ওঠে! 🎉</Callout>
