import { Callout } from "nextra/components";

<img src="https://upload.wikimedia.org/wikipedia/commons/a/a7/React-icon.svg" alt="React Logo" width="70" style={{marginBottom: '1rem'}} />

# ইভেন্টের প্রতিক্রিয়া – ইন্টারভিউ প্রশ্ন ও উত্তর 🎯

এই প্রশ্নগুলো ইন্টারভিউ প্রস্তুতির জন্য বা রিঅ্যাক্টে ইভেন্ট হ্যান্ডলিং সম্পর্কে আপনার বোঝা যাচাই করতে ব্যবহার করুন।

> **💡 প্রো টিপ:** প্রতিটি প্রশ্নের উত্তর নিজে চেষ্টা করুন, তারপর উত্তর দেখুন। এতে আপনার মনে রাখার ক্ষমতা বাড়বে!

---

## ১.১ – ইভেন্ট হ্যান্ডলিং ফান্ডামেন্টালস 🖱️

<div style={{ background: 'rgba(0, 100, 200, 0.10)', padding: '15px', borderRadius: '5px', marginBottom: '20px', border: '1px solid rgba(0, 100, 200, 0.15)', color: 'inherit' }}>

**Q1: রিঅ্যাক্টে ইভেন্ট হ্যান্ডলার কী?**  
উত্তর: ইভেন্ট হ্যান্ডলার হলো এমন একটি ফাংশন, যা ইউজারের কোনো অ্যাকশনের (যেমন ক্লিক, টাইপিং, ফর্ম সাবমিশন) প্রতিক্রিয়া হিসেবে চলে। রিঅ্যাক্টে, এগুলো প্রপ হিসেবে (যেমন, `onClick`, `onChange`) যোগ করা হয়।

**Q2: কীভাবে একটি বাটনে ক্লিক ইভেন্ট হ্যান্ডলার যোগ করবেন?**  
উত্তর: `onClick` প্রপ-এ ফাংশন রেফারেন্স দিন:
```jsx
<button onClick={handleClick}>Click me</button>
```

**Q3: কেন ইভেন্ট প্রপ-এ ফাংশন রেফারেন্স (কল নয়) দিতে হবে?**  
উত্তর: রেফারেন্স দিলে (যেমন, `onClick={handleClick}`) ফাংশনটি শুধু ইভেন্ট হলে চলে। কল করলে (যেমন, `onClick={handleClick()}`) এটি সাথে সাথে রেন্ডারেই চলে যাবে।

</div>

---

## ১.২ – ইনলাইন হ্যান্ডলার ও বেস্ট প্র্যাকটিস 💡

<div style={{ background: 'rgba(230, 150, 70, 0.10)', padding: '15px', borderRadius: '5px', marginBottom: '20px', border: '1px solid rgba(230, 150, 70, 0.15)', color: 'inherit' }}>

**Q4: কখন ইনলাইন ইভেন্ট হ্যান্ডলার ব্যবহার করবেন?**  
উত্তর: ছোট, দ্রুত কাজের জন্য ইনলাইন হ্যান্ডলার (যেমন, `onClick={() => alert('Hi!')}`) ব্যবহার করুন। বড় বা জটিল লজিকের জন্য আলাদা ফাংশন লিখুন।

**Q5: প্রপ-এ সরাসরি ফাংশন কল করলে কী হয়?**  
উত্তর: ফাংশনটি প্রতিবার কম্পোনেন্ট রেন্ডার হলে চলে, শুধু ইউজার ইন্টারঅ্যাকশনে নয়।

**Q6: রিঅ্যাক্টে ফর্ম সাবমিশনের ডিফল্ট বিহেভিয়ার কীভাবে বন্ধ করবেন?**  
উত্তর: হ্যান্ডলারের ভিতরে `e.preventDefault()` কল করুন:
```jsx
function handleSubmit(e) {
  e.preventDefault();
  // custom logic
}
```

</div>

---

## ১.৩ – সিনারিও ও প্র্যাকটিক্যাল উদাহরণ 🏆

<div style={{ background: 'rgba(20, 120, 220, 0.10)', padding: '20px', borderRadius: '5px', marginBottom: '20px', border: '1px solid rgba(20, 120, 220, 0.15)', color: 'inherit' }}>

### সিনারিও-ভিত্তিক প্রশ্ন:

আপনার একটি ফর্ম আছে, যেখানে ইনপুট ও সাবমিট বাটন রয়েছে। ফর্ম সাবমিট করলে ইনপুটের ভ্যালু দিয়ে অ্যালার্ট দেখাতে চান, কিন্তু পেজ রিলোড যেন না হয়।

**প্রশ্ন:** রিঅ্যাক্টে কীভাবে এটি করবেন? কী কী বেস্ট প্র্যাকটিস অনুসরণ করবেন?

উত্তর ভাবুন, তারপর নিচের স্যাম্পল দেখুন।

<details>
<summary>**স্যাম্পল উত্তর দেখতে ক্লিক করুন**</summary>

**স্যাম্পল অ্যাপ্রোচ:**

```jsx
import { useState } from 'react';

export default function MyForm() {
  const [value, setValue] = useState('');
  const handleSubmit = (e) => {
    e.preventDefault();
    alert(value);
  };
  return (
    <form onSubmit={handleSubmit}>
      <input value={value} onChange={e => setValue(e.target.value)} />
      <button type="submit">Submit</button>
    </form>
  );
}
```

- `e.preventDefault()` ব্যবহার করুন, যাতে ডিফল্ট বিহেভিয়ার বন্ধ থাকে।
- ইনপুট ভ্যালু ট্র্যাক করতে স্টেট ব্যবহার করুন।
- ক্লিয়ারিটি জন্য আলাদা হ্যান্ডলার ফাংশন ব্যবহার করুন।

</details>
</div>

---

<Callout type="info">এই প্রশ্নগুলো পড়ে নিজে নিজে উত্তর দেয়ার চেষ্টা করুন—এটাই সেরা প্রস্তুতি!</Callout>

---

> **🚀 চূড়ান্ত পরামর্শ:** ইন্টারভিউতে শুধু ইভেন্ট হ্যান্ডলিং জানলেই হবে না, পরিষ্কার, মেইনটেইনেবল ও ইউজার-ফ্রেন্ডলি কোড লেখার দক্ষতাও দেখান।

<img src="https://images.unsplash.com/photo-1461749280684-dccba630e2f6?auto=format&fit=crop&w=800&q=80" alt="React Interview Success" width="100%" style={{borderRadius: '8px', marginTop: '2rem'}} />
