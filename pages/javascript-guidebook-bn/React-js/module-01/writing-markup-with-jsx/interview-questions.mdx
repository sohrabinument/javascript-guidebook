import { Callout } from "nextra/components";

# JSX ইন্টারভিউ প্রশ্ন ও উত্তর ✨

<img
  src="https://upload.wikimedia.org/wikipedia/commons/a/a7/React-icon.svg"
  alt="React Logo"
  width="80"
  style={{ marginBottom: "1rem" }}
/>

এই প্রশ্নগুলো ব্যবহার করুন ইন্টারভিউ প্রস্তুতির জন্য বা React-এ JSX সম্পর্কে আপনার বোঝা যাচাই করতে।

> **💡 প্রো টিপ:** প্রতিটি প্রশ্নের উত্তর নিজে চেষ্টা করুন, তারপর উত্তর দেখুন। এতে ধারণা আরও পরিষ্কার হবে!

---

## ১.১ – JSX মৌলিক বিষয় 🌱

<div style={{ background: 'rgba(0, 100, 200, 0.10)', padding: '15px', borderRadius: '5px', marginBottom: '20px', border: '1px solid rgba(0, 100, 200, 0.15)', color: 'inherit' }}>

**প্রশ্ন ১: JSX কী?**  
উত্তর: JSX হলো জাভাস্ক্রিপ্টের জন্য একটি সিনট্যাক্স এক্সটেনশন, যা React কম্পোনেন্টে HTML-এর মতো মার্কআপ লিখতে দেয়।

**প্রশ্ন ২: React-এ আমরা কেন JSX ব্যবহার করি?**  
উত্তর: JSX ব্যবহার করলে UI স্ট্রাকচার লেখা ও দেখা সহজ হয় এবং মার্কআপ ও লজিক একসাথে রাখা যায়।

**প্রশ্ন ৩: JSX ছাড়াও কি React কম্পোনেন্ট লেখা যায়?**  
উত্তর: হ্যাঁ, `React.createElement` ব্যবহার করা যায়, তবে JSX অনেক বেশি সংক্ষিপ্ত ও পড়তে সহজ।

</div>

---

## ১.২ – JSX বনাম HTML ⚡

<div style={{ background: 'rgba(230, 150, 70, 0.10)', padding: '15px', borderRadius: '5px', marginBottom: '20px', border: '1px solid rgba(230, 150, 70, 0.15)', color: 'inherit' }}>

**প্রশ্ন ৪: HTML ও JSX-এর মধ্যে কিছু পার্থক্য কী?**  
উত্তর: JSX-এ `class` এর পরিবর্তে `className` ব্যবহার করতে হয়, সব ট্যাগ ক্লোজ করতে হয়, এবং বেশিরভাগ অ্যাট্রিবিউটে camelCase ব্যবহার করতে হয়।

**প্রশ্ন ৫: JSX-এ কীভাবে জাভাস্ক্রিপ্ট এক্সপ্রেশন এম্বেড করবেন?**  
উত্তর: কার্লি ব্রেস `{}` দিয়ে এক্সপ্রেশন এম্বেড করা যায়।

**প্রশ্ন ৬: JSX-এ Fragment কী এবং কখন ব্যবহার করবেন?**  
উত্তর: Fragment (`<>...</>`) দিয়ে একাধিক এলিমেন্ট গ্রুপ করা যায়, কিন্তু DOM-এ বাড়তি কোনো নোড যোগ হয় না। একাধিক এলিমেন্ট রিটার্ন করতে হলে এটি ব্যবহার করুন।

</div>

---

## ১.৩ – অ্যাডভান্সড ও সিনারিও-ভিত্তিক 🧠

<div style={{ background: 'rgba(220, 50, 50, 0.10)', padding: '15px', borderRadius: '5px', marginBottom: '20px', border: '1px solid rgba(220, 50, 50, 0.15)', color: 'inherit' }}>

**প্রশ্ন ৭: প্যারেন্ট র‍্যাপার ছাড়া একাধিক JSX এলিমেন্ট রিটার্ন করলে কী হবে?**  
উত্তর: React এরর দিবে। অবশ্যই একটি প্যারেন্ট এলিমেন্ট বা Fragment (`<>...</>`) দিয়ে র‍্যাপ করতে হবে।

**প্রশ্ন ৮: নিচের HTML-কে বৈধ JSX-এ রূপান্তর করুন:**

```html
<p class="info">Hello <b>World</b></p>
```

উত্তর:

```jsx
<p className="info">
  Hello <b>World</b>
</p>
```

**প্রশ্ন ৯: JSX-এ কিছু অ্যাট্রিবিউটে camelCase কেন বাধ্যতামূলক?**  
উত্তর: JSX জাভাস্ক্রিপ্টে কম্পাইল হয়, আর জাভাস্ক্রিপ্ট অবজেক্টের কী কেস-সেন্সিটিভ এবং কিছু শব্দ (যেমন `class`) রিজার্ভড।

**প্রশ্ন ১০: HTML থেকে JSX-এ রূপান্তরের সময় সাধারণ ভুলগুলো কী কী?**  
উত্তর: ট্যাগ ক্লোজ না করা, `class` ব্যবহার করা, camelCase না করা, এবং প্যারেন্ট ছাড়া একাধিক এলিমেন্ট রিটার্ন করা।

</div>

---

<Callout type="info">
  এই প্রশ্নগুলো পড়ে নিজ ভাষায় ব্যাখ্যা দেয়ার চেষ্টা করুন—ইন্টারভিউ প্রস্তুতির জন্য দারুণ অনুশীলন!
</Callout>

---

## প্র্যাকটিস ইন্টারভিউ এক্সারসাইজ 🏋️‍♂️

<div style={{ background: 'rgba(20, 120, 220, 0.10)', padding: '20px', borderRadius: '5px', marginBottom: '20px', border: '1px solid rgba(20, 120, 220, 0.15)', color: 'inherit' }}>

### সিনারিও-ভিত্তিক প্রশ্ন:

ধরুন, আপনি একটি ফর্ম বানাচ্ছেন যেখানে অনেক ইনপুট ফিল্ড আছে এবং আপনি চান JSX যেন পরিষ্কার, পড়তে সহজ ও ভুলবিহীন হয়।

**প্রশ্ন:** এই ফর্মের JSX লেখার সময় কোন কোন সেরা চর্চা অনুসরণ করবেন? সাধারণ ভুল এড়াতে কী করবেন?

উত্তর ভাবুন, তারপর নিচের সাজেশন দেখুন।

<details>
<summary>**উত্তরের জন্য ক্লিক করুন**</summary>

**JSX সেরা চর্চা:**

1. স্টাইলিংয়ের জন্য `className` ব্যবহার করুন, `class` নয়।
2. সব ট্যাগ ক্লোজ করুন, বিশেষ করে `<input />`-এর মতো সেল্ফ-ক্লোজিং ট্যাগ।
3. camelCase অ্যাট্রিবিউট ব্যবহার করুন (যেমন, `htmlFor`, `onChange`)।
4. একাধিক এলিমেন্ট প্যারেন্ট এলিমেন্ট বা Fragment দিয়ে র‍্যাপ করুন।
5. JSX পড়তে সহজ ও সুন্দরভাবে ইন্ডেন্ট করুন।
6. কম্পোনেন্ট ও প্রপ্সের জন্য অর্থবহ নাম ব্যবহার করুন।

**উপকারিতা:**
- কম সিনট্যাক্স এরর
- কোড আরও পড়তে ও মেইনটেইন করতে সহজ
- HTML থেকে JSX-এ রূপান্তরে কম বাগ

</details>
</div>

---

**টিপ:** প্রশ্ন ও উত্তরগুলো পড়ে নিজ ভাষায় ব্যাখ্যা দেয়ার চেষ্টা করুন—ইন্টারভিউ প্রস্তুতির জন্য দারুণ অনুশীলন!

> **🚀 চূড়ান্ত পরামর্শ:** ইন্টারভিউতে শুধু JSX-এর নিয়ম জানলেই হবে না, কেন React-এর এই পদ্ধতি কার্যকর এবং বাস্তবে কীভাবে ব্যবহার করবেন, সেটাও বোঝাতে হবে।
