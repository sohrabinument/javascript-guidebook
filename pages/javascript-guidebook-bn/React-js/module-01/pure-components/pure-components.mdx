# রিয়্যাক্টে পিউর কম্পোনেন্টস

## পিউর কম্পোনেন্টস কি? 🧪

একটি পিউর কম্পোনেন্টকে একটি নির্ভরযোগ্য ক্যালকুলেটরের মত ভাবুন: যখন আপনি `2+2` ইনপুট দেন, তখন এটি সবসময় `4` রিটার্ন করে - কখনই ভিন্ন উত্তর দেয় না, এবং গণনা করার সময় অন্য কিছু পরিবর্তন করে না।

রিয়্যাক্টে, একটি **পিউর কম্পোনেন্ট**:

- একই ইনপুট (props এবং state) দেওয়া হলে সবসময় একই UI আউটপুট দেয়
- নিজের বাইরে কিছু পরিবর্তন করে না (কোন সাইড ইফেক্ট নেই)

এটি একটি ভালো রান্নার রেসিপির মতো - যদি আপনি একই উপাদান দিয়ে সঠিকভাবে অনুসরণ করেন, তাহলে প্রতিবার একই খাবার পাবেন।

## আপনার কেন ভাবনা দরকার? 🤔

পিউর কম্পোনেন্টস আপনার রিয়্যাক্ট অ্যাপগুলোকে ভালো করে কারণ:

- ✅ **প্রেডিক্টেবল আচরণ**: আপনার UI সুসংগত এবং নির্ভরযোগ্য থাকে
- ✅ **বেহতর পারফরম্যান্স**: রিয়্যাক্ট অপ্রয়োজনীয় পুনঃরেন্ডারিং এড়াতে পারে
- ✅ **সহজ টেস্টিং**: কম্পোনেন্টগুলি টেস্টে সুসংগত আচরণ করে
- ✅ **কম বাগ**: সাইড ইফেক্ট অনেক বাগের উৎস
- ✅ **ভবিষ্যৎ-প্রস্তুত**: রিয়্যাক্টের আধুনিক ফিচারগুলি যেমন কনকারেন্ট রেন্ডারিং পিউর কম্পোনেন্টের উপর নির্ভর করে

## কম্পোনেন্টগুলোকে পিউর রাখার সোনালী নিয়ম 📝

### নিয়ম ১: আগে থেকে বিদ্যমান ডেটা পরিবর্তন করবেন না

```jsx
// ❌ খারাপ: বাইরের ভেরিয়েবল পরিবর্তন করা
let counter = 0;
function Counter() {
  // এটি কম্পোনেন্টের বাইরের ডেটা পরিবর্তন করছে!
  counter++;
  return <div>গণনা: {counter}</div>;
}

// ✅ ভালো: রিয়্যাক্টের স্টেট ব্যবহার করা
function Counter() {
  // রিয়্যাক্ট স্টেট হল ডেটা পরিবর্তন করার সঠিক উপায়
  const [count, setCount] = useState(0);

  function handleClick() {
    setCount(count + 1); // আপডেট করার সঠিক উপায়
  }

  return (
    <div>
      গণনা: {count} <button onClick={handleClick}>+১</button>
    </div>
  );
}
```

### নিয়ম ২: সরাসরি Props পরিবর্তন করবেন না

```jsx
// ❌ খারাপ: Props পরিবর্তন করা (এটি বাগ সৃষ্টি করে!)
function ProfileCard({ user }) {
  // কখনোই আপনি যে props পান তা পরিবর্তন করবেন না!
  user.lastSeen = Date.now();

  return <div>{user.name} এইমাত্র দেখা গেছে</div>;
}

// ✅ ভালো: পরিবর্তে নতুন অবজেক্ট তৈরি করুন
function ProfileCard({ user }) {
  // আপডেটগুলি সহ একটি নতুন অবজেক্ট তৈরি করুন
  const updatedUser = {
    ...user,
    lastSeen: Date.now(),
  };

  return <div>{updatedUser.name} এইমাত্র দেখা গেছে</div>;
}
```

### নিয়ম ৩: একই ইনপুট → একই আউটপুট

```jsx
// ❌ খারাপ: আউটপুট props/state ছাড়া অন্য জিনিসের উপর নির্ভর করে
function Greeting({ name }) {
  // এটি বর্তমান সময়ের উপর নির্ভর করে, শুধু props নয়!
  const greeting = new Date().getHours() < 12 ? "শুভ সকাল" : "শুভ দিন";

  return (
    <h1>
      {greeting}, {name}!
    </h1>
  );
}

// ✅ ভালো: সমস্ত ইনপুট props হিসেবে গ্রহণ করা
function Greeting({ name, timeOfDay }) {
  // এখন এটি নির্ভরযোগ্য - একই ইনপুট সবসময় একই আউটপুট দেয়
  const greeting = timeOfDay === "morning" ? "শুভ সকাল" : "শুভ দিন";

  return (
    <h1>
      {greeting}, {name}!
    </h1>
  );
}

// ব্যবহার
<Greeting
  name="তৌফিক"
  timeOfDay={new Date().getHours() < 12 ? "morning" : "day"}
/>;
```

## "লোকাল মিউটেশন" এর ব্যতিক্রম 🔄

"কোন মিউটেশন নয়" নিয়মের একটি ব্যতিক্রম আছে: রেন্ডারিং সময়ে তৈরি করা **লোকাল ভেরিয়েবলস**।

```jsx
function ItemList({ items }) {
  // ✅ এটা ঠিক আছে! আমরা শুধুমাত্র একটি নতুন অ্যারে পরিবর্তন করছি যা আমরা এইমাত্র তৈরি করেছি
  const processedItems = [];

  for (let i = 0; i < items.length; i++) {
    processedItems.push(<li key={i}>{items[i].toUpperCase()}</li>);
  }

  return <ul>{processedItems}</ul>;
}
```

এটি ঠিক আছে কারণ:

1. আপনি `processedItems` অ্যারেটি একেবারে নতুন করে তৈরি করেছেন
2. অন্য কোন কম্পোনেন্ট এই অ্যারে দেখতে বা অ্যাক্সেস করতে পারে না
3. পরিবর্তনগুলি শুধুমাত্র এই একক রেন্ডারের মধ্যে সীমাবদ্ধ

এটি একটি গণিতের সমস্যা সমাধান করার সময় একটি স্ক্র্যাচ পেপারে নোট নেওয়ার মতো - স্ক্র্যাচ পেপারটি আপনি শুরু করার আগে ছিল না, এবং আপনি শেষ হলে এটি ফেলে দেবেন।

## সাইড ইফেক্টস কোথায় রাখবেন ↪️

কখনও কখনও আপনাকে সত্যিই জিনিস পরিবর্তন করতে হয় (ডাটাবেস আপডেট করা, ডেটা ফেচ করা, ইত্যাদি)। এগুলোকে "সাইড ইফেক্ট" বলা হয় এবং নির্দিষ্ট জায়গায় রাখা উচিত:

### ১. ইভেন্ট হ্যান্ডলার - সবচেয়ে ভালো জায়গা

```jsx
function SaveButton({ document }) {
  // ✅ সাইড ইফেক্টের জন্য উত্তম জায়গা
  function handleSave() {
    // ইভেন্ট হ্যান্ডলারে সাইড ইফেক্ট ভালো!
    saveToDatabase(document);
    showNotification("সংরক্ষিত হয়েছে!");
  }

  return <button onClick={handleSave}>ডকুমেন্ট সংরক্ষণ করুন</button>;
}
```

### ২. useEffect - রেন্ডারিং পরে ইফেক্টের জন্য

```jsx
function AutoSavingField({ text, onSave }) {
  // ✅ useEffect-এ সঠিকভাবে সাইড ইফেক্ট
  useEffect(() => {
    // এটি রেন্ডারের সময় নয়, পরে চলে
    const timeoutId = setTimeout(() => {
      onSave(text);
    }, 2000);

    return () => clearTimeout(timeoutId);
  }, [text, onSave]);

  return <textarea value={text} />;
}
```

## সাধারণ অপিউর কম্পোনেন্ট ভুলগুলো 🚫

### ভুল ১: সরাসরি DOM ম্যানিপুলেশন

```jsx
// ❌ খারাপ: রিয়্যাক্ট বাইপাস করে DOM পরিবর্তন করা
function Header({ title }) {
  // এটা করবেন না! DOM হ্যান্ডেলিং রিয়্যাক্টকে করতে দিন
  document.title = title;
  document.getElementById("header").className = "active";

  return <h1 id="header">{title}</h1>;
}

// ✅ ভালো: রিয়্যাক্টকে হ্যান্ডল করতে দিন
function Header({ title }) {
  // সাইড ইফেক্ট useEffect-এ থাকা উচিত
  useEffect(() => {
    document.title = title;
  }, [title]);

  // className-এর জন্য props ব্যবহার করুন
  return <h1 className="active">{title}</h1>;
}
```

### ভুল ২: রেন্ডারের সময় ফেচিং

```jsx
// ❌ খারাপ: রেন্ডারের সময় ডেটা ফেচিং
function UserProfile({ userId }) {
  // এটি রেন্ডারের সময় চলে! বড় সমস্যা!
  const userData = fetchUserData(userId);

  return <div>{userData.name}</div>;
}

// ✅ ভালো: useEffect দিয়ে ফেচিং
function UserProfile({ userId }) {
  const [userData, setUserData] = useState(null);
  const [isLoading, setIsLoading] = useState(true);

  useEffect(() => {
    // ডেটা ফেচিংয়ের সঠিক জায়গা
    async function fetchData() {
      setIsLoading(true);
      const data = await fetchUserData(userId);
      setUserData(data);
      setIsLoading(false);
    }

    fetchData();
  }, [userId]);

  if (isLoading) return <div>লোড হচ্ছে...</div>;
  return <div>{userData.name}</div>;
}
```

### ভুল ৩: গ্লোবাল ভেরিয়েবল ব্যবহার বা পরিবর্তন

```jsx
// ❌ খারাপ: সরাসরি গ্লোবাল স্টেট ব্যবহার করা
const globalSettings = { theme: "dark" };

function ThemeDisplay() {
  // রিয়্যাক্টের বাইরের গ্লোবাল ভেরিয়েবলের উপর নির্ভর করে
  return <div>বর্তমান থিম: {globalSettings.theme}</div>;
}

// ✅ ভালো: প্রপস হিসেবে পাস করা বা কনটেক্সট ব্যবহার করা
function ThemeDisplay({ theme }) {
  // কম্পোনেন্ট স্পষ্টভাবে সমস্ত ইনপুট গ্রহণ করে
  return <div>বর্তমান থিম: {theme}</div>;
}
```

## StrictMode দিয়ে অপিউর কম্পোনেন্ট খুঁজে বের করা 🔍

রিয়্যাক্ট একটি সহায়ক ডেভেলপমেন্ট টুল প্রদান করে যার নাম "StrictMode" যা অপিউর কম্পোনেন্ট খুঁজে বের করতে সাহায্য করে:

```jsx
// আপনার অ্যাপের উপরের স্তরে এটি যোগ করুন
<React.StrictMode>
  <App />
</React.StrictMode>
```

StrictMode কাজ করে:

- ডেভেলপমেন্টের সময় আপনার কম্পোনেন্ট ফাংশন দুবার চালিয়ে
- আপনার ইফেক্টগুলো দুবার চালিয়ে
- যদি আপনার কম্পোনেন্ট পিউর হয়, তাহলে সবকিছু ঠিকভাবে কাজ করবে!
- যদি সেগুলো অপিউর হয়, তাহলে আপনি অদ্ভুত আচরণ দেখবেন, যা আপনাকে বাগ খুঁজে পেতে সাহায্য করবে

এটি অপিউর কম্পোনেন্টগুলোর জন্য একটি মেটাল ডিটেক্টরের মতো।

## দৃশ্যমান অনুরূপতা: পিউর কম্পোনেন্ট যেন ভেন্ডিং মেশিন 🧠

একটি পিউর কম্পোনেন্ট একটি ভেন্ডিং মেশিনের মতো:

1. আপনি টাকা দিয়ে একটি আইটেম নির্বাচন করেন (ইনপুট/props)
2. এটি সবসময় আপনাকে প্রত্যাশিত পণ্য দেয় (একই আউটপুট)
3. এটি নিজের চারপাশের কিছুই পরিবর্তন করে না
4. আপনি কতবার ব্যবহার করেন তা কোন ব্যাপার না - একই ইনপুট, একই আউটপুট

বিপরীতে, একটি অপিউর কম্পোনেন্ট এমন একটি ভেন্ডিং মেশিনের মতো যা:

- একই নির্বাচনের জন্য কখনও কখনও ভিন্ন আইটেম দেয়
- ঘরের তাপমাত্রা পরিবর্তন করে
- অন্য লোকেরা আগে কী কিনেছে তার উপর নির্ভর করে
- সাধারণভাবে অনির্ভরযোগ্য আচরণ করে

## সারাংশ: পিউরিটি কেন গুরুত্বপূর্ণ

পিউর কম্পোনেন্টগুলো রিয়্যাক্টের ডিজাইন দর্শনের ভিত্তি:

- তারা আপনার অ্যাপকে আরও নির্ভরযোগ্য ও ডিবাগ করা সহজ করে
- তারা রিয়্যাক্টের পারফরম্যান্স অপটিমাইজেশন সক্ষম করে
- তারা টেস্টিং অনেক সহজ করে
- তারা সাইড ইফেক্ট সম্পর্কিত বাগের একটি সম্পূর্ণ শ্রেণীকে প্রতিরোধ করে
- তারা উন্নত রিয়্যাক্ট ফিচারগুলোর জন্য আপনার অ্যাপকে প্রস্তুত করে

মনে রাখবেন: একই ইনপুটের জন্য একই JSX রিটার্ন করুন, এবং রেন্ডারের সময় আগে থেকে বিদ্যমান ডেটা পরিবর্তন করবেন না। সমস্ত সাইড ইফেক্ট ইভেন্ট হ্যান্ডলার বা useEffect-এ রাখুন।

এই নীতিগুলি অনুসরণ করে, আপনি আরও নির্ভরযোগ্য ও রক্ষণাবেক্ষণযোগ্য রিয়্যাক্ট অ্যাপ্লিকেশন তৈরি করবেন।
