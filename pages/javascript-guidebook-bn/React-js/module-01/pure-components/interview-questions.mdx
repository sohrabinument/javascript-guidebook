# ইন্টারভিউ প্রশ্নাবলী: রিয়্যাক্টে পিউর কম্পোনেন্টস

এই গাইডে রিয়্যাক্টের পিউর কম্পোনেন্টস সম্পর্কে সাধারণ ইন্টারভিউ প্রশ্ন রয়েছে।

## 🌱 প্রাথমিক প্রশ্নাবলী

### Q1: সহজ ভাষায় রিয়্যাক্টে পিউর কম্পোনেন্ট কি?

**উত্তর:**
একটি পিউর কম্পোনেন্ট একটি নির্ভরযোগ্য ক্যালকুলেটরের মতো - একই ইনপুট দিলে, এটি সবসময় একই আউটপুট দেয়। রিয়্যাক্টে, এর অর্থ:

1. যদি আপনি একই props এবং state একটি কম্পোনেন্টে পাস করেন, তবে এটি সবসময় একই UI রেন্ডার করবে
2. এটি নিজের বাইরে কিছুই পরিবর্তন করে না (কোন সাইড ইফেক্ট নেই)

### Q2: একটি ফাংশনকে "পিউর" কি করে?

**উত্তর:**
একটি পিউর ফাংশন:

- ✅ একই ইনপুট দেওয়া হলে সবসময় একই আউটপুট দেয়
- ✅ নিজের বাইরে কিছুই পরিবর্তন করে না
- ✅ তাকে যে ডেটা পাস করা হয়েছে তা পরিবর্তন করে না
- ✅ এমন বাহ্যিক ফ্যাক্টরের উপর নির্ভর করে না যা পরিবর্তন হতে পারে

```javascript
// পিউর ফাংশনের উদাহরণ
function add(a, b) {
  return a + b; // একই ইনপুটের জন্য সবসময় একই রেজাল্ট
}

// অপিউর ফাংশনের উদাহরণ
let total = 0;
function addToTotal(num) {
  total += num; // বাইরের ভেরিয়েবল পরিবর্তন করে!
  return total; // ফলাফল আগের কলের উপর নির্ভর করে
}
```

### Q3: রিয়্যাক্টে পিউর কম্পোনেন্টস কেন এত গুরুত্বপূর্ণ?

**উত্তর:**
পিউর কম্পোনেন্টস গুরুত্বপূর্ণ কারণ:

1. **আপনার কোড নির্ভরযোগ্য করে** - এগুলো সুসংগতভাবে আচরণ করে, তাই আপনার UI প্রেডিক্টেবল
2. **পারফরম্যান্স উন্নত করে** - রিয়্যাক্ট সেই কম্পোনেন্টগুলোর জন্য অপ্টিমাইজ করতে এবং পুনঃরেন্ডার এড়াতে পারে যেগুলো পিউর
3. **সার্ভার রেন্ডারিং সমর্থন করে** - পিউর কম্পোনেন্টস সার্ভার এবং ক্লায়েন্ট উভয়েই একইভাবে কাজ করে
4. **উন্নত ফিচারগুলি সক্ষম করে** - কনকারেন্ট রেন্ডারিংয়ের মতো ফিচারগুলির জন্য পিউর কম্পোনেন্ট প্রয়োজন
5. **টেস্টিং সহজ করে** - পিউর কম্পোনেন্টগুলো নির্ভরযোগ্য হওয়ায় পরীক্ষা করা সহজ

## 💻 প্র্যাকটিক্যাল উদাহরণ

### Q4: `React.PureComponent` এবং `React.memo` এর মধ্যে পার্থক্য কি?

**উত্তর:**
উভয়ই অপ্রয়োজনীয় রেন্ডার এড়িয়ে পারফরম্যান্স উন্নত করতে সাহায্য করে, তবে:

| বৈশিষ্ট্য       | React.PureComponent                                          | React.memo                                                   |
| --------------- | ------------------------------------------------------------ | ------------------------------------------------------------ |
| কম্পোনেন্ট টাইপ | ক্লাস কম্পোনেন্টের জন্য                                      | ফাংশন কম্পোনেন্টের জন্য                                      |
| কীভাবে কাজ করে  | স্বয়ংক্রিয়ভাবে `shouldComponentUpdate` বাস্তবায়ন করে      | কম্পোনেন্টকে props-এর উপর ভিত্তি করে মেমোইজ করে              |
| কাস্টম তুলনা    | উপলব্ধ নয়                                                   | দ্বিতীয় আর্গুমেন্ট হিসেবে উপলব্ধ                            |
| কখন সাহায্য করে | যখন আপনার কম্পোনেন্ট একই props/state-এর জন্য একই আউটপুট দেয় | যখন আপনার ফাংশন কম্পোনেন্ট একই props-এর জন্য একই আউটপুট দেয় |

```jsx
// PureComponent উদাহরণ (ক্লাস)
class UserProfile extends React.PureComponent {
  render() {
    return <div>{this.props.name}</div>;
  }
}

// React.memo উদাহরণ (ফাংশন)
const UserProfile = React.memo(function UserProfile({ name }) {
  return <div>{name}</div>;
});

// কাস্টম তুলনা সহ React.memo
const UserProfile = React.memo(
  function UserProfile({ user }) {
    return <div>{user.name}</div>;
  },
  (prevProps, nextProps) => {
    // শুধুমাত্র যদি নাম পরিবর্তন হয় তবেই পুনরায় রেন্ডার করুন
    return prevProps.user.name === nextProps.user.name;
  }
);
```

### Q5: এই ঘড়ি কম্পোনেন্টটি পিউর নয়। আপনি কিভাবে এটি ঠিক করবেন?

```jsx
function Clock() {
  // বর্তমান সময় নিন
  const currentTime = new Date();

  // সরাসরি DOM ম্যানিপুলেট করছে! খারাপ!
  if (currentTime.getHours() < 12) {
    document.getElementById("clock").className = "morning";
  } else {
    document.getElementById("clock").className = "afternoon";
  }

  return <h1 id="clock">{currentTime.toLocaleTimeString()}</h1>;
}
```

**উত্তর:**
কম্পোনেন্টটি অপিউর কারণ এটি `document.getElementById` দিয়ে সরাসরি DOM ম্যানিপুলেট করছে। এখানে একটি পিউর ভার্সন দেওয়া হল যা DOM আপডেট রিয়্যাক্টকে হ্যান্ডেল করতে দেয়:

```jsx
function Clock() {
  // বর্তমান সময় নিন
  const currentTime = new Date();

  // সময়ের উপর ভিত্তি করে ক্লাস নির্ধারণ করুন
  const className = currentTime.getHours() < 12 ? "morning" : "afternoon";

  // DOM হ্যান্ডেল করার জন্য props ব্যবহার করে রিয়্যাক্টকে ব্যবহার করুন
  return <h1 className={className}>{currentTime.toLocaleTimeString()}</h1>;
}
```

### Q6: এই কম্পোনেন্টটি কি পিউর? যদি না হয়, তাহলে কিভাবে এটি ঠিক করবেন?

```jsx
// কম্পোনেন্টের বাইরে গ্লোবাল অ্যারে
const userHistory = [];

function UserList({ newUser }) {
  // প্রদত্ত হলে নতুন ইউজার হিস্টোরিতে যোগ করুন
  if (newUser) {
    userHistory.push(newUser);
  }

  return (
    <ul>
      {userHistory.map((user, i) => (
        <li key={i}>{user.name}</li>
      ))}
    </ul>
  );
}
```

**উত্তর:**
না, এই কম্পোনেন্টটি পিউর নয়, দুটি প্রধান কারণে:

1. এটি একটি গ্লোবাল অ্যারে (`userHistory`) পরিবর্তন করে যা কম্পোনেন্টের বাইরে বিদ্যমান
2. এটি আগের রেন্ডারের উপর ভিত্তি করে ভিন্ন আউটপুট দেয় (যেহেতু `userHistory` ক্রমাগত বাড়ছে)

এখানে একটি পিউর ভার্সন:

```jsx
function UserList({ users, newUser }) {
  // বাইরের কিছু পরিবর্তন না করে একটি নতুন অ্যারে তৈরি করুন
  const displayUsers = newUser ? [...users, newUser] : users;

  return (
    <ul>
      {displayUsers.map((user, i) => (
        <li key={i}>{user.name}</li>
      ))}
    </ul>
  );
}

// ব্যবহার
<UserList users={existingUsers} newUser={latestUser} />;
```

## 🚀 মধ্যবর্তী ধারণাসমূহ

### Q7: রিয়্যাক্ট কম্পোনেন্টে "লোকাল মিউটেশন" কি এবং কেন এটি ঠিক আছে?

**উত্তর:**
লোকাল মিউটেশন মানে এমন ভেরিয়েবল তৈরি এবং পরিবর্তন করা যা:

- বর্তমান রেন্ডারের সময় তৈরি হয়েছে
- কম্পোনেন্টের বাইরে বিদ্যমান নেই
- ভবিষ্যতের রেন্ডারে প্রভাব ফেলবে না

এটি সম্পূর্ণ ঠিক কারণ এই পরিবর্তনগুলি বর্তমান রেন্ডারে সীমাবদ্ধ এবং অপ্রত্যাশিত আচরণ সৃষ্টি করবে না।

```jsx
function ProcessList({ items }) {
  // ✅ ঠিক আছে: এই অ্যারেটি বর্তমান রেন্ডারে নতুন করে তৈরি হয়েছে
  const processed = [];

  // ✅ ঠিক আছে: আমরা শুধু সেই অ্যারেটি পরিবর্তন করছি যা আমরা এইমাত্র তৈরি করেছি
  for (const item of items) {
    processed.push(<li key={item.id}>{item.name.toUpperCase()}</li>);
  }

  return <ul>{processed}</ul>;
}
```

এটিকে গণিতের সমস্যা সমাধান করার সময় অস্থায়ী হিসাব করার মতো ভাবুন - আপনি অবাধে নোট নিতে পারেন যা আপনি শেষ হলে ফেলে দেবেন।

### Q8: রিয়্যাক্টের StrictMode কীভাবে অপিউর কম্পোনেন্ট খুঁজে বের করতে সাহায্য করে?

**উত্তর:**
StrictMode হল অপিউর কম্পোনেন্ট খুঁজে বের করার জন্য একটি ডিটেক্টিভের মতো। এটি কাজ করে:

1. ডেভেলপমেন্টের সময় আপনার কম্পোনেন্টগুলো **দুবার রেন্ডার** করে
2. ইফেক্টস, ইভেন্ট হ্যান্ডলার এবং অন্যান্য ফাংশনগুলোকে **দুবার কল** করে

যদি আপনার কম্পোনেন্টগুলো পিউর হয়, তবে সেগুলোকে দুবার কল করা কোন সমস্যা সৃষ্টি করবে না, কারণ তারা একই ইনপুটের জন্য সবসময় একই আউটপুট দেবে। কিন্তু অপিউর কম্পোনেন্টগুলো দুবার কল করলে অদ্ভুত আচরণ করবে, যা স্পষ্টভাবে দেখায় যে কিছু ভুল আছে।

উদাহরণস্বরূপ, যদি আপনার কম্পোনেন্ট রেন্ডারের সময় একটি কাউন্টার ভেরিয়েবল আপডেট করে:

- প্রথম রেন্ডার: counter = 1
- দ্বিতীয় রেন্ডার (StrictMode): counter = 2 (উপস, একই আউটপুট নয়!)

এটি আপনাকে প্রোডাকশনে বাগ সৃষ্টি করার আগে এই সমস্যাগুলো খুঁজে বের করতে এবং ঠিক করতে সাহায্য করে।

### Q9: আমার রিয়্যাক্ট কম্পোনেন্টে সাইড ইফেক্ট কোথায় রাখা উচিত?

**উত্তর:**
সাইড ইফেক্টগুলো (যেমন API কল, DOM ম্যানিপুলেশন, সাবস্ক্রিপশন) এই জায়গাগুলোতে রাখা উচিত:

1. **ইভেন্ট হ্যান্ডলার** (যখন সম্ভব সেরা বিকল্প)

   ```jsx
   function SaveButton() {
     function handleClick() {
       // ✅ ইভেন্ট হ্যান্ডলারে সাইড ইফেক্ট
       saveData();
       showToast("সংরক্ষিত হয়েছে!");
     }

     return <button onClick={handleClick}>সংরক্ষণ করুন</button>;
   }
   ```

2. **useEffect হুক** (রেন্ডারের পরে চলবে এমন ইফেক্টের জন্য)

   ```jsx
   function ProfilePage({ userId }) {
     const [userData, setUserData] = useState(null);

     // ✅ useEffect-এ সাইড ইফেক্ট
     useEffect(() => {
       fetchUserData(userId).then((data) => setUserData(data));
     }, [userId]);

     // ...
   }
   ```

3. **কাস্টম হুক** (পুনঃব্যবহারযোগ্য ইফেক্ট লজিকের জন্য)

   ```jsx
   // সাইড ইফেক্টসহ কাস্টম হুক
   function useUserData(userId) {
     const [data, setData] = useState(null);

     useEffect(() => {
       fetchUserData(userId).then((data) => setData(data));
     }, [userId]);

     return data;
   }
   ```

প্রধান নিয়ম: **কম্পোনেন্টের বডিতে (রেন্ডার ফেজে) সরাসরি সাইড ইফেক্ট রাখবেন না**।

### Q10: অপ্রয়োজনীয় পুনঃরেন্ডার রোধ করতে আমি এই কম্পোনেন্টটি কীভাবে অপ্টিমাইজ করতে পারি?

```jsx
function ExpensiveChart({ data, config }) {
  // এই ক্যালকুলেশন ব্যয়বহুল!
  const processedData = processData(data);

  return (
    <div>
      <Chart data={processedData} config={config} />
      <OtherComponents />
    </div>
  );
}
```

**উত্তর:**
যেহেতু এই কম্পোনেন্টটি পিউর, আমরা এটি দুই উপায়ে অপ্টিমাইজ করতে পারি:

1. props পরিবর্তন না হলে পুনঃরেন্ডার রোধ করতে `React.memo` ব্যবহার করুন:

   ```jsx
   const ExpensiveChart = React.memo(function ExpensiveChart({ data, config }) {
     const processedData = processData(data);

     return (
       <div>
         <Chart data={processedData} config={config} />
         <OtherComponents />
       </div>
     );
   });
   ```

2. ব্যয়বহুল ক্যালকুলেশন ক্যাশ করতে `useMemo` ব্যবহার করুন:

   ```jsx
   const ExpensiveChart = React.memo(function ExpensiveChart({ data, config }) {
     // শুধুমাত্র data পরিবর্তন হলে পুনরায় ক্যালকুলেট করুন
     const processedData = useMemo(() => {
       return processData(data);
     }, [data]);

     return (
       <div>
         <Chart data={processedData} config={config} />
         <OtherComponents />
       </div>
     );
   });
   ```

এটি নিশ্চিত করে যে:

- কম্পোনেন্টটি শুধুমাত্র তখনই পুনঃরেন্ডার হবে যখন তার props আসলেই পরিবর্তন হয়
- ব্যয়বহুল ক্যালকুলেশন শুধুমাত্র তখনই চলবে যখন ডেটা পরিবর্তন হয়

## 🔍 অ্যাডভান্সড টপিকস

### Q11: কোন সাধারণ ভুলগুলো কম্পোনেন্টকে অপিউর বানায়?

**উত্তর:**
কম্পোনেন্টগুলোতে এই সাধারণ অপিউরতাগুলো থেকে সাবধান থাকুন:

1. **প্রপস পরিবর্তন করা**

   ```jsx
   function ProfileCard({ user }) {
     user.lastSeen = Date.now(); // ❌ কখনই প্রপস পরিবর্তন করবেন না!
     // ...
   }
   ```

2. **আগে থেকে বিদ্যমান ভেরিয়েবল পরিবর্তন করা**

   ```jsx
   let count = 0;
   function Counter() {
     count++; // ❌ বাহ্যিক স্টেট পরিবর্তন করা!
     // ...
   }
   ```

3. **সরাসরি DOM ম্যানিপুলেশন**

   ```jsx
   function Header() {
     document.title = "নতুন পেজ"; // ❌ সরাসরি DOM ম্যানিপুলেশন
     // ...
   }
   ```

4. **রেন্ডারের সময় API কল**

   ```jsx
   function UserData({ id }) {
     const data = fetchUserData(id); // ❌ রেন্ডারের সময় API কল!
     // ...
   }
   ```

5. **রেন্ডারে র‍্যান্ডম মান**

   ```jsx
   function RandomGreeting() {
     // ❌ প্রতি রেন্ডারে ভিন্ন আউটপুট!
     const greeting = Math.random() > 0.5 ? "হ্যালো" : "হাই";
     // ...
   }
   ```

6. **প্রপস ছাড়া রেন্ডারে তারিখ/সময়**

   ```jsx
   function Clock() {
     // ❌ প্রতি রেন্ডারে ভিন্ন!
     return <div>{new Date().toLocaleTimeString()}</div>;
   }
   ```

7. **গ্লোবাল স্টেট থেকে পড়া**
   ```jsx
   function ThemeButton() {
     // ❌ রিয়্যাক্টের বাইরের গ্লোবাল থেকে পড়া
     return <button className={globalTheme}>ক্লিক করুন</button>;
   }
   ```

### Q12: আপনি এই অপিউর আবহাওয়া কম্পোনেন্টটি কীভাবে ঠিক করবেন?

```jsx
function WeatherWidget() {
  const [temperature, setTemperature] = useState(null);

  // সমস্যা: রেন্ডারের সময় API কল!
  if (temperature === null) {
    // এটি রেন্ডারিংয়ের সময় চলে!
    fetch("https://weather-api.example/current")
      .then((res) => res.json())
      .then((data) => {
        setTemperature(data.temperature);
      });
    return <div>আবহাওয়া লোড হচ্ছে...</div>;
  }

  return <div>বর্তমান তাপমাত্রা: {temperature}°C</div>;
}
```

**উত্তর:**
কম্পোনেন্টটি অপিউর কারণ এটি রেন্ডারের সময় একটি API কল করে। এখানে একটি সংশোধিত সংস্করণ:

```jsx
function WeatherWidget() {
  const [temperature, setTemperature] = useState(null);
  const [isLoading, setIsLoading] = useState(true);

  // ✅ সাইড ইফেক্ট useEffect-এ সরান
  useEffect(() => {
    // এখন ফেচ রেন্ডারের সময় নয়, পরে হয়
    fetch("https://weather-api.example/current")
      .then((res) => res.json())
      .then((data) => {
        setTemperature(data.temperature);
        setIsLoading(false);
      });
  }, []); // খালি অ্যারে মানে প্রথম রেন্ডারের পরে একবার চালান

  if (isLoading) {
    return <div>আবহাওয়া লোড হচ্ছে...</div>;
  }

  return <div>বর্তমান তাপমাত্রা: {temperature}°C</div>;
}
```

### Q13: পিউর কম্পোনেন্টগুলোর সাথে কাজ করার সময় "রেফারেনশিয়াল ইকুয়ালিটি" কেন গুরুত্বপূর্ণ?

**উত্তর:**
রেফারেনশিয়াল ইকুয়ালিটি হল দুটি ভেরিয়েবল মেমোরিতে একই অবজেক্টকে পয়েন্ট করে কিনা (‍`===` ব্যবহার করে) তা নিয়ে।

এটি পিউর কম্পোনেন্টগুলোর জন্য গুরুত্বপূর্ণ কারণ রিয়্যাক্ট props পরিবর্তন হয়েছে কিনা চেক করতে `===` ব্যবহার করে:

```jsx
// পেরেন্ট কম্পোনেন্ট
function Parent() {
  // ❌ সমস্যা: প্রতি রেন্ডারে একটি নতুন অ্যারে তৈরি করে
  const items = [1, 2, 3];

  // এটি প্রতিবার একটি নতুন অ্যারে রেফারেন্স পাস করে
  return <PureChildList items={items} />;
}

// React.memo ব্যবহার করা হলেও
const PureChildList = React.memo(function ChildList({ items }) {
  // পেরেন্ট যতবার রেন্ডার হবে ততবার পুনরায় রেন্ডার হবে!
  // কারণ items প্রতিবার একটি নতুন অ্যারে রেফারেন্স
  return (
    <ul>
      {items.map((item) => (
        <li key={item}>{item}</li>
      ))}
    </ul>
  );
});
```

সমাধান: এমন কৌশল ব্যবহার করুন যা কনটেন্ট আসলেই পরিবর্তন না হলে রেফারেনশিয়াল ইকুয়ালিটি বজায় রাখে:

```jsx
function Parent() {
  // ✅ অ্যারেটি একবার তৈরি করুন, একই রেফারেন্স পুনঃব্যবহার করুন
  const items = useMemo(() => [1, 2, 3], []);

  // অথবা ফাংশনের জন্য
  const handleClick = useCallback(() => {
    console.log("ক্লিক করা হয়েছে!");
  }, []);

  return <PureChildList items={items} onClick={handleClick} />;
}
```

এটি নিশ্চিত করে যে পিউর চাইল্ড কম্পোনেন্টগুলো শুধুমাত্র তখনই পুনঃরেন্ডার হবে যখন তাদের ইনপুট সত্যিই পরিবর্তন হয়।

### Q14: "আইডেমপোটেন্স" কি এবং এটি কীভাবে রিয়্যাক্ট কম্পোনেন্টের সাথে সম্পর্কিত?

**উত্তর:**
আইডেমপোটেন্স মানে আপনি একটি অপারেশন একবার বা একাধিকবার প্রয়োগ করলেও একই ফলাফল পাবেন।

রিয়্যাক্টে, এর অর্থ হল একটি কম্পোনেন্ট কতবার রেন্ডার হয় তা নির্বিশেষে একই props এবং state-এর জন্য একই UI আউটপুট উৎপন্ন করা উচিত।

এটি গুরুত্বপূর্ণ কারণ রিয়্যাক্ট আপনার কম্পোনেন্টগুলোকে একাধিকবার রেন্ডার করতে পারে:

- StrictMode-এর সাথে ডেভেলপমেন্টের সময়
- কনকারেন্ট মোডে রেন্ডারিং বিরতি দেওয়া এবং পুনরায় চালু করার সময়
- জরুরি আপডেটগুলোকে অগ্রাধিকার দেওয়ার সময়

পিউর কম্পোনেন্টগুলো স্বাভাবিকভাবেই আইডেমপোটেন্ট কারণ:

- তারা কতবার কল করা হয়েছে তার উপর নির্ভর করে না
- রেন্ডারের সময় বাহ্যিক স্টেট পরিবর্তন করে না
- একই ইনপুটের জন্য সবসময় একই আউটপুট উৎপন্ন করে

এটিকে এভাবে ভাবুন: যদি আপনি একটি অপারেশনের পরে ক্যালকুলেটরে একাধিকবার "=" চাপেন, আপনার প্রতিবার একই ফলাফল পাওয়া উচিত - এটাই আইডেমপোটেন্স।

### Q15: পিউর কম্পোনেন্টগুলো কীভাবে রিয়্যাক্টের রিকনসিলিয়েশন প্রসেসকে উপকৃত করে?

**উত্তর:**
রিয়্যাক্টের রিকনসিলিয়েশন প্রসেস (কীভাবে এটি DOM আপডেট করবে তা ঠিক করে) পিউর কম্পোনেন্টের সাথে সবচেয়ে ভালো কাজ করে:

1. **পারফরম্যান্স অপ্টিমাইজেশন**: রিয়্যাক্ট নিরাপদে পিউর কম্পোনেন্টগুলোর পুনঃরেন্ডারিং এড়াতে পারে যখন তাদের ইনপুট পরিবর্তন হয়

   ```jsx
   // title পরিবর্তন না হলে রিয়্যাক্ট PureSection পুনঃরেন্ডার করা এড়াতে পারে
   <PureSection title="আমাদের সম্পর্কে" />
   ```

2. **প্রেডিক্টেবল আপডেট**: যেহেতু পিউর কম্পোনেন্টগুলো একই ইনপুটের জন্য সবসময় একই আউটপুট দেয়, রিয়্যাক্ট নিরাপদে তারা কী রেন্ডার করবে তা অনুমান করতে পারে

3. **মেমোইজেশন**: React.memo এবং PureComponent নিরাপদে ফলাফল ক্যাশ করতে পারে যেহেতু পিউর কম্পোনেন্টগুলো প্রেডিক্টেবল

   ```jsx
   // ExpensiveComponent পিউর হওয়ায় সঠিকভাবে কাজ করে
   const MemoizedExpensive = React.memo(ExpensiveComponent);
   ```

4. **কনকারেনসি**: রিয়্যাক্ট পিউর কম্পোনেন্টগুলোর রেন্ডারিং বিরতি দিতে, পুনরায় শুরু করতে বা বাতিল করতে পারে কোন ভিজুয়াল গ্লিচ ছাড়াই

5. **টাইম-স্লাইসিং**: রিয়্যাক্ট রেন্ডারিং কাজকে একাধিক ফ্রেমে ছড়িয়ে দিতে পারে ভালো রেসপন্সিভনেসের জন্য, যার জন্য কম্পোনেন্টগুলো পিউর হওয়া প্রয়োজন

পুরো রিয়্যাক্ট মডেল এই ধারণার উপর ভিত্তি করে যে রেন্ডারিং একটি পিউর অপারেশন যা সাইড ইফেক্ট সৃষ্টি করে না।

## 📝 ইন্টারভিউ টিপস

পিউর কম্পোনেন্ট সম্পর্কে প্রশ্নের উত্তর দেওয়ার সময়:

1. প্রথমে ধারণাটি সহজ ভাষায় ব্যাখ্যা করুন (নির্ভরযোগ্য, কোন সাইড ইফেক্ট নেই)
2. ব্যবহারিক সুবিধাগুলো উল্লেখ করুন (নির্ভরযোগ্যতা, পারফরম্যান্স, টেস্টিং)
3. কোড উদাহরণ দেখালে, স্পষ্টভাবে হাইলাইট করুন কি জিনিস একটি কম্পোনেন্টকে পিউর বা অপিউর করে
4. আলোচনা করুন যে সাইড ইফেক্ট কোথায় থাকা উচিত (ইভেন্ট হ্যান্ডলার, useEffect)
5. ধারণাটিকে অন্যান্য রিয়্যাক্ট ফিচারের সাথে সংযুক্ত করুন (মেমোইজেশন, StrictMode, কনকারেন্ট রেন্ডারিং)

রিয়্যাক্টের আর্কিটেকচার কম্পোনেন্ট পিউরিটির চারপাশে তৈরি করা হয়েছে, তাই এই ধারণার বিষয়ে আপনার বোঝাপড়া প্রদর্শন করে যে আপনি রিয়্যাক্টের ডিজাইনের মূল নীতিগুলি বুঝতে পেরেছেন।
