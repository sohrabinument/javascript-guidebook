import { Callout } from "nextra/components";

# কম্পোনেন্ট ইম্পোর্ট ও এক্সপোর্ট – ইন্টারভিউ প্রশ্ন ও উত্তর 📦

<img src="https://upload.wikimedia.org/wikipedia/commons/a/a7/React-icon.svg" alt="React Logo" width="80" style={{marginBottom: '1rem'}} />

এই প্রশ্নগুলো ব্যবহার করুন ইন্টারভিউ প্রস্তুতির জন্য বা React-এ কম্পোনেন্ট ইম্পোর্ট ও এক্সপোর্ট সম্পর্কে আপনার বোঝা যাচাই করতে।

> **💡 প্রো টিপ:** প্রতিটি প্রশ্নের উত্তর নিজে চেষ্টা করুন, তারপর উত্তর দেখুন। এতে ধারণা আরও পরিষ্কার হবে!

---

## ১.১ – কম্পোনেন্ট সংগঠন ও এক্সপোর্ট 🗂️

<div style={{ background: 'rgba(0, 100, 200, 0.10)', padding: '15px', borderRadius: '5px', marginBottom: '20px', border: '1px solid rgba(0, 100, 200, 0.15)', color: 'inherit' }}>

**প্রশ্ন ১: কেন কম্পোনেন্ট আলাদা ফাইলে ভাগ করবেন?**  
উত্তর: কোড আরও সংগঠিত, পুনঃব্যবহারযোগ্য ও রক্ষণাবেক্ষণযোগ্য হয়।

**প্রশ্ন ২: React-এ ডিফল্ট ও নেমড এক্সপোর্টের মধ্যে পার্থক্য কী?**  
উত্তর: ডিফল্ট এক্সপোর্টে প্রতি ফাইলে একটি এক্সপোর্ট থাকে এবং যেকোনো নামে ইম্পোর্ট করা যায়। নেমড এক্সপোর্টে একাধিক এক্সপোর্ট রাখা যায় এবং নির্দিষ্ট নামেই ইম্পোর্ট করতে হয়।

**প্রশ্ন ৩: ডিফল্ট এক্সপোর্ট কীভাবে ইম্পোর্ট করবেন?**  
উত্তর: `import ComponentName from './ComponentFile';`

**প্রশ্ন ৪: নেমড এক্সপোর্ট কীভাবে ইম্পোর্ট করবেন?**  
উত্তর: `import { ComponentName } from './ComponentFile';`

</div>

---

## ১.২ – অ্যাডভান্সড ব্যবহার ও সিনারিও 🚀

<div style={{ background: 'rgba(230, 150, 70, 0.10)', padding: '15px', borderRadius: '5px', marginBottom: '20px', border: '1px solid rgba(230, 150, 70, 0.15)', color: 'inherit' }}>

**প্রশ্ন ৫: একই ফাইলে ডিফল্ট ও নেমড এক্সপোর্ট রাখা যায় কি? ইম্পোর্ট করবেন কীভাবে?**  
উত্তর: হ্যাঁ। ডিফল্ট এক্সপোর্ট ব্রেস ছাড়া এবং নেমড এক্সপোর্ট ব্রেস দিয়ে ইম্পোর্ট করতে হয়:  
`import DefaultComponent, { NamedComponent } from './ComponentFile';`

**প্রশ্ন ৬: নেমড এক্সপোর্টকে ডিফল্ট হিসেবে বা ডিফল্টকে নেমড হিসেবে ইম্পোর্ট করলে কী হবে?**  
উত্তর: এরর হবে; ইম্পোর্ট/এক্সপোর্ট সিনট্যাক্স অবশ্যই মিলতে হবে।

**প্রশ্ন ৭: ধরুন, আপনি একই ফাইল থেকে `Gallery` ও `Profile` ব্যবহার করতে চান। কীভাবে এক্সপোর্ট ও ইম্পোর্ট করবেন?**  
উত্তর: একটিকে ডিফল্ট ও অন্যটিকে নেমড, অথবা দুটোই নেমড এক্সপোর্ট করুন। ইম্পোর্ট করুন:  
`import Gallery, { Profile } from './Gallery';` অথবা `import { Gallery, Profile } from './Gallery';`

**প্রশ্ন ৮: অ্যানোনিমাস ডিফল্ট এক্সপোর্ট (যেমন `export default () => {}`) ব্যবহার না করার কারণ কী?**  
উত্তর: এতে ডিবাগিং কঠিন হয়, কারণ কম্পোনেন্টের অর্থবহ নাম error message বা React DevTools-এ দেখা যায় না।

</div>

---

<Callout type="info">ছোট React প্রজেক্টে কম্পোনেন্ট ভাগ ও ইম্পোর্ট/এক্সপোর্ট প্র্যাকটিস করুন—ধারণা আরও পরিষ্কার হবে!</Callout>

---

## প্র্যাকটিস ইন্টারভিউ এক্সারসাইজ 🏋️‍♂️

<div style={{ background: 'rgba(20, 120, 220, 0.10)', padding: '20px', borderRadius: '5px', marginBottom: '20px', border: '1px solid rgba(20, 120, 220, 0.15)', color: 'inherit' }}>

### সিনারিও-ভিত্তিক প্রশ্ন:

ধরুন, আপনি একটি React অ্যাপে কাজ করছেন যেখানে কম্পোনেন্টের সংখ্যা বাড়ছে। দেখলেন, কিছু ফাইল অনেক বড় ও ম্যানেজ করা কঠিন হয়ে যাচ্ছে।

**প্রশ্ন:** আরও ভালোভাবে মেইনটেইন ও স্কেল করার জন্য কম্পোনেন্ট ও তাদের এক্সপোর্ট/ইম্পোর্ট কীভাবে সংগঠিত করবেন?

উত্তর ভাবুন, তারপর নিচের সাজেশন দেখুন।

<details>
<summary>**উত্তরের জন্য ক্লিক করুন**</summary>

**সেরা চর্চা:**

1. ফাংশনালিটির ভিত্তিতে কম্পোনেন্ট আলাদা ফাইলে ভাগ করুন।
2. ফাইলে প্রধান কম্পোনেন্টের জন্য ডিফল্ট এক্সপোর্ট, হেল্পারদের জন্য নেমড এক্সপোর্ট ব্যবহার করুন।
3. অর্থবহ ও ধারাবাহিক নাম ব্যবহার করুন ফাইল/কম্পোনেন্টের জন্য।
4. সম্পর্কিত কম্পোনেন্ট ফোল্ডারে রাখুন।
5. দরকার হলে index.js ব্যবহার করুন ক্লিন ইম্পোর্টের জন্য।
6. ডিবাগিং সহজ করতে অ্যানোনিমাস ডিফল্ট এক্সপোর্ট এড়িয়ে চলুন।

**উপকারিতা:**
- কোড খুঁজে পাওয়া ও মেইনটেইন করা সহজ
- পুনঃব্যবহারযোগ্যতা বাড়ে
- টিমে কম মার্জ কনফ্লিক্ট

</details>
</div>

---

**টিপ:** প্রশ্ন ও উত্তরগুলো পড়ে নিজ ভাষায় ব্যাখ্যা দেয়ার চেষ্টা করুন—ইন্টারভিউ প্রস্তুতির জন্য দারুণ অনুশীলন!

> **🚀 চূড়ান্ত পরামর্শ:** ইন্টারভিউতে শুধু সিনট্যাক্স জানলেই হবে না, কেন ভালো সংগঠন দরকার এবং বাস্তবে কীভাবে করবেন, সেটাও বোঝাতে হবে।
