import { Callout } from "nextra/components";

---

<img
  src="https://upload.wikimedia.org/wikipedia/commons/a/a7/React-icon.svg"
  alt="React Logo"
  width="100"
  style={{ marginBottom: "1rem" }}
/>

# 📦 রিয়্যাক্টে কম্পোনেন্ট ইম্পোর্ট ও এক্সপোর্ট

স্বাগতম! আপনার React কম্পোনেন্টগুলোকে আলাদা ফাইলে ভাগ করা এবং সেগুলো ইম্পোর্ট/এক্সপোর্ট করা স্কেলেবল, রক্ষণাবেক্ষণযোগ্য অ্যাপ বানানোর জন্য খুবই গুরুত্বপূর্ণ। চলুন সহজভাবে শিখে নেই! 🚀

---

## 🗂️ কেন কম্পোনেন্ট আলাদা ফাইলে ভাগ করবেন?

- **সংগঠন:** কোড খুঁজে পাওয়া ও রক্ষণাবেক্ষণ সহজ হয়।
- **পুনঃব্যবহারযোগ্যতা:** একই কম্পোনেন্ট একাধিক জায়গায় ব্যবহার করা যায়।
- **স্কেলেবিলিটি:** বড় অ্যাপ সহজে ম্যানেজ করা যায়।

<Callout type="info">
  প্রতিটি ফাইলকে ভাবুন আপনার অ্যাপের জন্য একটি LEGO ব্লকের কন্টেইনার হিসেবে!
</Callout>

---

## 🚀 কম্পোনেন্ট এক্সপোর্ট করা

কম্পোনেন্ট এক্সপোর্ট করার দুটি প্রধান উপায় আছে:

### ১. ডিফল্ট এক্সপোর্ট

যদি ফাইলে শুধু একটি প্রধান কম্পোনেন্ট থাকে, তাহলে `export default` ব্যবহার করুন।

```jsx
// Profile.js
export default function Profile() {
  return (
    <img
      src="https://images.unsplash.com/photo-1511367461989-f85a21fda167?auto=format&fit=facearea&w=400&h=400&q=80"
      alt="Profile Example"
    />
  );
}
```

### ২. নেমড এক্সপোর্ট

একই ফাইলে একাধিক কম্পোনেন্ট এক্সপোর্ট করতে চাইলে `export` (default ছাড়া) ব্যবহার করুন।

```jsx
// Gallery.js
export function Profile() {
  return (
    <img
      src="https://images.unsplash.com/photo-1511367461989-f85a21fda167?auto=format&fit=facearea&w=400&h=400&q=80"
      alt="Profile Example"
    />
  );
}

export function Gallery() {
  return (
    <section>
      <h1>অসাধারণ বিজ্ঞানীরা</h1>
      <Profile />
      <Profile />
      <Profile />
    </section>
  );
}
```

---

## 📥 কম্পোনেন্ট ইম্পোর্ট করা

কীভাবে ইম্পোর্ট করবেন, তা নির্ভর করে কীভাবে এক্সপোর্ট করেছেন তার ওপর:

### ডিফল্ট এক্সপোর্ট ইম্পোর্ট

```jsx
import Profile from "./Profile";
```

### নেমড এক্সপোর্ট ইম্পোর্ট

```jsx
import { Profile, Gallery } from "./Gallery";
```

<Callout type="warning">
  প্রতি ফাইলে শুধু একটি ডিফল্ট এক্সপোর্ট থাকতে পারে, কিন্তু যত খুশি নেমড এক্সপোর্ট রাখা যায়।
</Callout>

---

## 🔄 ডিফল্ট ও নেমড এক্সপোর্ট একসাথে

দুটো একসাথে ব্যবহার করা যায়, তবে ইম্পোর্ট সিনট্যাক্সে সতর্ক থাকুন:

```jsx
// Gallery.js
export function Profile() {
  /* ... */
}
export default function Gallery() {
  /* ... */
}

// App.js
import Gallery, { Profile } from "./Gallery";
```

---

## 🏆 সেরা চর্চা

- ফাইলে প্রধান কম্পোনেন্টের জন্য ডিফল্ট এক্সপোর্ট ব্যবহার করুন।
- হেল্পার বা অতিরিক্ত কম্পোনেন্টের জন্য নেমড এক্সপোর্ট ব্যবহার করুন।
- সবসময় অর্থবহ নাম ব্যবহার করুন কম্পোনেন্ট ও ফাইলের জন্য।
- এক্সপোর্ট/ইম্পোর্ট স্টাইলে ধারাবাহিকতা বজায় রাখুন।

<Callout type="success">
  প্রো টিপ: নামকরণ ও স্ট্রাকচারে ধারাবাহিকতা রাখলে কোডবেস সহজে নেভিগেট ও রক্ষণাবেক্ষণ করা যায়!
</Callout>

---

## 🧩 উদাহরণ: কম্পোনেন্ট ভাগ করা

ধরুন, আপনার কাছে `Gallery` ও `Profile` কম্পোনেন্ট আছে। এগুলো আলাদা ফাইলে রাখতে পারেন:

**Profile.js**

```jsx
export default function Profile() {
  return (
    <img
      src="https://images.unsplash.com/photo-1511367461989-f85a21fda167?auto=format&fit=facearea&w=400&h=400&q=80"
      alt="Profile Example"
    />
  );
}
```

**Gallery.js**

```jsx
import Profile from "./Profile";

export default function Gallery() {
  return (
    <section>
      <h1>অসাধারণ বিজ্ঞানীরা</h1>
      <Profile />
      <Profile />
      <Profile />
    </section>
  );
}
```

**App.js**

```jsx
import Gallery from "./Gallery";

export default function App() {
  return <Gallery />;
}
```

---

আরও জানতে পড়ুন: [React অফিসিয়াল ডকুমেন্টেশন – কম্পোনেন্ট ইম্পোর্ট ও এক্সপোর্ট](https://react.dev/learn/importing-and-exporting-components)

---

<Callout type="info">
  নিজে নিজে কম্পোনেন্ট ফাইলে ভাগ করে ইম্পোর্ট/এক্সপোর্ট প্র্যাকটিস করুন। বাস্তব React ডেভেলপমেন্টে এটি খুবই গুরুত্বপূর্ণ দক্ষতা!
</Callout>
