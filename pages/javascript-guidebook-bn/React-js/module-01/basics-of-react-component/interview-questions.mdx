import { Callout } from "nextra/components";

# রিয়্যাক্ট কম্পোনেন্ট – ইন্টারভিউ প্রশ্ন ও উত্তর 🎯

এই প্রশ্নগুলো ব্যবহার করুন ইন্টারভিউ প্রস্তুতির জন্য বা React কম্পোনেন্ট সম্পর্কে আপনার বোঝাপড়া যাচাই করতে।

> **💡 প্রো টিপ:** প্রতিটি প্রশ্নের উত্তর নিজে নিজে দেয়ার চেষ্টা করুন, তারপর উত্তর মিলিয়ে দেখুন। এতে ধারণাগুলো আরও ভালোভাবে মনে থাকবে!

---

## ১.১ – কম্পোনেন্টের বেসিক 🧩

<div style={{ background: 'rgba(0, 100, 200, 0.10)', padding: '15px', borderRadius: '5px', marginBottom: '20px', border: '1px solid rgba(0, 100, 200, 0.15)', color: 'inherit' }}>

**প্রশ্ন ১: রিয়্যাক্ট কম্পোনেন্ট কী?**  
উত্তর: React-এ একটি পুনঃব্যবহারযোগ্য, স্বয়ংসম্পূর্ণ UI অংশ, যা ফাংশন বা ক্লাস হিসেবে ডিফাইন করা হয়।

**প্রশ্ন ২: কম্পোনেন্টের নাম বড় হাতের অক্ষরে শুরু করা কেন জরুরি?**  
উত্তর: React ছোট হাতের ট্যাগকে HTML এলিমেন্ট এবং বড় হাতের ট্যাগকে কাস্টম কম্পোনেন্ট হিসেবে ধরে।

**প্রশ্ন ৩: React-এ ফাংশন কম্পোনেন্ট কীভাবে তৈরি করবেন?**  
উত্তর: একটি জাভাস্ক্রিপ্ট ফাংশন ডিফাইন করে, যা JSX রিটার্ন করে এবং বড় হাতের অক্ষরে শুরু হয়।

**প্রশ্ন ৪: এক কম্পোনেন্টকে আরেকটিতে কীভাবে ব্যবহার করবেন?**  
উত্তর: ইম্পোর্ট করে কাস্টম ট্যাগ হিসেবে ব্যবহার করুন, যেমন `<Profile />`।

</div>

---

## ১.২ – সেরা চর্চা ও সিনারিও 🏆

<div style={{ background: 'rgba(230, 150, 70, 0.10)', padding: '15px', borderRadius: '5px', marginBottom: '20px', border: '1px solid rgba(230, 150, 70, 0.15)', color: 'inherit' }}>

**প্রশ্ন ৫: React-এ কম্পোনেন্ট ব্যবহারের সুবিধা কী?**  
উত্তর: পুনঃব্যবহারযোগ্যতা, দায়িত্ব বিভাজন, সহজ রক্ষণাবেক্ষণ ও ভালো সংগঠন।

**প্রশ্ন ৬: কম্পোনেন্টের ভিতরে ফাংশন ডিফাইন করা যায়? কেন নয়?**  
উত্তর: না, কম্পোনেন্টের ভিতরে ফাংশন ডিফাইন করা উচিত নয়। হেল্পার ফাংশন বাইরে বা আলাদা কম্পোনেন্ট হিসেবে ডিফাইন করুন—এতে কোড পরিষ্কার ও রক্ষণাবেক্ষণযোগ্য থাকে।

**প্রশ্ন ৭: সিনারিও: একটি বাটন বিভিন্ন জায়গায় বিভিন্ন লেবেল নিয়ে ব্যবহৃত হচ্ছে। কীভাবে এটি পুনঃব্যবহারযোগ্য কম্পোনেন্ট বানাবেন?**  
উত্তর: একটি Button কম্পোনেন্ট তৈরি করুন, যা label prop নেয় এবং যেখানে দরকার সেখানে বিভিন্ন label পাঠিয়ে ব্যবহার করুন।

**প্রশ্ন ৮: কম্পোনেন্টের নাম বড় হাতের অক্ষরে না লিখলে কী হয়?**  
উত্তর: React এটিকে সাধারণ HTML ট্যাগ হিসেবে ধরে, ফলে আপনার কম্পোনেন্ট প্রত্যাশিতভাবে রেন্ডার হবে না।

**প্রশ্ন ৯: কখন একটি কম্পোনেন্ট আলাদা ফাইলে ভাগ করবেন?**  
উত্তর: যখন কম্পোনেন্ট বড়, জটিল বা একাধিক জায়গায় ব্যবহৃত হয়।

</div>

---

<Callout type="info">এই প্রশ্ন-উত্তরগুলো পড়ে নিজের ভাষায় ব্যাখ্যা দিন—ইন্টারভিউ প্রস্তুতিতে দারুণ কাজে দেবে!</Callout>
