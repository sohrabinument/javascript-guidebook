import { Steps } from "nextra/components";
import { Callout } from "nextra/components";

---

<img src="https://upload.wikimedia.org/wikipedia/commons/a/a7/React-icon.svg" alt="React Logo" width="100" style={{marginBottom: '1rem'}} />

# 🧩 রিয়্যাক্ট কম্পোনেন্টের বেসিকস

স্বাগতম! কম্পোনেন্ট হলো React-এর প্রাণ। এগুলো আপনার UI-কে পুনঃব্যবহারযোগ্য, সংগঠিত ও মজাদার করে তোলে। চলুন দেখে নেই কীভাবে আপনি এগুলো আয়ত্ত করতে পারেন! 🚀

## 🏗️ রিয়্যাক্ট কম্পোনেন্ট কী?

একটি **কম্পোনেন্ট** হলো React-এর একটি পুনঃব্যবহারযোগ্য, স্বয়ংসম্পূর্ণ UI অংশ। কম্পোনেন্টগুলো হলো React অ্যাপ্লিকেশনের বিল্ডিং ব্লক—আপনার UI-এর প্রতিটি অংশ, একটি বাটন থেকে শুরু করে পুরো পেজ পর্যন্ত, কম্পোনেন্ট হতে পারে। কম্পোনেন্ট ব্যবহার করে জটিল ইন্টারফেসকে ছোট, সহজে ব্যবস্থাপনাযোগ্য ও পুনঃব্যবহারযোগ্য অংশে ভাগ করা যায়।

<Callout type="info">কম্পোনেন্টগুলোকে ভাবুন আপনার UI-এর LEGO ব্লক হিসেবে!</Callout>

## 💡 কেন কম্পোনেন্ট ব্যবহার করবেন?

- **পুনঃব্যবহারযোগ্যতা:** একবার লিখুন, অ্যাপের যেকোনো জায়গায় ব্যবহার করুন।
- **দায়িত্ব বিভাজন:** প্রতিটি কম্পোনেন্ট নিজের লজিক ও UI পরিচালনা করে।
- **রক্ষণাবেক্ষণযোগ্যতা:** আপডেট, ডিবাগ ও টেস্ট করা সহজ।

## 🧑‍💻 কম্পোনেন্টের ধরন

- **ফাংশন কম্পোনেন্ট:** সবচেয়ে প্রচলিত ও আধুনিক উপায়। এগুলো জাভাস্ক্রিপ্ট ফাংশন যা JSX রিটার্ন করে।
- **ক্লাস কম্পোনেন্ট:** পুরনো পদ্ধতি, এখন কম ব্যবহৃত, ES6 ক্লাস ব্যবহার করে।

## ✨ ফাংশন কম্পোনেন্ট তৈরি

```jsx
export default function Profile() {
  return <img src="https://images.unsplash.com/photo-1511367461989-f85a21fda167?auto=format&fit=facearea&w=400&h=400&q=80" alt="Profile Example" />;
}
```

<Callout type="success">প্রো টিপ: ডেমোতে নির্ভরযোগ্য ইমেজ URL-এর জন্য Unsplash বা Wikimedia ব্যবহার করুন!</Callout>

**মূল পয়েন্ট:**
- ফাংশনের নাম বড় হাতের অক্ষরে শুরু করতে হবে।
- ফাংশনটি JSX (মার্কআপ) রিটার্ন করে।
- অন্যত্র ইম্পোর্টের জন্য `export default` ব্যবহার করুন।

## 🧩 কম্পোনেন্ট ব্যবহার

একটি কম্পোনেন্টকে আরেকটি কম্পোনেন্টের ভিতরে কাস্টম HTML ট্যাগের মতো ব্যবহার করা যায়:

```jsx
import Profile from "./Profile";

export default function Gallery() {
  return (
    <section>
      <h1>অসাধারণ বিজ্ঞানীরা</h1>
      <Profile />
      <Profile />
      <Profile />
    </section>
  );
}
```

## 🏆 সেরা চর্চা ও নিয়ম

<Callout type="warning">কম্পোনেন্টের নাম অবশ্যই বড় হাতের অক্ষরে শুরু করতে হবে!</Callout>

- **কম্পোনেন্ট ফাংশন থেকে JSX রিটার্ন করুন।**
- **কম্পোনেন্টের ভিতরে আরেকটি ফাংশন ডিফাইন করবেন না (হেল্পার ফাংশন বাইরে বা আলাদা কম্পোনেন্ট হিসেবে ডিফাইন করুন)।**
- **ছোট ও পুনঃব্যবহার না হলে একই ফাইলে রাখুন, বড় বা পুনঃব্যবহারযোগ্য হলে আলাদা ফাইলে রাখুন।**

## ✅ সংক্ষেপে ও পরবর্তী ধাপ

- কম্পোনেন্ট আপনার UI-কে মডুলার ও পুনঃব্যবহারযোগ্য করে তোলে।
- সবসময় কম্পোনেন্টের নাম বড় হাতের অক্ষরে শুরু করুন।
- নিজে নিজে ছোট কম্পোনেন্ট তৈরি করে প্র্যাকটিস করুন!

<Callout type="info">আপনার জ্ঞান যাচাই করতে চান? এই অধ্যায়ের ইন্টারভিউ প্রশ্নগুলো দেখে নিন!</Callout>
