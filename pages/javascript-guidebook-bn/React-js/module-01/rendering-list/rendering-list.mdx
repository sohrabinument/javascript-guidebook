import { Callout } from "nextra/components";

<img src="https://upload.wikimedia.org/wikipedia/commons/a/a7/React-icon.svg" alt="React Logo" width="70" style={{marginBottom: '1rem'}} />

# রিয়্যাক্টে লিস্ট রেন্ডারিং

> **👋 ফ্রেন্ডলি পরিচিতি:**
>
> রিয়্যাক্টে লিস্ট রেন্ডারিং মানে যেকোনো ডেটার অ্যারে থেকে ডাইনামিক, ইন্টারঅ্যাকটিভ UI বানানো—একদম সুপারপাওয়ার! চলুন, কিভাবে প্রোদের মতো করবেন দেখে নেই। 🚀

---

## অ্যারে থেকে ডেটা রেন্ডার করা 🗂️

ধরুন, আপনার কাছে কিছু মানুষের নামের লিস্ট আছে, যেটা দেখাতে চান:

```jsx
const people = [
  'Creola Katherine Johnson: mathematician',
  'Mario José Molina-Pasquel Henríquez: chemist',
  'Mohammad Abdus Salam: physicist',
  'Percy Lavon Julian: chemist',
  'Subrahmanyan Chandrasekhar: astrophysicist'
];

export default function List() {
  const listItems = people.map(person => <li>{person}</li>);
  return <ul>{listItems}</ul>;
}
```

<Callout type="tip"><code>map()</code> ব্যবহার করে অ্যারের প্রতিটি আইটেমকে React এলিমেন্টে রূপান্তর করুন। লিস্ট রেন্ডার করার সবচেয়ে সহজ ও জনপ্রিয় উপায়!</Callout>

---

## নির্দিষ্ট আইটেম ফিল্টার করা 🔍

<code>filter()</code> মেথড ব্যবহার করে শুধু নির্দিষ্ট কিছু আইটেম দেখাতে পারেন। যেমন, শুধু কেমিস্টদের দেখাতে চাইলে:

```jsx
const people = [
  { id: 0, name: 'Creola Katherine Johnson', profession: 'mathematician' },
  { id: 1, name: 'Mario José Molina-Pasquel Henríquez', profession: 'chemist' },
  { id: 2, name: 'Mohammad Abdus Salam', profession: 'physicist' },
  { id: 3, name: 'Percy Lavon Julian', profession: 'chemist' },
  { id: 4, name: 'Subrahmanyan Chandrasekhar', profession: 'astrophysicist' }
];

export default function ChemistList() {
  const chemists = people.filter(person => person.profession === 'chemist');
  const listItems = chemists.map(person => <li key={person.id}>{person.name}</li>);
  return <ul>{listItems}</ul>;
}
```

<Callout type="info"><code>filter()</code> ও <code>map()</code> একসাথে ব্যবহার করে যেটা দরকার শুধু সেটাই দেখান। সার্চ, ক্যাটাগরি ইত্যাদির জন্য দারুণ!</Callout>

---

## <code>key</code> দিয়ে লিস্ট আইটেমের অর্ডার ঠিক রাখা 🗝️

লিস্ট রেন্ডার করার সময় প্রতিটি চাইল্ডের জন্য ইউনিক <code>key</code> প্রপ দিন:

```jsx
const listItems = people.map(person =>
  <li key={person.id}>{person.name}</li>
);
```

<Callout type="warning">প্রতিটি লিস্ট আইটেমের জন্য ইউনিক ও স্টেবল <code>key</code> (যেমন ID) ব্যবহার করুন। লিস্টে অর্ডার বদলাতে পারে বা আইটেম যোগ/বিয়োগ হতে পারে—এমন ক্ষেত্রে অ্যারে ইনডেক্স ব্যবহার এড়িয়ে চলুন।</Callout>

**কেন key দরকার?**  
key React-কে বলে দেয় কোন আইটেম চেঞ্জ হয়েছে, যোগ বা বাদ হয়েছে। এতে পারফরম্যান্স বাড়ে এবং UI সবসময় ডেটার সাথে সিঙ্ক থাকে।

---

## একাধিক এলিমেন্ট রেন্ডার করা 🧩

প্রতিটি আইটেমের জন্য একাধিক এলিমেন্ট দেখাতে চাইলে <code>&lt;React.Fragment&gt;</code> বা <code>&lt;&gt;...&lt;/&gt;</code> ব্যবহার করুন, সাথে key দিন:

```jsx
import { Fragment } from 'react';

const listItems = people.map(person =>
  <Fragment key={person.id}>
    <h2>{person.name}</h2>
    <p>{person.profession}</p>
  </Fragment>
);
```

<Callout type="tip">ফ্র্যাগমেন্ট দিয়ে একাধিক এলিমেন্ট গ্রুপ করুন, কিন্তু DOM-এ বাড়তি কোনো নোড যোগ হবে না। জটিল লিস্ট আইটেমের জন্য পারফেক্ট!</Callout>

---

## রিক্যাপ ও পরবর্তী ধাপ 🚦

- <code>map()</code> দিয়ে অ্যারে থেকে কম্পোনেন্ট বানান।
- <code>filter()</code> দিয়ে নির্দিষ্ট আইটেম দেখান।
- প্রতিটি লিস্ট আইটেমে ইউনিক <code>key</code> দিন।
- অ্যারে ইনডেক্স key হিসেবে ব্যবহার এড়িয়ে চলুন।

<Callout type="success">প্র্যাকটিস হিসেবে সার্চ বা ফিল্টার ফিচারসহ লিস্ট বানান। শুভ কোডিং! 🎉</Callout>
