import { Callout } from "nextra/components";

<img src="https://upload.wikimedia.org/wikipedia/commons/a/a7/React-icon.svg" alt="React Logo" width="70" style={{marginBottom: '1rem'}} />

# রেন্ডারিং লিস্ট – ইন্টারভিউ প্রশ্ন ও উত্তর 🎯

এই প্রশ্নগুলো ব্যবহার করুন ইন্টারভিউ প্রস্তুতির জন্য বা রিয়্যাক্টে লিস্ট রেন্ডারিং সম্পর্কে আপনার বোঝা যাচাই করতে।

> **💡 প্রো টিপ:** প্রতিটি প্রশ্নের উত্তর নিজে চেষ্টা করুন, তারপর উত্তর দেখুন। এতে ধারণা আরও পরিষ্কার হবে!

---

## ১.১ – লিস্ট রেন্ডারিং বেসিক 🌱

<div style={{ background: 'rgba(0, 100, 200, 0.10)', padding: '15px', borderRadius: '5px', marginBottom: '20px', border: '1px solid rgba(0, 100, 200, 0.15)', color: 'inherit' }}>

**প্রশ্ন ১: রিয়্যাক্টে কিভাবে লিস্ট রেন্ডার করবেন?**  
উত্তর: JavaScript-এর `map()` ফাংশন ব্যবহার করে ডেটার অ্যারেকে JSX এলিমেন্টের অ্যারেতে রূপান্তর করুন। যেমন:
```jsx
const items = ["Apple", "Banana", "Cherry"];
const listItems = items.map((item) => <li>{item}</li>);
return <ul>{listItems}</ul>;
```

**প্রশ্ন ২: লিস্ট রেন্ডার করার সময় `key` প্রপ কেন গুরুত্বপূর্ণ?**  
উত্তর: `key` প্রপ React-কে বলে দেয় কোন আইটেম চেঞ্জ হয়েছে, যোগ বা বাদ হয়েছে। এতে React শুধু পরিবর্তিত এলিমেন্টগুলো আপডেট করে, পারফরম্যান্স বাড়ে ও বাগ কমে।

**প্রশ্ন ৩: লিস্ট রেন্ডার করার সময় `key` না দিলে কী হয়?**  
উত্তর: React কনসোলে ওয়ার্নিং দেখাবে। ইউনিক key না থাকলে React ঠিকমতো কোন আইটেম চেঞ্জ হয়েছে তা ধরতে পারে না, ফলে আপডেট ও UI-তে সমস্যা হতে পারে।

</div>

---

## ১.২ – ইন্টারমিডিয়েট ব্যবহার 💡

<div style={{ background: 'rgba(230, 150, 70, 0.10)', padding: '15px', borderRadius: '5px', marginBottom: '20px', border: '1px solid rgba(230, 150, 70, 0.15)', color: 'inherit' }}>

**প্রশ্ন ৪: অ্যারে ইনডেক্সকে key হিসেবে ব্যবহার করলে কী সমস্যা হতে পারে?**  
উত্তর: অ্যারে ইনডেক্স key হিসেবে ব্যবহার করলে লিস্টে আইটেম যোগ/বিয়োগ/অর্ডার চেঞ্জ হলে React ভুলভাবে আইটেম ট্র্যাক করতে পারে, ফলে অপ্রত্যাশিত আচরণ, স্টেট হারানো বা অপ্রয়োজনীয় রেন্ডার হতে পারে।

**প্রশ্ন ৫: রেন্ডার করার আগে কিভাবে লিস্ট ফিল্টার করবেন?**  
উত্তর: JavaScript-এর `filter()` মেথড দিয়ে নতুন অ্যারে বানান, তারপর `map()` দিয়ে রেন্ডার করুন। যেমন:
```jsx
const fruits = ["Apple", "Banana", "Cherry"];
const filtered = fruits.filter((fruit) => fruit.startsWith("B"));
const listItems = filtered.map((fruit) => <li>{fruit}</li>);
return <ul>{listItems}</ul>;
```

**প্রশ্ন ৬: লিস্টের প্রতিটি আইটেমের জন্য একাধিক এলিমেন্ট কিভাবে রেন্ডার করবেন?**  
উত্তর: প্রতিটি আইটেমের জন্য একাধিক এলিমেন্ট রেন্ডার করতে `<React.Fragment>` (বা `<>... </>`) দিয়ে গ্রুপ করুন এবং key দিন:
```jsx
import { Fragment } from "react";
const listItems = people.map((person) => (
  <Fragment key={person.id}>
    <h2>{person.name}</h2>
    <p>{person.profession}</p>
  </Fragment>
));
```

</div>

---

## ১.৩ – সেরা চর্চা ও সিনারিও-ভিত্তিক 🧠

<div style={{ background: 'rgba(220, 50, 50, 0.10)', padding: '15px', borderRadius: '5px', marginBottom: '20px', border: '1px solid rgba(220, 50, 50, 0.15)', color: 'inherit' }}>

**প্রশ্ন ৭: ডাইনামিক লিস্ট (যেখানে আইটেম যোগ/বিয়োগ হতে পারে) কীভাবে হ্যান্ডেল করবেন?**  
উত্তর: প্রতিটি আইটেমের জন্য ডেটা থেকে ইউনিক ও স্টেবল key (যেমন ID) ব্যবহার করুন। এতে React ঠিকমতো লিস্ট আপডেট করতে পারে।

**প্রশ্ন ৮: সিনারিও: আপনার কাছে টাস্কের লিস্ট আছে, ইউজার টাস্ক যোগ/বিয়োগ করতে পারে। React-এ কীভাবে বাগ ছাড়াই লিস্ট আপডেট নিশ্চিত করবেন?**  
উত্তর: প্রতিটি টাস্কের জন্য ইউনিক ID কে key হিসেবে ব্যবহার করুন। ইনডেক্স ব্যবহার করবেন না। টাস্ক যোগ/বিয়োগ হলে অ্যারে আপডেট করুন, React বাকি কাজ করবে।

**প্রশ্ন ৯: সিনারিও: লিস্ট খালি হলে একটি মেসেজ দেখাতে চান। কীভাবে করবেন?**  
উত্তর: রেন্ডার করার আগে কন্ডিশন চেক করুন:
```jsx
{items.length === 0 ? <p>No items found.</p> : <ul>{listItems}</ul>}
```

</div>

---

<Callout type="info">এই প্রশ্নগুলো পড়ে নিজ ভাষায় ব্যাখ্যা দেয়ার চেষ্টা করুন—ইন্টারভিউ প্রস্তুতির জন্য দারুণ অনুশীলন!</Callout>

---

## প্র্যাকটিস ইন্টারভিউ এক্সারসাইজ 🏋️‍♂️

<div style={{ background: 'rgba(20, 120, 220, 0.10)', padding: '20px', borderRadius: '5px', marginBottom: '20px', border: '1px solid rgba(20, 120, 220, 0.15)', color: 'inherit' }}>

### সিনারিও-ভিত্তিক প্রশ্ন:

আপনি একটি কন্টাক্ট লিস্ট অ্যাপ বানাচ্ছেন। ইউজাররা কন্টাক্ট যোগ, মুছতে ও সার্চ করতে পারে। প্রতিটি কন্টাক্টের ইউনিক ID, নাম ও ইমেইল আছে।

**প্রশ্ন:** কিভাবে লিস্ট রেন্ডার করবেন, ডাইনামিক আপডেট হ্যান্ডেল করবেন এবং সার্চে কিছু না মিললে মেসেজ দেখাবেন?

উত্তর ভাবুন, তারপর নিচের সাজেশন দেখুন।

<details>
<summary>**উত্তরের জন্য ক্লিক করুন**</summary>

**উত্তরের ধরন:**

```jsx
const filteredContacts = contacts.filter(/* আপনার সার্চ লজিক */);
if (filteredContacts.length === 0) {
  return <p>No contacts found.</p>;
}
return (
  <ul>
    {filteredContacts.map(contact => (
      <li key={contact.id}>{contact.name} ({contact.email})</li>
    ))}
  </ul>
);
```

- প্রতিটি কন্টাক্টের জন্য ইউনিক key ব্যবহার করুন।
- রেন্ডার করার আগে লিস্ট ফিল্টার করুন।
- ফিল্টার করা লিস্ট খালি হলে মেসেজ দেখান।

</details>
</div>

---

**টিপ:** প্রশ্ন ও উত্তরগুলো পড়ে নিজ ভাষায় ব্যাখ্যা দেয়ার চেষ্টা করুন—ইন্টারভিউ প্রস্তুতির জন্য দারুণ অনুশীলন!

> **🚀 চূড়ান্ত পরামর্শ:** ইন্টারভিউতে শুধু লিস্ট রেন্ডারিং জানলেই হবে না, কীভাবে পরিষ্কার, মেইনটেইনেবল ও কার্যকর কোড লিখবেন, সেটাও বোঝাতে হবে।
