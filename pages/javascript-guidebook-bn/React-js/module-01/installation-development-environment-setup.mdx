import { Callout } from "nextra/components";

---

# 🚀 রিয়্যাক্ট ডেভেলপমেন্ট এনভায়রনমেন্ট সেটআপ

স্বাগতম! আপনার রিয়্যাক্ট এনভায়রনমেন্ট সেটআপ করা অসাধারণ ওয়েব অ্যাপ তৈরি করার প্রথম ধাপ। এই গাইডটি আপনাকে সহজ ও মজার উপায়ে পুরো প্রক্রিয়াটি দেখাবে। চলুন শুরু করি! 🎉

---

## 🛠️ প্রয়োজনীয়তা

শুরু করার আগে নিশ্চিত করুন, আপনার কাছে আছে:

- [Node.js](https://nodejs.org/) (v16+)
- একটি কোড এডিটর (আমরা সাজেস্ট করি [VS Code](https://code.visualstudio.com/))
- npm বা yarn প্যাকেজ ম্যানেজার

<Callout type="info">টিপ: আপনার Node.js ভার্সন চেক করতে টার্মিনালে <code>node -v</code> লিখুন।</Callout>

---

## ⚡ Vite দিয়ে নতুন রিয়্যাক্ট প্রজেক্ট তৈরি

Vite দিয়ে রিয়্যাক্ট প্রজেক্ট শুরু করা খুবই দ্রুত ও সহজ। নিচের ধাপগুলো অনুসরণ করুন:

1. টার্মিনাল খুলে লিখুন:

```bash
npm create vite@latest my-react-app -- --template react
```

2. প্রজেক্ট ফোল্ডারে যান:

```bash
cd my-react-app
```

3. ডিপেন্ডেন্সি ইন্সটল করুন:

```bash
npm install
```

4. ডেভেলপমেন্ট সার্ভার চালু করুন:

```bash
npm run dev
```

<Callout type="success">আপনার অ্যাপ লাইভ হবে <code>http://localhost:5173</code> এ! 🎈</Callout>

---

## 🗂️ প্রজেক্ট স্ট্রাকচার

সেটআপের পর, আপনার প্রজেক্ট দেখতে এমন হবে:

```
my-react-app/
├── node_modules/
├── public/
├── src/
│   ├── assets/
│   ├── App.css
│   ├── App.jsx
│   ├── index.css
│   └── main.jsx
├── .gitignore
├── index.html
├── package.json
├── vite.config.js
└── README.md
```

### গুরুত্বপূর্ণ ফাইলসমূহ
- <code>src/main.jsx</code>: অ্যাপের এন্ট্রি পয়েন্ট
- <code>src/App.jsx</code>: প্রধান App কম্পোনেন্ট
- <code>vite.config.js</code>: Vite কনফিগারেশন
- <code>index.html</code>: HTML টেমপ্লেট

---

## 📦 অতিরিক্ত টুল ইন্সটল

আরও শক্তিশালী করতে চাইলে, এই জনপ্রিয় প্যাকেজগুলো যোগ করুন:

```bash
npm install react-router-dom @reduxjs/toolkit react-redux axios
```

---

## 💡 VS Code এক্সটেনশন ও সেটিংস

আপনার প্রোডাক্টিভিটি বাড়াতে এই এক্সটেনশনগুলো ব্যবহার করুন:
- ESLint
- Prettier
- React Developer Tools
- JavaScript (ES6) কোড স্নিপেটস

<code>.vscode/settings.json</code> এর জন্য সাজেস্টেড সেটিংস:

```json
{
  "editor.defaultFormatter": "esbenp.prettier-vscode",
  "editor.formatOnSave": true,
  "editor.codeActionsOnSave": {
    "source.fixAll.eslint": "explicit",
    "source.fixAll.tslint": "explicit",
    "source.organizeImports": "explicit"
  },
  "eslint.run": "onSave",
  "emmet.includeLanguages": {
    "javascript": "javascriptreact"
  },
  "path-autocomplete.extensionOnImport": true,
  "path-autocomplete.excludedItems": {
    "*/.js": {
      "when": "**"
    },
    "*/.jsx": {
      "when": "**"
    }
  },
  "javascript.validate.enable": false,
  "typescript.validate.enable": false
}
```

---

## ✨ দ্রুত রিয়্যাক্ট কম্পোনেন্ট স্নিপেট

দ্রুত রিয়্যাক্ট কম্পোনেন্ট তৈরির জন্য এই স্নিপেটটি VS Code-এ যোগ করুন:

```json
{
  "React component": {
    "prefix": "rfc",
    "body": [
      "export default function $1(){",
      "    return (",
      "        $2",
      "    );",
      "}",
    ],
    "description": "React functional component"
  }
}
```

---

## 🌐 দরকারী রিসোর্স

- [Vite ডকুমেন্টেশন](https://vitejs.dev/guide/)
- [React ডকুমেন্টেশন](https://react.dev/)
- [VS Code এক্সটেনশন](https://code.visualstudio.com/docs/editor/extension-marketplace)

---

## ✅ সংক্ষেপে & পরবর্তী ধাপ

আপনি এখন আপনার রিয়্যাক্ট এনভায়রনমেন্ট সেটআপ করেছেন! এখন আপনি করতে পারেন:

- আপনার প্রথম রিয়্যাক্ট কম্পোনেন্ট তৈরি
- React Router দিয়ে রাউটিং সেটআপ
- Redux দিয়ে স্টেট ম্যানেজমেন্ট যোগ
- CSS modules বা CSS-in-JS দিয়ে স্টাইলিং

<Callout type="info">আপনার ডিপেন্ডেন্সি আপডেট রাখুন এবং সেরা প্র্যাকটিস অনুসরণ করুন। শুভ কোডিং! 🚀</Callout>
