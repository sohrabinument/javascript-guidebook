import { Callout } from "nextra/components";

# JSX-এ কার্লি ব্রেস দিয়ে জাভাস্ক্রিপ্ট – ইন্টারভিউ প্রশ্ন ও উত্তর 🟦

<img src="https://upload.wikimedia.org/wikipedia/commons/a/a7/React-icon.svg" alt="React Logo" width="80" style={{marginBottom: '1rem'}} />

এই প্রশ্নগুলো ব্যবহার করুন ইন্টারভিউ প্রস্তুতির জন্য বা JSX-এ কার্লি ব্রেস দিয়ে জাভাস্ক্রিপ্ট ব্যবহারের ধারণা যাচাই করতে।

> **💡 প্রো টিপ:** প্রতিটি প্রশ্নের উত্তর নিজে চেষ্টা করুন, তারপর উত্তর দেখুন। এতে ধারণা আরও পরিষ্কার হবে!

---

## ১.১ – কার্লি ব্রেসের বেসিক 🌱

<div style={{ background: 'rgba(0, 100, 200, 0.10)', padding: '15px', borderRadius: '5px', marginBottom: '20px', border: '1px solid rgba(0, 100, 200, 0.15)', color: 'inherit' }}>

**প্রশ্ন ১: JSX-এ কার্লি ব্রেস `{}` কেন ব্যবহার করা হয়?**  
উত্তর: কার্লি ব্রেস দিয়ে JSX মার্কআপে জাভাস্ক্রিপ্ট এক্সপ্রেশন, ভ্যারিয়েবল বা ফাংশন কল এম্বেড করা যায়।

**প্রশ্ন ২: JSX-এ কার্লি ব্রেস কোথায় ব্যবহার করা যায়?**  
উত্তর: ট্যাগ কনটেন্টে (যেমন, `<h1>{name}</h1>`) এবং অ্যাট্রিবিউটের মান হিসেবে (যেমন, `<img src={avatar} />`)।

</div>

---

## ১.২ – এক্সপ্রেশন ও ইনলাইন স্টাইল 💡

<div style={{ background: 'rgba(230, 150, 70, 0.10)', padding: '15px', borderRadius: '5px', marginBottom: '20px', border: '1px solid rgba(230, 150, 70, 0.15)', color: 'inherit' }}>

**প্রশ্ন ৩: JSX-এ `src="avatar.png"` আর `src={avatar}`-এর মধ্যে পার্থক্য কী?**  
উত্তর: `src="avatar.png"` স্ট্রিং "avatar.png" পাঠায়, আর `src={avatar}` জাভাস্ক্রিপ্ট ভ্যারিয়েবল `avatar`-এর মান পাঠায়।

**প্রশ্ন ৪: JSX-এ ইনলাইন স্টাইল দিতে অবজেক্ট কীভাবে ব্যবহার করবেন?**  
উত্তর: ডাবল কার্লি ব্রেস ব্যবহার করুন: `<div style={{ color: 'red' }}></div>`। বাইরের ব্রেস JSX-এর জন্য, ভেতরের ব্রেস জাভাস্ক্রিপ্ট অবজেক্টের জন্য।

</div>

---

## ১.৩ – অ্যাডভান্সড ও সিনারিও-ভিত্তিক 🧠

<div style={{ background: 'rgba(220, 50, 50, 0.10)', padding: '15px', borderRadius: '5px', marginBottom: '20px', border: '1px solid rgba(220, 50, 50, 0.15)', color: 'inherit' }}>

**প্রশ্ন ৫: নিচের JSX কি বৈধ? কেন?**
```jsx
<{tag}>Hello</{tag}>
```
উত্তর: অবৈধ। JSX-এ কার্লি ব্রেস দিয়ে ট্যাগের নাম ব্যবহার করা যায় না।

**প্রশ্ন ৬: JSX-এ `<img src="{avatar}" />` লিখলে কী হবে?**  
উত্তর: এটি স্ট্রিং `"{avatar}"` পাঠাবে, ভ্যারিয়েবল নয়। সঠিকভাবে করতে হলে `src={avatar}` ব্যবহার করুন।

**প্রশ্ন ৭: JSX-এ কার্লি ব্রেসের মধ্যে ফাংশন কল ব্যবহার করা যায় কি? উদাহরণ দিন।**  
উত্তর: হ্যাঁ। উদাহরণ: `<h1>{formatName(user)}</h1>`

**প্রশ্ন ৮: নিচের কোডের আউটপুট কী হবে?
```jsx
const count = 5;
return <p>{count + 1}</p>;
```
উত্তর: এটি `<p>6</p>` রেন্ডার করবে, কারণ কার্লি ব্রেসের মধ্যে এক্সপ্রেশনটি হিসাব করা হয়।

</div>

---

<Callout type="info">এই প্রশ্নগুলো পড়ে নিজ ভাষায় ব্যাখ্যা দেয়ার চেষ্টা করুন—ইন্টারভিউ প্রস্তুতির জন্য দারুণ অনুশীলন!</Callout>

---

## প্র্যাকটিস ইন্টারভিউ এক্সারসাইজ 🏋️‍♂️

<div style={{ background: 'rgba(20, 120, 220, 0.10)', padding: '20px', borderRadius: '5px', marginBottom: '20px', border: '1px solid rgba(20, 120, 220, 0.15)', color: 'inherit' }}>

### সিনারিও-ভিত্তিক প্রশ্ন:

ধরুন, আপনি একটি React কম্পোনেন্ট বানাচ্ছেন যা ইউজারের প্রোফাইল দেখাবে। আপনি ইউজারের নাম, ছবি এবং সময় অনুযায়ী ডাইনামিক গ্রিটিং দেখাতে চান।

**প্রশ্ন:** JSX-এ কার্লি ব্রেস কীভাবে ব্যবহার করবেন? কোড পরিষ্কার ও পড়তে সহজ রাখতে কী কী সেরা চর্চা অনুসরণ করবেন?

উত্তর ভাবুন, তারপর নিচের সাজেশন দেখুন।

<details>
<summary>**উত্তরের জন্য ক্লিক করুন**</summary>

**সেরা চর্চা:**

1. ভ্যারিয়েবল (যেমন, `{user.name}`) ও ফাংশন কল (যেমন, `{getGreeting()}`) এম্বেড করতে কার্লি ব্রেস ব্যবহার করুন।
2. ডাইনামিক অ্যাট্রিবিউট মানের জন্য কার্লি ব্রেস ব্যবহার করুন (যেমন, `src={user.avatar}`)।
3. JSX-এ এক্সপ্রেশন সহজ ও পড়তে সুবিধাজনক রাখুন।
4. জটিল লজিক JSX-এ না লিখে হেল্পার ফাংশন ব্যবহার করুন।

**উদাহরণ:**
```jsx
<h1>{getGreeting()}, {user.name}!</h1>
<img src={user.avatar} alt={user.name} />
```

</details>
</div>

---

**টিপ:** প্রশ্ন ও উত্তরগুলো পড়ে নিজ ভাষায় ব্যাখ্যা দেয়ার চেষ্টা করুন—ইন্টারভিউ প্রস্তুতির জন্য দারুণ অনুশীলন!

> **🚀 চূড়ান্ত পরামর্শ:** ইন্টারভিউতে শুধু কার্লি ব্রেসের নিয়ম জানলেই হবে না, কেন ও কীভাবে এগুলো কার্যকরভাবে ব্যবহার করবেন, সেটাও বোঝাতে হবে।
