import { Callout } from "nextra/components";

---

<img src="https://upload.wikimedia.org/wikipedia/commons/a/a7/React-icon.svg" alt="React Logo" width="100" style={{ marginBottom: "1rem" }} />

# 🟦 JSX-এ কার্লি ব্রেস দিয়ে জাভাস্ক্রিপ্ট

স্বাগতম! JSX আপনাকে জাভাস্ক্রিপ্ট ও মার্কআপ একসাথে লিখতে দেয়, আর কার্লি ব্রেস `{}` হলো ডাইনামিক কনটেন্টের জন্য জাদুর জানালা। চলুন, এগুলো দক্ষতার সাথে ব্যবহার করা শিখি! 🚀

---

## 🧐 JSX-এ কার্লি ব্রেস কোথায় ব্যবহার করা যায়?

1. **ট্যাগ কনটেন্টের মধ্যে:**
   ```jsx
   <h1>{name} is a great teacher</h1>
   ```
   এখানে `{name}` একটি জাভাস্ক্রিপ্ট ভ্যারিয়েবল।

2. **অ্যাট্রিবিউটের মান হিসেবে:**
   ```jsx
   <img src={avatarUrl} alt={description} />
   ```
   কার্লি ব্রেসের মধ্যে যেকোনো জাভাস্ক্রিপ্ট এক্সপ্রেশন ব্যবহার করা যায়।

---

## 💡 কার্লি ব্রেসের মধ্যে কী রাখা যায়?
- ভ্যারিয়েবল
- ফাংশন কল
- এক্সপ্রেশন (যেমন, `{count + 1}`)
- অবজেক্ট (যেমন, ইনলাইন স্টাইলের জন্য)

**উদাহরণ:**
```jsx
const name = 'Hedy Lamarr';
const avatar = 'https://i.imgur.com/yXOvdOSs.jpg';
const style = { color: 'blue', backgroundColor: 'lightgray' };

function Profile() {
  return (
    <div style={style}>
      <h1>{name}'s Profile</h1>
      <img src={avatar} alt={name} />
    </div>
  );
}
```

---

## 📏 JSX-এ কার্লি ব্রেস ব্যবহারের নিয়ম

1. **শুধু দুই জায়গায় কার্লি ব্রেস ব্যবহার করা যায়:**
   - JSX ট্যাগের কনটেন্টে: `<h1>{name}</h1>`
   - অ্যাট্রিবিউটের মানে: `<img src={avatar} />`

2. **কার্লি ব্রেস দিয়ে ট্যাগের নাম ব্যবহার করা যায় না:**
   - ❌ `<{tag}>Hello</{tag}>` অবৈধ।

3. **স্ট্রিং অ্যাট্রিবিউট বনাম জাভাস্ক্রিপ্ট এক্সপ্রেশন:**
   - `src="avatar.png"` স্ট্রিং পাঠায়।
   - `src={avatar}` ভ্যারিয়েবল `avatar`-এর মান পাঠায়।

4. **অবজেক্ট (যেমন ইনলাইন স্টাইল) দিতে ডাবল কার্লি ব্রেস ব্যবহার করুন:**
   - `<div style={{ color: 'red' }}></div>`
   - বাইরের `{}` JSX-এর জন্য, ভেতরের `{}` জাভাস্ক্রিপ্ট অবজেক্টের জন্য।

---

<Callout type="warning">
  <b>সাধারণ ভুল:</b><br />
  • কার্লি ব্রেস দিয়ে ট্যাগের নাম ব্যবহার করা।<br />
  • কোটস ও কার্লি ব্রেস একসাথে ব্যবহার: <code>src="{'{avatar}'}"</code> (এটি <code>"{'{avatar}'}"</code> স্ট্রিং পাঠায়, ভ্যারিয়েবল নয়)।<br />
  • ট্যাগ ক্লোজ করতে ভুলে যাওয়া।
</Callout>

---

## 📚 রেফারেন্স
- [React Docs: JavaScript in JSX with Curly Braces](https://react.dev/learn/javascript-in-jsx-with-curly-braces)

---

<Callout type="success">
  নিজের JSX-এ কার্লি ব্রেস ব্যবহার করে দেখুন! যত বেশি প্র্যাকটিস করবেন, তত স্বাভাবিক লাগবে। শুভ কোডিং! 🎉
</Callout>
