import { Callout } from "nextra/components";

<img src="https://upload.wikimedia.org/wikipedia/commons/a/a7/React-icon.svg" alt="React Logo" width="70" style={{marginBottom: '1rem'}} />

# কন্ডিশনাল রেন্ডারিং – ইন্টারভিউ প্রশ্ন ও উত্তর 🎯

এই প্রশ্নগুলো ব্যবহার করুন ইন্টারভিউ প্রস্তুতির জন্য বা রিয়্যাক্টে কন্ডিশনাল রেন্ডারিং সম্পর্কে আপনার বোঝা যাচাই করতে।

> **💡 প্রো টিপ:** প্রতিটি প্রশ্নের উত্তর নিজে চেষ্টা করুন, তারপর উত্তর দেখুন। এতে ধারণা আরও পরিষ্কার হবে!

---

## ১.১ – কন্ডিশনাল রেন্ডারিংয়ের বেসিক 🌱

<div style={{ background: 'rgba(0, 100, 200, 0.10)', padding: '15px', borderRadius: '5px', marginBottom: '20px', border: '1px solid rgba(0, 100, 200, 0.15)', color: 'inherit' }}>

**প্রশ্ন ১: রিয়্যাক্টে কন্ডিশনাল রেন্ডারিং কী?**  
উত্তর: কন্ডিশনাল রেন্ডারিং মানে হচ্ছে নির্দিষ্ট কন্ডিশনের উপর ভিত্তি করে ভিন্ন ভিন্ন UI এলিমেন্ট বা কম্পোনেন্ট দেখানো। সাধারণত state বা props-এর উপর ভিত্তি করে কোনটা রেন্ডার হবে তা নির্ধারণ করা হয়।

**প্রশ্ন ২: JSX-এ কন্ডিশনাল রেন্ডারিং কীভাবে করবেন?**  
উত্তর: JSX-এ কন্ডিশনাল রেন্ডারিং করতে পারেন if/else স্টেটমেন্ট, টার্নারি অপারেটর (`condition ? expr1 : expr2`), এবং লজিক্যাল AND (`&&`) অপারেটর দিয়ে। যেমন:  
```jsx
{isLoggedIn ? <Dashboard /> : <Login />}
```

**প্রশ্ন ৩: React কম্পোনেন্ট থেকে `null` রিটার্ন করলে কী হয়?**  
উত্তর: হ্যাঁ, React কম্পোনেন্ট থেকে `null` রিটার্ন করলে কিছুই রেন্ডার হয় না। অর্থাৎ, DOM-এ কিছুই দেখাবে না।

</div>

---

## ১.২ – ইন্টারমিডিয়েট ব্যবহার 💡

<div style={{ background: 'rgba(230, 150, 70, 0.10)', padding: '15px', borderRadius: '5px', marginBottom: '20px', border: '1px solid rgba(230, 150, 70, 0.15)', color: 'inherit' }}>

**প্রশ্ন ৪: if/else আর টার্নারি অপারেটরের মধ্যে পার্থক্য কী?**  
উত্তর: if/else সাধারণত JSX-এর বাইরে ব্যবহার করা হয় কোন JSX রিটার্ন হবে তা ঠিক করতে। টার্নারি অপারেটর সরাসরি JSX-এর মধ্যে ইনলাইন কন্ডিশনের জন্য ব্যবহার করা যায় এবং এটি অনেক সংক্ষিপ্ত।

**প্রশ্ন ৫: কন্ডিশনাল রেন্ডারিংয়ে লজিক্যাল AND (`&&`) অপারেটর কীভাবে কাজ করে?**  
উত্তর: লজিক্যাল AND (`&&`) অপারেটর কন্ডিশন সত্য হলে ডান পাশের এক্সপ্রেশন রেন্ডার করে, আর মিথ্যা হলে কিছুই রেন্ডার হয় না। যেমন:  
```jsx
{hasUnread && <p>You have unread messages!</p>}
```

**প্রশ্ন ৬: কিভাবে শুধু তখনই কম্পোনেন্ট রেন্ডার করবেন যখন লিস্ট খালি না?**  
উত্তর: লজিক্যাল AND (`&&`) অপারেটর ব্যবহার করুন:  
```jsx
{items.length > 0 && <ItemList items={items} />}
```

</div>

---

## ১.৩ – সেরা চর্চা ও সিনারিও-ভিত্তিক 🧠

<div style={{ background: 'rgba(220, 50, 50, 0.10)', padding: '15px', borderRadius: '5px', marginBottom: '20px', border: '1px solid rgba(220, 50, 50, 0.15)', color: 'inherit' }}>

**প্রশ্ন ৭: কন্ডিশনাল রেন্ডারিং কোড পড়তে সহজ রাখার কিছু সেরা চর্চা কী?**  
উত্তর:
- সহজ কন্ডিশনের জন্য টার্নারি অপারেটর ব্যবহার করুন।
- জটিল লজিকের জন্য if/else বা ভ্যারিয়েবল ব্যবহার করুন।
- বেশি নেস্টেড টার্নারি অপারেটর এড়িয়ে চলুন।
- জটিল কন্ডিশন আলাদা ফাংশন বা কম্পোনেন্টে নিয়ে যান।

**প্রশ্ন ৮: সিনারিও: ডেটা লোড হচ্ছে, তখন স্পিনার দেখাতে চান, আর ডেটা লোড হলে ডেটা দেখাতে চান। কীভাবে করবেন?**  
উত্তর: একটি state ভ্যারিয়েবল (যেমন `isLoading`) ব্যবহার করুন। `isLoading` সত্য হলে স্পিনার, না হলে ডেটা দেখান। যেমন:  
```jsx
{isLoading ? <Spinner /> : <DataView data={data} />}
```

**প্রশ্ন ৯: সিনারিও: শুধু তখনই এরর মেসেজ দেখাতে চান যখন এরর আছে। কীভাবে করবেন?**  
উত্তর: লজিক্যাল AND অপারেটর ব্যবহার করুন:  
```jsx
{error && <p className="error">{error}</p>}
```

</div>

---

<Callout type="info">এই প্রশ্নগুলো পড়ে নিজ ভাষায় ব্যাখ্যা দেয়ার চেষ্টা করুন—ইন্টারভিউ প্রস্তুতির জন্য দারুণ অনুশীলন!</Callout>

---

## প্র্যাকটিস ইন্টারভিউ এক্সারসাইজ 🏋️‍♂️

<div style={{ background: 'rgba(20, 120, 220, 0.10)', padding: '20px', borderRadius: '5px', marginBottom: '20px', border: '1px solid rgba(20, 120, 220, 0.15)', color: 'inherit' }}>

### সিনারিও-ভিত্তিক প্রশ্ন:

আপনার একটি ড্যাশবোর্ড আছে যেখানে ইউজার ডেটা দেখানো হয়। ইউজার লগইন না করলে লগইন প্রম্পট দেখাতে চান। ইউজার লগইন করলে কিন্তু ডেটা না থাকলে একটি মেসেজ দেখাতে চান। আর ইউজার লগইন ও ডেটা থাকলে ড্যাশবোর্ড দেখাতে চান।

**প্রশ্ন:** এই কন্ডিশনাল রেন্ডারিং লজিক কীভাবে সাজাবেন?

উত্তর ভাবুন, তারপর নিচের সাজেশন দেখুন।

<details>
<summary>**উত্তরের জন্য ক্লিক করুন**</summary>

**উত্তরের ধরন:**

```jsx
if (!isLoggedIn) {
  return <LoginPrompt />;
}
if (data.length === 0) {
  return <p>No data available.</p>;
}
return <Dashboard data={data} />;
```

- পরিষ্কার, পড়তে সহজ লজিকের জন্য early return ব্যবহার করুন।
- বেশি নেস্টেড টার্নারি এড়িয়ে চলুন।
- প্রতিটি স্টেটের জন্য শুধু প্রাসঙ্গিক UI দেখান।

</details>
</div>

---

**টিপ:** প্রশ্ন ও উত্তরগুলো পড়ে নিজ ভাষায় ব্যাখ্যা দেয়ার চেষ্টা করুন—ইন্টারভিউ প্রস্তুতির জন্য দারুণ অনুশীলন!

> **🚀 চূড়ান্ত পরামর্শ:** ইন্টারভিউতে শুধু কন্ডিশনাল রেন্ডারিং জানলেই হবে না, কীভাবে পরিষ্কার, মেইনটেইনেবল লজিক লিখবেন, সেটাও বোঝাতে হবে।
