import { Callout } from "nextra/components";

<img src="https://upload.wikimedia.org/wikipedia/commons/a/a7/React-icon.svg" alt="React Logo" width="70" style={{marginBottom: '1rem'}} />

# রিয়্যাক্টে কন্ডিশনাল রেন্ডারিং

> **🎯 ফ্রেন্ডলি পরিচিতি:**
>
> কন্ডিশনাল রেন্ডারিং মানে আপনার রিয়্যাক্ট অ্যাপে পরিস্থিতি অনুযায়ী ভিন্ন ভিন্ন জিনিস দেখানো—একদম জাদুর মতো! ✨ এটি আপনার UI-কে ডাইনামিক ও ইন্টারঅ্যাকটিভ করার সুপারপাওয়ার।

---

## কন্ডিশনাল রেন্ডারিং করার সাধারণ উপায়

### ১. if/else স্টেটমেন্ট ব্যবহার

```jsx
function Greeting({ isLoggedIn }) {
  if (isLoggedIn) {
    return <h1>Welcome back!</h1>;
  }
  return <h1>Please sign in.</h1>;
}
```

<Callout type="tip"><strong>if/else</strong> ব্যবহার করুন যখন আপনার লজিক একটু জটিল বা কম্পোনেন্ট থেকে আগেভাগে রিটার্ন করতে হয়।</Callout>

---

### ২. টার্নারি অপারেটর ⚡

টার্নারি অপারেটর দিয়ে সহজেই কন্ডিশনের উপর ভিত্তি করে কনটেন্ট রেন্ডার করা যায়:

```jsx
function Greeting({ isLoggedIn }) {
  return (
    <div>
      {isLoggedIn ? <h1>Welcome back!</h1> : <h1>Please sign in.</h1>}
    </div>
  );
}
```

<Callout type="info">টার্নারি অপারেটর ছোট, ইনলাইন কন্ডিশনের জন্য দারুণ। তবে বেশি নেস্টেড হলে পড়তে কঠিন হয়!</Callout>

---

### ৩. লজিক্যাল AND (&&) অপারেটর 🟢

শুধু যখন কন্ডিশন সত্যি, তখন কিছু রেন্ডার করতে <code>&&</code> অপারেটর ব্যবহার করুন:

```jsx
function Notification({ hasUnread }) {
  return (
    <div>
      {hasUnread && <p>You have unread messages!</p>}
    </div>
  );
}
```

<Callout type="warning">মনে রাখুন: <code>false</code>, <code>null</code>, <code>undefined</code>, <code>0</code>, এবং <code>''</code> (খালি স্ট্রিং) — এগুলো সব "falsy"। কন্ডিশন ফালসি হলে কিছুই রেন্ডার হবে না।</Callout>

---

### ৪. JSX-কে ভ্যারিয়েবলে রাখা 🧩

জটিল কন্ডিশনের জন্য JSX-কে ভ্যারিয়েবলে রেখে পরে রিটার্নে ব্যবহার করুন:

```jsx
function UserStatus({ isOnline }) {
  let status;
  if (isOnline) {
    status = <span className="online">Online</span>;
  } else {
    status = <span className="offline">Offline</span>;
  }
  return <div>User is: {status}</div>;
}
```

<Callout type="tip">এইভাবে <code>return</code> স্টেটমেন্ট পরিষ্কার ও পড়তে সহজ থাকে, বিশেষ করে একাধিক কন্ডিশন থাকলে।</Callout>

---

## সেরা চর্চা 🌟

- ✅ সহজ কন্ডিশনের জন্য টার্নারি অপারেটর ব্যবহার করুন।
- ✅ জটিল লজিকের জন্য if/else বা ভ্যারিয়েবল ব্যবহার করুন, এতে JSX পরিষ্কার থাকে।
- 🚫 বেশি নেস্টেড টার্নারি অপারেটর এড়িয়ে চলুন—এতে কোড পড়তে কষ্ট হয়।

<Callout type="info">আপনার UI লজিক সহজ ও পরিষ্কার রাখুন। পড়তে সহজ কোডই সবচেয়ে মেইনটেইনেবল!</Callout>

---

## অফিসিয়াল ডকস থেকে উদাহরণ 📚

```jsx
function Item({ name, isPacked }) {
  return (
    <li className="item">
      {isPacked ? <del>{name + ' ✅'}</del> : name}
    </li>
  );
}
```
[আরও দেখুন অফিসিয়াল React ডকসে](https://react.dev/learn/conditional-rendering)

---

## 🚀 রিক্যাপ ও পরবর্তী ধাপ

- কন্ডিশনাল রেন্ডারিং দিয়ে অ্যাপের স্টেট অনুযায়ী ভিন্ন জিনিস দেখাতে পারবেন।
- কোন পদ্ধতি (if/else, টার্নারি, &&, ভ্যারিয়েবল) ব্যবহার করবেন, সেটা কোড পরিষ্কার ও পড়তে সহজ রাখার জন্য বেছে নিন।
- ছোট ছোট কম্পোনেন্ট বানিয়ে বিভিন্ন কন্ডিশন প্র্যাকটিস করুন!

<Callout type="success">আরও এগিয়ে যেতে চাইলে: কন্ডিশনাল রেন্ডারিং লিস্ট বা ফর্মের সাথে মিশিয়ে আরও ডাইনামিক UI বানানোর চেষ্টা করুন!</Callout>
