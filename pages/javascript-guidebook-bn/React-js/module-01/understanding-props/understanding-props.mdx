import { Callout } from "nextra/components";

---

<img src="https://upload.wikimedia.org/wikipedia/commons/a/a7/React-icon.svg" alt="React Logo" width="100" style={{ marginBottom: "1rem" }} />

# 🧩 রিয়্যাক্টে প্রপস (Props) বোঝা

স্বাগতম! Props হলো আপনার React কম্পোনেন্টকে ডাইনামিক ও পুনঃব্যবহারযোগ্য করার গোপন চাবিকাঠি। চলুন, এগুলো দক্ষতার সাথে ব্যবহার করা শিখি! 🚀

---

## 🤔 প্রপস (Props) কী?

Props (properties-এর সংক্ষিপ্ত রূপ) হলো React কম্পোনেন্টের মধ্যে যোগাযোগের মাধ্যম। এগুলোর মাধ্যমে আপনি প্যারেন্ট কম্পোনেন্ট থেকে চাইল্ড কম্পোনেন্টে ডেটা পাঠাতে পারেন, ঠিক যেমন ফাংশনে আর্গুমেন্ট পাঠান।

<Callout type="info">
  প্রপস ভাবুন—এক কম্পোনেন্ট থেকে আরেকটিতে উপহার (ডেটা) পাঠানোর উপায়!
</Callout>

---

## 📦 কম্পোনেন্টে প্রপস পাঠানো

JSX-এ অ্যাট্রিবিউট হিসেবে প্রপস পাঠানো যায়:

```jsx
function Avatar(props) {
  return <img src={props.src} alt={props.alt} />;
}

function Profile() {
  return <Avatar src="avatar.png" alt="Profile picture" />;
}
```

এখানে `Profile` কম্পোনেন্ট `Avatar`-এ `src` ও `alt` প্রপস পাঠাচ্ছে।

---

## 🔍 কম্পোনেন্টে প্রপস পড়া

ফাংশনের প্যারামিটারে প্রপস অ্যাক্সেস করা যায়:

```jsx
function Avatar({ src, alt }) {
  return <img src={src} alt={alt} />;
}
```

এটাকে বলে "destructuring"—কোড আরও পরিষ্কার হয়।

---

## 🏷️ ডিফল্ট প্রপস

Destructuring-এর সময় প্রপসের জন্য ডিফল্ট মান নির্ধারণ করা যায়:

```jsx
function Avatar({ src, alt = "User avatar" }) {
  return <img src={src} alt={alt} />;
}
```

যদি `alt` না পাঠানো হয়, তাহলে "User avatar" ডিফল্ট হবে।

<Callout type="info">
  ডিফল্ট প্রপস UI-তে মিসিং বা আনডিফাইন্ড ভ্যালু প্রতিরোধে সাহায্য করে!
</Callout>

---

## 🚫 প্রপস রিড-অনলি

চাইল্ড কম্পোনেন্ট প্রপস পরিবর্তন করতে পারে না। পরিবর্তন করতে চাইলে প্যারেন্টে মান আপডেট করে নতুন প্রপস পাঠাতে হবে।

<Callout type="warning">
  কখনোই চাইল্ড কম্পোনেন্টে প্রপস পরিবর্তন করার চেষ্টা করবেন না। সবসময় প্যারেন্টে আপডেট করুন!
</Callout>

---

## 🔄 প্রপস ফরওয়ার্ড করা

স্প্রেড সিনট্যাক্স ব্যবহার করে এক কম্পোনেন্ট থেকে আরেকটিতে সব প্রপস পাঠানো যায়:

```jsx
function Profile(props) {
  return <Avatar {...props} />;
}
```

সতর্ক থাকুন—এটি কোড কম স্পষ্ট করতে পারে।

---

## 🧒 JSX-এ চিলড্রেন (children) প্রপস পাঠানো

এক কম্পোনেন্টের মধ্যে অন্য কম্পোনেন্ট বা এলিমেন্ট নেস্ট করা যায়। এই নেস্টেড কনটেন্ট `children` প্রপস হিসেবে পাওয়া যায়:

```jsx
function Card({ children }) {
  return <div className="card">{children}</div>;
}

function Profile() {
  return (
    <Card>
      <Avatar src="avatar.png" alt="Profile" />
    </Card>
  );
}
```

<Callout type="success">
  `children` প্রপস দিয়ে ফ্লেক্সিবল লেআউট ও পুনঃব্যবহারযোগ্য কম্পোনেন্ট বানানো খুব সহজ!
</Callout>

---

## 📝 মূল পয়েন্ট
- প্রপস দিয়ে প্যারেন্ট থেকে চাইল্ড কম্পোনেন্টে ডেটা পাঠানো যায়।
- চাইল্ডে প্রপস রিড-অনলি।
- প্রপসের ডিফল্ট মান সেট করা যায়।
- স্প্রেড সিনট্যাক্স দিয়ে প্রপস ফরওয়ার্ড করা যায়।
- `children` প্রপস দিয়ে কম্পোনেন্টের মধ্যে কনটেন্ট নেস্ট করা যায়।

---

## 📚 রেফারেন্স
- [React Docs: Passing Props to a Component](https://react.dev/learn/passing-props-to-a-component)
