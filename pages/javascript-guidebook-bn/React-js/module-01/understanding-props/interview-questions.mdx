import { Callout } from "nextra/components";

# রিয়্যাক্টে প্রপস (Props) – ইন্টারভিউ প্রশ্ন ও উত্তর 🧩

<img src="https://upload.wikimedia.org/wikipedia/commons/a/a7/React-icon.svg" alt="React Logo" width="80" style={{marginBottom: '1rem'}} />

এই প্রশ্নগুলো ব্যবহার করুন ইন্টারভিউ প্রস্তুতির জন্য বা রিয়্যাক্টে প্রপস সম্পর্কে আপনার বোঝা যাচাই করতে।

> **💡 প্রো টিপ:** প্রতিটি প্রশ্নের উত্তর নিজে চেষ্টা করুন, তারপর উত্তর দেখুন। এতে ধারণা আরও পরিষ্কার হবে!

---

## ১.১ – প্রপসের বেসিক 🌱

<div style={{ background: 'rgba(0, 100, 200, 0.10)', padding: '15px', borderRadius: '5px', marginBottom: '20px', border: '1px solid rgba(0, 100, 200, 0.15)', color: 'inherit' }}>

**প্রশ্ন ১: রিয়্যাক্টে প্রপস কী?**  
উত্তর: প্রপস হলো রিয়্যাক্ট কম্পোনেন্টের ইনপুট, যা প্যারেন্ট কম্পোনেন্ট থেকে চাইল্ড কম্পোনেন্টে ডেটা পাঠাতে দেয়।

**প্রশ্ন ২: কম্পোনেন্টে কীভাবে প্রপস পাঠাবেন?**  
উত্তর: JSX-এ কম্পোনেন্টে অ্যাট্রিবিউট হিসেবে পাঠাতে হয়, যেমন: `<Avatar src="avatar.png" />`।

</div>

---

## ১.২ – ইন্টারমিডিয়েট ব্যবহার 💡

<div style={{ background: 'rgba(230, 150, 70, 0.10)', padding: '15px', borderRadius: '5px', marginBottom: '20px', border: '1px solid rgba(230, 150, 70, 0.15)', color: 'inherit' }}>

**প্রশ্ন ৩: প্রপসের জন্য ডিফল্ট মান কীভাবে সেট করবেন?**  
উত্তর: ডেস্ট্রাকচারিং-এর সময় ডিফল্ট মান দিন, যেমন: `function Avatar({ size = 100 }) { ... }`

**প্রশ্ন ৪: চাইল্ড কম্পোনেন্ট কি নিজের প্রপস পরিবর্তন করতে পারে?**  
উত্তর: না, প্রপস চাইল্ডে রিড-অনলি। শুধু প্যারেন্ট মান পরিবর্তন করে নতুন প্রপস পাঠাতে পারে।

**প্রশ্ন ৫: রিয়্যাক্টে `children` প্রপস কী?**  
উত্তর: `children` প্রপস হলো একটি বিশেষ প্রপস, যাতে কম্পোনেন্ট ট্যাগের মধ্যে নেস্ট করা JSX বা কম্পোনেন্ট থাকে।

</div>

---

## ১.৩ – অ্যাডভান্সড ও সিনারিও-ভিত্তিক 🧠

<div style={{ background: 'rgba(220, 50, 50, 0.10)', padding: '15px', borderRadius: '5px', marginBottom: '20px', border: '1px solid rgba(220, 50, 50, 0.15)', color: 'inherit' }}>

**প্রশ্ন ৬: ডিফল্ট মানসহ প্রপসে `undefined`, `null` বা `false` পাঠালে কী হয়?**  
উত্তর: ডিফল্ট মান শুধু তখনই ব্যবহৃত হয়, যখন প্রপস `undefined` বা পাঠানো হয়নি। `null` বা `false` পাঠালে সেটাই ব্যবহৃত হবে।

**প্রশ্ন ৭: এক কম্পোনেন্ট থেকে আরেকটিতে সব প্রপস কীভাবে ফরওয়ার্ড করবেন?**  
উত্তর: স্প্রেড সিনট্যাক্স ব্যবহার করুন: `<Avatar {...props} />`

**প্রশ্ন ৮: ফাংশনকে প্রপস হিসেবে পাঠানো যায় কি? উদাহরণ দিন।**  
উত্তর: হ্যাঁ। উদাহরণ: `<Button onClick={handleClick} />` যেখানে `handleClick` একটি ফাংশন।

**প্রশ্ন ৯: স্প্রেড সিনট্যাক্স দিয়ে প্রপস পাঠানোর সময় সতর্ক থাকতে হবে কেন?**  
উত্তর: কারণ এতে কোড কম স্পষ্ট হয় এবং কোন কোন প্রপস পাঠানো হচ্ছে বোঝা কঠিন হতে পারে।

</div>

---

<Callout type="info">এই প্রশ্নগুলো পড়ে নিজ ভাষায় ব্যাখ্যা দেয়ার চেষ্টা করুন—ইন্টারভিউ প্রস্তুতির জন্য দারুণ অনুশীলন!</Callout>

---

## প্র্যাকটিস ইন্টারভিউ এক্সারসাইজ 🏋️‍♂️

<div style={{ background: 'rgba(20, 120, 220, 0.10)', padding: '20px', borderRadius: '5px', marginBottom: '20px', border: '1px solid rgba(20, 120, 220, 0.15)', color: 'inherit' }}>

### সিনারিও-ভিত্তিক প্রশ্ন:

ধরুন, আপনি একটি `ProfileCard` কম্পোনেন্ট বানাচ্ছেন, যেখানে ইউজারের নাম, ছবি এবং ফলো/আনফলো বাটন থাকবে। বাটনের লেবেল ও অ্যাকশন প্যারেন্ট কম্পোনেন্ট কন্ট্রোল করবে।

**প্রশ্ন:** কীভাবে প্রপস ব্যবহার করে `ProfileCard`-কে ফ্লেক্সিবল ও পুনঃব্যবহারযোগ্য করবেন? এই ক্ষেত্রে প্রপস ব্যবহারের সেরা চর্চা কী?

উত্তর ভাবুন, তারপর নিচের সাজেশন দেখুন।

<details>
<summary>**উত্তরের জন্য ক্লিক করুন**</summary>

**সেরা চর্চা:**

1. সব ডাইনামিক ডেটা (নাম, ছবি, বাটন লেবেল ও অ্যাকশন) প্রপস হিসেবে পাঠান।
2. কার্ডের মধ্যে কাস্টম কনটেন্ট রাখতে চাইলে `children` প্রপস ব্যবহার করুন।
3. অপশনাল প্রপসের জন্য ডিফল্ট মান দিন।
4. চাইল্ডে প্রপস রিড-অনলি রাখুন; প্যারেন্টে আপডেট করুন।

**উদাহরণ:**
```jsx
function ProfileCard({ name, avatar, buttonLabel, onButtonClick }) {
  return (
    <div>
      <img src={avatar} alt={name} />
      <h2>{name}</h2>
      <button onClick={onButtonClick}>{buttonLabel}</button>
    </div>
  );
}
```

</details>
</div>

---

**টিপ:** প্রশ্ন ও উত্তরগুলো পড়ে নিজ ভাষায় ব্যাখ্যা দেয়ার চেষ্টা করুন—ইন্টারভিউ প্রস্তুতির জন্য দারুণ অনুশীলন!

> **🚀 চূড়ান্ত পরামর্শ:** ইন্টারভিউতে শুধু প্রপস জানলেই হবে না, কীভাবে এগুলো দিয়ে ফ্লেক্সিবল, মেইনটেইনেবল কম্পোনেন্ট বানাবেন, সেটাও বোঝাতে হবে।
