import { Callout, Tabs, Tab } from 'nextra/components'

# 🔄 React Effects-এর সম্পূর্ণ গাইড: মূল ধারণা ও মৌলিক বিষয়াবলী

_React-এর useEffect হুক আয়ত্ত করার জন্য আমাদের বিস্তৃত গাইডের প্রথম অংশ_

---

## 📋 পাঠক্রমের তালিকা

1. 🎯 আপনি যা আয়ত্ত করবেন
2. 🌟 Effects কী? (সংক্ষিপ্ত বিবরণ)
3. 🚦 কখন আপনার Effects ব্যবহার করা উচিত
4. 📚 ধাপে ধাপে Effects শেখা
5. 🎯 বাস্তব জীবনের উদাহরণ
6. 🔧 Development-এ Effects দুইবার চালু হওয়ার সমস্যা সমাধান
7. 🌐 Effects দিয়ে ডেটা আনা
8. 🤷‍♂️ আপনার হয়তো Effect-এর প্রয়োজন নেই

---

## 🎯 আপনি যা আয়ত্ত করবেন

এই প্রথম অংশের শেষে, আপনি সক্ষম হবেন:

- ✅ সঠিকভাবে কাজ করে এমন মৌলিক Effects লিখতে
- 🐛 সাধারণ Effect সমস্যাগুলো ডিবাগ করতে
- 🧠 কখন Effects ব্যবহার করবেন এবং কখন এড়িয়ে চলবেন তা জানতে
- 🌐 মৌলিক ডেটা আনার কাজ পরিচালনা করতে
- 🔄 Effect-এর জীবনচক্র বুঝতে

---

## 🌟 Effects কী? (সংক্ষিপ্ত বিবরণ)

### 🤔 Effects কী? (বিস্তারিত ব্যাখ্যা)

Effects হলো React-এর একটি উপায় যার মাধ্যমে আপনি functional components-এ **side effects** সম্পাদন করতে পারেন। কিন্তু "side effect" আসলে কী?

আপনার React component-কে একটি শুদ্ধ গাণিতিক ফাংশনের মতো ভাবুন:

```
Component(props, state) → JSX
```

একটি শুদ্ধ ফাংশন:

- ✅ একই ইনপুটের জন্য সর্বদা একই আউটপুট প্রদান করে
- ✅ নিজের বাইরে কিছু পরিবর্তন করে না
- ✅ কোনো side effect নেই

কিন্তু বাস্তব অ্যাপ্লিকেশনগুলোতে "অশুদ্ধ" কাজ করতে হয়:

- 🌐 সার্ভার থেকে ডেটা আনা
- 👂 ব্রাউজার ইভেন্ট শোনা
- 📝 ডকুমেন্ট টাইটেল আপডেট করা
- ⏰ টাইমার শুরু করা
- 📊 অ্যানালিটিক্স ইভেন্ট লগ করা

এগুলো **side effects** কারণ এরা শুদ্ধ রেন্ডার ফাংশনের বাইরে কিছু প্রভাবিত করে।

### 🌍 দুই জগতের ভিজ্যুয়ালাইজেশন

<div className="grid grid-cols-[1fr_auto_1fr] gap-4 items-center my-8 p-6 border-2 border-slate-200 rounded-xl bg-slate-50">

<div className="p-6 bg-blue-100 rounded-lg border-2 border-blue-500">
  <div className="font-bold text-xl text-slate-900">🌟 REACT-এর জগত</div>
  <ul className="text-sm mt-2 m-0 text-slate-700">
    <li>• Components</li>
    <li>• Props ও State</li>
    <li>• JSX Elements</li>
    <li>• Event Handlers</li>
    <li>• Render Cycle</li>
    <li>• Virtual DOM</li>
  </ul>
  <div className="mt-4 text-xs text-emerald-600">
    <div>✅ পূর্বাভাসযোগ্য</div>
    <div>✅ শুদ্ধ ফাংশন</div>
    <div>✅ পরীক্ষাযোগ্য</div>
  </div>
</div>

<div className="text-3xl font-bold text-indigo-500 text-center">
  🌉
  <br />
  useEffect
</div>

  <div className="p-6 bg-yellow-100 rounded-lg border-2 border-amber-500">
    <div className="font-bold text-xl text-slate-900">🌍 বাইরের জগত</div>
    <ul className="text-sm mt-2 m-0 text-slate-700">
      <li>• REST APIs</li>
      <li>• WebSocket connections</li>
      <li>• Browser APIs</li>
      <li>• LocalStorage</li>
      <li>• Timers ও Intervals</li>
      <li>• Analytics Services</li>
    </ul>
  </div>
</div>

### 🎭 থিয়েটার রূপক

React components-কে একটি থিয়েটার মঞ্চের অভিনেতা হিসেবে কল্পনা করুন:

<Tabs items={['🎬 পারফরম্যান্সের সময়', '🎪 মঞ্চের পেছনে']}>
  <Tab>
    **পারফরম্যান্সের সময় (Rendering):** - 🎭 অভিনেতারা তাদের সংলাপ বলেন (JSX) - 🎪 মঞ্চের প্রপস সাজানো হয় (state/props) - 📜 সবকিছু স্ক্রিপ্ট অনুযায়ী এবং পূর্বাভাসযোগ্য
  </Tab>
  <Tab>
    **মঞ্চের পেছনে (Effects):** - 💡 আলো কর্মীরা স্পটলাইট সমন্বয় করেন - 🔊 সাউন্ড ইঞ্জিনিয়াররা অডিও লেভেল পরিচালনা করেন - 🎪 স্টেজহ্যান্ডরা দৃশ্যের মাঝে প্রপস সরান - 🎫 বক্স অফিস টিকিট বিক্রি করে (analytics) - 🔒 নিরাপত্তা দর্শকদের নজরদারি করে
  </Tab>
</Tabs>

Effects-গুলো মঞ্চের পেছনের দলের মতো—তারা সবকিছু কাজ করায়, কিন্তু মূল পারফরম্যান্সে হস্তক্ষেপ করে না।

### 📊 উদ্দেশ্য অনুযায়ী Effect-এর শ্রেণীবিভাগ

<div className="grid grid-cols-[repeat(auto-fit,minmax(300px,1fr))] gap-4 my-6">

<div className="p-4 bg-sky-50 rounded-lg border border-sky-600">
<div className="font-bold text-xl text-slate-900">🌐 ডেটা সিঙ্ক্রোনাইজেশন</div>
```jsx
useEffect(() => {
 fetchUserProfile(userId);
}, [userId]);
```
</div>
<div className="p-4 bg-green-50 rounded-lg border border-green-600">
<div className="font-bold text-xl text-slate-900">👂 ইভেন্ট সাবস্ক্রিপশন</div>
```jsx
useEffect(() => {
  window.addEventListener('resize', handleResize);
  return () => window.removeEventListener('resize', handleResize);
}, []);
```
</div>
<div className="p-4 bg-yellow-50 rounded-lg border border-yellow-600">
<div className="font-bold text-xl text-slate-900">⏰ সময়-ভিত্তিক অপারেশন</div>
```jsx
useEffect(() => {
  const timer = setInterval(updateClock, 1000);
  return () => clearInterval(timer);
}, []);
```
</div>
</div>
---

## 🚦 কখন আপনার Effects ব্যবহার করা উচিত

### ✅ Effects-এর জন্য নিখুঁত ব্যবহারের ক্ষেত্র

#### 1. 🌐 নেটওয়ার্ক অপারেশন

<Tabs items={['API ডেটা আনা', 'WebSocket সংযোগ', 'রিয়েল-টাইম সাবস্ক্রিপশন']}>
<Tab>
```jsx
function UserProfile({ userId }) {
  const [user, setUser] = useState(null)

  useEffect(
    () => {
      async function fetchUser() {
        const response = await fetch(`/api/users/${userId}`)
        const userData = await response.json()
        setUser(userData)
      }

      fetchUser()
    },
    [userId],
  )

  return user ? <div>হ্যালো, {user.name}!</div> : <div>লোড হচ্ছে...</div>
}
```
</Tab>
<Tab>
```jsx
function LiveChat({ roomId }) {
  const [messages, setMessages] = useState([])

  useEffect(
    () => {
      const ws = new WebSocket(`ws://chat.example.com/${roomId}`)

      ws.onmessage = event => {
        const message = JSON.parse(event.data)
        setMessages(prev => [...prev, message])
      }

      return () => ws.close()
    },
    [roomId],
  )

  return (
    <div>
      {messages.map(msg => (
        <div key={msg.id}>{msg.text}</div>
      ))}
    </div>
  )
}
```

</Tab>
<Tab>
```jsx
function StockPrice({ symbol }) {
  const [price, setPrice] = useState(null)

  useEffect(
    () => {
      const subscription = stockAPI.subscribe(symbol, setPrice)
      return () => subscription.unsubscribe()
    },
    [symbol],
  )

  return (
    <div>
      ${symbol}: ${price}
    </div>
  )
}
```
</Tab>
</Tabs>

#### 2. 🖱️ ব্রাউজার ইভেন্ট ব্যবস্থাপনা

<Tabs items={['উইন্ডো ইভেন্ট', 'কীবোর্ড শর্টকাট', 'স্ক্রল ট্র্যাকিং']}>
<Tab>
```jsx
function WindowDimensions() {
  const [dimensions, setDimensions] = useState({ width: 0, height: 0 })

  useEffect(() => {
    function updateDimensions() {
      setDimensions({
        width: window.innerWidth,
        height: window.innerHeight,
      })
    }

    updateDimensions() // প্রাথমিক মান সেট করুন
    window.addEventListener('resize', updateDimensions)

    return () => window.removeEventListener('resize', updateDimensions)
  }, [])

  return (
    <div>
      {dimensions.width} x {dimensions.height}
    </div>
  )
}
```

</Tab>
<Tab>
```jsx
function KeyboardShortcuts({ onSave, onUndo }) {
  useEffect(
    () => {
      function handleKeyDown(event) {
        if (event.ctrlKey || event.metaKey) {
          switch (event.key) {
            case 's':
              event.preventDefault()
              onSave()
              break
            case 'z':
              event.preventDefault()
              onUndo()
              break
          }
        }
      }

      document.addEventListener('keydown', handleKeyDown)
      return () => document.removeEventListener('keydown', handleKeyDown)
    },
    [onSave, onUndo],
  )

  return null // এই component শুধু শর্টকাট হ্যান্ডেল করে
}
```
</Tab>
<Tab>
```jsx
function ScrollTracker() {
  const [scrollY, setScrollY] = useState(0)

  useEffect(() => {
    function handleScroll() {
      setScrollY(window.scrollY)
    }

    window.addEventListener('scroll', handleScroll, { passive: true })
    return () => window.removeEventListener('scroll', handleScroll)
  }, [])

  return (
    <div className={`scroll-indicator ${scrollY > 100 ? 'scrolled' : ''}`}>
      স্ক্রল: {scrollY}px
    </div>
  )
}
```

</Tab>
</Tabs>

### ❌ কখন Effects ব্যবহার করবেন না

<Callout type="warning" emoji="⚠️">
  **সাধারণ ভুল প্যাটার্ন সতর্কতা!** অনেক ডেভেলপার Effects অতিরিক্ত ব্যবহার করেন। এখানে কখন এড়িয়ে চলতে হবে:
</Callout>

#### 1. 🧮 Props/State থেকে মান গণনা করা

<Tabs items={['❌ ভুল উপায়', '✅ সঠিক উপায়', '✅ ব্যয়বহুল গণনার জন্য']}>
<Tab>
```jsx
// ❌ এটি করবেন না - অপ্রয়োজনীয় Effect
function ExpensiveCalculation({ items, taxRate }) {
  const [total, setTotal] = useState(0)

  useEffect(
    () => {
      const subtotal = items.reduce((sum, item) => sum + item.price, 0)
      const totalWithTax = subtotal * (1 + taxRate);
      setTotal(totalWithTax)
    },
    [items, taxRate],
  )

  return <div>মোট: ${total}</div>
}
```
</Tab>

<Tab>
```jsx
// ✅ এর পরিবর্তে এটি করুন - রেন্ডারের সময় গণনা করুন
function ExpensiveCalculation({ items, taxRate }) {
  // এই গণনা রেন্ডারের সময় ঘটে - কোনো Effect প্রয়োজন নেই!
  const subtotal = items.reduce((sum, item) => sum + item.price, 0)
  const total = subtotal * (1 + taxRate)

  return <div>মোট: ${total}</div>
}
```

</Tab>
<Tab>
```jsx
// ✅ ব্যয়বহুল গণনার জন্য - useMemo ব্যবহার করুন
function ExpensiveCalculation({ items, taxRate }) {
  const total = useMemo(
    () => {
      console.log('মোট গণনা করা হচ্ছে...') // শুধুমাত্র ইনপুট পরিবর্তনের সময় চলে
      const subtotal = items.reduce((sum, item) => sum + item.price, 0)
      return subtotal * (1 + taxRate)
    },
    [items, taxRate],
  )

  return <div>মোট: ${total}</div>
}
```
</Tab>
</Tabs>

#### ২. 🎨 রেন্ডারিংয়ের জন্য ডেটা রূপান্তর

<Tabs items={['❌ ভুল উপায়', '✅ সঠিক উপায়', '✅ অপ্টিমাইজেশন সহ']}>
<Tab>
```jsx
// ❌ এটি করবেন না
function SearchResults({ items, searchTerm }) {
  const [filteredItems, setFilteredItems] = useState([])

  useEffect(
    () => {
      const filtered = items.filter(item =>
        item.name.toLowerCase().includes(searchTerm.toLowerCase()),
      )
      setFilteredItems(filtered)
    },
    [items, searchTerm],
  )

  return (
    <ul>
      {filteredItems.map(item => (
        <li key={item.id}>{item.name}</li>
      ))}
    </ul>
  )
}
```

</Tab>
<Tab>
```jsx
// ✅ এর পরিবর্তে এটি করুন
function SearchResults({ items, searchTerm }) {
  // রেন্ডারের সময় ফিল্টার করুন - কোনো Effect প্রয়োজন নেই!
  const filteredItems = items.filter(item =>
    item.name.toLowerCase().includes(searchTerm.toLowerCase()),
  )

  return (
    <ul>
      {filteredItems.map(item => (
        <li key={item.id}>{item.name}</li>
      ))}
    </ul>
  )
}
```
</Tab>

<Tab>
```jsx
// ✅ ব্যয়বহুল ফিল্টারিংয়ের জন্য - useMemo ব্যবহার করুন
function SearchResults({ items, searchTerm }) {
  const filteredItems = useMemo(
    () => {
      return items.filter(item => item.name.toLowerCase().includes(searchTerm.toLowerCase()))
    },
    [items, searchTerm],
  )

  return (
    <ul>
      {filteredItems.map(item => (
        <li key={item.id}>{item.name}</li>
      ))}
    </ul>
  )
}
```

</Tab>
</Tabs>

---

## 📚 ধাপে ধাপে Effects শেখা

### ধাপ ১: আপনার প্রথম Effect

চলুন একদম মৌলিক বিষয় দিয়ে শুরু করি:

```jsx
import { useEffect, useState } from 'react'

function MyFirstEffect() {
  const [count, setCount] = useState(0)

  // এই Effect প্রতি রেন্ডারের পরে চলে
  useEffect(() => {
    console.log('Effect চললো! Count হল:', count)
  })

  return (
    <div>
      <p>Count: {count}</p>
      <button onClick={() => setCount(count + 1)}>বৃদ্ধি করুন</button>
    </div>
  )
}
```

<div className="p-4 bg-sky-50 dark:bg-sky-50 rounded-lg border border-sky-300 dark:border-sky-300 my-4 text-black">

**🔍 এটি চালালে কী হয়?**

1. কম্পোনেন্ট রেন্ডার হয় → "Count: 0" দেখায়
2. Effect চলে → "Effect চললো! Count হল: 0" লগ করে
3. আপনি বাটনে ক্লিক করেন → `setCount(1)` → কম্পোনেন্ট পুনরায় রেন্ডার হয়
4. Effect আবার চলে → "Effect চললো! Count হল: 1" লগ করে

**💡 মূল শিক্ষা**: ডিফল্টভাবে, Effects প্রতি রেন্ডারের পরে চলে!

</div>

### ধাপ ২: Effects কখন চলবে তা নিয়ন্ত্রণ করা

প্রতি রেন্ডারের পরে চালানো সাধারণত অতিরিক্ত। এটি কীভাবে নিয়ন্ত্রণ করবেন:

#### প্যাটার্ন ক: শুধুমাত্র একবার চালান (Mount এ)

```jsx
function RunOnce() {
  const [data, setData] = useState(null)

  useEffect(() => {
    console.log('এটি শুধুমাত্র কম্পোনেন্ট মাউন্ট হওয়ার সময় একবার চলে!')

    // API কল সিমুলেট করা
    setTimeout(() => {
      setData('ডেটা লোড হয়েছে!')
    }, 2000)
  }, []) // ← খালি dependency array

  return <div>{data || 'লোড হচ্ছে...'}</div>
}
```

<Callout type="info" emoji="📝">
  **খালি array `[]` মানে**: "এই Effect এর কোনো dependency নেই, তাই কম্পোনেন্ট মাউন্ট হওয়ার সময় শুধুমাত্র একবার চালান।"
</Callout>

#### প্যাটার্ন খ: নির্দিষ্ট মান পরিবর্তিত হলে চালান

```jsx
function RunOnChange({ userId }) {
  const [user, setUser] = useState(null)

  useEffect(
    () => {
      console.log('User ID পরিবর্তিত হয়েছে:', userId)

      // নতুন ব্যবহারকারীর ডেটা আনুন
      fetchUser(userId).then(setUser)
    },
    [userId],
  ) // ← userId পরিবর্তিত হলে চলে

  return <div>{user ? `হ্যালো, ${user.name}!` : 'ব্যবহারকারী লোড হচ্ছে...'}</div>
}
```

<Callout type="info" emoji="🔄">
  **dependency array `[userId]` মানে**: "যখনই `userId` পরিবর্তিত হয় তখন এই Effect চালান।"
</Callout>

### ধাপ ৩: ক্লিনআপ (সবচেয়ে গুরুত্বপূর্ণ ধারণা!)

অনেক Effects নিজেদের পরে "পরিষ্কার" করতে হয়। এটি মেমরি লিক এবং বাগ প্রতিরোধ করে।

#### মৌলিক ক্লিনআপ উদাহরণ

```jsx
function Timer() {
  const [seconds, setSeconds] = useState(0)

  useEffect(() => {
    console.log('⚡ টাইমার সেট আপ করা হচ্ছে')

    // সেটআপ: টাইমার শুরু করুন
    const intervalId = setInterval(() => {
      setSeconds(prevSeconds => prevSeconds + 1)
    }, 1000)

    // ক্লিনআপ ফাংশন: টাইমার বন্ধ করুন
    return () => {
      console.log('🧹 টাইমার পরিষ্কার করা হচ্ছে')
      clearInterval(intervalId)
    }
  }, []) // একবার চালান

  return <div>টাইমার: {seconds} সেকেন্ড</div>
}
```

<div className="p-4 bg-amber-100 rounded-lg border border-amber-500 my-4 text-black">

**🔍 এখানে কী হয়?**

1. কম্পোনেন্ট মাউন্ট হয় → "টাইমার সেট আপ করা হচ্ছে" → টাইমার শুরু হয়
2. প্রতি সেকেন্ডে → টাইমার `seconds` বৃদ্ধি করে
3. কম্পোনেন্ট আনমাউন্ট হয় → "টাইমার পরিষ্কার করা হচ্ছে" → টাইমার বন্ধ হয়

**⚠️ ক্লিনআপ ছাড়া**: টাইমার চিরকাল চলতে থাকবে, কম্পোনেন্ট চলে যাওয়ার পরেও! 💥

</div>

---

## 🎯 বাস্তব জীবনের উদাহরণ

### উদাহরণ ১: সম্পূর্ণ আবহাওয়া অ্যাপ

```jsx
function WeatherApp() {
  const [location, setLocation] = useState('')
  const [weather, setWeather] = useState(null)
  const [loading, setLoading] = useState(false)
  const [error, setError] = useState(null)
  const [units, setUnits] = useState('celsius')

  // Effect ১: আবহাওয়ার ডেটা আনুন
  useEffect(
    () => {
      if (!location.trim()) {
        setWeather(null)
        return
      }

      setLoading(true)
      setError(null)

      // রিকুয়েস্ট বাতিল করতে AbortController ব্যবহার করুন
      const controller = new AbortController()

      async function fetchWeather() {
        try {
          const response = await fetch(
            `/api/weather?location=${encodeURIComponent(location)}&units=${units}`,
            { signal: controller.signal },
          )

          if (!response.ok) {
            throw new Error(`আবহাওয়া সেবা ত্রুটি: ${response.status}`)
          }

          const weatherData = await response.json()
          setWeather(weatherData)
        } catch (err) {
          if (err.name !== 'AbortError') {
            setError(err.message)
          }
        } finally {
          setLoading(false)
        }
      }

      fetchWeather()

      // ক্লিনআপ: স্থান বা একক পরিবর্তিত হলে রিকুয়েস্ট বাতিল করুন
      return () => {
        controller.abort()
      }
    },
    [location, units],
  )

  // Effect ২: ডকুমেন্ট টাইটেল আপডেট করুন
  useEffect(
    () => {
      if (weather) {
        document.title = `${weather.temperature}° in ${location} - আবহাওয়া অ্যাপ`
      } else {
        document.title = 'আবহাওয়া অ্যাপ'
      }

      // ক্লিনআপ: কম্পোনেন্ট আনমাউন্ট হলে টাইটেল রিসেট করুন
      return () => {
        document.title = 'আমার অ্যাপ'
      }
    },
    [weather, location],
  )

  // Effect ৩: ব্যবহারকারীর পছন্দ সংরক্ষণ করুন
  useEffect(
    () => {
      localStorage.setItem('weatherApp_units', units)
    },
    [units],
  )

  // Effect ৪: মাউন্টের সময় সংরক্ষিত পছন্দ লোড করুন
  useEffect(() => {
    const savedUnits = localStorage.getItem('weatherApp_units')
    if (savedUnits) {
      setUnits(savedUnits)
    }
  }, []) // মাউন্টে একবার চালান

  return (
    <div className="weather-app">
      <h1>🌤️ আবহাওয়া অ্যাপ</h1>

      <div className="controls">
        <input
          type="text"
          value={location}
          onChange={e => setLocation(e.target.value)}
          placeholder="শহরের নাম লিখুন..."
        />

        <select value={units} onChange={e => setUnits(e.target.value)}>
          <option value="celsius">সেলসিয়াস</option>
          <option value="fahrenheit">ফারেনহাইট</option>
        </select>
      </div>

      {loading && <div className="loading">🌀 আবহাওয়া লোড হচ্ছে...</div>}

      {error && <div className="error">❌ ত্রুটি: {error}</div>}

      {weather &&
        !loading && (
          <div className="weather-display">
            <h2>{location}</h2>
            <div className="temperature">
              {weather.temperature}°{units === 'celsius' ? 'C' : 'F'}
            </div>
            <div className="condition">{weather.condition}</div>
            <div className="details">
              <p>আর্দ্রতা: {weather.humidity}%</p>
              <p>বাতাস: {weather.windSpeed} mph</p>
            </div>
          </div>
        )}
    </div>
  )
}
```

---

## 🔧 ডেভেলপমেন্টে Effects দুইবার চালানো পরিচালনা

<Callout type="info" emoji="🧪">
  React Strict Mode এ, বাগ ধরতে সহায়তার জন্য Effects মাউন্টে দুইবার চলে। এটি শুধুমাত্র ডেভেলপমেন্টে হয়!
</Callout>

```jsx
useEffect(() => {
  // এটি Strict Mode এ দুইবার চলবে (শুধু dev এ)
  console.log('Effect চলছে...')

  return () => {
    // ক্লিনআপও দুইবার চলে!
    console.log('ক্লিনআপ চলছে...')
  }
}, [])
```

**React কেন এটি করে?** এটি আপনাকে এমন Effects খুঁজে পেতে সাহায্য করে যেগুলো সঠিকভাবে পরিষ্কার করা হয়নি। যদি আপনার Effect দুইবার চালানোতে সমস্যা হয়, তাহলে সম্ভবত আপনার আরও ভালো ক্লিনআপ প্রয়োজন!

<Callout type="warning" emoji="⚠️">
  মেমরি লিক বা ডুপ্লিকেট সাবস্ক্রিপশন এড়াতে ক্লিনআপ ফাংশন যোগ করুন।
</Callout>

---

## 🌐 Effects দিয়ে ডেটা আনা

ডেটা আনা effects এর জন্য একটি খুবই সাধারণ ব্যবহার। এভাবে নিরাপদে করবেন:

<Tabs items={['মৌলিক প্যাটার্ন', 'AbortController সহ', 'কাস্টম হুক সহ']}>
<Tab>
```jsx
useEffect(() => {
  let ignore = false

  fetch('/api/data')
    .then(res => res.json())
    .then(data => {
      if (!ignore) {
        setData(data)
      }
    })

  return () => {
    ignore = true
  }
}, [])
```
</Tab>

<Tab>
```jsx
useEffect(() => {
  const controller = new AbortController()

  async function fetchData() {
    try {
      const response = await fetch('/api/data', {
        signal: controller.signal,
      })
      const data = await response.json()
      setData(data)
    } catch (error) {
      if (error.name !== 'AbortError') {
        setError(error.message)
      }
    }
  }

  fetchData()

  return () => {
    controller.abort()
  }
}, [])
```

</Tab>

<Tab>
```jsx
function useFetch(url) {
  const [data, setData] = useState(null)
  const [loading, setLoading] = useState(true)
  const [error, setError] = useState(null)

  useEffect(
    () => {
      const controller = new AbortController()

      async function fetchData() {
        try {
          setLoading(true)
          const response = await fetch(url, {
            signal: controller.signal,
          })
          const result = await response.json()
          setData(result)
        } catch (err) {
          if (err.name !== 'AbortError') {
            setError(err.message)
          }
        } finally {
          setLoading(false)
        }
      }

      fetchData()

      return () => controller.abort()
    },
    [url],
  )

  return { data, loading, error }
}

// ব্যবহার:
function MyComponent() {
  const { data, loading, error } = useFetch('/api/users')

  if (loading) return <div>লোড হচ্ছে...</div>
  if (error) return <div>ত্রুটি: {error}</div>
  return <div>{JSON.stringify(data)}</div>
}
```

</Tab>
</Tabs>

<Callout type="warning" emoji="⚠️">
আনমাউন্ট হওয়া কম্পোনেন্টে স্টেট সেট করা এড়াতে সর্বদা async effects পরিষ্কার করুন!
</Callout>

---

## 🤷‍♂️ আপনার Effect এর প্রয়োজন নাও হতে পারে

যদি আপনি শুধুমাত্র প্রদর্শনের জন্য ডেটা রূপান্তর করছেন, তাহলে Effects এর পরিবর্তে রেন্ডারিং বা মেমোইজেশন ব্যবহার করুন।

<div className="grid grid-cols-1 md:grid-cols-2 gap-4 my-4">

<div className="p-4 bg-red-50 rounded-lg border border-red-400 text-black">
<div className="font-bold text-lg">❌ এটি করবেন না:</div>
```jsx
const [filtered, setFiltered] = useState([])

useEffect(
  () => {
    setFiltered(items.filter(item => item.active))
  },
  [items],
)
```

</div>

<div className="p-4 bg-green-50 rounded-lg border border-green-500 overflow-x-auto text-black">
<div className="font-bold text-lg">✅ এর পরিবর্তে এটি করুন:</div>
```jsx
// রেন্ডারের সময় গণনা করুন
const filtered = items.filter(item => item.active)

// অথবা ব্যয়বহুল গণনার জন্য useMemo ব্যবহার করুন
const filtered = useMemo(() => {
  return items.filter(item => item.active), [items]
})
```
</div>

</div>

<Callout type="info" emoji="💡">
Effects সাইড ইফেক্ট এর জন্য, রেন্ডারে করা যায় এমন গণনার জন্য নয়!
</Callout>

---

পার্ট ২: [উন্নত প্যাটার্ন ও সর্বোত্তম অনুশীলনে](/javascript-guidebook-bn/React-js/module-04/synchronizing-with-effects-overview/synchronizing-with-effects-advanced) এগিয়ে যান, যেখানে আপনি জানতে পারবেন:

- 🔄 Effect জীবনচক্র এবং Dependencies 
- ⚡ Effects রিঅ্যাক্টিভ মানের সাথে প্রতিক্রিয়া করে
- 🖱️ Effects থেকে ইভেন্ট আলাদা করা
- ✂️ Effect Dependencies অপসারণ
- 🪝 কাস্টম হুক দিয়ে লজিক পুনরায় ব্যবহার
- 🛠️ উন্নত প্যাটার্ন
- 🐛 সাধারণ ভুল এবং ডিবাগিং
- সম্পূর্ণ রেফারেন্স
