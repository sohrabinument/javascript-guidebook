# React Effects (useEffect) ইন্টারভিউ প্রশ্ন ও উত্তর 🎯

এই গাইডে React Effects (useEffect), অ্যাডভান্সড প্যাটার্ন ও বেস্ট প্র্যাকটিস নিয়ে ইন্টারভিউ প্রশ্ন ও উত্তর রয়েছে। ইন্টারভিউ প্রস্তুতি বা নিজের বোঝাপড়া যাচাই করতে ব্যবহার করুন।

> **💡 প্রো টিপ:** নিজেকে যাচাই করতে প্রশ্নগুলো পড়ুন, উত্তর কভার করুন এবং নিজে চেষ্টা করুন। এতে আপনার মনে রাখার ক্ষমতা বাড়বে।

---

## ৪.১ – ইফেক্ট ফান্ডামেন্টালস 🌱

<div style={{ background: 'rgba(0, 100, 200, 0.10)', padding: '15px', borderRadius: '5px', marginBottom: '20px', border: '1px solid rgba(0, 100, 200, 0.15)', color: 'inherit' }}>

**Q1: React-এ সাইড ইফেক্ট কী?**  
উত্তর: সাইড ইফেক্ট হলো এমন কোনো অপারেশন যা কম্পোনেন্টের বাইরে কিছু পরিবর্তন করে, যেমন ডেটা ফেচ, সাবস্ক্রিপশন, টাইমার, বা DOM ম্যানুয়ালি পরিবর্তন।

**Q2: সাইড ইফেক্ট হ্যান্ডল করার জন্য React কোন হুক দেয়?**  
উত্তর: `useEffect` হুক।

**Q3: useEffect কখন চলে?**  
উত্তর: কম্পোনেন্ট রেন্ডার হওয়ার পর (DOM আপডেট হওয়ার পর)।

**Q4: useEffect-এর সাধারণ ইউজ কেস কী কী?**  
উত্তর: ডেটা ফেচ, ইভেন্ট লিসেনার, সাবস্ক্রিপশন, টাইমার, ডকুমেন্ট টাইটেল আপডেট, ব্রাউজার API ইন্টারঅ্যাকশন।

**Q5: useEffect-এর বেসিক সিনট্যাক্স কী?**  
উত্তর: `useEffect(() => { /* effect code */ }, [/* dependencies */])`

**Q6: ডিপেন্ডেন্সি অ্যারে না দিলে কী হয়?**  
উত্তর: ইফেক্টটি প্রতিবার রেন্ডারের পর চলে।

**Q7: কিভাবে শুধু একবার (মাউন্টে) ইফেক্ট চালাবেন?**  
উত্তর: খালি ডিপেন্ডেন্সি অ্যারে দিন: `useEffect(() => { ... }, [])`

**Q8: নির্দিষ্ট ভ্যালু পরিবর্তনে ইফেক্ট চালাতে হলে কী করবেন?**  
উত্তর: সেই ভ্যালুগুলো ডিপেন্ডেন্সি অ্যারেতে দিন: `useEffect(() => { ... }, [value1, value2])`

**Q9: useEffect-এ ক্লিনআপ ফাংশনের উদ্দেশ্য কী?**  
উত্তর: রিসোর্স (যেমন সাবস্ক্রিপশন, টাইমার, ইভেন্ট লিসেনার) ক্লিন করা, যখন কম্পোনেন্ট আনমাউন্ট হয় বা ইফেক্ট আবার চলে।

উদাহরণ:
```jsx
useEffect(() => {
  const handler = () => window.alert('resize');
  window.addEventListener('resize', handler);
  return () => window.removeEventListener('resize', handler);
}, []);
```

**Q10: useEffect-এ ক্লিনআপ ফাংশন কীভাবে দেবেন?**  
উত্তর: ইফেক্ট থেকে একটি ফাংশন রিটার্ন করুন: `useEffect(() => { ...; return () => { /* cleanup */ } }, [...])`

</div>

> **🔍 ইন্টারভিউ ইনসাইট:** ইফেক্ট লাইফসাইকেল ও ক্লিনআপ বোঝা রিয়েল-ওয়ার্ল্ড React অ্যাপে খুব গুরুত্বপূর্ণ।

---

## ৪.২ – ইফেক্ট লাইফসাইকেল ও ডিপেন্ডেন্সি 🔄

<div style={{ background: 'rgba(230, 150, 70, 0.10)', padding: '15px', borderRadius: '5px', marginBottom: '20px', border: '1px solid rgba(230, 150, 70, 0.15)', color: 'inherit' }}>

**Q11: ইফেক্ট লাইফসাইকেলের তিনটি প্রধান ধাপ কী?**  
উত্তর: মাউন্ট (প্রথম রেন্ডারের পর ইফেক্ট চলে), আপডেট (ডিপেন্ডেন্সি পরিবর্তনে চলে), আনমাউন্ট (কম্পোনেন্ট রিমুভ হলে ক্লিনআপ চলে)।

**Q12: useEffect-এ ডিপেন্ডেন্সি অ্যারে কী?**  
উত্তর: কোন ভ্যালু পরিবর্তনে ইফেক্ট চলবে তা React-কে বলে দেয়।

**Q13: ডিপেন্ডেন্সি হিসেবে অবজেক্ট/অ্যারে দিলে কী হয়?**  
উত্তর: ইফেক্ট বেশি বার চলে যেতে পারে, কারণ অবজেক্ট/অ্যারে রেফারেন্স দিয়ে তুলনা হয়।

**Q14: অবাঞ্ছিত ইফেক্ট রান এড়াতে কী করবেন?**  
উত্তর: `useMemo` বা `useCallback` দিয়ে মেমোাইজ করুন, অথবা প্রিমিটিভ ডিপেন্ডেন্সি ব্যবহার করুন।

উদাহরণ:
```jsx
const memoizedSettings = useMemo(() => ({ theme }), [theme]);
useEffect(() => {
  // শুধু theme পরিবর্তনে চলে
}, [memoizedSettings]);
```

**Q15: রেফারেন্স ইকুয়ালিটি ট্র্যাপ কী?**  
উত্তর: React রেফারেন্স দিয়ে তুলনা করে, তাই নতুন অবজেক্ট/অ্যারে/ফাংশন দিলে ইফেক্ট সবসময় চলে।

**Q16: ফাংশন ডিপেন্ডেন্সি কিভাবে হ্যান্ডল করবেন?**  
উত্তর: প্যারেন্টে `useCallback` ব্যবহার করুন, অথবা রেফ-এ ফাংশন রাখুন।

**Q17: ডিপেন্ডেন্সি মিস করার সাধারণ ভুল কী?**  
উত্তর: ইফেক্টে ব্যবহৃত সব ভ্যালু ডিপেন্ডেন্সি অ্যারেতে না দেয়া, ফলে স্টেইল ক্লোজার ও বাগ হতে পারে।

**Q18: ডিপেন্ডেন্সি সমস্যা ডিবাগ করবেন কীভাবে?**  
উত্তর: React DevTools, কনসোল লগ, এবং exhaustive-deps ESLint রুল ব্যবহার করুন।

**Q19: ডিপেন্ডেন্সি পরিবর্তনে ক্লিনআপ কখন চলে?**  
উত্তর: নতুন ইফেক্ট চলার আগে ক্লিনআপ চলে।

**Q20: useEffect কিছু রিটার্ন না করলে কী হয়?**  
উত্তর: কোনো ক্লিনআপ হয় না।

</div>

---

## ৪.৩ – রিয়েল-ওয়ার্ল্ড ইফেক্ট প্যাটার্ন 🌐

<div style={{ background: 'rgba(50, 180, 50, 0.10)', padding: '15px', borderRadius: '5px', marginBottom: '20px', border: '1px solid rgba(50, 180, 50, 0.15)', color: 'inherit' }}>

**Q21: useEffect দিয়ে ডেটা ফেচ করবেন কীভাবে?**  
উত্তর: ইফেক্টের ভিতরে ফেচ শুরু করুন, রেজাল্ট দিয়ে স্টেট আপডেট করুন, এবং দরকার হলে AbortController দিয়ে ক্লিনআপ করুন।

উদাহরণ:
```jsx
useEffect(() => {
  const controller = new AbortController();
  async function fetchData() {
    const res = await fetch('/api/data', { signal: controller.signal });
    const data = await res.json();
    setData(data);
  }
  fetchData();
  return () => controller.abort();
}, []);
```

**Q22: ডেটা ফেচ ইফেক্টে AbortController কেন ব্যবহার করবেন?**  
উত্তর: কম্পোনেন্ট আনমাউন্ট হলে বা ডিপেন্ডেন্সি পরিবর্তনে রিকোয়েস্ট ক্যান্সেল করতে, যাতে মেমরি লিক বা অবাঞ্ছিত স্টেট আপডেট না হয়।

**Q23: useEffect-এ ইভেন্ট লিসেনার সেটআপ ও ক্লিনআপ করবেন কীভাবে?**  
উত্তর: ইফেক্টে লিসেনার অ্যাড করুন, ক্লিনআপে রিমুভ করুন।

**Q24: useEffect-এ টাইমার/ইন্টারভ্যাল হ্যান্ডল করবেন কীভাবে?**  
উত্তর: ইফেক্টে সেটআপ করুন, ক্লিনআপে ক্লিয়ার করুন।

উদাহরণ:
```jsx
useEffect(() => {
  const timer = setInterval(() => setCount(c => c + 1), 1000);
  return () => clearInterval(timer);
}, []);
```

**Q25: ক্যালকুলেশনের জন্য ইফেক্ট ব্যবহার করার সাধারণ ভুল কী?**  
উত্তর: এমন ক্যালকুলেশন ইফেক্টে করা যা রেন্ডার বা `useMemo`-তে করা যেত।

**Q26: কখন useEffect ব্যবহার না করাই ভালো?**  
উত্তর: যখন props/state থেকে সরাসরি ভ্যালু ডেরাইভ করা যায় বা `useMemo`-তে করা যায়।

**Q27: useEffect দিয়ে ডকুমেন্ট টাইটেল আপডেট করবেন কীভাবে?**  
উত্তর: ইফেক্টে `document.title = ...` দিন, চাইলে ক্লিনআপে রিসেট করুন।

**Q28: useEffect দিয়ে localStorage-এ স্টেট সেভ করবেন কীভাবে?**  
উত্তর: স্টেট পরিবর্তনে ইফেক্ট চালান, এবং ভিতরে `localStorage.setItem` ব্যবহার করুন।

উদাহরণ:
```jsx
useEffect(() => {
  localStorage.setItem('count', count);
}, [count]);
```

**Q29: মাউন্টে localStorage থেকে ডেটা পড়বেন কীভাবে?**  
উত্তর: খালি ডিপেন্ডেন্সি অ্যারে দিয়ে ইফেক্ট চালান, এবং localStorage থেকে পড়ে স্টেট সেট করুন।

**Q30: সাবস্ক্রিপশন (যেমন WebSocket, এক্সটার্নাল API) useEffect-এ হ্যান্ডল করবেন কীভাবে?**  
উত্তর: ইফেক্টে সাবস্ক্রিপশন সেটআপ করুন, ক্লিনআপে আনসাবস্ক্রাইব/ডিসকানেক্ট করুন।

</div>

---

## ৪.৪ – অ্যাডভান্সড ইফেক্ট প্যাটার্ন ও বেস্ট প্র্যাকটিস 🛠️

<div style={{ background: 'rgba(100, 100, 200, 0.10)', padding: '15px', borderRadius: '5px', marginBottom: '20px', border: '1px solid rgba(100, 100, 200, 0.15)', color: 'inherit' }}>

**Q31: ইফেক্টে ডিবাউন্সিং কী, এবং কিভাবে করবেন?**  
উত্তর: ডিবাউন্সিং মানে ভ্যালু পরিবর্তন থেমে গেলে নির্দিষ্ট সময় পর ইফেক্ট চালানো, সাধারণত `setTimeout` ও `clearTimeout` দিয়ে।

**Q32: ইফেক্টে থ্রটলিং কী?**  
উত্তর: থ্রটলিং মানে নির্দিষ্ট সময় পরপর ইফেক্ট চালানো, সাধারণত শেষ রান টাইম ট্র্যাক করে।

**Q33: useEffect দিয়ে পোলিং করবেন কীভাবে?**  
উত্তর: ইফেক্টে `setInterval` দিয়ে বারবার ডেটা ফেচ করুন, ক্লিনআপে ইন্টারভ্যাল ক্লিয়ার করুন।

**Q34: অ্যাসিঙ্ক ইফেক্টে রেস কন্ডিশন কী, এবং কিভাবে প্রতিরোধ করবেন?**  
উত্তর: একাধিক অ্যাসিঙ্ক অপারেশন ওভারল্যাপ করলে রেস কন্ডিশন হয়; ফ্ল্যাগ বা AbortController দিয়ে শুধু লেটেস্ট রিকোয়েস্টের রেজাল্ট নিন।

**Q35: জটিল ইফেক্ট ছোট ছোট ইফেক্টে ভাগ করবেন কীভাবে?**  
উত্তর: একাধিক useEffect ব্যবহার করুন, প্রতিটা আলাদা কনসার্নে ফোকাস করুন।

**Q36: ফোকাসড ইফেক্টের উপকারিতা কী?**  
উত্তর: ডিবাগ, মেইনটেইন ও অবাঞ্ছিত রান এড়ানো সহজ হয়।

**Q37: ইফেক্ট ডিপেন্ডেন্সি কমাবেন কীভাবে?**  
উত্তর: নন-রিঅ্যাক্টিভ ভ্যালু ইফেক্টের ভিতরে নিন, রেফ ব্যবহার করুন, কনস্ট্যান্ট/ফাংশন কম্পোনেন্টের বাইরে রাখুন।

**Q38: অপ্রয়োজনীয় ইফেক্ট রান এড়াতে রেফ ব্যবহার করবেন কীভাবে?**  
উত্তর: রেফ-এ ভ্যালু রাখুন, চাইলে ইফেক্ট ট্রিগার হবে না।

**Q39: ইভেন্ট হ্যান্ডলার ও ইফেক্টের পার্থক্য কী?**  
উত্তর: ইভেন্ট হ্যান্ডলার ইউজার অ্যাকশনে চলে; ইফেক্ট ডেটা পরিবর্তনে চলে।

**Q40: কোন লজিক ইভেন্ট হ্যান্ডলার, আর কোনটা ইফেক্টে রাখবেন?**  
উত্তর: ইউজার অ্যাকশনে হলে ইভেন্ট হ্যান্ডলার, ডেটা পরিবর্তনে হলে ইফেক্ট।

</div>

---

## ৪.৫ – কাস্টম হুক ও ইফেক্ট রিইউজ 🪝

<div style={{ background: 'rgba(50, 180, 150, 0.10)', padding: '15px', borderRadius: '5px', marginBottom: '20px', border: '1px solid rgba(50, 180, 150, 0.15)', color: 'inherit' }}>

**Q41: React-এ কাস্টম হুক কী?**  
উত্তর: `use` দিয়ে শুরু এমন ফাংশন, যা রিইউজেবল স্টেটফুল লজিক (ইফেক্টসহ) ইনক্যাপসুলেট করে।

**Q42: ইফেক্টের জন্য কাস্টম হুক কেন ব্যবহার করবেন?**  
উত্তর: ইফেক্ট লজিক শেয়ার, কোড DRY রাখা, ও টেস্টেবিলিটি বাড়াতে।

**Q43: ডেটা ফেচের জন্য কাস্টম হুক লিখবেন কীভাবে?**  
উত্তর: যেমন `useFetch`—ভিতরে `useEffect` ও `useState` ব্যবহার করুন, এবং ডেটা, লোডিং, এরর স্টেট রিটার্ন করুন।

উদাহরণ:
```jsx
function useFetch(url) {
  const [data, setData] = useState(null);
  const [loading, setLoading] = useState(true);
  useEffect(() => {
    let ignore = false;
    fetch(url)
      .then(res => res.json())
      .then(json => { if (!ignore) setData(json); })
      .finally(() => { if (!ignore) setLoading(false); });
    return () => { ignore = true; };
  }, [url]);
  return { data, loading };
}
```

**Q44: কাস্টম হুকে ক্লিনআপ হ্যান্ডল করবেন কীভাবে?**  
উত্তর: ইফেক্টের ভিতরে ক্লিনআপ ফাংশন রিটার্ন করুন, ঠিক কম্পোনেন্টের মতো।

**Q45: ইফেক্টসহ কাস্টম হুকের বেস্ট প্র্যাকটিস কী?**  
উত্তর: ফোকাসড রাখুন, ডিপেন্ডেন্সি ডকুমেন্ট করুন, ক্লিনআপ হ্যান্ডল করুন, স্টেবল API রিটার্ন করুন।

**Q46: কাস্টম হুক থেকে অন্য হুক কল করা যায় কি?**  
উত্তর: হ্যাঁ, কাস্টম হুক থেকে useEffect, useState, useRef ইত্যাদি কল করা যায়।

**Q47: ইফেক্টসহ কাস্টম হুকে সাধারণ ভুল কী?**  
উত্তর: সব ডিপেন্ডেন্সি না দেয়া, বা ক্লিনআপ না করা।

**Q48: ইফেক্টসহ কাস্টম হুক টেস্ট করবেন কীভাবে?**  
উত্তর: `@testing-library/react-hooks` দিয়ে হুক রেন্ডার করে বিহেভিয়ার টেস্ট করুন।

**Q49: অ্যাডভান্সড লজিকের জন্য একাধিক হুক কম্পোজ করবেন কীভাবে?**  
উত্তর: কাস্টম হুকের ভিতরে একাধিক হুক কল করুন, এবং রেজাল্ট/লজিক কম্বাইন করুন।

**Q50: কাস্টম হুক থেকে অবজেক্ট রিটার্নের উপকারিতা কী?**  
উত্তর: অবজেক্টে নামসহ প্রপার্টি থাকে, তাই API বেশি ফ্লেক্সিবল ও রিডেবল।

</div>

---

## ৪.৬ – সাধারণ ভুল, ডিবাগিং ও সিনারিও 🐛

<div style={{ background: 'rgba(220, 50, 50, 0.10)', padding: '15px', borderRadius: '5px', marginBottom: '20px', border: '1px solid rgba(220, 50, 50, 0.15)', color: 'inherit' }}>

**Q51: ইফেক্টে ক্লিনআপ ভুলে গেলে কী হয়?**  
উত্তর: মেমরি লিক, ডুপ্লিকেট সাবস্ক্রিপশন, বা অবাঞ্ছিত সাইড ইফেক্ট হতে পারে।

**Q52: useEffect-এ ইনফিনিট লুপের কারণ কী?**  
উত্তর: ইফেক্টে স্টেট আপডেট করলে এবং সেই স্টেট ডিপেন্ডেন্সিতে থাকলে, বা ডিপেন্ডেন্সি ভুল হলে।

**Q53: ইফেক্ট সমস্যা ডিবাগ করবেন কীভাবে?**  
উত্তর: কনসোল লগ, React DevTools, ডিপেন্ডেন্সি অ্যারে চেক, ক্লিনআপ ফাংশন যাচাই।

**Q54: exhaustive-deps ESLint রুল কী?**  
উত্তর: ইফেক্টের ডিপেন্ডেন্সি অ্যারেতে কিছু বাদ পড়লে ওয়ার্ন করে।

**Q55: অ্যাসিঙ্ক ইফেক্টে এরর হ্যান্ডল করবেন কীভাবে?**  
উত্তর: try/catch ব্লক ব্যবহার করুন, এবং দরকারে এরর স্টেট সেট করুন।

**Q56: কোন ক্ষেত্রে ইফেক্ট ব্যবহার না করাই ভালো?**  
উত্তর: যখন আলাদা লজিক (যেমন ডেটা ফেচ, ইভেন্ট লিসেনার) থাকে।

**Q57: ইফেক্ট ক্লিনআপ কাজ করছে কিনা টেস্ট করবেন কীভাবে?**  
উত্তর: কম্পোনেন্ট আনমাউন্ট করে দেখুন রিসোর্স (টাইমার, লিসেনার) রিলিজ হচ্ছে কিনা।

**Q58: ইফেক্ট ইউজের সিনারিও-ভিত্তিক প্রশ্ন কী?**  
উত্তর: নিচে একটি প্র্যাকটিক্যাল সিনারিও ও স্যাম্পল উত্তর দেখুন।

</div>

---

## প্র্যাকটিস ইন্টারভিউ এক্সারসাইজ 🏋️‍♂️

<div style={{ background: 'rgba(20, 120, 220, 0.10)', padding: '20px', borderRadius: '5px', marginBottom: '20px', border: '1px solid rgba(20, 120, 220, 0.15)', color: 'inherit' }}>

### সিনারিও-ভিত্তিক প্রশ্ন:

আপনি একটি চ্যাট অ্যাপ বানাচ্ছেন। ইউজার চ্যাট রুমে ঢুকলে আপনাকে:
- WebSocket-এ কানেক্ট করতে হবে
- নতুন মেসেজ শুনতে হবে
- UI রিয়েল-টাইমে আপডেট করতে হবে
- ইউজার রুম ছাড়লে কানেকশন ক্লিন করতে হবে

**প্রশ্ন:** useEffect দিয়ে কীভাবে ইমপ্লিমেন্ট করবেন? সঠিক বিহেভিয়ার ও বাগ এড়াতে কী কী খেয়াল রাখবেন?

উত্তর ভাবুন, তারপর স্যাম্পল উত্তর দেখুন।

<details>
<summary>**স্যাম্পল উত্তর দেখুন**</summary>

**স্যাম্পল অ্যাপ্রোচ:**

```jsx
useEffect(() => {
  const connection = createConnection(roomId);
  connection.connect();
  connection.on('message', handleMessage);
  return () => {
    connection.disconnect();
  };
}, [roomId]);
```

- ইফেক্টে কানেকশন সেটআপ করুন
- ক্লিনআপে ডিসকানেক্ট করুন
- সব ডিপেন্ডেন্সি (যেমন roomId, handleMessage) দিন
- মেমরি লিক ও ডুপ্লিকেট লিসেনার এড়ান

</details>
</div>

---

## ৪.৭ – অ্যাডভান্সড সিনারিও, পারফরম্যান্স ও অ্যাক্সেসিবিলিটি ⚡️

<div style={{ background: 'rgba(100, 180, 255, 0.10)', padding: '15px', borderRadius: '5px', marginBottom: '20px', border: '1px solid rgba(100, 180, 255, 0.15)', color: 'inherit' }}>

**Q59: বড় React অ্যাপে ইফেক্ট পারফরম্যান্স অপ্টিমাইজ করবেন কীভাবে?**  
উত্তর: ডিপেন্ডেন্সি মিনিমাইজ করুন, ইফেক্ট ভাগ করুন, মেমোাইজেশন (`useMemo`, `useCallback`) ব্যবহার করুন, অপ্রয়োজনীয় স্টেট আপডেট এড়ান, দ্রুত ক্লিনআপ করুন।

**Q60: ইফেক্টে অ্যাক্সেসিবিলিটি কনসিডারেশন কী কী?**  
উত্তর: ইফেক্টের পর ফোকাস ম্যানেজ করুন, স্ক্রিন রিডারের জন্য স্টেট চেঞ্জ ঘোষণা করুন, কিবোর্ড ন্যাভিগেশন বা স্ক্রিন রিডার কনটেক্সট বিঘ্ন করবেন না।

**Q61: আগের props/state ভ্যালু লাগলে ইফেক্টে কীভাবে হ্যান্ডল করবেন?**  
উত্তর: রেফ-এ আগের ভ্যালু রাখুন, বা স্টেট সেটারের ফাংশনাল ফর্ম ব্যবহার করুন।

উদাহরণ:
```jsx
const prevValue = useRef();
useEffect(() => {
  if (prevValue.current !== undefined) {
    // prevValue.current ও value তুলনা করুন
  }
  prevValue.current = value;
}, [value]);
```

**Q62: ডিপেন্ডেন্সি ভুলে স্টেট আপডেট করলে কী ঝুঁকি?**  
উত্তর: ইনফিনিট লুপ বা স্টেইল স্টেট বাগ হতে পারে।

**Q63: ক্লাস কম্পোনেন্ট লাইফসাইকেল মেথড (componentDidMount, componentDidUpdate, componentWillUnmount) useEffect-এ কনভার্ট করবেন কীভাবে?**  
উত্তর: `componentDidMount`-এর জন্য `useEffect(() => { ... }, [])`, `componentDidUpdate`-এর জন্য ডিপেন্ডেন্সি দিন, `componentWillUnmount`-এর জন্য ক্লিনআপ ফাংশন রিটার্ন করুন।

উদাহরণ:
```jsx
// componentDidMount
useEffect(() => {
  // একবার চলবে
}, []);

// componentDidUpdate (prop পরিবর্তনে)
useEffect(() => {
  // প্রপ পরিবর্তনে চলবে
}, [prop]);

// componentWillUnmount
useEffect(() => {
  // সেটআপ
  return () => {
    // ক্লিনআপ
  };
}, []);
```

**Q64: ডেটা ফেচ ইফেক্টে সিকিউরিটি নিশ্চিত করবেন কীভাবে?**  
উত্তর: API রেসপন্স ভ্যালিডেট ও স্যানিটাইজ করুন, এরর হ্যান্ডল করুন, সেনসিটিভ ডেটা লগ বা স্টেটে রাখবেন না।

**Q65: useEffect-এ সরাসরি async ফাংশন দিলে কী সমস্যা?**  
উত্তর: ইফেক্ট কলব্যাক async করা যায় না; ভিতরে async ফাংশন ডিফাইন ও কল করুন।

উদাহরণ:
```jsx
useEffect(() => {
  async function fetchData() {
    // await ...
  }
  fetchData();
}, []);
```

**Q66: শুধু প্রোডাকশনে ইফেক্ট চালাতে হলে কী করবেন?**  
উত্তর: ইফেক্টের ভিতরে environment চেক করুন (যেমন `if (process.env.NODE_ENV === 'production')`)।

**Q67: কোন ক্ষেত্রে ইউজার ইন্টারঅ্যাকশনের পর ইফেক্ট চালানো উচিত?**  
উত্তর: অ্যানালিটিক্স, খরচবহুল ক্যালকুলেশন, বা ডেটা ফেচ—যা শুধু বাটন ক্লিক বা ফর্ম সাবমিশনের পর দরকার।

**Q68: সাবস্ক্রিপশন বা টাইমার ইফেক্টে মেমরি লিক এড়াবেন কীভাবে?**  
উত্তর: সবসময় ক্লিনআপ ফাংশন রিটার্ন করুন, যা আনসাবস্ক্রাইব, ডিসকানেক্ট, বা টাইমার ক্লিয়ার করে।

উদাহরণ:
```jsx
useEffect(() => {
  const timer = setInterval(() => { /* ... */ }, 1000);
  return () => clearInterval(timer);
}, []);
```

</div>

---

**টিপ:** প্রশ্ন-উত্তরগুলো পড়ে নিজের ভাষায় বোঝার চেষ্টা করুন—এটাই সেরা প্রস্তুতি!

> **🚀 ফাইনাল অ্যাডভাইস:** ইন্টারভিউতে শুধু ইফেক্টের ফিচার নয়, কেন ও কীভাবে ব্যবহার করবেন, সেটাও বোঝান—এটাই প্রকৃত দক্ষতা।
