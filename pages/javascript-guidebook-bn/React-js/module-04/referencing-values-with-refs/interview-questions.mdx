import { Callout } from "nextra/components";

<img
  src="https://upload.wikimedia.org/wikipedia/commons/a/a7/React-icon.svg"
  alt="React Logo"
  width="70"
  style={{ marginBottom: "1rem" }}
/>

# রেফারেন্সিং ভ্যালুজ উইথ রিফস – ইন্টারভিউ প্রশ্ন ও উত্তর

এই প্রশ্নগুলো ব্যবহার করুন ইন্টারভিউ প্রস্তুতির জন্য বা React-এ রিফস সম্পর্কে আপনার বোঝাপড়া যাচাই করতে।

> **💡 প্রো টিপ:** প্রতিটি প্রশ্নের উত্তর নিজে চেষ্টা করুন, তারপর উত্তর দেখুন। এতে আপনি আরও ভালোভাবে মনে রাখতে পারবেন!

---

## ৪.১ – মৌলিক বিষয়াবলী 🧩

<div style={{ background: 'rgba(0, 100, 200, 0.10)', padding: '15px', borderRadius: '5px', marginBottom: '20px', border: '1px solid rgba(0, 100, 200, 0.15)', color: 'inherit' }}>

**প্রশ্ন ১: React-এ ref কী?**  
উত্তর: ref হলো একটি বিশেষ অবজেক্ট যা `useRef` হুক থেকে পাওয়া যায় এবং এটি রেন্ডারগুলোর মধ্যে মান ধরে রাখে, কিন্তু মান পরিবর্তন করলে রি-রেন্ডার হয় না।

**প্রশ্ন ২: ফাংশনাল কম্পোনেন্টে কীভাবে ref তৈরি ও ব্যবহার করবেন?**  
উত্তর:
```jsx
import { useRef } from "react";
function MyComponent() {
  const myRef = useRef(0);
  // myRef.current দিয়ে মান পড়া বা লেখা যায়
}
```

**প্রশ্ন ৩: state ও ref-এর প্রধান পার্থক্য কী?**  
উত্তর: state আপডেট করলে কম্পোনেন্ট রি-রেন্ডার হয়, কিন্তু ref আপডেট করলে হয় না।

**প্রশ্ন ৪: কখন state-এর বদলে ref ব্যবহার করবেন?**  
উত্তর: যখন এমন মান রাখতে চান যা UI-তে দেখাতে হবে না বা রি-রেন্ডার ট্রিগার করার দরকার নেই (যেমন: timer ID, আগের মান, DOM এলিমেন্ট)।

**প্রশ্ন ৫: ref দিয়ে কীভাবে DOM এলিমেন্ট অ্যাক্সেস করবেন?**  
উত্তর: JSX-এ `ref` অ্যাট্রিবিউট দিয়ে ref অ্যাসাইন করুন:
```jsx
const inputRef = useRef();
return <input ref={inputRef} />;
```
তারপর DOM নোড অ্যাক্সেস করুন: `inputRef.current`

</div>

---

## ৪.২ – নিয়ম ও সতর্কতা ⚠️

<div style={{ background: 'rgba(230, 150, 70, 0.10)', padding: '15px', borderRadius: '5px', marginBottom: '20px', border: '1px solid rgba(230, 150, 70, 0.15)', color: 'inherit' }}>

**প্রশ্ন ৬: রেন্ডারিং-এর সময় ref আপডেট করলে কী হয়?**  
উত্তর: এটি অনির্দেশ্য আচরণ ঘটাতে পারে। শুধুমাত্র ইভেন্ট হ্যান্ডলার, ইফেক্ট বা কলব্যাকের মধ্যে ref আপডেট করা উচিত।

**প্রশ্ন ৭: UI-তে দেখানোর জন্য ref ব্যবহার করলে কী সমস্যা হয়?**  
উত্তর: UI আপডেট হবে না, কারণ ref পরিবর্তন রি-রেন্ডার ট্রিগার করে না। UI-তে দেখাতে চাইলে state ব্যবহার করুন।

**প্রশ্ন ৮: ref-এ যেকোনো টাইপের মান রাখা যায় কি?**  
উত্তর: হ্যাঁ! সংখ্যা, স্ট্রিং, অবজেক্ট, ফাংশন, এমনকি DOM নোডও রাখা যায়।

**প্রশ্ন ৯: useRef থেকে পাওয়া অবজেক্টের গঠন কেমন?**  
উত্তর: `{ current: initialValue }`

**প্রশ্ন ১০: ref দিয়ে কি মান রেন্ডারগুলোর মধ্যে ধরে রাখা যায়?**  
উত্তর: হ্যাঁ, ref কম্পোনেন্টের পুরো লাইফটাইমে মান ধরে রাখে।

</div>

---

## ৪.৩ – সিনারিও ও ব্যবহারিক উদাহরণ 🏆

<div style={{ background: 'rgba(20, 120, 220, 0.10)', padding: '20px', borderRadius: '5px', marginBottom: '20px', border: '1px solid rgba(20, 120, 220, 0.15)', color: 'inherit' }}>

### সিনারিও-ভিত্তিক প্রশ্ন:

**ধরা যাক, আপনি একটি stopwatch বানাতে চান যা start ও stop করা যায়, এবং timer ID সংরক্ষণ করতে হবে যাতে পরে clear করা যায়। timer ID-এর জন্য state নাকি ref ব্যবহার করবেন? কেন?**

<details>
<summary>**উত্তর দেখতে ক্লিক করুন**</summary>

**উত্তর:**  
এখানে ref ব্যবহার করা উচিত, কারণ timer ID UI-তে দেখাতে হয় না এবং রি-রেন্ডার দরকার নেই। state ব্যবহার করলে অপ্রয়োজনীয় রি-রেন্ডার হবে।

```jsx
const timerRef = useRef(null);
function startTimer() {
  timerRef.current = setInterval(...);
}
function stopTimer() {
  clearInterval(timerRef.current);
}
```

</details>

---

### সিনারিও-ভিত্তিক প্রশ্ন:

**কম্পোনেন্ট কতবার রেন্ডার হয়েছে তা গুনতে চান, কিন্তু প্রতিবার রি-রেন্ডার করতে চান না। কীভাবে ref দিয়ে করবেন?**

<details>
<summary>**উত্তর দেখতে ক্লিক করুন**</summary>

**উত্তর:**  
ref-এ render count রাখে কম্পোনেন্ট বডি বা ইফেক্টে বাড়াতে পারেন।

```jsx
const renderCount = useRef(0);
renderCount.current++;
console.log(`Rendered ${renderCount.current} times`);
```

</details>

---

### সিনারিও-ভিত্তিক প্রশ্ন:

**কীভাবে একটি বাটনে ক্লিক করলে ইনপুট ফিল্ডে প্রোগ্রামেটিক্যালি ফোকাস দিবেন?**

<details>
<summary>**উত্তর দেখতে ক্লিক করুন**</summary>

**উত্তর:**  
ref দিয়ে ইনপুট DOM নোড অ্যাক্সেস করে, বাটনের ক্লিক হ্যান্ডলারে `.focus()` কল করুন।

```jsx
const inputRef = useRef();
return (
  <>
    <input ref={inputRef} />
    <button onClick={() => inputRef.current.focus()}>Focus Input</button>
  </>
);
```

</details>

</div>

---

<Callout type="info">
  এই প্রশ্নগুলো রিভিউ করুন এবং নিজের ভাষায় ব্যাখ্যা দেয়ার চেষ্টা করুন—এটাই সেরা প্রস্তুতি!
</Callout>

---

## ৪.৪ – অ্যাডভান্সড ও ব্যবহারিক প্রশ্ন 🚀

<div style={{ background: 'rgba(100, 200, 100, 0.10)', padding: '15px', borderRadius: '5px', marginBottom: '20px', border: '1px solid rgba(100, 200, 100, 0.15)', color: 'inherit' }}>

**প্রশ্ন ১১: আগের prop বা state মান ref দিয়ে কীভাবে সংরক্ষণ করবেন?**  
উত্তর: `useEffect`-এর মধ্যে ref ব্যবহার করে আগের মান রাখা যায়:
```jsx
const prevValue = useRef();
useEffect(() => {
  prevValue.current = value;
}, [value]);
```

**প্রশ্ন ১২: `useRef` ও `createRef`-এর মধ্যে পার্থক্য কী?**  
উত্তর: `useRef` ফাংশনাল কম্পোনেন্টে ব্যবহার হয় এবং প্রতিবার একই ref অবজেক্ট রিটার্ন করে। `createRef` ক্লাস কম্পোনেন্টে ব্যবহার হয় এবং প্রতিবার নতুন ref দেয়।

**প্রশ্ন ১৩: React-এ debounce করার জন্য কীভাবে ref ব্যবহার করবেন?**  
উত্তর: timeout ID ref-এ রাখলে তা রেন্ডারগুলোর মধ্যে থাকবে এবং রি-রেন্ডার হবে না:
```jsx
const timeoutRef = useRef();
function handleChange(e) {
  clearTimeout(timeoutRef.current);
  timeoutRef.current = setTimeout(() => {
    // ডিবাউন্সের পর কিছু করুন
  }, 300);
}
```

**প্রশ্ন ১৪: কীভাবে ref চাইল্ড কম্পোনেন্টে পাঠাবেন?**  
উত্তর: `forwardRef` ব্যবহার করুন:
```jsx
const MyInput = React.forwardRef((props, ref) => (
  <input ref={ref} {...props} />
));
const inputRef = useRef();
<MyInput ref={inputRef} />;
```

**প্রশ্ন ১৫: `useImperativeHandle` কী এবং এটি ref-এর সাথে কীভাবে কাজ করে?**  
উত্তর: `useImperativeHandle` দিয়ে parent কম্পোনেন্টে ref-এর মাধ্যমে যেসব মেথড বা প্রপার্টি অ্যাক্সেস করা যাবে, তা কাস্টমাইজ করা যায়।

</div>

---

> **🚀 চূড়ান্ত পরামর্শ:** ইন্টারভিউতে শুধু রিফসের জ্ঞান নয়, পারফরম্যান্স, DOM অ্যাক্সেস, এবং অপ্রয়োজনীয় রি-রেন্ডার এড়াতে কীভাবে ব্যবহার করবেন—এটাও দেখান।

<img
  src="https://upload.wikimedia.org/wikipedia/commons/a/a7/React-icon.svg"
  alt="React Interview Success"
  width="100"
  style={{ borderRadius: "8px", marginTop: "2rem" }}
/>
