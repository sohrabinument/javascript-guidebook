import { Callout } from "nextra/components";

<img
  src="https://upload.wikimedia.org/wikipedia/commons/a/a7/React-icon.svg"
  alt="React Logo"
  width="70"
  style={{ marginBottom: "1rem" }}
/>

# React-এ Forwarding Refs – ইন্টারভিউ প্রশ্ন ও উত্তর

এই প্রশ্নগুলো ব্যবহার করুন ইন্টারভিউ প্রস্তুতির জন্য বা React-এ forwarding refs সম্পর্কে আপনার বোঝা যাচাই করতে।

> **💡 প্রো টিপ:** প্রতিটি প্রশ্নের উত্তর নিজে দেওয়ার চেষ্টা করুন, তারপর উত্তর দেখুন। এতে আপনি বিষয়গুলো আরও ভালোভাবে মনে রাখতে পারবেন!

---

## ৪.১ – মৌলিক বিষয়াবলী 🧩

<div style={{ background: 'rgba(0, 100, 200, 0.10)', padding: '15px', borderRadius: '5px', marginBottom: '20px', border: '1px solid rgba(0, 100, 200, 0.15)', color: 'inherit' }}>

**Q1: React-এ ref forwarding কী?**  
উত্তর: Ref forwarding একটি টেকনিক, যা আপনাকে একটি কম্পোনেন্টের ভেতর দিয়ে ref স্বয়ংক্রিয়ভাবে তার কোনো চাইল্ড (সাধারণত DOM node) এ পাঠাতে দেয়।

**Q2: কিভাবে একটি functional কম্পোনেন্টে ref forwarding সক্রিয় করবেন?**  
উত্তর: কম্পোনেন্টটিকে `React.forwardRef` দিয়ে wrap করে এবং কাঙ্ক্ষিত চাইল্ডে `ref` প্যারামিটার ব্যবহার করে।

**Q3: `React.forwardRef` কী রিটার্ন করে?**  
উত্তর: এটি এমন একটি React কম্পোনেন্ট রিটার্ন করে, যা `ref` prop নিতে পারে এবং সেটি চাইল্ডে ফরওয়ার্ড করতে পারে।

**Q4: একটি কম্পোনেন্ট লাইব্রেরিতে ref forwarding কেন ব্যবহার করবেন?**  
উত্তর: যাতে লাইব্রেরি ব্যবহারকারীরা ফোকাস, স্ক্রল বা থার্ড-পার্টি লাইব্রেরির সাথে ইন্টিগ্রেশনের জন্য DOM node-এ অ্যাক্সেস পেতে পারে।

**Q5: আপনি কি class কম্পোনেন্টে ref ফরওয়ার্ড করতে পারেন?**  
উত্তর: হ্যাঁ, তবে class কম্পোনেন্টকে ref সঠিকভাবে হ্যান্ডেল করতে হবে। সাধারণত, ref forwarding সবচেয়ে বেশি কাজে লাগে DOM node বা function কম্পোনেন্টের জন্য।

</div>

---

## ৪.২ – নিয়ম ও সতর্কতা ⚠️

<div style={{ background: 'rgba(230, 150, 70, 0.10)', padding: '15px', borderRadius: '5px', marginBottom: '20px', border: '1px solid rgba(230, 150, 70, 0.15)', color: 'inherit' }}>

**Q6: `forwardRef` ব্যবহার করার সময় সাধারণ ভুল কী?**  
উত্তর: চাইল্ড DOM node বা কম্পোনেন্টে `ref` অ্যাটাচ করতে ভুলে যাওয়া, ফলে `ref.current` হবে `null`।

**Q7: আপনি কি `forwardRef`-এর render ফাংশনের ভেতরে `useRef`-এর মতো হুক ব্যবহার করতে পারেন?**  
উত্তর: হ্যাঁ, তবে অবশ্যই হুকের নিয়ম মেনে চলতে হবে (লুপ বা কন্ডিশনের ভেতরে নয়, টপ-লেভেলে কল করতে হবে)।

**Q8: আপনি যদি সাধারণ ফাংশন কম্পোনেন্টে prop হিসেবে `ref` ব্যবহার করেন, কী হবে?**  
উত্তর: `ref` prop হবে `undefined`, কারণ শুধুমাত্র `forwardRef` দিয়ে তৈরি কম্পোনেন্টই `ref` আর্গুমেন্ট পায়।

**Q9: ফরওয়ার্ডেড ref-এ কোনো মেথড কল করার আগে কী চেক করবেন?**  
উত্তর: সবসময় চেক করুন `ref.current` null নয়, নাহলে রানটাইম এরর হতে পারে।

**Q10: ফরওয়ার্ডেড ref দিয়ে সরাসরি DOM node মিউটেট করলে কী ঝুঁকি আছে?**  
উত্তর: এতে React-এর virtual DOM ও real DOM-এর মধ্যে অসামঞ্জস্য তৈরি হতে পারে, ফলে বাগ বা ক্র্যাশ হতে পারে।

</div>

---

## ৪.৩ – সেরা চর্চা ও মধ্যবর্তী 💡

<div style={{ background: 'rgba(200, 200, 100, 0.10)', padding: '15px', borderRadius: '5px', marginBottom: '20px', border: '1px solid rgba(200, 200, 100, 0.15)', color: 'inherit' }}>

**Q11: কখন `forwardRef`-এর সাথে `useImperativeHandle` ব্যবহার করবেন?**  
উত্তর: যখন আপনি parent-এ raw DOM node-এর বদলে কাস্টম API (মেথড বা প্রপার্টি) এক্সপোজ করতে চান।

**Q12: কিভাবে একাধিক কম্পোনেন্টের মধ্য দিয়ে ref ফরওয়ার্ড করবেন?**  
উত্তর: চেইনের প্রতিটি কম্পোনেন্টে `forwardRef` ব্যবহার করতে হবে এবং ref পরবর্তী চাইল্ডে পাঠাতে হবে।

**Q13: আপনার কম্পোনেন্টের API-তে ref-এর আচরণ ডকুমেন্ট করা কেন জরুরি?**  
উত্তর: যাতে ব্যবহারকারীরা জানে ref কোন জিনিসকে পয়েন্ট করবে (DOM node, custom API ইত্যাদি) এবং কীভাবে নিরাপদে ব্যবহার করতে হবে।

**Q14: আপনি কি callback ref `forwardRef`-এর সাথে ব্যবহার করতে পারেন?**  
উত্তর: হ্যাঁ, আপনি callback ref পাঠাতে পারেন এমন কম্পোনেন্টে, যা `forwardRef` ব্যবহার করে।

**Q15: ফরওয়ার্ডিং ref-এর একটি বাস্তব উদাহরণ কী?**  
উত্তর: parent কম্পোনেন্ট থেকে কাস্টম input-এ ফোকাস দেয়া বা কাস্টম list কম্পোনেন্টকে স্ক্রলে নিয়ে আসা।

</div>

---

## ৪.৪ – সিনারিও ও ব্যবহারিক উদাহরণ 🏆

<div style={{ background: 'rgba(20, 120, 220, 0.10)', padding: '20px', borderRadius: '5px', marginBottom: '20px', border: '1px solid rgba(20, 120, 220, 0.15)', color: 'inherit' }}>

### সিনারিও-ভিত্তিক প্রশ্ন:

**আপনার একটি কাস্টম `TextInput` কম্পোনেন্ট আছে এবং parent চায় একটি বাটনে ক্লিক করলে ইনপুটটি ফোকাস হবে। ref forwarding দিয়ে কীভাবে করবেন?**

<details>
<summary>**উত্তর দেখতে ক্লিক করুন**</summary>

**উত্তর:**  
`TextInput`-এ `React.forwardRef` ব্যবহার করুন এবং `<input>`-এ ref অ্যাটাচ করুন। parent-এ `useRef` ব্যবহার করে বাটনে ক্লিক করলে `ref.current.focus()` কল করুন।

```jsx
const TextInput = React.forwardRef((props, ref) => <input ref={ref} {...props} />);

function Parent() {
  const inputRef = React.useRef(null);
  return (
    <>
      <TextInput ref={inputRef} />
      <button onClick={() => inputRef.current?.focus()}>Focus</button>
    </>
  );
}
```

</details>

---

### সিনারিও-ভিত্তিক প্রশ্ন:

**আপনি চান একটি চাইল্ড ইনপুট কম্পোনেন্ট থেকে parent-এ কাস্টম `clear()` মেথড এক্সপোজ করতে। `forwardRef` ও `useImperativeHandle` দিয়ে কীভাবে করবেন?**

<details>
<summary>**উত্তর দেখতে ক্লিক করুন**</summary>

**উত্তর:**  
চাইল্ডে `useImperativeHandle` ব্যবহার করে কাস্টম API এক্সপোজ করুন।

```jsx
const CustomInput = React.forwardRef((props, ref) => {
  const inputRef = React.useRef(null);
  React.useImperativeHandle(ref, () => ({
    clear: () => { inputRef.current.value = ""; },
    focus: () => { inputRef.current.focus(); }
  }));
  return <input ref={inputRef} {...props} />;
});

function Parent() {
  const customInputRef = React.useRef(null);
  return (
    <>
      <CustomInput ref={customInputRef} />
      <button onClick={() => customInputRef.current?.clear()}>Clear</button>
      <button onClick={() => customInputRef.current?.focus()}>Focus</button>
    </>
  );
}
```

</details>

</div>

---

## ৪.৫ – অ্যাডভান্সড ও ডিপ ডাইভ 🚀

<div style={{ background: 'rgba(100, 200, 100, 0.10)', padding: '15px', borderRadius: '5px', marginBottom: '20px', border: '1px solid rgba(100, 200, 100, 0.15)', color: 'inherit' }}>

**Q16: কিভাবে একটি higher-order component (HOC)-এর মধ্য দিয়ে ref ফরওয়ার্ড করবেন?**  
উত্তর: HOC-তে `React.forwardRef` ব্যবহার করে ref-টি wrapped কম্পোনেন্টে পাঠান।

**Q17: `forwardRef` দিয়ে তৈরি কম্পোনেন্টে display name সেট করার সুবিধা কী?**  
উত্তর: এতে ডিবাগিং সহজ হয় এবং React DevTools-এ কম্পোনেন্টটি সহজে চেনা যায়।

**Q18: কিভাবে ref forwarding ব্যবহার করা কম্পোনেন্ট টেস্ট করবেন?**  
উত্তর: টেস্ট ইউটিলিটি (যেমন React Testing Library) দিয়ে ref তৈরি করুন, কম্পোনেন্ট রেন্ডার করুন এবং ref-এর মান বা মেথডে অ্যাসার্ট করুন।

**Q19: থার্ড-পার্টি কম্পোনেন্টে ref ফরওয়ার্ড করার ঝুঁকি কী?**  
উত্তর: থার্ড-পার্টি কম্পোনেন্ট ref সঠিকভাবে হ্যান্ডেল নাও করতে পারে, তাই ডকুমেন্টেশন দেখে নিন।

**Q20: আপনি কি `forwardRef`-কে memoization (`React.memo`) এর সাথে ব্যবহার করতে পারেন?**  
উত্তর: হ্যাঁ! কম্পোনেন্টকে `React.memo(React.forwardRef(...))` দিয়ে wrap করলে দুই সুবিধাই পাবেন।

</div>

---

> **🚀 চূড়ান্ত পরামর্শ:** ইন্টারভিউতে শুধু ref forwarding-এর জ্ঞান নয়, কখন ও কেন ব্যবহার করবেন এবং সাধারণ ভুল এড়ানোর কৌশলও দেখান।

<img
  src="https://upload.wikimedia.org/wikipedia/commons/a/a7/React-icon.svg"
  alt="React Interview Success"
  width="100"
  style={{ borderRadius: "8px", marginTop: "2rem" }}
/>
