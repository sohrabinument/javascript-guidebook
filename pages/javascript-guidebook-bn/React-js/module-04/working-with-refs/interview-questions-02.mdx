import { Callout } from "nextra/components";

<img
  src="https://upload.wikimedia.org/wikipedia/commons/a/a7/React-icon.svg"
  alt="React Logo"
  width="70"
  style={{ marginBottom: "1rem" }}
/>

# রিফ দিয়ে DOM ম্যানিপুলেশন – ইন্টারভিউ প্রশ্ন ও উত্তর

এই প্রশ্নগুলো ব্যবহার করুন ইন্টারভিউ প্রস্তুতির জন্য বা React-এ রিফ দিয়ে DOM ম্যানিপুলেশন সম্পর্কে আপনার বোঝাপড়া যাচাই করতে।

> **💡 প্রো টিপ:** প্রতিটি প্রশ্নের উত্তর নিজে চেষ্টা করুন, তারপর উত্তর দেখুন। এতে আপনি আরও ভালোভাবে মনে রাখতে পারবেন!

---

## ৪.১ – মৌলিক বিষয়াবলী 🧩

<div style={{ background: 'rgba(0, 100, 200, 0.10)', padding: '15px', borderRadius: '5px', marginBottom: '20px', border: '1px solid rgba(0, 100, 200, 0.15)', color: 'inherit' }}>

**প্রশ্ন ১: React-এ DOM ম্যানিপুলেশনের জন্য রিফ ব্যবহারের মূল উদ্দেশ্য কী?**  
উত্তর: রিফ ব্যবহার করে আপনি সরাসরি DOM নোডে অ্যাক্সেস ও ইন্টারঅ্যাক্ট করতে পারেন, যেমন ফোকাস, স্ক্রল, বা মাপ নেওয়া—যা React ডিক্লেয়ারেটিভভাবে হ্যান্ডেল করে না।

**প্রশ্ন ২: React-এ কিভাবে একটি DOM এলিমেন্টে রিফ অ্যাটাচ করবেন?**  
উত্তর:
```jsx
const myRef = useRef(null);
return <div ref={myRef}>Hello</div>;
```

**প্রশ্ন ৩: রিফ ব্যবহার করে ইনপুট এলিমেন্টে ফোকাস কিভাবে দিবেন?**  
উত্তর:
```jsx
const inputRef = useRef(null);
<input ref={inputRef} />;
// ইভেন্ট হ্যান্ডলারে:
inputRef.current.focus();
```

**প্রশ্ন ৪: React রিফ অ্যাটাচ করার পর `myRef.current`-এ কী থাকে?**  
উত্তর: আসল DOM নোড (যেমন, HTML এলিমেন্ট)।

**প্রশ্ন ৫: রিফ আপডেট করলে কি রি-রেন্ডার হয়?**  
উত্তর: না, রিফ আপডেট করলে রি-রেন্ডার হয় না।

</div>

---

## ৪.১.২ – আরও মৌলিক প্রশ্ন 🧩

<div style={{ background: 'rgba(0, 100, 200, 0.08)', padding: '15px', borderRadius: '5px', marginBottom: '20px', border: '1px solid rgba(0, 100, 200, 0.10)', color: 'inherit' }}>

**প্রশ্ন ৬: `useRef()` দিয়ে তৈরি রিফের প্রাথমিক মান কী?**  
উত্তর: আপনি যা পাঠান সেটাই (সাধারণত DOM রিফের জন্য `null`)।

**প্রশ্ন ৭: রিফে কি DOM নোড ছাড়া অন্য মান রাখা যায়?**  
উত্তর: হ্যাঁ! যেকোনো mutable value (সংখ্যা, অবজেক্ট, ফাংশন ইত্যাদি) রাখা যায়।

**প্রশ্ন ৮: ফাংশন কম্পোনেন্টের রিফ ও ক্লাস কম্পোনেন্টের instance variable-এর মধ্যে পার্থক্য কী?**  
উত্তর: ফাংশন কম্পোনেন্টে (useRef দিয়ে) রিফ রেন্ডারগুলোর মধ্যে টিকে থাকে, যেমন ক্লাস কম্পোনেন্টে instance variable, তবে এটি this-এর সাথে বাঁধা নয়।

**প্রশ্ন ৯: কখন React DOM নোডের জন্য `ref.current` সেট করে?**  
উত্তর: কম্পোনেন্ট মাউন্ট হওয়ার পর এবং যখনই রিফ নতুন নোডে অ্যাটাচ হয় তখন।

**প্রশ্ন ১০: রিফ দিয়ে কি অ্যানিমেশন ট্রিগার করা যায়?**  
উত্তর: হ্যাঁ! রিফ দিয়ে DOM নোডে অ্যাক্সেস করে ব্রাউজার API বা লাইব্রেরি দিয়ে অ্যানিমেশন চালানো যায়।

</div>

---

## ৪.২ – নিয়ম ও সতর্কতা ⚠️

<div style={{ background: 'rgba(230, 150, 70, 0.10)', padding: '15px', borderRadius: '5px', marginBottom: '20px', border: '1px solid rgba(230, 150, 70, 0.15)', color: 'inherit' }}>

**প্রশ্ন ১১: রিফ দিয়ে DOM ম্যানিপুলেশনে সাধারণ ভুল কী?**  
উত্তর: রিফ দিয়ে সরাসরি DOM structure (children যোগ/বিয়োগ) পরিবর্তন করা, যা React-এর রেন্ডারিংয়ের সাথে কনফ্লিক্ট করে এবং বাগ তৈরি করতে পারে।

**প্রশ্ন ১২: লুপ বা কন্ডিশনের মধ্যে `useRef` ব্যবহার করা যাবে কি? কেন?**  
উত্তর: না, সব হুক কম্পোনেন্টের টপ-লেভেলে কল করতে হয়, যাতে রেন্ডারগুলোর মধ্যে হুকের অর্ডার ঠিক থাকে।

**প্রশ্ন ১৩: রিফে কোনো মেথড কল করার আগে (যেমন `ref.current.focus()`) কী চেক করা উচিত?**  
উত্তর: সবসময় চেক করুন `ref.current` null নয়, নইলে runtime error হতে পারে।

**প্রশ্ন ১৪: কখন রিফ দিয়ে DOM ম্যানিপুলেশন নিরাপদ?**  
উত্তর: যখন non-destructive action (ফোকাস, স্ক্রল, মাপ নেওয়া) করেন এবং React-এ ম্যানেজড DOM structure পরিবর্তন করেন না।

**প্রশ্ন ১৫: রিফ ব্যবহার করে সাইড ইফেক্ট (যেমন timer, listener) কিভাবে ক্লিন করবেন?**  
উত্তর: `useEffect`-এর cleanup ফাংশনে listener remove বা timer clear করুন।

</div>

---

## ৪.২.২ – ইন্টারমিডিয়েট ও বেস্ট প্র্যাকটিস 💡

<div style={{ background: 'rgba(200, 200, 100, 0.10)', padding: '15px', borderRadius: '5px', marginBottom: '20px', border: '1px solid rgba(200, 200, 100, 0.15)', color: 'inherit' }}>

**প্রশ্ন ১৬: কেন এমন মানের জন্য রিফ ব্যবহার করা উচিত নয় যা UI-তে দেখাতে হবে?**  
উত্তর: কারণ রিফ আপডেট করলে রি-রেন্ডার হয় না, তাই UI আপডেট হবে না।

**প্রশ্ন ১৭: কিভাবে নিশ্চিত করবেন রিফ অ্যাটাচ হয়েছে, ইফেক্টে ব্যবহারের আগে?**  
উত্তর: `useEffect` ব্যবহার করুন, কারণ এটি DOM আপডেট ও রিফ অ্যাটাচ হওয়ার পর চলে।

**প্রশ্ন ১৮: রিফ অবজেক্ট ও রিফ কলব্যাকের পার্থক্য কী?**  
উত্তর: রিফ অবজেক্ট `useRef` দিয়ে তৈরি হয় এবং `.current` property থাকে। রিফ কলব্যাক একটি ফাংশন, যা ডাইনামিক লিস্ট বা কাস্টম লজিকের জন্য উপযোগী।

**প্রশ্ন ১৯: রিফ দিয়ে কি থার্ড-পার্টি লাইব্রেরি ইন্টিগ্রেট করা যায়?**  
উত্তর: হ্যাঁ! D3, Chart.js, jQuery plugin ইত্যাদিতে DOM node পাঠাতে রিফ ব্যবহার হয়।

**প্রশ্ন ২০: রিফ দিয়ে সরাসরি DOM নোড mutate করলে কী ঝুঁকি আছে?**  
উত্তর: এতে React-এর virtual DOM ও real DOM-এর মধ্যে অসামঞ্জস্য হতে পারে, ফলে বাগ বা ক্র্যাশ হতে পারে।

</div>

---

## ৪.৩ – সিনারিও ও ব্যবহারিক উদাহরণ 🏆

<div style={{ background: 'rgba(20, 120, 220, 0.10)', padding: '20px', borderRadius: '5px', marginBottom: '20px', border: '1px solid rgba(20, 120, 220, 0.15)', color: 'inherit' }}>

### সিনারিও-ভিত্তিক প্রশ্ন:

**আপনার কাছে অনেকগুলো আইটেমের একটি লিস্ট আছে এবং একটি বাটনে ক্লিক করলে নির্দিষ্ট আইটেমে স্ক্রল করতে চান। রিফ দিয়ে কীভাবে করবেন?**

<details>
<summary>**উত্তর দেখতে ক্লিক করুন**</summary>

**উত্তর:**  
প্রতিটি আইটেমের জন্য রিফ তৈরি করুন (বা ডাইনামিক লিস্টের জন্য রিফ কলব্যাক ব্যবহার করুন), এবং বাটনে ক্লিক করলে কাঙ্ক্ষিত আইটেমের রিফে `ref.current.scrollIntoView({ behavior: 'smooth' })` কল করুন।

```jsx
const itemRefs = useRef({});
function scrollToItem(id) {
  itemRefs.current[id]?.scrollIntoView({ behavior: 'smooth' });
}
```

</details>

---

### সিনারিও-ভিত্তিক প্রশ্ন:

**React-এ রিফ ব্যবহার করে কিভাবে DOM এলিমেন্টের উচ্চতা মাপবেন?**

<details>
<summary>**উত্তর দেখতে ক্লিক করুন**</summary>

**উত্তর:**  
এলিমেন্টে রিফ অ্যাটাচ করুন, তারপর কম্পোনেন্ট মাউন্ট বা আপডেট হলে `ref.current.offsetHeight` পড়ুন।

```jsx
const divRef = useRef(null);
useEffect(() => {
  if (divRef.current) {
    console.log(divRef.current.offsetHeight);
  }
}, []);
```

</details>

---

### সিনারিও-ভিত্তিক প্রশ্ন:

**আপনি চান, একটি modal ওপেন হলে ইনপুট ফিল্ডে ফোকাস চলে আসুক। রিফ দিয়ে কীভাবে করবেন?**

<details>
<summary>**উত্তর দেখতে ক্লিক করুন**</summary>

**উত্তর:**  
ইনপুটে রিফ অ্যাটাচ করুন এবং modal ওপেন হলে চলা `useEffect`-এ `inputRef.current.focus()` কল করুন।

```jsx
const inputRef = useRef(null);
useEffect(() => {
  if (modalOpen) {
    inputRef.current?.focus();
  }
}, [modalOpen]);
```

</details>

</div>

---

## ৪.৩.২ – আরও সিনারিও ও ব্যবহারিক প্রশ্ন 🏆

<div style={{ background: 'rgba(20, 120, 220, 0.07)', padding: '20px', borderRadius: '5px', marginBottom: '20px', border: '1px solid rgba(20, 120, 220, 0.10)', color: 'inherit' }}>

### সিনারিও-ভিত্তিক প্রশ্ন:

**হোভার করলে একটি কাস্টম টুলটিপ নির্দিষ্ট এলিমেন্টের পাশে দেখাতে চান। রিফ কীভাবে সাহায্য করতে পারে?**

<details>
<summary>**উত্তর দেখতে ক্লিক করুন**</summary>

**উত্তর:**  
টার্গেট এলিমেন্টে রিফ অ্যাটাচ করুন এবং তার পজিশন (যেমন, `getBoundingClientRect()`) ব্যবহার করে টুলটিপ পজিশন করুন।

```jsx
const targetRef = useRef(null);
function showTooltip() {
  const rect = targetRef.current.getBoundingClientRect();
  // rect দিয়ে tooltip পজিশন করুন
}
```

</details>

---

### সিনারিও-ভিত্তিক প্রশ্ন:

**আপনার কাছে চ্যাট মেসেজের একটি লিস্ট আছে এবং নতুন মেসেজ এলে সর্বশেষ মেসেজে স্ক্রল করতে চান। রিফ দিয়ে কীভাবে করবেন?**

<details>
<summary>**উত্তর দেখতে ক্লিক করুন**</summary>

**উত্তর:**  
শেষ মেসেজ এলিমেন্টে রিফ অ্যাটাচ করুন এবং নতুন মেসেজ এলে `ref.current.scrollIntoView({ behavior: 'smooth' })` কল করুন।

```jsx
const lastMsgRef = useRef(null);
useEffect(() => {
  lastMsgRef.current?.scrollIntoView({ behavior: 'smooth' });
}, [messages]);
```

</details>

---

### সিনারিও-ভিত্তিক প্রশ্ন:

**আপনি চান, সার্চ ইনপুটে ইউজার টাইপ করা বন্ধ করলে ৫০০ms পরে সার্চ ট্রিগার হোক (debounce)। রিফ কীভাবে সাহায্য করতে পারে?**

<details>
<summary>**উত্তর দেখতে ক্লিক করুন**</summary>

**উত্তর:**  
timeout ID রিফে রাখুন, যাতে তা রেন্ডারগুলোর মধ্যে থাকে এবং রি-রেন্ডার না হয়।

```jsx
const timeoutRef = useRef();
function handleChange(e) {
  clearTimeout(timeoutRef.current);
  timeoutRef.current = setTimeout(() => {
    // সার্চ ট্রিগার করুন
  }, 500);
}
```

</details>

</div>

---

## ৪.৪ – অ্যাডভান্সড ও ব্যবহারিক প্রশ্ন 🚀

<div style={{ background: 'rgba(100, 200, 100, 0.10)', padding: '15px', borderRadius: '5px', marginBottom: '20px', border: '1px solid rgba(100, 200, 100, 0.15)', color: 'inherit' }}>

**প্রশ্ন ২১: React-এ ডাইনামিক লিস্টের জন্য কীভাবে রিফ ম্যানেজ করবেন?**  
উত্তর: একটি রিফ অবজেক্ট (যেমন, `useRef({})`) ব্যবহার করুন এবং কলব্যাক দিয়ে অ্যাসাইন করুন: `ref={el => refObj.current[id] = el}`।

**প্রশ্ন ২২: রিফ কলব্যাক কী এবং কখন ব্যবহার করবেন?**  
উত্তর: রিফ কলব্যাক একটি ফাংশন, যা `ref` অ্যাট্রিবিউটে পাঠানো হয়। React এটি DOM নোড অ্যাটাচ হলে কল করে এবং ডিটাচ হলে null পাঠায়। ডাইনামিক লিস্টে ব্যবহৃত হয়।

**প্রশ্ন ২৩: কীভাবে চাইল্ড কম্পোনেন্টে রিফ ফরওয়ার্ড করবেন?**  
উত্তর: `React.forwardRef` ব্যবহার করুন, যাতে parent থেকে child-এ রিফ পাঠানো যায়।

```jsx
const MyInput = React.forwardRef((props, ref) => <input ref={ref} {...props} />);
```

**প্রশ্ন ২৪: রিফ দিয়ে DOM ম্যানিপুলেশনের বাস্তব উদাহরণ কী?**  
উত্তর: অ্যাক্সেসিবিলিটির জন্য ফোকাস ম্যানেজ করা, error message-এ স্ক্রল, অ্যানিমেশনের জন্য মাপ নেওয়া, বা থার্ড-পার্টি লাইব্রেরি ইন্টিগ্রেট করা।

**প্রশ্ন ২৫: DOM ম্যানিপুলেশনে রিফ ব্যবহার করার সময় কী এড়ানো উচিত?**  
উত্তর: সরাসরি DOM structure (children যোগ/বিয়োগ) পরিবর্তন এড়ান, এতে React-এর রেন্ডারিং লজিক নষ্ট হতে পারে।

</div>

---

## ৪.৫ – অ্যাডভান্সড ও ডিপ ডাইভ 🚀

<div style={{ background: 'rgba(100, 100, 200, 0.10)', padding: '15px', borderRadius: '5px', marginBottom: '20px', border: '1px solid rgba(100, 100, 200, 0.15)', color: 'inherit' }}>

**প্রশ্ন ২৬: ড্র্যাগ-এন্ড-ড্রপ ফাংশনালিটি ইমপ্লিমেন্ট করতে রিফ কীভাবে ব্যবহার করবেন?**  
উত্তর: ড্র্যাগযোগ্য এলিমেন্টে রিফ অ্যাটাচ করুন এবং DOM API (যেমন, `getBoundingClientRect`) দিয়ে পজিশন ট্র্যাক ও ইভেন্ট হ্যান্ডল করুন।

**প্রশ্ন ২৭: অ্যাডভান্সড রিফ ব্যবহারে `useImperativeHandle`-এর ভূমিকা কী?**  
উত্তর: `forwardRef`-এর সাথে parent-এ কাস্টম মেথড বা প্রপার্টি এক্সপোজ করতে এটি ব্যবহার হয়।

**প্রশ্ন ২৮: দুটি এলিমেন্টের স্ক্রল পজিশন সিঙ্ক করতে রিফ কীভাবে ব্যবহার করবেন?**  
উত্তর: উভয় এলিমেন্টে রিফ অ্যাটাচ করুন এবং একটির স্ক্রল ইভেন্টে অন্যটির স্ক্রল পজিশন আপডেট করুন।

**প্রশ্ন ২৯: পারফরম্যান্স (যেমন, render count, সময়) মাপতে রিফ ব্যবহার করা যায় কি?**  
উত্তর: হ্যাঁ! রিফে কাউন্টার বা টাইমস্ট্যাম্প রেখে রেন্ডার ছাড়াই মান রাখা যায়।

**প্রশ্ন ৩০: কীভাবে parent থেকে চাইল্ড কম্পোনেন্টের DOM নোডে রিফ দিয়ে অ্যাক্সেস করবেন?**  
উত্তর: চাইল্ডে `forwardRef` ব্যবহার করুন এবং parent-এ রিফ অ্যাটাচ করুন; parent তখন চাইল্ডের DOM নোডে রিফ দিয়ে অ্যাক্সেস করতে পারবে।

</div>

---

> **🚀 চূড়ান্ত পরামর্শ:** ইন্টারভিউতে শুধু রিফ ও DOM ম্যানিপুলেশনের জ্ঞান নয়, কখন ও কেন ব্যবহার করবেন এবং সাধারণ ভুল এড়াতে জানাটাও দেখান।

<img
  src="https://upload.wikimedia.org/wikipedia/commons/a/a7/React-icon.svg"
  alt="React Interview Success"
  width="100"
  style={{ borderRadius: "8px", marginTop: "2rem" }}
/>
