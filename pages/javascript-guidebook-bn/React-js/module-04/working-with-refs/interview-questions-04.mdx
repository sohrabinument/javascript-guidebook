import { Callout } from "nextra/components";

<img
  src="https://upload.wikimedia.org/wikipedia/commons/a/a7/React-icon.svg"
  alt="React Logo"
  width="70"
  style={{ marginBottom: "1rem" }}
/>

# সিঙ্ক্রোনাস স্টেট আপডেট ফ্লাশিং – ইন্টারভিউ প্রশ্ন ও উত্তর

`flushSync` দিয়ে React-এ সিঙ্ক্রোনাস স্টেট আপডেট ফ্লাশিং সম্পর্কে ইন্টারভিউ প্রস্তুতির জন্য বা আপনার বোধগম্যতা পরীক্ষা করার জন্য এই প্রশ্নগুলি ব্যবহার করুন।

> **💡 প্রো টিপ:** উত্তর দেখার আগে প্রতিটি প্রশ্ন নিজে উত্তর করার চেষ্টা করুন। এটি ধারণাগুলি আরও ভালভাবে মনে রাখতে সাহায্য করবে!

---

## ৪.১ – মৌলিক বিষয়সমূহ ⚡

<div style={{ background: 'rgba(0, 100, 200, 0.10)', padding: '15px', borderRadius: '5px', marginBottom: '20px', border: '1px solid rgba(0, 100, 200, 0.15)', color: 'inherit' }}>

**প্রশ্ন ১: React-এ `flushSync` কি করে?**  
উত্তর: এটি React-কে যেকোনো পেন্ডিং স্টেট আপডেট ফ্লাশ করতে এবং DOM-কে সিঙ্ক্রোনাসভাবে আপডেট করতে বাধ্য করে, অ্যাসিঙ্ক্রোনাসভাবে আপডেট ব্যাচ করার পরিবর্তে।

**প্রশ্ন ২: কখন `flushSync` ব্যবহার করার প্রয়োজন হতে পারে?**  
উত্তর: যখন আপনার DOM-কে অবিলম্বে স্টেট পরিবর্তন প্রতিফলিত করতে হয়, যেমন নতুন আইটেমে স্ক্রল করার জন্য, লেআউট পরিমাপ করার জন্য, বা স্টেট আপডেটের পরপরই অ্যানিমেশন সমন্বয় করার জন্য।

**প্রশ্ন ৩: React কম্পোনেন্টে `flushSync` কিভাবে ব্যবহার করবেন?**  
উত্তর: `react-dom` থেকে এটি ইম্পোর্ট করুন এবং আপনার স্টেট আপডেট(গুলি) `flushSync(() => { ... })` কলের মধ্যে রাখুন।

**প্রশ্ন ৪: `flushSync` অতিরিক্ত ব্যবহারের ঝুঁকি কি?**  
উত্তর: অতিরিক্ত ব্যবহার React-এর ব্যাচিং অপ্টিমাইজেশন ভেঙে ফেলে পারফরম্যান্স ক্ষতিগ্রস্ত করতে পারে, যার ফলে আরও ঘন ঘন এবং ব্যয়বহুল DOM আপডেট হতে পারে।

</div>

---

## ৪.২ – পরিস্থিতি এবং ব্যবহারিক উদাহরণ 🛠️

<div style={{ background: 'rgba(200, 200, 100, 0.10)', padding: '15px', borderRadius: '5px', marginBottom: '20px', border: '1px solid rgba(200, 200, 100, 0.15)', color: 'inherit' }}>

### পরিস্থিতি-ভিত্তিক প্রশ্ন:

**আপনার একটি টুডু লিস্ট আছে এবং নতুন আইটেম যোগ করার পরপরই সেখানে স্ক্রল করতে চান। কিভাবে নিশ্চিত করবেন যে DOM আপডেট হওয়ার পর স্ক্রল হবে?**

<details>
<summary>**উত্তর দেখতে ক্লিক করুন**</summary>

**নমুনা উত্তর:**  
স্টেট আপডেট `flushSync`-এ মুড়ে রাখুন যাতে `scrollIntoView()` কল করার আগে DOM আপডেট হয়:

```jsx
import { flushSync } from "react-dom";

function handleAdd() {
  flushSync(() => {
    setTodos([...todos, newTodo]);
  });
  listRef.current.lastChild.scrollIntoView({ behavior: "smooth" });
}
```

</details>

**প্রশ্ন ৫: একাধিক স্টেট আপডেটের সাথে `flushSync` ব্যবহার করতে পারেন?**  
উত্তর: হ্যাঁ! আপনি একাধিক আপডেট একটি `flushSync` কলের মধ্যে ব্যাচ করতে পারেন যাতে DOM পড়া বা ম্যানিপুলেট করার আগে সবগুলি প্রয়োগ করা হয়।

**প্রশ্ন ৬: `flushSync` ব্যবহার করার সময় একটি সাধারণ ভুল কি?**  
উত্তর: স্টেট আপডেট ফ্লাশ হওয়ার আগে DOM মেথড (যেমন `scrollIntoView`) কল করা, বা যখন সাধারণ ব্যাচিং যথেষ্ট তখন অপ্রয়োজনীয়ভাবে `flushSync` ব্যবহার করা।

</div>

---

## ৪.৩ – সেরা অনুশীলন এবং সমস্যা 🚩

<div style={{ background: 'rgba(230, 150, 70, 0.10)', padding: '15px', borderRadius: '5px', marginBottom: '20px', border: '1px solid rgba(230, 150, 70, 0.15)', color: 'inherit' }}>

**প্রশ্ন ৭: কখন `flushSync` ব্যবহার করা এড়ানো উচিত?**  
উত্তর: নিয়মিত স্টেট আপডেট বা প্রতিটি ইভেন্ট হ্যান্ডলারে এড়িয়ে চলুন—পারফরম্যান্সের জন্য React-এর ডিফল্ট ব্যাচিং পছন্দ করুন, যদি না আপনার সিঙ্ক্রোনাস DOM আপডেটের বিশেষ প্রয়োজন থাকে।

**প্রশ্ন ৮: `flushSync` ছাড়া স্টেট আপডেটের পর সরাসরি DOM ম্যানিপুলেট করলে কি হতে পারে?**  
উত্তর: DOM সর্বশেষ স্টেট প্রতিফলিত নাও করতে পারে, যার ফলে ভুল এলিমেন্টে স্ক্রল করা বা পুরানো লেআউট পরিমাপ করা ইত্যাদি বাগ দেখা দিতে পারে।

**প্রশ্ন ৯: DOM-এর সাথে সিঙ্ক্রোনাইজ করার জন্য `flushSync` `useLayoutEffect`-এর সাথে কিভাবে তুলনা করে?**  
উত্তর: `flushSync` স্টেট পরিবর্তনের পরপরই DOM আপডেট করে, অন্যদিকে `useLayoutEffect` DOM আপডেট হওয়ার পর চলে কিন্তু ইভেন্টের সাপেক্ষে এখনও অ্যাসিঙ্ক্রোনাস থাকে।

</div>

---

> **🚀 চূড়ান্ত পরামর্শ:** শুধুমাত্র যখন অবিলম্বে DOM আপডেট প্রয়োজন তখনই `flushSync` ব্যবহার করুন। বেশিরভাগ ক্ষেত্রে, React-এর ব্যাচিং আরও দক্ষ এবং কম ত্রুটিপূর্ণ!

<img
  src="https://upload.wikimedia.org/wikipedia/commons/a/a7/React-icon.svg"
  alt="React Interview Success"
  width="100"
  style={{ borderRadius: "8px", marginTop: "2rem" }}
/>
