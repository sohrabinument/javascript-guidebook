import { Callout } from "nextra/components";

<img src="https://upload.wikimedia.org/wikipedia/commons/a/a7/React-icon.svg" alt="React Logo" width="70" style={{marginBottom: '1rem'}} />

# রিডিউসার-এ স্টেট লজিক এক্সট্রাক্ট করা – ইন্টারভিউ প্রশ্ন ও উত্তর 🧠🔄

এই প্রশ্নগুলো ব্যবহার করুন ইন্টারভিউ প্রস্তুতির জন্য বা React-এ রিডিউসার ও স্টেট ম্যানেজমেন্ট সম্পর্কে আপনার বোঝা যাচাই করতে।

> **💡 প্রো টিপ:** প্রতিটি প্রশ্নের উত্তর নিজে দেওয়ার চেষ্টা করুন, তারপর উত্তর দেখুন। এতে আপনি আরও ভালোভাবে মনে রাখতে পারবেন!

---

## ১.১ – বিগিনার প্রশ্ন 🌱

<div style={{ background: 'rgba(0, 200, 100, 0.10)', padding: '15px', borderRadius: '5px', marginBottom: '20px', border: '1px solid rgba(0, 200, 100, 0.15)', color: 'inherit' }}>

**Q1: React-এ রিডিউসার কী এবং কেন এটি ব্যবহার করবেন?**  
উত্তর: রিডিউসার হলো একটি ফাংশন, যা বর্তমান state এবং একটি action নিয়ে নতুন state রিটার্ন করে। যখন একাধিক স্টেট আপডেট একে অপরের সাথে সম্পর্কিত বা নির্ভরশীল হয়, তখন জটিল স্টেট লজিককে কেন্দ্রীভূত ও সংগঠিত করতে এটি ব্যবহার করা হয়।

**Q2: `useReducer`-এ ডিফল্ট ভ্যালু হিসেবে কী পাস করেন?**  
উত্তর: আপনার রিডিউসারের জন্য প্রাথমিক state (যেমন, খালি অ্যারে, অবজেক্ট, বা নাম্বার)।

**Q3: ক্লাস কম্পোনেন্টে `useReducer` ব্যবহার করা যায় কি?**  
উত্তর: না, `useReducer` একটি React Hook, এটি শুধুমাত্র ফাংশন কম্পোনেন্টে কাজ করে।

**Q4: action অবজেক্টের `type` প্রপার্টি কী বোঝায়?**  
উত্তর: এটি বলে দেয়, কী ধরনের আপডেট হবে (যেমন, 'add', 'remove', 'toggle')।

**Q5: রিডিউসার ফাংশনের আর্গুমেন্ট কী কী?**  
উত্তর: বর্তমান state এবং একটি action অবজেক্ট।

**Q6: রিডিউসারে action বলতে কী বোঝায়?**  
উত্তর: action হলো একটি অবজেক্ট, যা কী ঘটেছে তা বর্ণনা করে (সাধারণত একটি `type` প্রপার্টি ও প্রয়োজনীয় ডেটা থাকে)।

**Q7: `dispatch` ফাংশন কী করে?**  
উত্তর: এটি একটি action রিডিউসারে পাঠায়, যা নতুন state হিসাব করে রিটার্ন করে।

**Q8: যদি রিডিউসারে নতুন state রিটার্ন করতে ভুলে যান, কী হবে?**  
উত্তর: state হবে `undefined`, যা অ্যাপ ভেঙে দিতে পারে।

**Q9: action ছাড়া `useReducer` ব্যবহার করা যায় কি?**  
উত্তর: না, action ছাড়া রিডিউসার জানবে না কী করতে হবে।

**Q10: রিডিউসার কেন pure function হতে হবে?**  
উত্তর: pure function সবসময় একই ইনপুটে একই আউটপুট দেয় এবং কোনো সাইড ইফেক্ট থাকে না। এতে state আপডেট পূর্বানুমানযোগ্য ও ডিবাগ করা সহজ হয়।

</div>

---

## ১.২ – প্র্যাকটিক্যাল ও কোড-ভিত্তিক প্রশ্ন 🧩

<div style={{ background: 'rgba(200, 200, 0, 0.10)', padding: '15px', borderRadius: '5px', marginBottom: '20px', border: '1px solid rgba(200, 200, 0, 0.15)', color: 'inherit' }}>

**Q11: একটি কাউন্টার রিডিউসার লিখুন, যা increment, decrement ও reset করতে পারে।**
<details>
<summary>**উত্তর দেখতে ক্লিক করুন**</summary>

```js
function counterReducer(state, action) {
  switch (action.type) {
    case 'increment':
      return state + 1;
    case 'decrement':
      return state - 1;
    case 'reset':
      return 0;
    default:
      throw new Error('Unknown action: ' + action.type);
  }
}
```

</details>

---

**Q12: যদি রিডিউসারের ভিতরে state ডাইরেক্টলি মিউটেট করেন, কী হবে?**
<details>
<summary>**উত্তর দেখতে ক্লিক করুন**</summary>

উত্তর:
- state ডাইরেক্টলি মিউটেট করলে বাগ ও অপ্রত্যাশিত UI আপডেট হতে পারে। React চায় আপনি নতুন state অবজেক্ট/অ্যারে রিটার্ন করুন, পুরনোটা পরিবর্তন না করে।

```js
// ভুল: state ডাইরেক্টলি মিউটেট করা
function badReducer(state, action) {
  if (action.type === 'add') {
    state.push(action.item); // ❌
    return state;
  }
  return state;
}

// সঠিক: নতুন state রিটার্ন করা
function goodReducer(state, action) {
  if (action.type === 'add') {
    return [...state, action.item]; // ✅
  }
  return state;
}
```

</details>

---

**Q13: কীভাবে রিডিউসারে সব state initial value-তে রিসেট করবেন?**
<details>
<summary>**উত্তর দেখতে ক্লিক করুন**</summary>

উত্তর:
- 'reset' action হ্যান্ডেল করুন, যা initial state রিটার্ন করবে।

```js
function reducer(state, action) {
  switch (action.type) {
    case 'reset':
      return initialState;
    // ...other cases
    default:
      return state;
  }
}
```

</details>

---

**Q14: অনেক ফিল্ড ও জটিল validation-সহ ফর্ম কীভাবে রিডিউসার দিয়ে ম্যানেজ করবেন?**
<details>
<summary>**উত্তর দেখতে ক্লিক করুন**</summary>

উত্তর:
- ফর্ম state অবজেক্ট হিসেবে রিডিউসারে রাখুন, action দিয়ে ফিল্ড আপডেট, validation ও reset হ্যান্ডেল করুন। এতে জটিল লজিকও সহজে ম্যানেজ করা যায়।

```js
function formReducer(state, action) {
  switch (action.type) {
    case 'update_field':
      return { ...state, [action.field]: action.value };
    case 'reset':
      return initialState;
    // ...validation logic
    default:
      return state;
  }
}
```

</details>

---

**Q15: সাইড ইফেক্ট (যেমন API কল) কীভাবে হ্যান্ডেল করবেন?**  
উত্তর: রিডিউসারের বাইরে (যেমন event handler বা useEffect-এ) সাইড ইফেক্ট করুন, কাজ শেষ হলে action dispatch করুন।

**Q16: বড় রিডিউসারে অনেক action কীভাবে সংগঠিত করবেন?**  
উত্তর: switch স্টেটমেন্ট বা action type-এর map ব্যবহার করুন। খুব বড় হলে ছোট ফাংশনে ভাগ করুন বা কম্বাইনিং ইউটিলিটি ব্যবহার করুন।

**Q17: action type-এর জন্য কনস্ট্যান্ট ব্যবহার করলে কী সুবিধা?**  
উত্তর: টাইপো এড়ানো যায়, বড় কোডবেসে refactor সহজ হয়।

**Q18: context-এর সাথে রিডিউসার ব্যবহার করা যায়? কেন?**  
উত্তর: হ্যাঁ! Context দিয়ে state ও dispatch অনেক কম্পোনেন্টে শেয়ার করা যায় (mini-Redux-এর মতো)।

</div>

---

## ১.৩ – ইন্টারমিডিয়েট ও অ্যাডভান্সড প্রশ্ন 🧠

<div style={{ background: 'rgba(200, 0, 100, 0.10)', padding: '15px', borderRadius: '5px', marginBottom: '20px', border: '1px solid rgba(200, 0, 100, 0.15)', color: 'inherit' }}>

**Q19: Optimistic UI আপডেট কীভাবে করবেন?**  
উত্তর: রিডিউসারে UI সঙ্গে সঙ্গে আপডেট করুন, পরে async রেজাল্ট অনুযায়ী success/failure action হ্যান্ডেল করুন।

**Q20: localStorage-এ রিডিউসার state কীভাবে রাখবেন?**  
উত্তর: useEffect দিয়ে state পরিবর্তন হলে localStorage-এ সেভ করুন, এবং initial state হিসেবে সেভ করা ডেটা ব্যবহার করুন।

**Q21: একাধিক রিডিউসার কীভাবে কম্বাইন করবেন?**  
উত্তর: আলাদা ফাংশনে state slices-এর জন্য রিডিউসার লিখুন, parent রিডিউসারে কম্বাইন করুন বা একাধিক useReducer ব্যবহার করুন।

**Q22: সব state-এ রিডিউসার ব্যবহার করলে trade-off কী?**  
উত্তর: জটিল state-এ structure ও clarity বাড়ে, কিন্তু সহজ state-এ বাড়তি কোড ও boilerplate হয়।

**Q23: asynchronous action-এর জন্য রিডিউসার কীভাবে টেস্ট করবেন?**  
উত্তর: রিডিউসার pure function হিসেবে sync action দিয়ে টেস্ট করুন; async লজিক আলাদাভাবে টেস্ট করুন (যেমন thunk বা effect-এ)।

**Q24: রিডিউসারে সাধারণ ভুল কী কী?**  
উত্তর: state মিউটেট করা, রিডিউসারে সাইড ইফেক্ট রাখা, বা অজানা action হ্যান্ডেল না করা।

**Q25: রিডিউসারে async কোড (যেমন fetch) ব্যবহার করা যায়? কেন?**  
উত্তর: না। রিডিউসার pure ও synchronous হতে হবে। async কাজ বাইরে করে, শেষে action dispatch করুন।

**Q26: রিডিউসার ফাংশন কীভাবে টেস্ট করবেন?**  
উত্তর: বিভিন্ন state ও action দিয়ে কল করে রিটার্ন ভ্যালু চেক করুন।

**Q27: বড় অ্যাপে রিডিউসার-ভিত্তিক state লজিক কীভাবে debug করবেন?**  
উত্তর: রিডিউসারে লগিং, React DevTools, বা কাস্টম middleware ব্যবহার করুন।

**Q28: useState থেকে useReducer-এ কীভাবে মাইগ্রেট করবেন?**  
উত্তর: সব state ও আপডেট লজিক চিহ্নিত করুন, রিডিউসার ফাংশনে লিখুন, useState-এর জায়গায় useReducer ব্যবহার করুন, event handler-এ action dispatch করুন।

**Q29: কখন useReducer বেছে নেবেন?**  
উত্তর: যখন state লজিক জটিল, একাধিক সাব-ভ্যালু আছে, বা কেন্দ্রীভূত আপডেট দরকার।

**Q30: এক কম্পোনেন্টে একাধিক রিডিউসার ব্যবহার করা যায়?**  
উত্তর: হ্যাঁ! আলাদা state-এর জন্য একাধিক useReducer বা কম্বাইন করা রিডিউসার ব্যবহার করুন।

**Q31: Immer কী, এবং রিডিউসারে কীভাবে সাহায্য করে?**
<details>
<summary>**উত্তর দেখতে ক্লিক করুন**</summary>

উত্তর:
- Immer দিয়ে রিডিউসার এমনভাবে লেখা যায় যেন state ডাইরেক্টলি মিউটেট করছেন, কিন্তু ভিতরে ভিতরে নতুন state তৈরি হয়। এতে জটিল আপডেটও সহজ হয়।

```js
import { useImmerReducer } from 'use-immer';

function tasksReducer(draft, action) {
  switch (action.type) {
    case 'added':
      draft.push({ id: action.id, text: action.text, done: false });
      break;
    case 'toggled':
      const task = draft.find(t => t.id === action.id);
      if (task) task.done = !task.done;
      break;
    case 'deleted':
      return draft.filter(t => t.id !== action.id);
    default:
      throw new Error('Unknown action: ' + action.type);
  }
}
```

</details>

</div>

---

## ১.৪ – সিনারিও ও বাস্তব উদাহরণভিত্তিক প্রশ্ন 🌍

<div style={{ background: 'rgba(0, 150, 200, 0.10)', padding: '15px', borderRadius: '5px', marginBottom: '20px', border: '1px solid rgba(0, 150, 200, 0.15)', color: 'inherit' }}>

**Q32: সিনারিও – টু-ডু লিস্ট রিফ্যাক্টর:**
কেন আপনি useState থেকে useReducer-এ রিফ্যাক্টর করবেন?
<details>
<summary>**উত্তর দেখতে ক্লিক করুন**</summary>

উত্তর:
- অ্যাপ বড় হলে add, toggle, delete-এর লজিক ছড়িয়ে যায়। রিডিউসারে সব কেন্দ্রীভূত করলে কোড মেইনটেইন, ডিবাগ ও এক্সটেন্ড করা সহজ হয়।

```js
function tasksReducer(tasks, action) {
  switch (action.type) {
    case 'added':
      return [...tasks, { id: action.id, text: action.text, done: false }];
    case 'toggled':
      return tasks.map(task =>
        task.id === action.id ? { ...task, done: !task.done } : task
      );
    case 'deleted':
      return tasks.filter(task => task.id !== action.id);
    default:
      throw new Error('Unknown action: ' + action.type);
  }
}
```

</details>

---

**Q33: সিনারিও – মাল্টি-স্টেপ ফর্ম:**
একটি মাল্টি-স্টেপ ফর্ম (যেমন চেকআউট) কীভাবে রিডিউসার দিয়ে ম্যানেজ করবেন?
<details>
<summary>**উত্তর দেখতে ক্লিক করুন**</summary>

উত্তর:
- রিডিউসারে current step, form value ও validation error একসাথে ম্যানেজ করুন। 'next_step', 'prev_step', 'update_field', 'reset' action দিয়ে লজিক পরিষ্কার ও মেইনটেইনেবল হয়।

```js
function formReducer(state, action) {
  switch (action.type) {
    case 'next_step':
      return { ...state, step: state.step + 1 };
    case 'prev_step':
      return { ...state, step: state.step - 1 };
    case 'update_field':
      return { ...state, [action.field]: action.value };
    case 'reset':
      return initialState;
    default:
      return state;
  }
}
```

</details>

---

**Q34: সিনারিও – নোটিফিকেশন সিস্টেম:**
রিডিউসার দিয়ে অ্যাপে নোটিফিকেশন কীভাবে ম্যানেজ করবেন?
<details>
<summary>**উত্তর দেখতে ক্লিক করুন**</summary>

উত্তর:
- 'add_notification', 'remove_notification', 'clear_notifications' action দিয়ে নোটিফিকেশন লিস্ট সহজে ম্যানেজ করা যায়।

```js
function notificationReducer(state, action) {
  switch (action.type) {
    case 'add_notification':
      return [...state, action.notification];
    case 'remove_notification':
      return state.filter(n => n.id !== action.id);
    case 'clear_notifications':
      return [];
    default:
      return state;
  }
}
```

</details>

---

**Q35: সিনারিও – রিয়েল-টাইম কল্যাবোরেশন:**
কীভাবে রিডিউসার দিয়ে collaborative document editor-এর user action ও document state ম্যানেজ করবেন?
<details>
<summary>**উত্তর দেখতে ক্লিক করুন**</summary>

উত্তর:
- edit, cursor move, অন্য user-এর sync action রিডিউসারে হ্যান্ডেল করলে state আপডেট পূর্বানুমানযোগ্য ও debug-friendly হয়।

</details>

---

**Q36: সিনারিও – গেম স্টেট:**
চেস বা টিক-ট্যাক-টো-র মতো turn-based গেমে রিডিউসার কীভাবে state ম্যানেজ করবে?
<details>
<summary>**উত্তর দেখতে ক্লিক করুন**</summary>

উত্তর:
- board, current player, move history, game status—সব রিডিউসারে, 'make_move', 'undo', 'reset', 'set_winner' action দিয়ে।

```js
function gameReducer(state, action) {
  switch (action.type) {
    case 'make_move':
      // ...update board and history
      return { ...state, /* ... */ };
    case 'undo':
      // ...revert to previous state
      return { ...state, /* ... */ };
    case 'reset':
      return initialState;
    case 'set_winner':
      return { ...state, winner: action.winner };
    default:
      return state;
  }
}
```

</details>

---

**Q37: সিনারিও – ড্রয়িং অ্যাপে undo/redo:**
ড্রয়িং ক্যানভাসে undo/redo সাপোর্টের জন্য রিডিউসার state কীভাবে স্ট্রাকচার করবেন?
<details>
<summary>**উত্তর দেখতে ক্লিক করুন**</summary>

উত্তর:
- current drawing ও history stack রাখুন। 'draw', 'undo', 'redo' action দিয়ে stack আপডেট করুন, যাতে ইউজার পরিবর্তন করতে পারে।

```js
function drawingReducer(state, action) {
  switch (action.type) {
    case 'draw':
      return {
        ...state,
        history: [...state.history, state.current],
        current: action.newDrawing,
        future: [],
      };
    case 'undo':
      const prev = state.history[state.history.length - 1];
      return {
        ...state,
        history: state.history.slice(0, -1),
        current: prev,
        future: [state.current, ...state.future],
      };
    case 'redo':
      const next = state.future[0];
      return {
        ...state,
        history: [...state.history, state.current],
        current: next,
        future: state.future.slice(1),
      };
    default:
      return state;
  }
}
```

</details>

</div>

---

## ১.৫ – অতিরিক্ত ও এজ কেস প্রশ্ন 🚀

<div style={{ background: 'rgba(100, 0, 200, 0.10)', padding: '15px', borderRadius: '5px', marginBottom: '20px', border: '1px solid rgba(100, 0, 200, 0.15)', color: 'inherit' }}>

**Q38: nested state আপডেট কীভাবে করবেন?**
<details>
<summary>**উত্তর দেখতে ক্লিক করুন**</summary>

উত্তর: spread operator বা Immer ব্যবহার করুন, যাতে nested অবজেক্ট মিউটেট না হয়।

```js
// Immer ছাড়া
function reducer(state, action) {
  switch (action.type) {
    case 'update_nested':
      return {
        ...state,
        user: {
          ...state.user,
          profile: {
            ...state.user.profile,
            name: action.name
          }
        }
      };
    default:
      return state;
  }
}
```

</details>

---

**Q39: useReducer-এ অপ্রয়োজনীয় re-render কীভাবে এড়াবেন?**
<details>
<summary>**উত্তর দেখতে ক্লিক করুন**</summary>

উত্তর: state না বদলালে আগের অবজেক্টই রিটার্ন করুন, এবং চাইল্ড কম্পোনেন্টে React.memo/useCallback ব্যবহার করুন।

</details>

---

**Q40: একসাথে একাধিক action কীভাবে হ্যান্ডেল করবেন?**
<details>
<summary>**উত্তর দেখতে ক্লিক করুন**</summary>

উত্তর: একাধিক action সিরিয়ালি dispatch করুন, বা 'batch' action বানিয়ে অ্যারে হিসেবে পাঠান।

```js
function reducer(state, action) {
  if (action.type === 'batch') {
    return action.actions.reduce(reducer, state);
  }
  // ...other cases
  return state;
}
```

</details>

---

**Q41: useReducer ও useContext দিয়ে গ্লোবাল state কীভাবে বানাবেন?**
<details>
<summary>**উত্তর দেখতে ক্লিক করুন**</summary>

উত্তর: state ও dispatch-এর জন্য context বানান, এবং পুরো অ্যাপে provide করুন।

```js
const StateContext = React.createContext();
const DispatchContext = React.createContext();

function AppProvider({ children }) {
  const [state, dispatch] = useReducer(reducer, initialState);
  return (
    <StateContext.Provider value={state}>
      <DispatchContext.Provider value={dispatch}>
        {children}
      </DispatchContext.Provider>
    </StateContext.Provider>
  );
}
```

</details>

---

**Q42: রিডিউসারে error state কীভাবে হ্যান্ডেল করবেন?**
<details>
<summary>**উত্তর দেখতে ক্লিক করুন**</summary>

উত্তর: state-এ error ফিল্ড রাখুন, 'set_error' ও 'clear_error' action হ্যান্ডেল করুন।

```js
function reducer(state, action) {
  switch (action.type) {
    case 'set_error':
      return { ...state, error: action.error };
    case 'clear_error':
      return { ...state, error: null };
    default:
      return state;
  }
}
```

</details>

---

**Q43: এনিমেশন state বা ট্রানজিশন ম্যানেজ করতে useReducer ব্যবহার করা যায়?**
<details>
<summary>**উত্তর দেখতে ক্লিক করুন**</summary>

উত্তর: হ্যাঁ, জটিল এনিমেশন স্টেট মেশিনে রিডিউসার দিয়ে ট্রানজিশন ও স্টেট পূর্বানুমানযোগ্যভাবে ম্যানেজ করা যায়।

</details>

---

**Q44: Redux রিডিউসার useReducer-এ কীভাবে মাইগ্রেট করবেন?**
<details>
<summary>**উত্তর দেখতে ক্লিক করুন**</summary>

উত্তর: রিডিউসার ফাংশন কপি করুন, useReducer-এ ব্যবহার করুন, Redux-specific middleware/কোড React hook/context দিয়ে রিপ্লেস করুন।

</details>

---

**Q45: রিডিউসারে derived state কীভাবে হ্যান্ডেল করবেন?**
<details>
<summary>**উত্তর দেখতে ক্লিক করুন**</summary>

উত্তর: শুধু মিনিমাল state রাখুন; count বা filtered list-এর মতো ডেরাইভড ভ্যালু render-এ বা সিলেক্টরে হিসাব করুন।

</details>

---

**Q46: বড় অ্যাপে রিডিউসার কীভাবে debug করবেন?**
<details>
<summary>**উত্তর দেখতে ক্লিক করুন**</summary>

উত্তর: রিডিউসারে লগিং, React DevTools, বা কাস্টম middleware ব্যবহার করুন।

</details>

---

**Q47: useReducer-এর সীমাবদ্ধতা কী Redux-এর তুলনায়?**
<details>
<summary>**উত্তর দেখতে ক্লিক করুন**</summary>

উত্তর: useReducer শুধু লোকাল কম্পোনেন্ট ট্রিতে, middleware/devtools নেই, তবে ছোট/মাঝারি state-এর জন্য সহজ।

</details>

---

**Q48: একাধিক স্বাধীন state-এ undo/redo কীভাবে করবেন?**
<details>
<summary>**উত্তর দেখতে ক্লিক করুন**</summary>

উত্তর: প্রতিটির জন্য আলাদা রিডিউসার ও history, বা parent রিডিউসার দিয়ে সব history ম্যানেজ করুন।

</details>

---

**Q49: async data দিয়ে useReducer-এর initial state কীভাবে সেট করবেন?**
<details>
<summary>**উত্তর দেখতে ক্লিক করুন**</summary>

উত্তর: data ফেচ করার পর 'load' action dispatch করুন, এবং state-এ loading ফ্ল্যাগ রাখুন।

```js
function reducer(state, action) {
  switch (action.type) {
    case 'load_start':
      return { ...state, loading: true };
    case 'load_success':
      return { ...state, loading: false, data: action.data };
    default:
      return state;
  }
}
```

</details>

---

**Q50: useReducer কি TypeScript-এ ব্যবহার করা যায়?**
<details>
<summary>**উত্তর দেখতে ক্লিক করুন**</summary>

উত্তর: হ্যাঁ! state ও action-এর জন্য টাইপ ডিফাইন করুন, সম্পূর্ণ টাইপ সেফটি পাবেন।

```ts
type State = { count: number };
type Action = { type: 'increment' } | { type: 'decrement' };

function reducer(state: State, action: Action): State {
  switch (action.type) {
    case 'increment': return { count: state.count + 1 };
    case 'decrement': return { count: state.count - 1 };
    default: return state;
  }
}
```

</details>

</div>

---

<Callout type="info">এই প্রশ্নগুলো পড়ে নিজে উত্তর দেওয়ার চেষ্টা করুন—ইন্টারভিউ প্রস্তুতির জন্য সবচেয়ে কার্যকর!</Callout>

---

> **🚀 ফাইনাল পরামর্শ:** ইন্টারভিউতে শুধু রিডিউসার সম্পর্কে জ্ঞান নয়, বাস্তব উদাহরণে পরিষ্কার, মেইনটেইনেবল ও ইউজার-ফ্রেন্ডলি কোড লেখার দক্ষতাও দেখান।

<img src="https://upload.wikimedia.org/wikipedia/commons/a/a7/React-icon.svg" alt="React Interview Success" width="100" style={{borderRadius: '8px', marginTop: '2rem'}} />
