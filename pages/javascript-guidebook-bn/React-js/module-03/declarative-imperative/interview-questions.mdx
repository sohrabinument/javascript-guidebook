import { Callout } from "nextra/components";

<img src="https://upload.wikimedia.org/wikipedia/commons/a/a7/React-icon.svg" alt="React Logo" width="70" style={{marginBottom: '1rem'}} />

# ডিক্লেয়ারেটিভ বনাম ইম্পেরেটিভ প্রোগ্রামিং – ইন্টারভিউ প্রশ্ন ও উত্তর 🚦

এই প্রশ্নগুলো ব্যবহার করুন ইন্টারভিউ প্রস্তুতির জন্য বা ডিক্লেয়ারেটিভ ও ইম্পেরেটিভ প্রোগ্রামিং সম্পর্কে আপনার বোঝা যাচাই করতে।

> **💡 টিপস:** প্রতিটি প্রশ্নের উত্তর নিজে চেষ্টা করুন, তারপর উত্তর দেখুন। এতে মনে রাখতে সুবিধা হবে!

---

## ১.১ – মৌলিক ধারণা 🧩

<div style={{ background: 'rgba(0, 100, 200, 0.10)', padding: '15px', borderRadius: '5px', marginBottom: '20px', border: '1px solid rgba(0, 100, 200, 0.15)', color: 'inherit' }}>

**প্রশ্ন ১: UI-র ক্ষেত্রে ইম্পেরেটিভ প্রোগ্রামিং কী?**  
উত্তর: ইম্পেরেটিভ প্রোগ্রামিং হল যখন আপনি কম্পিউটারকে ধাপে ধাপে বলে দেন কিভাবে UI আপডেট করতে হবে, প্রতিটি স্টেপ নিজে লিখে (যেমন: show/hide elements, style পরিবর্তন)।

**প্রশ্ন ২: UI-র ক্ষেত্রে ডিক্লেয়ারেটিভ প্রোগ্রামিং কী?**  
উত্তর: ডিক্লেয়ারেটিভ প্রোগ্রামিং হল যখন আপনি শুধু বলেন UI কেমন দেখতে চান, আর ফ্রেমওয়ার্ক (যেমন React) নিজে থেকে UI আপডেট করে।

**প্রশ্ন ৩: React কোন পদ্ধতি ব্যবহার করে?**  
উত্তর: React ডিক্লেয়ারেটিভ পদ্ধতি ব্যবহার করে, যেখানে আপনি UI-র অবস্থা বর্ণনা করেন এবং React নিজে আপডেট করে।

</div>

---

## ১.২ – নিয়ম ও সতর্কতা ⚠️

<div style={{ background: 'rgba(230, 150, 70, 0.10)', padding: '15px', borderRadius: '5px', marginBottom: '20px', border: '1px solid rgba(230, 150, 70, 0.15)', color: 'inherit' }}>

**প্রশ্ন ৪: ইম্পেরেটিভ UI প্রোগ্রামিং-এর সাধারণ সমস্যা কী?**  
উত্তর: UI বড় হলে প্রতিটি অংশ নিজে আপডেট করতে হয়, তাই ম্যানেজ করা কঠিন ও ভুল হওয়ার ঝুঁকি বেশি।

**প্রশ্ন ৫: জটিল UI-র জন্য ডিক্লেয়ারেটিভ প্রোগ্রামিং কেন বেশি পছন্দ করা হয়?**  
উত্তর: কারণ এতে কোড পড়তে, বুঝতে ও মেইনটেন করতে সহজ—React স্বয়ংক্রিয়ভাবে UI আপডেট করে।

**প্রশ্ন ৬: React-এ ইম্পেরেটিভ ও ডিক্লেয়ারেটিভ কোড মেশানো যায় কি?**  
উত্তর: হ্যাঁ, তবে যতটা সম্ভব ডিক্লেয়ারেটিভ থাকা ভালো। কিছু ক্ষেত্রে (যেমন: refs দিয়ে সরাসরি DOM ম্যানিপুলেশন) ইম্পেরেটিভ কোড লাগতে পারে।

</div>

---

## ১.৩ – সিনারিও ও ব্যবহারিক উদাহরণ 🏆

<div style={{ background: 'rgba(20, 120, 220, 0.10)', padding: '20px', borderRadius: '5px', marginBottom: '20px', border: '1px solid rgba(20, 120, 220, 0.15)', color: 'inherit' }}>

### সিনারিও-ভিত্তিক প্রশ্ন:

ধরা যাক, আপনি চান ফর্ম সাবমিট করার সময় একটি লোডিং স্পিনার দেখাতে এবং কাজ শেষ হলে সেটি লুকিয়ে ফেলতে। ইম্পেরেটিভ ও ডিক্লেয়ারেটিভ দুইভাবে কিভাবে করবেন?

উত্তর দেখার আগে নিজে ভাবুন।

<details>
<summary>**উত্তর দেখতে ক্লিক করুন**</summary>

**ইম্পেরেটিভ:**
- ইভেন্টে রেসপন্স করে স্পিনারের style/class পরিবর্তন করে দেখাতে হবে (যেমন: `spinner.style.display = 'block'`)।

**ডিক্লেয়ারেটিভ (React):**
- একটি state ভ্যারিয়েবল (যেমন `isLoading`) ব্যবহার করে কন্ডিশনালি স্পিনার দেখাবেন: `{isLoading && <Spinner />}`। React নিজে দেখাবে/লুকাবে।

</details>
</div>

---

<Callout type="info">এই প্রশ্নগুলো পড়ে নিজে নিজের ভাষায় বোঝানোর চেষ্টা করুন—এটাই সেরা প্রস্তুতি!</Callout>

---

> **🚀 চূড়ান্ত পরামর্শ:** ইন্টারভিউতে শুধু ডিক্লেয়ারেটিভ বনাম ইম্পেরেটিভ বোঝা নয়, বরং পরিষ্কার, মেইনটেনেবল ও ইউজার-ফ্রেন্ডলি কোড লেখার দক্ষতাও দেখান।

<img src="https://upload.wikimedia.org/wikipedia/commons/a/a7/React-icon.svg" alt="React Interview Success" width="100" style={{borderRadius: '8px', marginTop: '2rem'}} />
