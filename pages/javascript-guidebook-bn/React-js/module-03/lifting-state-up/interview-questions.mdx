import { Callout } from "nextra/components";

<img src="https://upload.wikimedia.org/wikipedia/commons/a/a7/React-icon.svg" alt="React Logo" width="70" style={{marginBottom: '1rem'}} />

# Lifting State Up – ইন্টারভিউ প্রশ্ন ও উত্তর 🚀

এই প্রশ্নগুলো ব্যবহার করুন ইন্টারভিউ প্রস্তুতির জন্য বা React-এ lifting state up ও state শেয়ারিং সম্পর্কে আপনার বোঝা যাচাই করতে।

> **💡 প্রো টিপ:** প্রতিটি প্রশ্নের উত্তর নিজে চেষ্টা করুন, তারপর উত্তর দেখুন। এতে আপনি আরও ভালোভাবে মনে রাখতে পারবেন!

---

## ৩.১ – মৌলিক ধারণা 🧩

<div style={{ background: 'rgba(0, 100, 200, 0.10)', padding: '15px', borderRadius: '5px', marginBottom: '20px', border: '1px solid rgba(0, 100, 200, 0.15)', color: 'inherit' }}>

**Q1: React-এ "Lifting State Up" বলতে কী বোঝায়?**  
উত্তর: state-কে শিশু কম্পোনেন্ট থেকে তাদের সবচেয়ে কাছের parent-এ নিয়ে যাওয়া, যাতে একাধিক কম্পোনেন্ট সেই state শেয়ার ও সমন্বয় করতে পারে।

**Q2: কেন state উপরে তুলতে হয়?**  
উত্তর: sibling কম্পোনেন্টের মধ্যে state শেয়ার করতে এবং UI কে একসাথে সমন্বিত রাখতে।

**Q3: Lifting State Up-এর জন্য বাস্তব উদাহরণ কী?**  
উত্তর: দুই ভাইবোন একটি খেলনা শেয়ার করতে চায়, বাবা-মা খেলনাটি রাখে এবং প্রয়োজনে দেয়—তখন সবাই খুশি ও সমন্বিত থাকে।

**Q4: কোডে lifting state up কিভাবে করবেন?**  
উত্তর: state-কে parent-এ নিয়ে যান, সেটি ও তার updater props হিসেবে শিশুদের পাঠান, এবং শিশুরা props দিয়ে state আপডেট করে।

```jsx
// উদাহরণ: দুই sibling কম্পোনেন্টের জন্য lifting state up
function Parent() {
  const [sharedValue, setSharedValue] = useState("");
  return (
    <>
      <ChildA value={sharedValue} setValue={setSharedValue} />
      <ChildB value={sharedValue} />
    </>
  );
}
function ChildA({ value, setValue }) {
  return <input value={value} onChange={e => setValue(e.target.value)} placeholder="Type here" />;
}
function ChildB({ value }) {
  return <div>Value: {value}</div>;
}
```

**Q5: Lifting state up-এর প্রধান সুবিধা কী?**  
উত্তর: এটি single source of truth নিশ্চিত করে, ফলে UI পূর্বানুমানযোগ্য ও ডিবাগ করা সহজ হয়।

**Q6: দুইয়ের বেশি কম্পোনেন্টের জন্য state lift করা যায়?**  
উত্তর: হ্যাঁ! যেসব কম্পোনেন্টে state শেয়ার করতে হবে, তাদের সবচেয়ে কাছের parent-এ state তুলুন।

</div>

---

## ৩.২ – নিয়ম ও সতর্কতা ⚠️

<div style={{ background: 'rgba(230, 150, 70, 0.10)', padding: '15px', borderRadius: '5px', marginBottom: '20px', border: '1px solid rgba(230, 150, 70, 0.15)', color: 'inherit' }}>

**Q7: Lifting state up-এর সাধারণ ভুল কী?**  
উত্তর: খুব বেশি state উপরে তুললে parent কম্পোনেন্ট জটিল ও কঠিন হয়ে যায়। শুধু প্রয়োজন হলে state lift করুন।

**Q8: যদি দুই sibling কম্পোনেন্টে আলাদা state রাখেন, তাহলে কী হয়?**  
উত্তর: state অসামঞ্জস্যপূর্ণ হয়ে পড়ে, ফলে বাগ ও UI mismatch হয়।

```jsx
// ❌ ভুল: sibling-এ আলাদা state (sync থাকবে না)
function Parent() {
  return (
    <>
      <ChildA />
      <ChildB />
    </>
  );
}
function ChildA() {
  const [value, setValue] = useState("");
  return <input value={value} onChange={e => setValue(e.target.value)} />;
}
function ChildB() {
  const [value, setValue] = useState("");
  return <div>Value: {value}</div>;
}
// এগুলো কখনোই sync থাকবে না!
```

**Q9: Lifting state up-এর সাথে controlled ও uncontrolled component-এর সম্পর্ক কী?**  
উত্তর: state lift করলে parent state নিয়ন্ত্রণ করে (controlled)। যদি শিশু নিজে state ম্যানেজ করে, সেটি uncontrolled।

**Q10: সবসময় state App কম্পোনেন্টে lift করা উচিত?**  
উত্তর: না, শুধু সবচেয়ে কাছের parent-এ lift করুন যেখান থেকে শেয়ার দরকার।

**Q11: আলাদা কম্পোনেন্ট instance-এ state শেয়ার হয়?**  
উত্তর: না, প্রতিটি instance-এর state আলাদা, যদি না parent থেকে props দিয়ে শেয়ার করা হয়।

</div>

---

## ৩.৩ – সিনারিও ও ব্যবহারিক উদাহরণ 🏆

<div style={{ background: 'rgba(20, 120, 220, 0.10)', padding: '20px', borderRadius: '5px', marginBottom: '20px', border: '1px solid rgba(20, 120, 220, 0.15)', color: 'inherit' }}>

### সিনারিও-ভিত্তিক প্রশ্ন:

ধরুন আপনার `Products` ও `Cart` নামে দুই sibling কম্পোনেন্ট আছে। আপনি চান Products থেকে cart-এ পণ্য যোগ করতে এবং Cart-এ তা দেখাতে। কিভাবে cart state শেয়ার করবেন?

উত্তর দেখার আগে নিজে চিন্তা করুন।

<details>
<summary>**উত্তর দেখতে ক্লিক করুন**</summary>

**উত্তর:**
- `cart` state তাদের common parent (যেমন, `App`)-এ তুলুন।
- `cart` ও `setCart` props হিসেবে Products ও Cart-এ পাঠান।
- Products cart আপডেট করতে পারবে, Cart দেখাতে পারবে।

```jsx
function App() {
  const [cart, setCart] = useState([]);
  return (
    <>
      <Products cart={cart} setCart={setCart} />
      <Cart cart={cart} />
    </>
  );
}
function Products({ cart, setCart }) {
  const products = [
    { id: 1, name: "Apple 🍎" },
    { id: 2, name: "Banana 🍌" },
  ];
  const addToCart = (product) => setCart([...cart, product]);
  return (
    <div>
      <h2>Products</h2>
      {products.map((product) => (
        <div key={product.id}>
          {product.name}
          <button onClick={() => addToCart(product)}>Add to Cart</button>
        </div>
      ))}
    </div>
  );
}
function Cart({ cart }) {
  return (
    <div>
      <h2>Cart</h2>
      {cart.length === 0 ? <p>No items in cart.</p> : (
        <ul>
          {cart.map((item, idx) => <li key={idx}>{item.name}</li>)}
        </ul>
      )}
    </div>
  );
}
```

</details>

---

### সিনারিও-ভিত্তিক প্রশ্ন:

আপনার কাছে দুইটি input field আছে। কিভাবে করবেন যাতে যেকোনো একটিতে টাইপ করলে দুটোতেই একই ভ্যালু দেখায়?

উত্তর দেখার আগে নিজে চিন্তা করুন।

<details>
<summary>**উত্তর দেখতে ক্লিক করুন**</summary>

**উত্তর:**
- state উপরে তুলুন (common parent-এ)।
- value ও updater function props হিসেবে দুই input-এ পাঠান।

```jsx
function SyncedInputs() {
  const [text, setText] = useState("");
  return (
    <>
      <input value={text} onChange={e => setText(e.target.value)} />
      <input value={text} onChange={e => setText(e.target.value)} />
    </>
  );
}
```

</details>

---

### সিনারিও-ভিত্তিক প্রশ্ন:

ধরুন আপনার একটি Accordion আছে, যার দুইটি panel আছে, এবং আপনি চান একসাথে শুধুমাত্র একটি panel খোলা থাকুক। কিভাবে open/close state ম্যানেজ করবেন?

উত্তর দেখার আগে নিজে চিন্তা করুন।

<details>
<summary>**উত্তর দেখতে ক্লিক করুন**</summary>

**উত্তর:**
- active panel-এর state parent Accordion-এ তুলুন।
- প্রতিটি Panel-এ `isActive` ও `onShow` props পাঠান।

```jsx
function Accordion() {
  const [activeIndex, setActiveIndex] = useState(0);
  return (
    <>
      <Panel
        title="About"
        isActive={activeIndex === 0}
        onShow={() => setActiveIndex(0)}
      >
        About content...
      </Panel>
      <Panel
        title="Etymology"
        isActive={activeIndex === 1}
        onShow={() => setActiveIndex(1)}
      >
        Etymology content...
      </Panel>
    </>
  );
}
function Panel({ title, children, isActive, onShow }) {
  return (
    <section>
      <h3>{title}</h3>
      {isActive ? <p>{children}</p> : <button onClick={onShow}>Show</button>}
    </section>
  );
}
```

</details>

</div>

---

<Callout type="info">এই প্রশ্নগুলো পড়ে নিজে নিজে উত্তর দেয়ার চেষ্টা করুন—এটাই সেরা প্রস্তুতি!</Callout>

---

> **🚀 চূড়ান্ত পরামর্শ:** ইন্টারভিউতে শুধু lifting state up-এর থিওরি নয়, বরং বাস্তব উদাহরণ ও পরিষ্কার, রক্ষণাবেক্ষণযোগ্য কোড লেখার দক্ষতা দেখান।

<img src="https://upload.wikimedia.org/wikipedia/commons/a/a7/React-icon.svg" alt="React Interview Success" width="100" style={{borderRadius: '8px', marginTop: '2rem'}} />
