import { Callout } from "nextra/components";

<img src="https://upload.wikimedia.org/wikipedia/commons/a/a7/React-icon.svg" alt="React Logo" width="70" style={{marginBottom: '1rem'}} />

# State সংরক্ষণ ও রিসেট – ইন্টারভিউ প্রশ্ন ও উত্তর 🧠🔄

এই প্রশ্নগুলো ব্যবহার করুন ইন্টারভিউ প্রস্তুতির জন্য বা React-এ state সংরক্ষণ ও রিসেট কিভাবে হয় তা যাচাই করতে।

> **💡 প্রো টিপ:** প্রতিটি প্রশ্নের উত্তর নিজে চেষ্টা করুন, তারপর উত্তর দেখুন। এতে আপনি আরও ভালোভাবে মনে রাখতে পারবেন!

---

## ১.১ – মৌলিক ধারণা 🌳

<div style={{ background: 'rgba(0, 100, 200, 0.10)', padding: '15px', borderRadius: '5px', marginBottom: '20px', border: '1px solid rgba(0, 100, 200, 0.15)', color: 'inherit' }}>

**Q1: React আসলে কোথায় component-এর state সংরক্ষণ করে?**  
উত্তর: React state-কে ইন্টারনালি সংরক্ষণ করে এবং UI tree-তে কম্পোনেন্টের অবস্থানের সাথে যুক্ত রাখে—JSX বা কম্পোনেন্ট ফাংশনের মধ্যে নয়।

**Q2: আপনি যদি একই কম্পোনেন্ট দুই জায়গায় রেন্ডার করেন, কী হয়?**  
উত্তর: প্রতিটি instance আলাদা, স্বাধীন state পায়।

**Q3: আপনি যদি কম্পোনেন্ট tree থেকে সরান, তার state-এর কী হয়?**  
উত্তর: তার state নষ্ট হয়ে যায়। আবার যোগ করলে, নতুন state দিয়ে শুরু হয়।

**Q4: React কীভাবে ঠিক করে state সংরক্ষণ করবে নাকি রিসেট করবে?**  
উত্তর: কম্পোনেন্টের tree-তে অবস্থান ও key (যদি থাকে) দেখে। অবস্থান বা key বদলালে state রিসেট হয়।

**Q5: state সংরক্ষণ বোঝা কেন গুরুত্বপূর্ণ?**  
উত্তর: এতে আপনি বাগ, অপ্রত্যাশিত রিসেট এড়াতে পারবেন এবং UI আরও পূর্বানুমানযোগ্য হবে।

</div>

---

## ১.২ – নিয়ম ও সতর্কতা ⚠️

<div style={{ background: 'rgba(230, 150, 70, 0.10)', padding: '15px', borderRadius: '5px', marginBottom: '20px', border: '1px solid rgba(230, 150, 70, 0.15)', color: 'inherit' }}>

**Q6: state ও component key নিয়ে সাধারণ ভুল কী?**  
উত্তর: component-এর key বদলালে state রিসেট হয়, তাই শুধু state রিসেট করতে চাইলে key বদলান।

**Q7: আপনি যদি অন্য কম্পোনেন্টের ভিতরে কম্পোনেন্ট ডিফাইন করেন, কী হয়?**  
উত্তর: nested কম্পোনেন্ট প্রতি রেন্ডারে নতুনভাবে তৈরি হয়, ফলে state বারবার রিসেট হয়।

**Q8: React-এ state কি global?**  
উত্তর: না, প্রতিটি component instance-এর state tree-র অবস্থানের সাথে যুক্ত এবং আলাদা।

**Q9: ফর্মে ইউজার ইনপুট কীভাবে হারিয়ে যেতে পারে?**  
উত্তর: ফর্ম tree-র বিভিন্ন অবস্থানে রেন্ডার করলে (যেমন, conditionally উপরে/নিচে), React state রিসেট করে দেয়।

</div>

---

## ১.৩ – সিনারিও ও ব্যবহারিক উদাহরণ 🏆

<div style={{ background: 'rgba(20, 120, 220, 0.10)', padding: '20px', borderRadius: '5px', marginBottom: '20px', border: '1px solid rgba(20, 120, 220, 0.15)', color: 'inherit' }}>

### সিনারিও-ভিত্তিক প্রশ্ন:

ধরুন আপনার পাশে পাশে দুইটি `<Counter />` কম্পোনেন্ট আছে। একটিতে ইনক্রিমেন্ট করলে অন্যটির state কি পরিবর্তন হবে?

<details>
<summary>**উত্তর দেখতে ক্লিক করুন**</summary>

**উত্তর:**
- প্রতিটি `<Counter />` tree-র আলাদা অবস্থানে রেন্ডার হয়, তাই প্রতিটির state আলাদা। একটিতে ইনক্রিমেন্ট করলে অন্যটির state পরিবর্তন হবে না।

```jsx
function App() {
  return (
    <div>
      <Counter />
      <Counter />
    </div>
  );
}

function Counter() {
  const [score, setScore] = useState(0);
  return (
    <div>
      <h1>{score}</h1>
      <button onClick={() => setScore(score + 1)}>Add one</button>
    </div>
  );
}
```

</details>

---

### সিনারিও-ভিত্তিক প্রশ্ন:

আপনার একটি কম্পোনেন্ট আছে যেটি conditionally রেন্ডার হয় (যেমন, চেকবক্স দিয়ে)। আনচেক করলে state হারিয়ে যায়, আবার চেক করলে নতুন state আসে—কেন?

<details>
<summary>**উত্তর দেখতে ক্লিক করুন**</summary>

**উত্তর:**
- tree থেকে কম্পোনেন্ট সরালে React তার state নষ্ট করে। আবার যোগ করলে নতুন state দিয়ে শুরু হয়।

```jsx
function App() {
  const [show, setShow] = useState(true);
  return (
    <div>
      {show && <Counter />}
      <button onClick={() => setShow(!show)}>Toggle Counter</button>
    </div>
  );
}
```

</details>

---

### সিনারিও-ভিত্তিক প্রশ্ন:

list-এর কম্পোনেন্টে key কিভাবে state সংরক্ষণে প্রভাব ফেলে?

<details>
<summary>**উত্তর দেখতে ক্লিক করুন**</summary>

**উত্তর:**
- React key দিয়ে প্রতিটি কম্পোনেন্ট আলাদা করে। key বদলালে state রিসেট হয়।

```jsx
function App() {
  const [player, setPlayer] = useState('Taylor');
  return (
    <div>
      <Counter key={player} player={player} />
      <button onClick={() => setPlayer(player === 'Taylor' ? 'Sarah' : 'Taylor')}>Next player!</button>
    </div>
  );
}

function Counter({ player }) {
  const [score, setScore] = useState(0);
  return (
    <div>
      <h1>{player}'s score: {score}</h1>
      <button onClick={() => setScore(score + 1)}>Add one</button>
    </div>
  );
}
```

</details>

---

### সিনারিও-ভিত্তিক প্রশ্ন:

নিচের উদাহরণে show/hide hint করলে input-এর লেখা কেন হারিয়ে যায়? কিভাবে ঠিক করবেন?

<details>
<summary>**উত্তর দেখতে ক্লিক করুন**</summary>

**উত্তর:**
- `<Form />` tree-র আলাদা অবস্থানে রেন্ডার হলে state রিসেট হয়। সমাধান: সবসময় `<Form />` একই অবস্থানে রেন্ডার করুন, শুধু hint টেক্সট conditionally দেখান।

```jsx
function App() {
  const [showHint, setShowHint] = useState(false);
  return (
    <div>
      <Form />
      {showHint && <p><i>Hint: Your favorite city?</i></p>}
      <button onClick={() => setShowHint(!showHint)}>
        {showHint ? 'Hide hint' : 'Show hint'}
      </button>
    </div>
  );
}

function Form() {
  const [text, setText] = useState('');
  return (
    <textarea
      value={text}
      onChange={e => setText(e.target.value)}
    />
  );
}
```

</details>

</div>

---

<Callout type="info">এই প্রশ্নগুলো পড়ে নিজে নিজে উত্তর দেয়ার চেষ্টা করুন—এটাই সেরা প্রস্তুতি!</Callout>

---

> **🚀 চূড়ান্ত পরামর্শ:** ইন্টারভিউতে শুধু state সংরক্ষণ ও রিসেট-এর থিওরি নয়, বরং বাস্তব উদাহরণ ও পরিষ্কার, রক্ষণাবেক্ষণযোগ্য কোড লেখার দক্ষতা দেখান।

<img src="https://upload.wikimedia.org/wikipedia/commons/a/a7/React-icon.svg" alt="React Interview Success" width="100" style={{borderRadius: '8px', marginTop: '2rem'}} />
