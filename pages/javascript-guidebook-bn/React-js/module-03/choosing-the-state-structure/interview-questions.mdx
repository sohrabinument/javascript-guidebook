import { Callout } from "nextra/components";

<img src="https://upload.wikimedia.org/wikipedia/commons/a/a7/React-icon.svg" alt="React Logo" width="70" style={{marginBottom: '1rem'}} />

# React-এ স্টেট স্ট্রাকচার – ইন্টারভিউ প্রশ্ন ও উত্তর 🏗️

ইন্টারভিউ প্রস্তুতির জন্য বা React-এ স্টেট স্ট্রাকচার সম্পর্কে আপনার বোধগম্যতা পরীক্ষা করার জন্য এই প্রশ্নগুলো ব্যবহার করুন।

> **💡 প্রো টিপ:** উত্তর দেখার আগে নিজে চেষ্টা করুন। এটি ধারণাগুলো মনে রাখতে সাহায্য করবে!

---

## ১.১ – মৌলিক বিষয়সমূহ 🌱

<div style={{ background: 'rgba(0, 100, 200, 0.10)', padding: '15px', borderRadius: '5px', marginBottom: '20px', border: '1px solid rgba(0, 100, 200, 0.15)', color: 'inherit' }}>

**প্রশ্ন ১: সম্পর্কিত স্টেট একসাথে রাখা কেন গুরুত্বপূর্ণ?**  
উত্তর: সম্পর্কিত স্টেট (যেমন x এবং y কোঅর্ডিনেট) একসাথে রাখলে আপনার ডেটা সিঙ্ক থাকে এবং কোড পরিষ্কার থাকে। যদি আপনি সবসময় এগুলো একসাথে আপডেট করেন, তাহলে এগুলো একটি অবজেক্টে রাখুন!

**উদাহরণ:**
```js
// ভালো
const [position, setPosition] = useState({ x: 0, y: 0 });
// খারাপ
const [x, setX] = useState(0);
const [y, setY] = useState(0);
```

**প্রশ্ন ২: সম্পর্কিত স্টেট আলাদা রাখলে কী সমস্যা হতে পারে?**  
উত্তর: আপনি দুটোই আপডেট করতে ভুলে যেতে পারেন, ফলে বাগ বা সিঙ্কের বাইরে চলে যাওয়া ডেটা হতে পারে।

**উদাহরণ:**
```js
setX(5); // ওহ! setY ভুলে গেছি!
```

**প্রশ্ন ৩: কখন স্টেট একসাথে রাখা উচিত নয়?**  
উত্তর: যদি দুটি স্টেট স্বাধীনভাবে পরিবর্তন হয়, তাহলে এগুলো আলাদা রাখুন।

**উদাহরণ:**
```js
const [isOpen, setIsOpen] = useState(false); // মোডাল খোলা/বন্ধ
const [count, setCount] = useState(0); // কাউন্টার
```

</div>

---

## ১.২ – নিয়ম ও সাধারণ ভুল 🚦

<div style={{ background: 'rgba(230, 150, 70, 0.10)', padding: '15px', borderRadius: '5px', marginBottom: '20px', border: '1px solid rgba(230, 150, 70, 0.15)', color: 'inherit' }}>

**প্রশ্ন ৪: স্টেটে বিরোধিতা কী?**  
উত্তর: যখন দুটি স্টেট ভেরিয়েবলের মান একসাথে থাকলে অর্থবহ হয় না (যেমন, একটি ট্রাফিক লাইট যা একই সাথে সবুজ এবং লাল)।

**উদাহরণ:**
```js
const [isLoading, setIsLoading] = useState(false);
const [isLoaded, setIsLoaded] = useState(false);
// দুটোই ভুল করে true হতে পারে!
```

**প্রশ্ন ৫: বিরোধিতা কীভাবে এড়ানো যায়?**  
উত্তর: একাধিক বুলিয়ান ভেরিয়েবলের পরিবর্তে একটি স্ট্যাটাস ভেরিয়েবল ব্যবহার করুন।

**উদাহরণ:**
```js
const [status, setStatus] = useState('idle'); // 'idle', 'loading', 'loaded'
```

**প্রশ্ন ৬: অপ্রয়োজনীয় স্টেট কী?**  
উত্তর: অন্য স্টেট বা props থেকে হিসাব করা যায় এমন স্টেট। এটা স্টোর করবেন না!

**উদাহরণ:**
```js
// অপ্রয়োজনীয়
const [fullName, setFullName] = useState(firstName + ' ' + lastName);
// ভালো
const fullName = firstName + ' ' + lastName;
```

**প্রশ্ন ৭: ডুপ্লিকেটেড স্টেট কী?**  
উত্তর: একই ডেটা একাধিক জায়গায় স্টোর করা।

**উদাহরণ:**
```js
// ডুপ্লিকেটেড
const [items, setItems] = useState(['a', 'b']);
const [count, setCount] = useState(2);
// ভালো
const count = items.length;
```

**প্রশ্ন ৮: গভীরভাবে নেস্টেড স্টেট এড়ানো কেন উচিত?**  
উত্তর: এটা আপডেট এবং মেইনটেইন করা কঠিন। ফ্ল্যাট স্টেট সহজ!

**উদাহরণ:**
```js
// গভীরভাবে নেস্টেড
const [state, setState] = useState({ user: { profile: { name: '' } } });
// ফ্ল্যাট
const [profile, setProfile] = useState({ name: '' });
```

</div>

---

## ১.৩ – পরিস্থিতি ও ব্যবহারিক উদাহরণ 🏆

<div style={{ background: 'rgba(20, 120, 220, 0.10)', padding: '20px', borderRadius: '5px', marginBottom: '20px', border: '1px solid rgba(20, 120, 220, 0.15)', color: 'inherit' }}>

### পরিস্থিতি ১: একটি ডট নড়ানো

ধরুন আপনার একটি লাল ডট আছে যা স্ক্রিনে নড়ে। x এবং y এর জন্য দুটি স্টেট ভেরিয়েবল ব্যবহার করবেন, নাকি একটি অবজেক্ট?

<details>
<summary>**উত্তর দেখতে ক্লিক করুন**</summary>

**সেরা:**
```js
const [position, setPosition] = useState({ x: 0, y: 0 });
```
কারণ x এবং y সবসময় একসাথে পরিবর্তন হয়!

</details>

---

### পরিস্থিতি ২: শপিং কার্ট

আপনার একটি শপিং কার্ট আছে যেখানে আইটেমের লিস্ট আছে। আইটেম কাউন্ট আলাদাভাবে স্টোর করবেন?

<details>
<summary>**উত্তর দেখতে ক্লিক করুন**</summary>

**না!**
```js
const [cartItems, setCartItems] = useState([]);
const itemCount = cartItems.length;
```
সবসময় অ্যারে থেকে কাউন্ট হিসাব করুন।

</details>

---

### পরিস্থিতি ৩: ইউজার প্রোফাইল

আপনি একজন ইউজারের নাম এবং বয়স স্টোর করতে চান। এগুলো গভীরভাবে নেস্ট করবেন?

<details>
<summary>**উত্তর দেখতে ক্লিক করুন**</summary>

**ফ্ল্যাট রাখুন!**
```js
const [profile, setProfile] = useState({ name: '', age: 0 });
```
ফ্ল্যাট স্টেট আপডেট করা সহজ।

</details>

</div>

---

## ১.৪ – ডেরাইভড স্টেট ও Props Mirroring 🧮🔄

<div style={{ background: 'rgba(0, 180, 100, 0.10)', padding: '15px', borderRadius: '5px', marginBottom: '20px', border: '1px solid rgba(0, 180, 100, 0.15)', color: 'inherit' }}>

**প্রশ্ন ৯: React-এ ডেরাইভড স্টেট (Derived State) কী?**  
উত্তর: ডেরাইভড স্টেট হচ্ছে এমন কোনো মান, যেটা অন্য state বা props থেকে হিসাব করা যায়, আলাদা state variable-এ না রেখে।

**উদাহরণ:**
```js
const [firstName, setFirstName] = useState('Ada');
const [lastName, setLastName] = useState('Lovelace');
// ডেরাইভড স্টেট:
const fullName = firstName + ' ' + lastName;
```

**প্রশ্ন ১০: ডেরাইভড স্টেট আলাদা করে রাখা উচিত নয় কেন?**  
উত্তর: আলাদা করে রাখলে মূল ডেটা থেকে mismatch হতে পারে এবং বাগ হতে পারে। যখন দরকার, তখন হিসাব করাই ভালো।

**প্রশ্ন ১১: Props Mirroring (mirrored state) কী?**  
উত্তর: যখন কোনো চাইল্ড কম্পোনেন্ট তার state-এ parent থেকে পাওয়া prop-এর মান কপি করে রাখে।

**প্রশ্ন ১২: সাধারণত Props Mirroring এড়ানো কেন উচিত?**  
উত্তর: চাইল্ডের state প্যারেন্টের prop থেকে আলাদা হয়ে যেতে পারে, ফলে ডেটার source of truth নিয়ে বিভ্রান্তি হয়।

**প্রশ্ন ১৩: কখন Props Mirroring করা যায়?**  
উত্তর: যখন শুধু initial value দরকার, এরপর চাইল্ড নিজে সেটি ম্যানেজ করবে। প্যারেন্টের সাথে sync রাখতে হলে mirroring না করে সরাসরি prop ব্যবহার করুন।

**উদাহরণ (ঠিক):**
```js
function Child({ initialValue }) {
  const [value, setValue] = useState(initialValue); // শুধু initial value
}
```

**উদাহরণ (ভুল):**
```js
function Child({ value }) {
  // ❌ প্যারেন্টের value বদলালেও এখানে আপডেট হবে না
  const [localValue, setLocalValue] = useState(value);
}
```

**প্রশ্ন ১৪: চাইল্ড কম্পোনেন্টকে সবসময় প্যারেন্টের prop-এর সাথে sync রাখতে চাইলে কী করবেন?**  
উত্তর: state-এ কপি না করে সরাসরি prop ব্যবহার করুন।

</div>

---

<Callout type="info">সেরা ইন্টারভিউ প্রস্তুতির জন্য এই প্রশ্নগুলো রিভিউ করুন এবং ধারণাগুলো নিজের ভাষায় ব্যাখ্যা করার চেষ্টা করুন!</Callout>

---

> **🚀 চূড়ান্ত পরামর্শ:** ইন্টারভিউতে শুধু স্টেট স্ট্রাকচার সম্পর্কে আপনার জ্ঞান দেখান না, বরং পরিষ্কার, মেইনটেইন করা সহজ এবং ইউজার-ফ্রেন্ডলি React কোড লেখার দক্ষতাও দেখান।

<img src="https://upload.wikimedia.org/wikipedia/commons/a/a7/React-icon.svg" alt="React Interview Success" width="100" style={{borderRadius: '8px', marginTop: '2rem'}} />
