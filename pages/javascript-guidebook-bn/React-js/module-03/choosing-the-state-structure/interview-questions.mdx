import { Callout } from "nextra/components";

<img src="https://upload.wikimedia.org/wikipedia/commons/a/a7/React-icon.svg" alt="React Logo" width="70" style={{marginBottom: '1rem'}} />

# React-এ State Structure বাছাই – ইন্টারভিউ প্রশ্ন ও উত্তর 🏗️

এই প্রশ্নগুলো ব্যবহার করুন ইন্টারভিউ প্রস্তুতির জন্য বা React-এ state structure কেমন হওয়া উচিত তা বোঝার জন্য।

> **💡 টিপস:** প্রতিটি প্রশ্নের উত্তর নিজে চেষ্টা করুন, তারপর উত্তর দেখুন। এতে মনে রাখতে সুবিধা হবে!

---

## ১.১ – মৌলিক ধারণা 🌱

<div style={{ background: 'rgba(0, 100, 200, 0.10)', padding: '15px', borderRadius: '5px', marginBottom: '20px', border: '1px solid rgba(0, 100, 200, 0.15)', color: 'inherit' }}>

**প্রশ্ন ১: সম্পর্কিত state একসাথে রাখা কেন গুরুত্বপূর্ণ?**  
উত্তর: সম্পর্কিত state (যেমন x ও y coordinate) একসাথে রাখলে ডেটা সিঙ্ক থাকে এবং কোড পরিষ্কার হয়। সবসময় একসাথে আপডেট হলে, এক object-এ রাখুন!

**উদাহরণ:**
```js
// ভালো
const [position, setPosition] = useState({ x: 0, y: 0 });
// খারাপ
const [x, setX] = useState(0);
const [y, setY] = useState(0);
```

**প্রশ্ন ২: আলাদা state রাখলে কী সমস্যা হতে পারে?**  
উত্তর: একসাথে আপডেট করতে ভুলে গেলে বাগ বা mismatch হতে পারে।

**উদাহরণ:**
```js
setX(5); // ওহ! setY ভুলে গেছি!
```

**প্রশ্ন ৩: কখন state আলাদা রাখা উচিত?**  
উত্তর: যদি দুইটি state আলাদাভাবে পরিবর্তন হয়, তাহলে আলাদা রাখুন।

**উদাহরণ:**
```js
const [isOpen, setIsOpen] = useState(false); // Modal open/close
const [count, setCount] = useState(0); // Counter
```

</div>

---

## ১.২ – নিয়ম ও সতর্কতা 🚦

<div style={{ background: 'rgba(230, 150, 70, 0.10)', padding: '15px', borderRadius: '5px', marginBottom: '20px', border: '1px solid rgba(230, 150, 70, 0.15)', color: 'inherit' }}>

**প্রশ্ন ৪: state-এ বিরোধ কী?**  
উত্তর: যখন দুইটি state variable এমন মান নিতে পারে, যা একসাথে যুক্তিসঙ্গত নয় (যেমন, ট্রাফিক লাইট একসাথে সবুজ ও লাল)।

**উদাহরণ:**
```js
const [isLoading, setIsLoading] = useState(false);
const [isLoaded, setIsLoaded] = useState(false);
// ভুলে গেলে দুটোই true হতে পারে!
```

**প্রশ্ন ৫: বিরোধ এড়াতে কী করবেন?**  
উত্তর: একাধিক boolean-এর বদলে একটি status variable ব্যবহার করুন।

**উদাহরণ:**
```js
const [status, setStatus] = useState('idle'); // 'idle', 'loading', 'loaded'
```

**প্রশ্ন ৬: অপ্রয়োজনীয় state কী?**  
উত্তর: যেটা অন্য state বা props থেকে হিসাব করা যায়, সেটি আলাদা করে রাখবেন না।

**উদাহরণ:**
```js
// অপ্রয়োজনীয়
const [fullName, setFullName] = useState(firstName + ' ' + lastName);
// ভালো
const fullName = firstName + ' ' + lastName;
```

**প্রশ্ন ৭: ডুপ্লিকেট state কী?**  
উত্তর: একই ডেটা একাধিক জায়গায় রাখা।

**উদাহরণ:**
```js
// ডুপ্লিকেট
const [items, setItems] = useState(['a', 'b']);
const [count, setCount] = useState(2);
// ভালো
const count = items.length;
```

**প্রশ্ন ৮: বেশি nested state কেন এড়াবেন?**  
উত্তর: আপডেট ও মেইনটেন করা কঠিন। flat state সহজ!

**উদাহরণ:**
```js
// বেশি nested
const [state, setState] = useState({ user: { profile: { name: '' } } });
// flat
const [profile, setProfile] = useState({ name: '' });
```

</div>

---

## ১.৩ – সিনারিও ও ব্যবহারিক উদাহরণ 🏆

<div style={{ background: 'rgba(20, 120, 220, 0.10)', padding: '20px', borderRadius: '5px', marginBottom: '20px', border: '1px solid rgba(20, 120, 220, 0.15)', color: 'inherit' }}>

### সিনারিও ১: ডট মুভ করা

ধরা যাক, স্ক্রিনে একটি লাল ডট আছে, যেটা মুভ করে। x ও y-এর জন্য আলাদা state নেবেন, নাকি এক object?

<details>
<summary>**উত্তর দেখতে ক্লিক করুন**</summary>

**সেরা:**
```js
const [position, setPosition] = useState({ x: 0, y: 0 });
```
x ও y সবসময় একসাথে বদলায়!

</details>

---

### সিনারিও ২: শপিং কার্ট

আপনার শপিং কার্টে আইটেমের লিস্ট আছে। আলাদা করে item count রাখবেন?

<details>
<summary>**উত্তর দেখতে ক্লিক করুন**</summary>

**না!**
```js
const [cartItems, setCartItems] = useState([]);
const itemCount = cartItems.length;
```
সবসময় array থেকে হিসাব করুন।

</details>

---

### সিনারিও ৩: ইউজার প্রোফাইল

একজন ইউজারের নাম ও বয়স রাখতে চান। deeply nested রাখবেন?

<details>
<summary>**উত্তর দেখতে ক্লিক করুন**</summary>

**flat রাখুন!**
```js
const [profile, setProfile] = useState({ name: '', age: 0 });
```
flat state আপডেট করা সহজ।

</details>

</div>

---

<Callout type="info">এই প্রশ্নগুলো পড়ে নিজে নিজের ভাষায় বোঝানোর চেষ্টা করুন—এটাই সেরা প্রস্তুতি!</Callout>

---

> **🚀 চূড়ান্ত পরামর্শ:** ইন্টারভিউতে শুধু state structure বোঝা নয়, বরং পরিষ্কার, মেইনটেনেবল ও ইউজার-ফ্রেন্ডলি React কোড লেখার দক্ষতাও দেখান।

<img src="https://upload.wikimedia.org/wikipedia/commons/a/a7/React-icon.svg" alt="React Interview Success" width="100" style={{borderRadius: '8px', marginTop: '2rem'}} />
