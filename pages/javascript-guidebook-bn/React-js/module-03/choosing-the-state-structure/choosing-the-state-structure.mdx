# 🏗️ React-এ স্টেট স্ট্রাকচার বেছে নেওয়া

## ভূমিকা

আপনার স্টেট স্ট্রাকচার ভালোভাবে তৈরি করলে আপনার React কম্পোনেন্টগুলো তৈরি, ডিবাগ এবং মেইনটেইন করা সহজ হয়ে যায়! চলুন শিখি কিভাবে আপনার অ্যাপের জন্য সেরা স্টেট স্ট্রাকচার বেছে নিতে হয়। 🚀

---

## 🌱 স্টেট স্ট্রাকচার করার মূলনীতি

যখন আপনি এমন একটি কম্পোনেন্ট লিখবেন যেটা স্টেট ধারণ করে, তখন আপনাকে সিদ্ধান্ত নিতে হবে:
- আপনার কয়টি স্টেট ভেরিয়েবল দরকার?
- তাদের ডেটা স্ট্রাকচার কেমন হওয়া উচিত?

এখানে কিছু সোনালি নিয়ম আছে যা আপনাকে সাহায্য করবে:

1. **🧩 সম্পর্কিত স্টেট একসাথে রাখুন।**
2. **🚫 স্টেটে বিরোধিতা এড়িয়ে চলুন।**
3. **🔄 অপ্রয়োজনীয় স্টেট এড়িয়ে চলুন।**
4. **📦 স্টেটে ডুপ্লিকেশন এড়িয়ে চলুন।**
5. **🌳 গভীরভাবে নেস্টেড স্টেট এড়িয়ে চলুন।**

চলুন প্রতিটি নীতি বিস্তারিতভাবে দেখি, ব্যবহারিক উদাহরণসহ!

---

## 🧩 ১. সম্পর্কিত স্টেট একসাথে রাখুন

যদি আপনি দুই বা তার বেশি স্টেট ভেরিয়েবল একসাথে আপডেট করেন, তাহলে সেগুলো একটি স্টেট ভেরিয়েবলে একত্রিত করার কথা ভাবুন। এটি আপনার ডেটা সিঙ্ক রাখে এবং কোড পরিষ্কার রাখে!

**বাস্তব জীবনের উদাহরণ:**
> কল্পনা করুন আপনি একটি গেমে প্লেয়ারের অবস্থান ট্র্যাক করছেন। প্লেয়ার যখন নড়ে, তখন আপনি সবসময় x এবং y দুটোই আপডেট করেন। এগুলো একসাথে রাখা সহজ!

**খারাপ উদাহরণ (আলাদা স্টেট):**
```js
const [x, setX] = useState(0);
const [y, setY] = useState(0);

function movePlayer(newX, newY) {
  setX(newX);
  setY(newY);
}
```
যদি আপনি একটি আপডেট করতে ভুলে যান, আপনার স্টেট সিঙ্কের বাইরে চলে যাবে! 😬

**ভালো উদাহরণ (একত্রিত স্টেট):**
```js
const [position, setPosition] = useState({ x: 0, y: 0 });

function movePlayer(newX, newY) {
  setPosition({ x: newX, y: newY });
}
```
এখন, আপনি সবসময় দুটোই একসাথে আপডেট করছেন। অনেক নিরাপদ! 🛡️

---

## 🚫 ২. স্টেটে বিরোধিতা এড়িয়ে চলুন

আপনার স্টেট ভেরিয়েবলগুলো যদি একে অপরের সাথে "বিরোধ" না করে! বিরোধপূর্ণ স্টেট বাগ তৈরি করতে পারে যা খুঁজে পাওয়া কঠিন।

**বাস্তব জীবনের উদাহরণ:**
> কল্পনা করুন একটি ট্রাফিক লাইট যা একই সাথে সবুজ এবং লাল। এটা একটি বিরোধ! ❌

**খারাপ উদাহরণ:**
```js
const [isSending, setIsSending] = useState(false);
const [isSent, setIsSent] = useState(false);

// ওহ! সাবধান না থাকলে দুটোই একসাথে true হতে পারে
```

**ভালো উদাহরণ:**
```js
const [status, setStatus] = useState('idle'); // 'idle', 'sending', বা 'sent'

// এখন, একবারে শুধু একটি স্ট্যাটাস true হতে পারে!
```

---

## 🔄 ৩. অপ্রয়োজনীয় স্টেট এড়িয়ে চলুন

যদি আপনি props বা অন্য স্টেট থেকে কিছু হিসাব করতে পারেন, তাহলে সেটা আলাদাভাবে স্টোর করবেন না! অপ্রয়োজনীয় স্টেট সিঙ্কের বাইরে চলে যেতে পারে এবং বাগ তৈরি করতে পারে।

**বাস্তব জীবনের উদাহরণ:**
> যদি আপনি কারো জন্ম সাল এবং বর্তমান সাল জানেন, তাহলে তার বয়স স্টোর করার দরকার নেই—আপনি সেটা হিসাব করতে পারেন! 🎂

**খারাপ উদাহরণ:**
```js
const [age, setAge] = useState(currentYear - birthYear); // অপ্রয়োজনীয়!
```

**ভালো উদাহরণ:**
```js
const age = currentYear - birthYear; // দরকার হলে হিসাব করুন
```

**আরেকটি উদাহরণ:**
```js
const [firstName, setFirstName] = useState('Ada');
const [lastName, setLastName] = useState('Lovelace');
// ❌ fullName স্টেটে স্টোর করবেন না
const fullName = firstName + ' ' + lastName; // ✅ শুধু হিসাব করুন
```

---

## 📦 ৪. স্টেটে ডুপ্লিকেশন এড়িয়ে চলুন

একই ডেটা একাধিক জায়গায় স্টোর করবেন না। ডুপ্লিকেটেড স্টেট সিঙ্ক রাখা কঠিন এবং বিভ্রান্তি তৈরি করতে পারে। 🤹‍♂️

**বাস্তব জীবনের উদাহরণ:**
> কল্পনা করুন আপনি আপনার বন্ধুদের দুটি আলাদা লিস্ট রাখছেন। যদি আপনি একটি লিস্টে বন্ধু যোগ করেন কিন্তু অন্যটিতে ভুলে যান, তাহলে তারা সিঙ্কের বাইরে চলে যাবে!

**খারাপ উদাহরণ:**
```js
const [items, setItems] = useState(['apple', 'banana']);
const [itemCount, setItemCount] = useState(2); // ডুপ্লিকেটেড!

function addItem(item) {
  setItems([...items, item]);
  setItemCount(itemCount + 1); // ভুলে যাওয়া সহজ!
}
```

**ভালো উদাহরণ:**
```js
const [items, setItems] = useState(['apple', 'banana']);
const itemCount = items.length; // সবসময় সঠিক!
```

---

## 🌳 ৫. গভীরভাবে নেস্টেড স্টেট এড়িয়ে চলুন

গভীরভাবে নেস্টেড স্টেট (যেমন অবজেক্টের ভিতরে অবজেক্টের ভিতরে অ্যারে) আপডেট এবং মেইনটেইন করা কঠিন। আপনার স্টেট যতটা সম্ভব ফ্ল্যাট রাখার চেষ্টা করুন! 🏞️

**বাস্তব জীবনের উদাহরণ:**
> কল্পনা করুন রাশিয়ান ডলসের একটি সেট। যদি আপনি সবচেয়ে ছোটটি পরিবর্তন করতে চান, তাহলে আগে বাকি সবগুলো খুলতে হবে! 🪆

**খারাপ উদাহরণ (গভীর নেস্টিং):**
```js
const [state, setState] = useState({
  user: {
    profile: {
      name: '',
      age: 0
    }
  }
});

// নাম আপডেট করা কঠিন:
setState(prev => ({
  ...prev,
  user: {
    ...prev.user,
    profile: {
      ...prev.user.profile,
      name: 'Grace'
    }
  }
}));
```

**ভালো উদাহরণ (ফ্ল্যাট স্টেট):**
```js
const [profile, setProfile] = useState({ name: '', age: 0 });
// আপডেট করা অনেক সহজ!
setProfile(prev => ({ ...prev, name: 'Grace' }));
```

---

## 🧠 সারসংক্ষেপ

- 🧩 সম্পর্কিত স্টেট একসাথে রাখুন যদি তারা সবসময় একসাথে পরিবর্তন হয়।
- 🚦 একটি স্ট্যাটাস ভেরিয়েবল ব্যবহার করে বিরোধপূর্ণ স্টেট এড়িয়ে চলুন।
- 🔄 যা হিসাব করা যায়, তা স্টোর করবেন না।
- 📦 স্টেটে ডেটা ডুপ্লিকেট করবেন না।
- 🌳 আপনার স্টেট যতটা সম্ভব ফ্ল্যাট রাখুন।
- 🏆 ভালোভাবে স্ট্রাকচার করা স্টেট = কম বাগ এবং সহজ কোড!

---

## 📚 আরও জানুন

আরও বিস্তারিত এবং উদাহরণের জন্য, [অফিসিয়াল React ডকুমেন্টেশন](https://react.dev/learn/choosing-the-state-structure#group-related-state) দেখুন।

---

## 🧮 ডেরাইভড স্টেট (Derived State)

**ডেরাইভড স্টেট** হচ্ছে এমন কোনো মান, যেটা আপনি props বা অন্য state থেকে হিসাব করতে পারেন, আলাদা state variable-এ না রেখে।

**কেন ডেরাইভড স্টেট আলাদা করে রাখা উচিত নয়?**
- এটি মূল ডেটা থেকে আলাদা হয়ে যেতে পারে, ফলে বাগ হতে পারে।
- যখন দরকার, তখন হিসাব করলে কোড সহজ ও কম ভুল হয়।

**উদাহরণ:**
```js
const [firstName, setFirstName] = useState('Ada');
const [lastName, setLastName] = useState('Lovelace');
// ❌ এটা করবেন না:
// const [fullName, setFullName] = useState(firstName + ' ' + lastName);
// ✅ এটা করুন:
const fullName = firstName + ' ' + lastName;
```
এখানে `fullName` হচ্ছে ডেরাইভড স্টেট, কারণ এটি `firstName` ও `lastName` থেকে হিসাব করা।

**সেরা অভ্যাস:**
> যেসব মান অন্য state বা props থেকে হিসাব করা যায়, সেগুলো আলাদা করে state-এ রাখবেন না। দরকার হলে render logic-এ হিসাব করুন! 🧠

---

## 🔄 Props Mirroring

**Props mirroring** (বা "mirrored state") হচ্ছে যখন কোনো child component তার state-এ parent থেকে পাওয়া prop-এর মান কপি করে রাখে।

**কেন সাধারণত এটি এড়ানো উচিত?**
- চাইল্ডের state প্যারেন্টের prop থেকে আলাদা হয়ে যেতে পারে।
- ডেটার "source of truth" নিয়ে বিভ্রান্তি তৈরি হয়।

**কখন props mirroring করা যায়?**
- যখন শুধু একবার prop-এর মান দরকার (যেমন initial value), এরপর চাইল্ড নিজে সেটি ম্যানেজ করবে।
- যখন চাইল্ডের state প্যারেন্টের prop-এর সাথে sync রাখতে হবে না।

**উদাহরণ:**
```js
function Child({ initialValue }) {
  // শুধু initial value সেট করতে prop ব্যবহার
  const [value, setValue] = useState(initialValue);
  // এরপর value চাইল্ড নিজে ম্যানেজ করবে
  // initialValue পরিবর্তন হলেও, value আর আপডেট হবে না
}
```

**ভুল উদাহরণ (sync করার জন্য mirroring):**
```js
function Child({ value }) {
  // ❌ এটা ভুল! প্যারেন্টের value বদলালেও এখানে আপডেট হবে না
  const [localValue, setLocalValue] = useState(value);
}
```

**সেরা অভ্যাস:**
> চাইল্ডকে সবসময় প্যারেন্টের prop reflect করতে চাইলে, state-এ কপি না করে সরাসরি prop ব্যবহার করুন। শুধু initial value দরকার হলে mirroring করা যায়।

**সারসংক্ষেপ টেবিল:**

| পরিস্থিতি                                              | Props mirror করবেন? |
|--------------------------------------------------------|---------------------|
| প্যারেন্টের সাথে sync রাখতে হবে                         | ❌ না               |
| শুধু initial value দরকার, এরপর নিজে ম্যানেজ করবে        | ✅ হ্যাঁ             |
| প্যারেন্ট থেকে চাইল্ড কন্ট্রোল করতে চাই                 | ❌ না               |

---
