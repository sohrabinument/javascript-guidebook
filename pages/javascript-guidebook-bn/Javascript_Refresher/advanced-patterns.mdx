# জাভাস্ক্রিপ্ট প্যাটার্ন মাস্টারি 🚀

আপনার জাভাস্ক্রিপ্ট দক্ষতাকে ভালো থেকে অসাধারণে রূপান্তর করুন! এই গাইড আপনাকে বিশ্বব্যাপী পেশাদার ডেভেলপারদের দ্বারা ব্যবহৃত সবচেয়ে শক্তিশালী কোডিং প্যাটার্নগুলি শেখাবে। প্রতিটি প্যাটার্নে সহজ ব্যাখ্যা, ভিজ্যুয়াল উদাহরণ এবং বাস্তব-বিশ্বের ব্যবহারের ক্ষেত্র অন্তর্ভুক্ত।

> 💡 **যাদের জন্য উপযুক্ত:** প্রাথমিক থেকে উন্নত ডেভেলপার, ইংরেজি-নন-নেটিভ স্পিকার, ইন্টারভিউ প্রস্তুতি, এবং প্রোডাকশন-রেডি অ্যাপ্লিকেশন তৈরি।

---

## 🎯 কেন এই প্যাটার্নগুলি শিখবেন?

| সুবিধা                | এর অর্থ                                                       | বাস্তব উদাহরণ                                          |
| --------------------- | ------------------------------------------------------------- | ------------------------------------------------------ |
| **পরিষ্কার কোড**      | সহজে পড়া এবং বোঝা যায় এমন কোড লিখুন                         | আপনার সহকর্মী ৬ মাস পরেও আপনার কোড পরিবর্তন করতে পারবে |
| **কম বাগ**            | সাধারণ ভুলগুলি ঘটার আগেই প্রতিরোধ করুন                        | আর "undefined is not a function" এরর নেই               |
| **দ্রুত ডেভেলপমেন্ট** | নতুন করে আবিষ্কার না করে প্রমাণিত সমাধান পুনরায় ব্যবহার করুন | আপনার নিজের পরীক্ষিত মডিউল কপি-পেস্ট করুন              |
| **ক্যারিয়ার বৃদ্ধি** | সিনিয়র ডেভেলপারদের ভাষায় কথা বলুন                           | আত্মবিশ্বাসের সাথে টেকনিক্যাল ইন্টারভিউ পাস করুন       |

---

## ১. মডিউল প্যাটার্ন 🗂️

### 📝 সহজ ব্যাখ্যা

ফোল্ডারে ফাইলগুলির মতো আপনার কোড সংগঠিত রাখুন। ব্যক্তিগত বিস্তারিত লুকিয়ে রাখুন, শুধুমাত্র অন্যদের প্রয়োজনীয় অংশ দেখান।

### 🌍 বাস্তব-বিশ্বের উদাহরণ

একটি **ব্যাংক অ্যাকাউন্ট** এর কথা ভাবুন:

- আপনি ব্যালেন্স চেক করতে পারেন ✅
- আপনি টাকা জমা দিতে পারেন ✅
- আপনি সরাসরি ব্যালেন্স পরিবর্তন করতে পারবেন না ❌

### 💻 কোড উদাহরণ

```js
// ✅ ভালো: মডিউল প্যাটার্ন
const BankAccount = (function () {
  let balance = 0; // 🔒 ব্যক্তিগত - কেউ সরাসরি এটি স্পর্শ করতে পারবে না

  return {
    // 🌐 পাবলিক মেথড - সবার জন্য উপলব্ধ
    deposit(amount) {
      if (amount > 0) {
        balance += amount;
        return `Deposited $${amount}. New balance: $${balance}`;
      }
      return "Amount must be positive";
    },

    withdraw(amount) {
      if (amount <= balance) {
        balance -= amount;
        return `Withdrew $${amount}. Remaining balance: $${balance}`;
      }
      return "Insufficient funds";
    },

    getBalance() {
      return balance;
    },
  };
})();

// ব্যবহার
console.log(BankAccount.deposit(100)); // "Deposited $100. New balance: $100"
console.log(BankAccount.withdraw(30)); // "Withdrew $30. Remaining balance: $70"
console.log(BankAccount.getBalance()); // 70

// ❌ এটি কাজ করবে না - ব্যালেন্স ব্যক্তিগত
console.log(BankAccount.balance); // undefined
```

### 🏷️ ক্লাস উদাহরণ

```js
// ক্লাস ব্যবহার করে মডিউল লজিক এনক্যাপসুলেট করা
class BankAccount {
  #balance = 0; // ব্যক্তিগত ফিল্ড (ES2022+)

  deposit(amount) {
    if (amount > 0) {
      this.#balance += amount;
      return `Deposited $${amount}. New balance: $${this.#balance}`;
    }
    return "Amount must be positive";
  }

  withdraw(amount) {
    if (amount <= this.#balance) {
      this.#balance -= amount;
      return `Withdrew $${amount}. Remaining balance: $${this.#balance}`;
    }
    return "Insufficient funds";
  }

  getBalance() {
    return this.#balance;
  }
}

// ব্যবহার
const myAccount = new BankAccount();
console.log(myAccount.deposit(100)); // Deposited $100. New balance: $100
console.log(myAccount.withdraw(30)); // Withdrew $30. Remaining balance: $70
console.log(myAccount.getBalance()); // 70
// console.log(myAccount.#balance); // ❌ এরর: ব্যক্তিগত ফিল্ড
```

> **ব্যাখ্যা:**
>
> - `#balance` ফিল্ডটি ব্যক্তিগত—শুধুমাত্র ক্লাসের ভিতরে অ্যাক্সেস করা যায়।
> - `deposit`, `withdraw`, এবং `getBalance` মেথডগুলি পাবলিক।
> - এটি এনক্যাপসুলেশন (মডিউলের মতো) অর্জনের একটি আধুনিক, ক্লাস-ভিত্তিক উপায়।

### 🚨 সাধারণ ভুলগুলি

```js
// ❌ ভুল - অবজেক্ট রিটার্ন করতে ভুলে গেছেন
const BadModule = (function () {
  let data = "secret";
  // রিটার্ন স্টেটমেন্ট নেই!
})();

// ❌ ভুল - ব্যক্তিগত ভেরিয়েবল এক্সপোজ করা হয়েছে
const BadModule2 = (function () {
  let secret = "password";
  return {
    secret, // এটি করবেন না!
    getSecret() {
      return secret;
    },
  };
})();
```

> **ব্যাখ্যা:**
>
> - প্রথম উদাহরণে, কিছুই রিটার্ন করা হয়নি, তাই আপনি মডিউলটি ব্যবহার করতে পারবেন না।
> - দ্বিতীয়টিতে, ব্যক্তিগত ভেরিয়েবল `secret` এক্সপোজ করা হয়েছে, যা মডিউল ব্যবহারের উদ্দেশ্যকে পরাজিত করে।

### ✅ কখন ব্যবহার করবেন

- ক্যালকুলেটর তৈরি করা
- শপিং কার্ট তৈরি করা
- ইউজার অথেনটিকেশন ম্যানেজ করা
- যখনই ব্যক্তিগত ভেরিয়েবল প্রয়োজন

---

## ২. রিভিলিং মডিউল প্যাটার্ন 🔍

### 📝 সহজ ব্যাখ্যা

প্রথমে সব ফাংশন লিখুন, তারপর সিদ্ধান্ত নিন কোনগুলি বিশ্বের সাথে শেয়ার করবেন।

### 🌍 বাস্তব-বিশ্বের উদাহরণ

একটি **রেস্টুরেন্টের রান্নাঘর** এর মতো: শেফরা অনেক টুল এবং ধাপ ব্যবহার করে খাবার প্রস্তুত করেন, কিন্তু গ্রাহকরা শুধুমাত্র মেনুতে চূড়ান্ত খাবারগুলি দেখেন।

### 💻 কোড উদাহরণ

```js
const Calculator = (function () {
  // 🔒 সব ফাংশন ডিফল্টভাবে ব্যক্তিগত
  function add(a, b) {
    return a + b;
  }

  function subtract(a, b) {
    return a - b;
  }

  function multiply(a, b) {
    return a * b;
  }

  function divide(a, b) {
    if (b === 0) {
      throw new Error("Cannot divide by zero");
    }
    return a / b;
  }

  // 🔒 ব্যক্তিগত হেল্পার ফাংশন
  function validateNumbers(a, b) {
    return typeof a === "number" && typeof b === "number";
  }

  // 🌐 শুধুমাত্র যা প্রকাশ করতে চান তা প্রকাশ করুন
  return {
    add, // প্রকাশিত ✅
    subtract, // প্রকাশিত ✅
    multiply, // প্রকাশিত ✅
    divide, // প্রকাশিত ✅
    // validateNumbers ব্যক্তিগত থাকবে 🔒
  };
})();

// ব্যবহার
console.log(Calculator.add(5, 3)); // 8
console.log(Calculator.multiply(4, 7)); // 28
console.log(Calculator.validateNumbers); // undefined (ব্যক্তিগত)
```

> **ব্যাখ্যা:**
>
> - সব গণিত ফাংশন ডিফল্টভাবে ব্যক্তিগত, তাই আপনি ঠিক কী প্রকাশ করবেন তা নিয়ন্ত্রণ করতে পারেন।
> - শুধুমাত্র যা ফাংশনগুলি আপনি রিটার্ন করেন (`add`, `subtract`, `multiply`, `divide`) সেগুলি ব্যবহারকারীদের জন্য উপলব্ধ।
> - হেল্পার ফাংশন `validateNumbers` লুকানো থাকে, ঠিক যেমন একটি রেস্টুরেন্টের রান্নাঘরে শেফের গোপন রেসিপি!

### 🆚 মডিউল বনাম রিভিলিং মডিউল

| মডিউল প্যাটার্ন                              | রিভিলিং মডিউল প্যাটার্ন                            |
| -------------------------------------------- | -------------------------------------------------- |
| রিটার্ন অবজেক্টে পাবলিক মেথড সংজ্ঞায়িত করুন | প্রথমে সব ফাংশন সংজ্ঞায়িত করুন, তারপর প্রকাশ করুন |
| ব্যক্তিগত এবং পাবলিক সংজ্ঞা মিশ্রিত করুন     | কনসার্নের স্পষ্ট বিভাজন                            |
| পড়া কঠিন হতে পারে                           | খুব স্পষ্ট কাঠামো                                  |

---

## ৩. সিঙ্গেলটন প্যাটার্ন 👑

### 📝 সহজ ব্যাখ্যা

আপনার পুরো অ্যাপ্লিকেশনে শুধুমাত্র একটি ইনস্ট্যান্স থাকবে তা নিশ্চিত করুন।

### 🌍 বাস্তব-বিশ্বের উদাহরণ

একটি **দেশের রাষ্ট্রপতি** এর মতো: আপনি যতবারই জিজ্ঞাসা করুন "রাষ্ট্রপতি কে?", একই উত্তর পাবেন - একই সময়ে শুধুমাত্র একজন রাষ্ট্রপতি থাকতে পারেন।

### 💻 কোড উদাহরণ

```js
const AppConfig = (function () {
  let instance = null;
  let config = {};

  function createInstance() {
    return {
      set(key, value) {
        config[key] = value;
      },
      get(key) {
        return config[key];
      },
      getAll() {
        return { ...config }; // মূল অবজেক্টের কপি রিটার্ন করুন
      },
    };
  }

  return {
    getInstance() {
      if (instance === null) {
        instance = createInstance();
        console.log("🔥 নতুন কনফিগ ইনস্ট্যান্স তৈরি করা হয়েছে");
      } else {
        console.log("♻️ বিদ্যমান কনফিগ ইনস্ট্যান্স রিটার্ন করা হচ্ছে");
      }
      return instance;
    },
  };
})();

// ব্যবহার
const config1 = AppConfig.getInstance(); // 🔥 নতুন কনফিগ ইনস্ট্যান্স তৈরি করা হয়েছে
const config2 = AppConfig.getInstance(); // ♻️ বিদ্যমান কনফিগ ইনস্ট্যান্স রিটার্ন করা হচ্ছে

config1.set("theme", "dark");
config1.set("language", "en");

console.log(config2.get("theme")); // "dark"
console.log(config1 === config2); // true - একই ইনস্ট্যান্স!
```

### 🏷️ ক্লাস উদাহরণ

```js
class AppConfig {
  static #instance;
  #config = {};

  constructor() {
    if (AppConfig.#instance) {
      return AppConfig.#instance;
    }
    AppConfig.#instance = this;
  }

  set(key, value) {
    this.#config[key] = value;
  }

  get(key) {
    return this.#config[key];
  }

  getAll() {
    return { ...this.#config };
  }
}

// ব্যবহার
const configA = new AppConfig();
const configB = new AppConfig();
configA.set("theme", "dark");
console.log(configB.get("theme")); // 'dark'
console.log(configA === configB); // true
```

> **ব্যাখ্যা:**
>
> - আপনি যতবারই `getInstance` কল করুন না কেন, আপনি সবসময় একই অবজেক্ট পাবেন।
> - এটি একটি দেশে শুধুমাত্র একজন রাষ্ট্রপতি থাকার মতো—আপনার অ্যাপের কনফিগারেশনের জন্য শুধুমাত্র একটি সত্যের উৎস আছে।
> - একটি রিফারেন্সের মাধ্যমে করা যেকোনো পরিবর্তন সর্বত্র প্রতিফলিত হয়।

### 🎯 উপযুক্ত ব্যবহারের ক্ষেত্র

- অ্যাপ্লিকেশন সেটিংস/কনফিগারেশন
- ডাটাবেস কানেকশন
- লগিং সার্ভিস
- ই-কমার্সে শপিং কার্ট
- ইউজার অথেনটিকেশন স্টেট

### ⚠️ সতর্কতা

সিঙ্গেলটন টেস্টিং কঠিন করে তুলতে পারে। শুধুমাত্র যখন আপনার সত্যিই একটি ইনস্ট্যান্স প্রয়োজন তখনই এটি ব্যবহার করুন।

---

## ৪. ফ্যাক্টরি প্যাটার্ন 🏭

### 📝 সহজ ব্যাখ্যা

একটি ফাংশন যা আপনার চাহিদা অনুযায়ী বিভিন্ন ধরনের অবজেক্ট তৈরি করে।

### 🌍 বাস্তব-বিশ্বের উদাহরণ

একটি **গাড়ির কারখানা** এর মতো: আপনি স্পেসিফিকেশন দিন (SUV, সেডান, ট্রাক), এবং এটি আপনার জন্য সঠিক ধরনের গাড়ি তৈরি করবে।

### 💻 কোড উদাহরণ

```js
// 🏭 ভেহিকল ফ্যাক্টরি
function VehicleFactory(type, options = {}) {
  const vehicles = {
    car: {
      type: "Car",
      wheels: 4,
      doors: options.doors || 4,
      start() {
        return `🚗 Car engine started!`;
      },
      honk() {
        return `🔊 Beep beep!`;
      },
    },

    motorcycle: {
      type: "Motorcycle",
      wheels: 2,
      doors: 0,
      start() {
        return `🏍️ Motorcycle engine roared!`;
      },
      honk() {
        return `🔊 Vroom vroom!`;
      },
    },

    truck: {
      type: "Truck",
      wheels: options.wheels || 6,
      doors: options.doors || 2,
      start() {
        return `🚛 Truck engine rumbled!`;
      },
      honk() {
        return `🔊 HOOOONK!`;
      },
    },
  };

  const vehicle = vehicles[type];
  if (!vehicle) {
    throw new Error(`❌ Vehicle type "${type}" not supported`);
  }

  return {
    ...vehicle,
    info() {
      return `${this.type} with ${this.wheels} wheels and ${this.doors} doors`;
    },
  };
}

// ব্যবহার
const myCar = VehicleFactory("car", { doors: 2 });
const myBike = VehicleFactory("motorcycle");
const myTruck = VehicleFactory("truck", { wheels: 8 });

console.log(myCar.info()); // "Car with 4 wheels and 2 doors"
console.log(myCar.start()); // "🚗 Car engine started!"
console.log(myBike.honk()); // "🔊 Vroom vroom!"
```

### 🏷️ ক্লাস উদাহরণ

```js
class Vehicle {
  constructor(type, options = {}) {
    this.type = type;
    this.wheels =
      options.wheels || (type === "car" ? 4 : type === "motorcycle" ? 2 : 6);
    this.doors =
      options.doors || (type === "car" ? 4 : type === "truck" ? 2 : 0);
  }

  start() {
    return `${this.type} engine started!`;
  }

  info() {
    return `${this.type} with ${this.wheels} wheels and ${this.doors} doors`;
  }
}

class VehicleFactory {
  static createVehicle(type, options) {
    return new Vehicle(type, options);
  }
}

// ব্যবহার
const car = VehicleFactory.createVehicle("car", { doors: 2 });
const bike = VehicleFactory.createVehicle("motorcycle");
console.log(car.info()); // car with 4 wheels and 2 doors
console.log(bike.start()); // motorcycle engine started!
```

> **ব্যাখ্যা:**
>
> - ফ্যাক্টরি ফাংশন আপনার দেওয়া `type` অনুযায়ী বিভিন্ন ধরনের ভেহিকল তৈরি করে।
> - প্রতিটি ভেহিকলের নিজস্ব মেথড এবং প্রপার্টি আছে, ঠিক যেমন একটি বাস্তব গাড়ি, মোটরসাইকেল, বা ট্রাক।
> - এই প্যাটার্নটি ছোট পার্থক্য সহ অনেকগুলি অনুরূপ অবজেক্ট তৈরি করার জন্য দুর্দান্ত।

### 🎯 কখন ফ্যাক্টরি প্যাটার্ন ব্যবহার করবেন

- UI কম্পোনেন্ট তৈরি করা (বাটন, মোডাল, ফর্ম)
- বিভিন্ন ধরনের ইউজার তৈরি করা (অ্যাডমিন, সাধারণ, অতিথি)
- বিভিন্ন ফাইল ফরম্যাট তৈরি করা (PDF, Excel, Word)
- বিভিন্ন ক্ষমতা সহ গেম ক্যারেক্টার তৈরি করা

---

## ৫. অবজার্ভার প্যাটার্ন 👀

### 📝 সহজ ব্যাখ্যা

যখন কিছু আকর্ষণীয় ঘটে, স্বয়ংক্রিয়ভাবে সবাইকে জানিয়ে দিন যারা জানতে চায়।

### 🌍 বাস্তব-বিশ্বের উদাহরণ

**ইউটিউব সাবস্ক্রিপশন** এর মতো: যখন আপনার প্রিয় চ্যানেল একটি ভিডিও আপলোড করে, ইউটিউব স্বয়ংক্রিয়ভাবে সব সাবস্ক্রাইবারকে জানিয়ে দেয়।

### 💻 কোড উদাহরণ

```js
class NewsChannel {
  constructor(name) {
    this.name = name;
    this.subscribers = [];
  }

  // 📝 নোটিফিকেশন পেতে সাবস্ক্রাইব করুন
  subscribe(observer) {
    this.subscribers.push(observer);
    console.log(`📺 ${observer.name} subscribed to ${this.name}`);
  }

  // 🚫 নোটিফিকেশন বন্ধ করুন
  unsubscribe(observer) {
    this.subscribers = this.subscribers.filter((sub) => sub !== observer);
    console.log(`📺 ${observer.name} unsubscribed from ${this.name}`);
  }

  // 📢 সবাইকে ব্রেকিং নিউজ জানান
  publishNews(news) {
    console.log(`\n🔥 BREAKING: ${news}`);
    this.subscribers.forEach((subscriber) => {
      subscriber.notify(news, this.name);
    });
  }
}

class Subscriber {
  constructor(name) {
    this.name = name;
  }

  notify(news, channelName) {
    console.log(`📱 ${this.name} received: "${news}" from ${channelName}`);
  }
}

// ব্যবহার
const techNews = new NewsChannel("Tech Daily");
const alice = new Subscriber("Alice");
const bob = new Subscriber("Bob");
const charlie = new Subscriber("Charlie");

// নোটিফিকেশনে সাবস্ক্রাইব করুন
techNews.subscribe(alice);
techNews.subscribe(bob);
techNews.subscribe(charlie);

// নিউজ প্রকাশ করুন - সবাই নোটিফিকেশন পাবে!
techNews.publishNews("New iPhone released!");
techNews.publishNews("JavaScript 2024 features announced!");

// Bob আনসাবস্ক্রাইব করল
techNews.unsubscribe(bob);
techNews.publishNews("React 19 is here!"); // শুধুমাত্র Alice এবং Charlie এটি পাবে
```

### 🏷️ ক্লাস উদাহরণ

```js
class Subject {
  constructor() {
    this.observers = [];
  }

  subscribe(observer) {
    this.observers.push(observer);
  }

  unsubscribe(observer) {
    this.observers = this.observers.filter((obs) => obs !== observer);
  }

  notify(data) {
    this.observers.forEach((observer) => observer.update(data));
  }
}

class Observer {
  constructor(name) {
    this.name = name;
  }

  update(data) {
    console.log(`${this.name} received: ${data}`);
  }
}

// ব্যবহার
const news = new Subject();
const alice = new Observer("Alice");
const bob = new Observer("Bob");
news.subscribe(alice);
news.subscribe(bob);
news.notify("New JavaScript release!");
// Alice received: New JavaScript release!
// Bob received: New JavaScript release!
```

> **ব্যাখ্যা:**
>
> - সাবস্ক্রাইবাররা নিউজ চ্যানেল থেকে আপডেট পেতে সাইন আপ করে।
> - যখন চ্যানেল নিউজ প্রকাশ করে, সব সাবস্ক্রাইবার স্বয়ংক্রিয়ভাবে নোটিফিকেশন পায়।
> - এটি বাস্তব জীবনে ইউটিউব বা টুইটার নোটিফিকেশনের মতো!

### 🎯 বাস্তব-বিশ্বের অ্যাপ্লিকেশন

- **সোশ্যাল মিডিয়া** নোটিফিকেশন
- **ই-কমার্স** মূল্য সতর্কতা
- **স্টক মার্কেট** আপডেট
- **গেমিং** অ্যাচিভমেন্ট সিস্টেম
- **ইমেইল** নিউজলেটার সিস্টেম

---

## ৬. ডিবাউন্স এবং থ্রটল ⏳

### 📝 সহজ ব্যাখ্যা

ফাংশনগুলি কতবার চালানো হবে তা নিয়ন্ত্রণ করুন পারফরম্যান্স এবং ইউজার এক্সপেরিয়েন্স উন্নত করার জন্য।

### 🌍 বাস্তব-বিশ্বের উদাহরণ

**ডিবাউন্স** = **লিফট**: শেষ ব্যক্তি বাটন চাপার পর কয়েক সেকেন্ড অপেক্ষা করে তারপর চলে।

**থ্রটল** = **ট্রাফিক লাইট**: কতগুলি গাড়ি আসুক না কেন, এটি প্রতি ৩০ সেকেন্ডে একবার পরিবর্তন হয়।

### 💻 ডিবাউন্স উদাহরণ

```js
// 🔍 ডিবাউন্স দিয়ে সার্চ
function debounce(func, delay) {
  let timeoutId;
  return function (...args) {
    // পূর্ববর্তী টাইমার বাতিল করুন
    clearTimeout(timeoutId);

    // নতুন টাইমার শুরু করুন
    timeoutId = setTimeout(() => {
      func.apply(this, args);
    }, delay);
  };
}

// ব্যয়বহুল সার্চ ফাংশন সিমুলেট করুন
function searchAPI(query) {
  console.log(`🔍 Searching for: "${query}"`);
  // কল্পনা করুন এটি একটি API কল করে
}

const debouncedSearch = debounce(searchAPI, 500); // 500ms অপেক্ষা করুন

// ইউজার টাইপিং সিমুলেট করুন
console.log("User starts typing...");
debouncedSearch("j"); // বাতিল করা হয়েছে
debouncedSearch("ja"); // বাতিল করা হয়েছে
debouncedSearch("jav"); // বাতিল করা হয়েছে
debouncedSearch("java"); // বাতিল করা হয়েছে
debouncedSearch("javascript"); // ✅ টাইপিং বন্ধ হওয়ার 500ms পর এটি চলে

setTimeout(() => {
  console.log("User continues typing...");
  debouncedSearch("javascrip"); // বাতিল করা হয়েছে
  debouncedSearch("javascript patterns"); // ✅ এটি চলে
}, 2000);
```

### 💻 থ্রটল উদাহরণ

```js
// 📊 থ্রটল দিয়ে স্ক্রল প্রগ্রেস
function throttle(func, limit) {
  let inThrottle;
  return function (...args) {
    if (!inThrottle) {
      func.apply(this, args);
      inThrottle = true;
      setTimeout(() => (inThrottle = false), limit);
    }
  };
}

function updateScrollProgress() {
  const scrolled =
    (window.scrollY / (document.body.scrollHeight - window.innerHeight)) * 100;
  console.log(`📊 Scroll progress: ${Math.round(scrolled)}%`);
}

const throttledScroll = throttle(updateScrollProgress, 250); // প্রতি 250ms এ সর্বোচ্চ একবার

// পেজে যোগ করুন (টেস্ট করতে আনকমেন্ট করুন)
// window.addEventListener('scroll', throttledScroll);
```

### 🏷️ ক্লাস উদাহরণ (ডিবাউন্স)

```js
class Debouncer {
  constructor(fn, delay) {
    this.fn = fn;
    this.delay = delay;
    this.timeout = null;
  }

  call(...args) {
    clearTimeout(this.timeout);
    this.timeout = setTimeout(() => this.fn(...args), this.delay);
  }
}

// ব্যবহার
const debouncedLog = new Debouncer((msg) => console.log(msg), 500);
debouncedLog.call("Hello");
debouncedLog.call("Hello again"); // শুধুমাত্র এটি 500ms পর চলে
```

### 🆚 ডিবাউন্স বনাম থ্রটল

| পরিস্থিতি         | ডিবাউন্স ব্যবহার করুন                                 | থ্রটল ব্যবহার করুন            |
| ----------------- | ----------------------------------------------------- | ----------------------------- |
| **সার্চ বক্স**    | ✅ ইউজার টাইপিং বন্ধ হওয়া পর্যন্ত অপেক্ষা করুন       | ❌ অনেকগুলি API কল            |
| **বাটন ক্লিক**    | ✅ ডাবল-ক্লিক প্রতিরোধ করুন                           | ❌ ক্লিক মিস হতে পারে         |
| **স্ক্রল ইভেন্ট** | ❌ ইউজার স্ক্রল চালিয়ে গেলে কখনও ফায়ার নাও হতে পারে | ✅ নিয়মিত আপডেট              |
| **রিসাইজ ইভেন্ট** | ✅ ইউজার রিসাইজ শেষ হওয়া পর্যন্ত অপেক্ষা করুন        | ❌ চূড়ান্ত সাইজ মিস হতে পারে |
| **মাউস মুভমেন্ট** | ❌ কখনও ফায়ার নাও হতে পারে                           | ✅ স্মুথ ট্র্যাকিং            |

### 🎯 বাস্তব-বিশ্বের অ্যাপ্লিকেশন

- **সোশ্যাল মিডিয়া** নোটিফিকেশন
- **ই-কমার্স** মূল্য সতর্কতা
- **স্টক মার্কেট** আপডেট
- **গেমিং** অ্যাচিভমেন্ট সিস্টেম
- **ইমেইল** নিউজলেটার সিস্টেম

---

## ৭. কারি এবং পার্শিয়াল অ্যাপ্লিকেশন 🍛

### 📝 সহজ ব্যাখ্যা

জটিল ফাংশনগুলিকে ছোট, পুনরায় ব্যবহারযোগ্য টুকরোতে ভেঙে ফেলুন।

### 🌍 বাস্তব-বিশ্বের উদাহরণ

একটি **স্যান্ডউইচ শপ** এর মতো: একটি বিশাল "যেকোনো স্যান্ডউইচ তৈরি করুন" মেশিনের পরিবর্তে, আপনার কাছে ব্রেড → মাংস → পনির → সবজির জন্য আলাদা স্টেশন আছে।

### 💻 কারি উদাহরণ

```js
// 🍛 সাধারণ ফাংশন
function traditionalMultiply(a, b, c) {
  return a * b * c;
}

// 🔗 কারি ভার্সন - একবারে একটি প্যারামিটার
function curriedMultiply(a) {
  return function (b) {
    return function (c) {
      return a * b * c;
    };
  };
}

// 🚀 আধুনিক অ্যারো ফাংশন ভার্সন
const multiply = (a) => (b) => (c) => a * b * c;

// ব্যবহারের উদাহরণ
console.log(traditionalMultiply(2, 3, 4)); // 24

// কারি - ধাপে ধাপে
const step1 = curriedMultiply(2); // 'b' এর জন্য অপেক্ষমান ফাংশন রিটার্ন করে
const step2 = step1(3); // 'c' এর জন্য অপেক্ষমান ফাংশন রিটার্ন করে
const result = step2(4); // চূড়ান্ত ফলাফল রিটার্ন করে: 24

// অথবা সব একসাথে চিয়ন করুন
console.log(curriedMultiply(2)(3)(4)); // 24
console.log(multiply(2)(3)(4)); // 24

// 🎯 আসল শক্তি - বিশেষায়িত ফাংশন তৈরি করুন
const double = multiply(2); // 2 দিয়ে গুণ করুন
const triple = multiply(3); // 3 দিয়ে গুণ করুন

const doubleAndSquare = double(4); // 2 * 4 * ?
const tripleAndSquare = triple(4); // 3 * 4 * ?

console.log(doubleAndSquare(5)); // 2 * 4 * 5 = 40
console.log(tripleAndSquare(5)); // 3 * 4 * 5 = 60
```

### 🎯 ব্যবহারিক অ্যাপ্লিকেশন

```js
// 🌐 API রিকোয়েস্ট বিল্ডার
const apiRequest = (method) => (url) => (data) => {
  return fetch(url, {
    method: method,
    headers: { "Content-Type": "application/json" },
    body: data ? JSON.stringify(data) : null,
  });
};

// বিশেষায়িত ফাংশন তৈরি করুন
const get = apiRequest("GET");
const post = apiRequest("POST");
const put = apiRequest("PUT");

// সেগুলি ব্যবহার করুন
const getUsers = get("/api/users");
const createUser = post("/api/users");

getUsers(); // GET /api/users
createUser({ name: "John" }); // POST /api/users with data

// 💰 ডিসকাউন্ট ক্যালকুলেটর
const discount = (percentage) => (price) => {
  return price - (price * percentage) / 100;
};

const tenPercentOff = discount(10);
const twentyPercentOff = discount(20);

console.log(tenPercentOff(100)); // 90
console.log(twentyPercentOff(100)); // 80
```

> **ব্যাখ্যা:**
>
> - কারি একটি ফাংশনকে ছোট ফাংশনে ভেঙে ফেলে, প্রতিটি একটি আর্গুমেন্ট নেয়।
> - এটি আপনাকে সহজেই বিশেষায়িত ফাংশন (যেমন `double` বা `triple`) তৈরি করতে দেয়।
> - এটি একটি স্যান্ডউইচ শপের মতো যেখানে আপনি প্রতিটি অর্ডারের জন্য ব্রেড বা সস আগে তৈরি করতে পারেন।

### 🎯 ব্যবহারিক অ্যাপ্লিকেশন

````js
// 🌐 API রিকোয়েস্ট বিল্ডার
const apiRequest = (method) => (url) => (data) => {
  return fetch(url, {
    method: method,
    headers: { "Content-Type": "application/json" },
    body: data ? JSON.stringify(data) : null,
  });
};

// বিশেষায়িত ফাংশন তৈরি করুন
const get = apiRequest("GET");
const post = apiRequest("POST");
const put = apiRequest("PUT");

// সেগুলি ব্যবহার করুন
const getUsers = get("/api/users");
const createUser = post("/api/users");

getUsers(); // GET /api/users
createUser({ name: "John" }); // POST /api/users with data

# জাভাস্ক্রিপ্ট ডিজাইন প্যাটার্ন গাইড 🚀

## 💰 ডিসকাউন্ট ক্যালকুলেটর
```js
const discount = (percentage) => (price) => {
  return price - (price * percentage) / 100;
};

const tenPercentOff = discount(10);
const twentyPercentOff = discount(20);

console.log(tenPercentOff(100)); // 90
console.log(twentyPercentOff(100)); // 80
````

### 🏷️ ক্লাস উদাহরণ

```js
// 🍛 ঐতিহ্যবাহী ফাংশন
function traditionalMultiply(a, b, c) {
  return a * b * c;
}

// 🔗 কারিড ভার্সন - একবারে একটি প্যারামিটার
function curriedMultiply(a) {
  return function (b) {
    return function (c) {
      return a * b * c;
    };
  };
}

// 🚀 আধুনিক অ্যারো ফাংশন ভার্সন
const multiply = (a) => (b) => (c) => a * b * c;

// ব্যবহারের উদাহরণ
console.log(traditionalMultiply(2, 3, 4)); // 24

// কারিড - ধাপে ধাপে
const step1 = curriedMultiply(2); // 'b' এর জন্য অপেক্ষায় থাকা ফাংশন রিটার্ন করে
const step2 = step1(3); // 'c' এর জন্য অপেক্ষায় থাকা ফাংশন রিটার্ন করে
const result = step2(4); // চূড়ান্ত ফলাফল রিটার্ন করে: 24

// অথবা সব একসাথে চেইন করুন
console.log(curriedMultiply(2)(3)(4)); // 24
console.log(multiply(2)(3)(4)); // 24

// 🎯 আসল শক্তি - বিশেষায়িত ফাংশন তৈরি করুন
const double = multiply(2); // ২ দিয়ে গুণ করুন
const triple = multiply(3); // ৩ দিয়ে গুণ করুন

const doubleAndSquare = double(4); // 2 * 4 * ?
const tripleAndSquare = triple(4); // 3 * 4 * ?

console.log(doubleAndSquare(5)); // 2 * 4 * 5 = 40
console.log(tripleAndSquare(5)); // 3 * 4 * 5 = 60
```

> **ব্যাখ্যা:**
>
> - কারিং একটি ফাংশনকে ছোট ফাংশনে ভেঙে দেয়, প্রতিটি একটি করে আর্গুমেন্ট নেয়।
> - এটি আপনাকে বিশেষায়িত ফাংশন (যেমন `double` বা `triple`) সহজে তৈরি করতে দেয়।
> - এটি একটি স্যান্ডউইচের দোকানের মতো যেখানে প্রতিটি স্টেশন একবারে একটি উপাদান যোগ করে।

### 🎯 ব্যবহারিক প্রয়োগ

```js
// 🌐 API রিকোয়েস্ট বিল্ডার
const apiRequest = (method) => (url) => (data) => {
  return fetch(url, {
    method: method,
    headers: { "Content-Type": "application/json" },
    body: data ? JSON.stringify(data) : null,
  });
};

// বিশেষায়িত ফাংশন তৈরি করুন
const get = apiRequest("GET");
const post = apiRequest("POST");
const put = apiRequest("PUT");

// এগুলো ব্যবহার করুন
const getUsers = get("/api/users");
const createUser = post("/api/users");

getUsers(); // GET /api/users
createUser({ name: "John" }); // POST /api/users ডেটা সহ

// 💰 ডিসকাউন্ট ক্যালকুলেটর
const discount = (percentage) => (price) => {
  return price - (price * percentage) / 100;
};

const tenPercentOff = discount(10);
const twentyPercentOff = discount(20);

console.log(tenPercentOff(100)); // 90
console.log(twentyPercentOff(100)); // 80
```

> **ব্যাখ্যা:**
>
> - কারিং আপনাকে API রিকোয়েস্ট বা ডিসকাউন্টের মতো জিনিসের জন্য পুনর্ব্যবহারযোগ্য, বিশেষায়িত ফাংশন তৈরি করতে দেয়।
> - আপনি একবার `get` বা `post` ফাংশন তৈরি করতে পারেন, তারপর সর্বত্র এটি পুনর্ব্যবহার করতে পারেন।
> - এটি একটি স্যান্ডউইচের দোকানের মতো যেখানে আপনি প্রতিটি অর্ডারের জন্য পূর্ব থেকে রুটি বা সস তৈরি করে রাখতে পারেন।

---

## 8. মেমোইজেশন 🧠

### 📝 সহজ ব্যাখ্যা

ব্যয়বহুল গণনার ফলাফল মনে রাখুন যাতে আপনাকে আবার সেগুলো করতে না হয়।

### 🌍 বাস্তব জগতের উপমা

গণিতের ছাত্রের **চিট শিটের** মতো: একবার যখন আপনি ৭ × ৮ = ৫৬ গণনা করেন, তখন আপনি এটি লিখে রাখেন এবং আর কখনো গণনা করেন না।

### 💻 মৌলিক মেমোইজেশন

```js
// 🧠 সাধারণ মেমোইজেশন ফাংশন
function memoize(fn) {
  const cache = new Map();

  return function (...args) {
    const key = JSON.stringify(args);

    if (cache.has(key)) {
      console.log(`💾 ক্যাশ হিট ${key} এর জন্য`);
      return cache.get(key);
    }

    console.log(`⚡ ${key} এর জন্য ফলাফল গণনা করা হচ্ছে`);
    const result = fn.apply(this, args);
    cache.set(key, result);
    return result;
  };
}

// 🔢 ব্যয়বহুল গণনার উদাহরণ
function fibonacci(n) {
  if (n <= 1) return n;
  return fibonacci(n - 1) + fibonacci(n - 2);
}

const memoizedFibonacci = memoize(fibonacci);

console.log(memoizedFibonacci(10)); // ⚡ গণনা হচ্ছে... (সময় লাগে)
console.log(memoizedFibonacci(10)); // 💾 ক্যাশ হিট! (তাৎক্ষণিক)
console.log(memoizedFibonacci(15)); // ⚡ গণনা হচ্ছে... (কিন্তু ক্যাশ করা মান পুনর্ব্যবহার করে)
```

### 💻 মেয়াদ সহ উন্নত মেমোইজেশন

```js
// 🕒 মেয়াদ সম্পন্ন মেমোইজেশন
function memoizeWithExpiry(fn, ttl = 60000) {
  // ডিফল্ট ১ মিনিট
  const cache = new Map();

  return function (...args) {
    const key = JSON.stringify(args);
    const now = Date.now();

    if (cache.has(key)) {
      const { result, timestamp } = cache.get(key);

      if (now - timestamp < ttl) {
        console.log(`💾 ${key} এর জন্য তাজা ক্যাশ হিট`);
        return result;
      } else {
        console.log(`🗑️ ${key} এর জন্য ক্যাশ মেয়াদ শেষ`);
        cache.delete(key);
      }
    }

    console.log(`⚡ ${key} এর জন্য তাজা ফলাফল গণনা করা হচ্ছে`);
    const result = fn.apply(this, args);
    cache.set(key, { result, timestamp: now });
    return result;
  };
}

// 🌐 API কল উদাহরণ
async function fetchUser(id) {
  console.log(`🌐 ইউজার ${id} এর জন্য API কল করা হচ্ছে`);
  // API বিলম্ব অনুকরণ
  await new Promise((resolve) => setTimeout(resolve, 1000));
  return { id, name: `ইউজার ${id}`, email: `user${id}@example.com` };
}

const memoizedFetchUser = memoizeWithExpiry(fetchUser, 30000); // ৩০ সেকেন্ড ক্যাশ

// ব্যবহার
memoizedFetchUser(1).then(console.log); // 🌐 API কল
memoizedFetchUser(1).then(console.log); // 💾 ক্যাশ হিট
```

### 🎯 নিখুঁত ব্যবহারের ক্ষেত্র

- **API রেসপন্স** যা প্রায়ই পরিবর্তন হয় না
- **জটিল গণনা** (গণিত, অ্যালগরিদম)
- **ডাটাবেস কোয়েরি** একই প্যারামিটার সহ
- **ইমেজ প্রসেসিং** ফলাফল
- **সার্চ রেজাল্ট** সাম্প্রতিক কোয়েরির জন্য

### 🏷️ ক্লাস উদাহরণ

```js
// 🧠 সাধারণ মেমোইজেশন ফাংশন
function memoize(fn) {
  const cache = new Map();

  return function (...args) {
    const key = JSON.stringify(args);

    if (cache.has(key)) {
      console.log(`💾 ক্যাশ হিট ${key} এর জন্য`);
      return cache.get(key);
    }

    console.log(`⚡ ${key} এর জন্য ফলাফল গণনা করা হচ্ছে`);
    const result = fn.apply(this, args);
    cache.set(key, result);
    return result;
  };
}

// 🔢 ব্যয়বহুল গণনার উদাহরণ
function fibonacci(n) {
  if (n <= 1) return n;
  return fibonacci(n - 1) + fibonacci(n - 2);
}

const memoizedFibonacci = memoize(fibonacci);

console.log(memoizedFibonacci(10)); // ⚡ গণনা হচ্ছে... (সময় লাগে)
console.log(memoizedFibonacci(10)); // 💾 ক্যাশ হিট! (তাৎক্ষণিক)
console.log(memoizedFibonacci(15)); // ⚡ গণনা হচ্ছে... (কিন্তু ক্যাশ করা মান পুনর্ব্যবহার করে)
```

> **ব্যাখ্যা:**
>
> - প্রথমবার যখন আপনি `memoizedFibonacci(10)` কল করেন, এটি সব কাজ করে এবং ফলাফল সংরক্ষণ করে।
> - পরের বার, এটি শুধু মেমরি থেকে উত্তর নিয়ে আসে—পুনর্গণনার প্রয়োজন নেই!
> - এটি একজন ছাত্রের মতো যে একটি কঠিন গণিত সমস্যার উত্তর লিখে রাখে যাতে তাকে আর কখনো সেটা সমাধান করতে না হয়।

### 💻 মেয়াদ সহ উন্নত মেমোইজেশন

```js
// 🕒 মেয়াদ সম্পন্ন মেমোইজেশন
function memoizeWithExpiry(fn, ttl = 60000) {
  // ডিফল্ট ১ মিনিট
  const cache = new Map();

  return function (...args) {
    const key = JSON.stringify(args);
    const now = Date.now();

    if (cache.has(key)) {
      const { result, timestamp } = cache.get(key);

      if (now - timestamp < ttl) {
        console.log(`💾 ${key} এর জন্য তাজা ক্যাশ হিট`);
        return result;
      } else {
        console.log(`🗑️ ${key} এর জন্য ক্যাশ মেয়াদ শেষ`);
        cache.delete(key);
      }
    }

    console.log(`⚡ ${key} এর জন্য তাজা ফলাফল গণনা করা হচ্ছে`);
    const result = fn.apply(this, args);
    cache.set(key, { result, timestamp: now });
    return result;
  };
}

// 🌐 API কল উদাহরণ
async function fetchUser(id) {
  console.log(`🌐 ইউজার ${id} এর জন্য API কল করা হচ্ছে`);
  // API বিলম্ব অনুকরণ
  await new Promise((resolve) => setTimeout(resolve, 1000));
  return { id, name: `ইউজার ${id}`, email: `user${id}@example.com` };
}

const memoizedFetchUser = memoizeWithExpiry(fetchUser, 30000); // ৩০ সেকেন্ড ক্যাশ

// ব্যবহার
memoizedFetchUser(1).then(console.log); // 🌐 API কল
memoizedFetchUser(1).then(console.log); // 💾 ক্যাশ হিট
```

> **ব্যাখ্যা:**
>
> - মেমোইজেশনের এই সংস্করণ নির্দিষ্ট সময় পর (যেমন ৩০ সেকেন্ড) পুরানো উত্তর ভুলে যায়।
> - এটি API কলের মতো জিনিসের জন্য নিখুঁত যেখানে ডেটা কিছুক্ষণ পর পরিবর্তন হতে পারে।
> - এটি একটি মুদি দোকানের মতো যে একদিন পর পুরানো খাবার ফেলে দেয় জিনিস তাজা রাখার জন্য!

### 🎯 নিখুঁত ব্যবহারের ক্ষেত্র

- **API রেসপন্স** যা প্রায়ই পরিবর্তন হয় না
- **জটিল গণনা** (গণিত, অ্যালগরিদম)
- **ডাটাবেস কোয়েরি** একই প্যারামিটার সহ
- **ইমেজ প্রসেসিং** ফলাফল
- **সার্চ রেজাল্ট** সাম্প্রতিক কোয়েরির জন্য

## 🗂️ প্যাটার্ন তুলনা গাইড

| প্যাটার্ন         | জটিলতা   | ব্যবহার করুন যখন            | এড়িয়ে চলুন যখন            |
| ----------------- | -------- | --------------------------- | --------------------------- |
| **মডিউল**         | 🟢 সহজ   | প্রাইভেট ভেরিয়েবল প্রয়োজন | সাধারণ স্ক্রিপ্ট            |
| **রিভিলিং মডিউল** | 🟢 সহজ   | স্পষ্ট কাঠামো চান           | খুব ছোট মডিউল               |
| **সিঙ্গেলটন**     | 🟡 মধ্যম | একটি ইন্সট্যান্স প্রয়োজন   | একাধিক ইন্সট্যান্স প্রয়োজন |
| **ফ্যাক্টরি**     | 🟡 মধ্যম | অনুরূপ অবজেক্ট তৈরি করা     | অবজেক্ট খুব আলাদা           |
| **অবজার্ভার**     | 🟡 মধ্যম | ইভেন্ট সিস্টেম              | সাধারণ নোটিফিকেশন           |
| **ডেবাউন্স**      | 🟢 সহজ   | ইউজার ইনপুট বিলম্ব          | তাৎক্ষণিক প্রতিক্রিয়া      |
| **থ্রটল**         | 🟢 সহজ   | ফ্রিকোয়েন্সি সীমিত করা     | সব ইভেন্ট প্রয়োজন          |
| **কারিং**         | 🔴 কঠিন  | বিশেষায়িত ফাংশন তৈরি করা   | সাধারণ অপারেশন              |
| **মেমোইজেশন**     | 🟡 মধ্যম | ব্যয়বহুল গণনা              | দ্রুত অপারেশন               |

---

## 🚀 পরবর্তী ধাপ

### 📚 অনুশীলন প্রকল্প

1. **টুডু অ্যাপ**: টাস্ক ম্যানেজমেন্টের জন্য মডিউল প্যাটার্ন ব্যবহার করুন
2. **থিম সুইচার**: অ্যাপ সেটিংসের জন্য সিঙ্গেলটন প্রয়োগ করুন
3. **ফর্ম ভ্যালিডেটর**: বিভিন্ন ভ্যালিডেটরের জন্য ফ্যাক্টরি প্যাটার্ন প্রয়োগ করুন
4. **রিয়েল-টাইম চ্যাট**: মেসেজ আপডেটের জন্য অবজার্ভার প্যাটার্ন ব্যবহার করুন
5. **সার্চ অ্যাপ**: ডেবাউন্স এবং মেমোইজেশন একসাথে ব্যবহার করুন

### 🔗 অতিরিক্ত রিসোর্স

- [JavaScript Design Patterns - Addy Osmani](https://addyosmani.com/resources/essentialjsdesignpatterns/book/)
- [MDN Web Docs - JavaScript Guide](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Guide)
- [Patterns.dev - Modern Web App Patterns](https://patterns.dev/)
- [Refactoring.Guru - Design Patterns](https://refactoring.guru/design-patterns/javascript)

### 💬 কমিউনিটি

- Discord/Reddit এ জাভাস্ক্রিপ্ট কমিউনিটিতে যোগ দিন
- LeetCode, Codewars এর মতো কোডিং প্ল্যাটফর্মে অনুশীলন করুন
- প্রকল্প তৈরি করুন এবং GitHub এ শেয়ার করুন
- আপনি যা শিখেন তা নিয়ে ব্লগ পোস্ট লিখুন

---

## 📋 দ্রুত রেফারেন্স চিট শিট

```js
// মডিউল প্যাটার্ন
const MyModule = (function () {
  let private = "গোপন";
  return { getPrivate: () => private };
})();

// সিঙ্গেলটন প্যাটার্ন
const Singleton = (function () {
  let instance;
  return { getInstance: () => instance || (instance = {}) };
})();

// ফ্যাক্টরি প্যাটার্ন
const Factory = (type) => (type === "A" ? { a: 1 } : { b: 2 });

// অবজার্ভার প্যাটার্ন
class Subject {
  constructor() {
    this.observers = [];
  }
  notify(data) {
    this.observers.forEach((fn) => fn(data));
  }
}

// ডেবাউন্স
const debounce = (fn, delay) => {
  let timeout;
  return (...args) => {
    clearTimeout(timeout);
    timeout = setTimeout(() => fn(...args), delay);
  };
};

// থ্রটল
const throttle = (fn, limit) => {
  let inThrottle;
  return (...args) => {
    if (!inThrottle) {
      fn(...args);
      inThrottle = true;
      setTimeout(() => (inThrottle = false), limit);
    }
  };
};

// কারিং
const curry = (a) => (b) => (c) => a + b + c;

// মেমোইজেশন
const memoize = (fn) => {
  const cache = {};
  return (arg) => cache[arg] || (cache[arg] = fn(arg));
};
```

> **ব্যাখ্যা:**
>
> - এই চিট শিট আপনাকে প্রতিটি প্যাটার্নের জন্য দ্রুত রেফারেন্স দেয়।
> - আপনার নিজের প্রকল্প বা ইন্টারভিউ প্রস্তুতির জন্য টেমপ্লেট হিসেবে ব্যবহার করুন!

---

**হ্যাপি কোডিং! 🎉** মনে রাখবেন: সেরা প্যাটার্ন হলো সেটি যা আপনার নির্দিষ্ট সমস্যাকে স্পষ্ট এবং সহজভাবে সমাধান করে।
