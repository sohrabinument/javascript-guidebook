# JavaScript Best Practices 🌟

দুর্দান্ত JavaScript লেখা মানে শুধু কোড কাজ করানো নয়—এটা এমন কোড তৈরি করা যা পড়া যায়, রক্ষণাবেক্ষণ করা যায় এবং স্কেল করা যায়। এই অনুশীলনগুলোকে আপনার টুলকিট হিসেবে ভাবুন যা সময়ের পরীক্ষায় টিকে থাকা সফটওয়্যার তৈরি করতে সাহায্য করে এবং অন্য ডেভেলপারদের (ভবিষ্যতের আপনি সহ) কৃতজ্ঞ করে তোলে।

আপনি একজন নতুন যিনি ভাল অভ্যাস গড়তে চান বা একজন অভিজ্ঞ ডেভেলপার যিনি আপনার দক্ষতা পরিমার্জন করতে চান, এই গাইড আপনাকে এমন JavaScript লিখতে সাহায্য করবে যা সত্যিই উজ্জ্বল।

---

## কেন Best Practices গুরুত্বপূর্ণ 🎯

নির্দিষ্ট অনুশীলনে ডুব দেওয়ার আগে, আসুন বুঝি কেন এগুলো অত্যন্ত গুরুত্বপূর্ণ:

### 🚀 **আপনার ক্যারিয়ারের জন্য**

- **কোড রিভিউ মসৃণ হয়** - সহকর্মীরা পরিষ্কার, পড়ার যোগ্য কোড পছন্দ করেন
- **ডিবাগিং দ্রুত হয়** - ভাল গঠিত কোড সমস্যা দ্রুত প্রকাশ করে
- **সহযোগিতা উন্নত হয়** - সুসংগত অনুশীলন টিমওয়ার্ক নির্বিঘ্ন করে

### 💰 **আপনার প্রকল্পের জন্য**

- **রক্ষণাবেক্ষণ খরচ কমে** - পরিষ্কার কোড সংশোধন এবং সম্প্রসারণ করা সস্তা
- **কম বাগ** - ভাল অনুশীলন সাধারণ সমস্যা প্রতিরোধ করে
- **ভাল পারফরম্যান্স** - অনেক অনুশীলন স্বাভাবিকভাবেই আরো দক্ষ কোড তৈরি করে

### 🧠 **আপনার মানসিক শান্তির জন্য**

- **কম মানসিক চাপ** - পরিষ্কার কোড বুঝতে সহজ
- **পরিবর্তনে আত্মবিশ্বাস** - ভাল গঠিত কোড সংশোধন করা নিরাপদ
- **আপনার কাজে গর্ব** - সুন্দর কোড লিখতে ভাল লাগে

---

## 1. Variable Declaration আয়ত্ত করুন: `var` এর পরিবর্তে `const` এবং `let` 📦

আধুনিক JavaScript আমাদের variable declaration এর জন্য ভাল টুল দেয়। কখন এবং কেন প্রতিটি ব্যবহার করতে হয় তা বোঝা নিরাপদ, পূর্বাভাসযোগ্য কোড লেখার জন্য মৌলিক।

### `var` এর সমস্যা

```js
// ❌ var বিভ্রান্তিকর আচরণ তৈরি করে
function confusingExample() {
  console.log(message); // undefined (একটি error নয়!)

  if (true) {
    var message = "Hello";
  }

  console.log(message); // "Hello" - ব্লক থেকে বেরিয়ে এসেছে!

  for (var i = 0; i < 3; i++) {
    setTimeout(() => console.log(i), 100); // তিনবার "3" প্রিন্ট করে!
  }
}
```

> **ব্যাখ্যা:**
> এই উদাহরণ দেখায় কেন `var` বিভ্রান্তিকর হতে পারে:
>
> 1. প্রথম `console.log` `undefined` দেখায় কারণ `var` ভেরিয়েবল "hoisted" (শীর্ষে সরানো) হয় কিন্তু initialize হয় না
> 2. `message` ভেরিয়েবল তার `if` ব্লক থেকে বেরিয়ে আসে - এটি ফাংশনের সব জায়গায় অ্যাক্সেসযোগ্য
> 3. `for` লুপে, `i` সব iteration জুড়ে শেয়ার হয়, তাই timeout গুলো চালু হলে, তারা সবাই শেষ মান (3) দেখে
>    এই আচরণ খুঁজে পাওয়া কঠিন bugs তৈরি করতে পারে!

### আধুনিক সমাধান

```js
// ✅ const এবং let পূর্বাভাসযোগ্য আচরণ প্রদান করে
function clearExample() {
  // console.log(message); // ReferenceError - ঘোষণার আগে ব্যবহার করা যায় না

  if (true) {
    const message = "Hello";
    let counter = 1;
  }

  // console.log(message); // ReferenceError - তার ব্লকে থেকে যায়

  for (let i = 0; i < 3; i++) {
    setTimeout(() => console.log(i), 100); // সঠিকভাবে "0", "1", "2" প্রিন্ট করে!
  }
}
```

> **ব্যাখ্যা:**
> এটি দেখায় কিভাবে `const` এবং `let` `var` এর সমস্যা ঠিক করে:
>
> 1. আপনি ঘোষণার আগে ভেরিয়েবল ব্যবহার করতে পারবেন না - এটি বিভ্রান্তি প্রতিরোধ করে
> 2. ভেরিয়েবল তাদের ব্লকের (`{ }` এর মধ্যে) ভিতরে থাকে - কোন লিকিং নেই!
> 3. প্রতিটি লুপ iteration `i` এর নিজস্ব কপি পায়, তাই timeout গুলো সঠিকভাবে কাজ করে
>    এটি আপনার কোড আরো পূর্বাভাসযোগ্য এবং ডিবাগ করা সহজ করে তোলে।

### সিদ্ধান্ত গ্রহণের ফ্রেমওয়ার্ক

```js
// ডিফল্ট হিসেবে const ব্যবহার করুন (90% সময়)
const API_URL = "https://api.example.com";
const users = ["Alice", "Bob"];
const config = { theme: "dark" };

// Array এবং object গুলো এখনও সংশোধন করা যায়
users.push("Charlie"); // ✅ এটি কাজ করে
config.theme = "light"; // ✅ এটিও কাজ করে
// users = []; // ❌ এটি কাজ করে না - reassignment ব্লক করা

// যখন reassignment প্রয়োজন তখন let ব্যবহার করুন (10% সময়)
let counter = 0;
let currentUser = null;
let isLoading = true;

for (let i = 0; i < 10; i++) {
  counter += i;
}

if (userLoggedIn) {
  currentUser = await fetchUser();
  isLoading = false;
}

// আধুনিক কোডে কখনো var ব্যবহার করবেন না
// var message = "Don't do this"; // ❌ সম্পূর্ণভাবে এড়িয়ে চলুন
```

> **ব্যাখ্যা:**
> এটি দেখায় কখন `const` বনাম `let` ব্যবহার করতে হয়:
>
> 1. এমন মানের জন্য `const` ব্যবহার করুন যা reassign হবে না (যেমন URL, কনফিগারেশন)
> 2. `const` দিয়েও, আপনি এখনও array এবং object সংশোধন করতে পারেন (আইটেম যোগ/সরানো, প্রপার্টি পরিবর্তন)
> 3. যখন একটি ভেরিয়েবল reassign করতে হয় তখন `let` ব্যবহার করুন (যেমন counter, flag, বা user data)
> 4. কমেন্ট দেখায় কী কাজ করে এবং কী করে না - আপনার কোডে এগুলো চেষ্টা করুন!

### বাস্তব জীবনের উদাহরণ

```js
// ✅ ভাল: স্পষ্ট variable scoping
function processShoppingCart(items) {
  const TAX_RATE = 0.08;
  const SHIPPING_THRESHOLD = 50;

  let subtotal = 0;
  let shippingCost = 5.99;

  for (const item of items) {
    subtotal += item.price * item.quantity;
  }

  if (subtotal >= SHIPPING_THRESHOLD) {
    shippingCost = 0;
  }

  const tax = subtotal * TAX_RATE;
  const total = subtotal + tax + shippingCost;

  return { subtotal, tax, shippingCost, total };
}
```

> **ব্যাখ্যা:**
> এটি একটি বাস্তব শপিং কার্ট ক্যালকুলেটর যা ভাল ভেরিয়েবল ব্যবহার দেখায়:
>
> 1. `const` এমন মানের জন্য ব্যবহৃত হয় যা পরিবর্তন হবে না (tax rate, free shipping threshold)
> 2. `let` এমন মানের জন্য ব্যবহৃত হয় যা পরিবর্তন করতে হয় (subtotal, shipping cost)
> 3. ফাংশন সব গণনা করা মান সহ একটি object রিটার্ন করে
> 4. প্রতিটি ভেরিয়েবলের একটি স্পষ্ট উদ্দেশ্য আছে এবং যেখানে অর্থপূর্ণ সেখানে ব্যবহৃত হয়

### Variables: নির্দিষ্ট এবং বর্ণনামূলক হন

```js
// ❌ অস্পষ্ট এবং বিভ্রান্তিকর
let x = 25;
let data = fetchData();
let flag = true;
let temp = user.name.split(" ")[0];

// ✅ স্পষ্ট এবং বর্ণনামূলক
let userAge = 25;
let customerOrders = fetchCustomerOrders();
let isEmailValid = true;
let firstName = user.name.split(" ")[0];
```

> **ব্যাখ্যা:**
> এটি দেখায় কিভাবে ভেরিয়েবল স্পষ্টভাবে নাম দিতে হয়:
>
> 1. খারাপ নাম (`x`, `data`, `flag`) আপনাকে বলে না ভেরিয়েবল কিসের জন্য
> 2. ভাল নাম (`userAge`, `customerOrders`, `isEmailValid`) ঠিক কী রয়েছে তা ব্যাখ্যা করে
> 3. Boolean ভেরিয়েবল প্রায়ই `is`, `has`, বা `can` দিয়ে শুরু হয় তাদের উদ্দেশ্য স্পষ্ট করতে
> 4. নির্দিষ্ট নাম অন্য ডেভেলপারদের (এবং ভবিষ্যতের আপনার) কোড বুঝতে সাহায্য করে

### Functions: কর্ম বর্ণনাকারী ক্রিয়াপদ ব্যবহার করুন

```js
// ❌ অস্পষ্ট ফাংশনের নাম
function handle(user) { ... }
function process(data) { ... }
function check(input) { ... }

// ✅ বর্ণনামূলক ফাংশনের নাম
function validateUserCredentials(user) { ... }
function transformApiResponse(data) { ... }
function isValidEmailAddress(input) { ... }
```

> **ব্যাখ্যা:**
> এটি দেখায় কিভাবে ফাংশন স্পষ্টভাবে নাম দিতে হয়:
>
> 1. খারাপ নাম (`handle`, `process`, `check`) খুবই অস্পষ্ট - তারা বলে না ফাংশন কী করে
> 2. ভাল নাম কর্ম বর্ণনা করতে ক্রিয়াপদ (`validate`, `transform`, `isValid`) ব্যবহার করে
> 3. ফাংশন কিসের সাথে কাজ করে তা অন্তর্ভুক্ত করুন (`UserCredentials`, `ApiResponse`, `EmailAddress`)
> 4. স্পষ্ট নাম আপনার কোড স্ব-ডকুমেন্টিং করে তোলে - আপনি কমেন্ট ছাড়াই বুঝতে পারেন

### সামঞ্জস্যপূর্ণ প্যাটার্ন

```js
// ✅ সামঞ্জস্যপূর্ণ নামকরণ প্যাটার্ন
class UserService {
  // Boolean ভেরিয়েবল 'is', 'has', 'can', 'should' দিয়ে শুরু হয়
  isUserActive(user) { return user.status === 'active'; }
  hasPermission(user, action) { return user.permissions.includes(action); }
  canEditProfile(user) { return user.role === 'admin' || user.isOwner; }

  // কালেকশনগুলো বহুবচন
  getAllUsers() { ... }
  getActiveUsers() { ... }

  // একক আইটেমগুলো একবচন
  getUser(id) { ... }
  getCurrentUser() { ... }

  // কর্মগুলো ক্রিয়াপদ-ভিত্তিক
  createUser(userData) { ... }
  updateUser(id, changes) { ... }
  deleteUser(id) { ... }

  // হ্যান্ডলাররা বর্ণনা করে তারা কী পরিচালনা করে
  handleUserRegistration(formData) { ... }
  handlePasswordReset(email) { ... }
}
```

> **ব্যাখ্যা:**
> এটি একটি ক্লাসে সামঞ্জস্যপূর্ণ নামকরণ প্যাটার্ন দেখায়:
>
> 1. Boolean মেথডগুলো `is`, `has`, `can`, বা `should` দিয়ে শুরু হয় যাতে তাদের উদ্দেশ্য স্পষ্ট হয়
> 2. যে মেথডগুলো একাধিক আইটেম রিটার্ন করে সেগুলো বহুবচন নাম ব্যবহার করে (`getAllUsers`)
> 3. যে মেথডগুলো একটি আইটেম রিটার্ন করে সেগুলো একবচন নাম ব্যবহার করে (`getUser`)
> 4. অ্যাকশন মেথডগুলো ক্রিয়াপদ ব্যবহার করে (`create`, `update`, `delete`)
> 5. হ্যান্ডলার মেথডগুলো বর্ণনা করে তারা কী পরিচালনা করে (`handleUserRegistration`)
>    এই প্যাটার্নগুলো আপনার কোডকে আরো পূর্বাভাসযোগ্য এবং বুঝতে সহজ করে তোলে

### প্রসঙ্গ-উপযুক্ত নামকরণ

```js
// ✅ নামগুলো তাদের প্রসঙ্গ ও পরিসরের সাথে মানানসই
function calculateShippingCost(order) {
  const STANDARD_RATE = 5.99;
  const EXPRESS_RATE = 12.99;
  const FREE_SHIPPING_THRESHOLD = 50;

  let cost = 0;

  // ছোট স্কোপে ছোট নাম ঠিক আছে
  for (const item of order.items) {
    cost += item.weight * 0.1;
  }

  if (order.shippingType === "express") {
    cost += EXPRESS_RATE;
  } else if (order.total < FREE_SHIPPING_THRESHOLD) {
    cost += STANDARD_RATE;
  }

  return Math.max(cost, 0);
}

// ✅ বড় স্কোপে দীর্ঘ, বর্ণনামূলক নাম
class EcommerceOrderProcessor {
  constructor(paymentGateway, inventoryService, emailService) {
    this.paymentGateway = paymentGateway;
    this.inventoryService = inventoryService;
    this.emailService = emailService;
  }

  async processCustomerOrder(orderDetails, customerInformation) {
    // বাস্তবায়ন...
  }
}
```

> **ব্যাখ্যা:**
> এটি দেখায় কীভাবে প্রসঙ্গের উপর ভিত্তি করে নামের দৈর্ঘ্য নির্বাচন করতে হয়:
>
> 1. ছোট স্কোপে (যেমন লুপ), ছোট নাম (`cost`, `item`) ঠিক আছে কারণ প্রসঙ্গ স্পষ্ট
> 2. কনস্ট্যান্টগুলো UPPER_SNAKE_CASE ব্যবহার করে আলাদা হতে (`STANDARD_RATE`, `FREE_SHIPPING_THRESHOLD`)
> 3. বড় স্কোপে (যেমন ক্লাস), দীর্ঘ, আরো বর্ণনামূলক নাম ব্যবহার করুন
> 4. কনস্ট্রাক্টর প্যারামিটার এবং ক্লাস প্রপার্টি স্পষ্টভাবে তাদের উদ্দেশ্য বর্ণনা করবে
> 5. মেথডের নামগুলো যথেষ্ট বর্ণনামূলক হওয়া উচিত যাতে বোঝা যায় তারা কী করে

### বিভ্রান্তিকর নাম এড়ানো

```js
// ❌ বিভ্রান্তিকর নাম
let userData = fetchPosts(); // পোস্ট রিটার্ন করে, ব্যবহারকারীর ডেটা নয়!
function isValid(input) {
  return input.length > 0; // শুধু দৈর্ঘ্য চেক করে, প্রকৃত বৈধতা নয়
}
let users = getUser(123); // একক ব্যবহারকারী রিটার্ন করে, অ্যারে নয়

// ✅ সঠিক নাম
let userPosts = fetchPosts();
function hasContent(input) {
  return input.length > 0;
}
let user = getUser(123);
let users = getAllUsers();
```

> **ব্যাখ্যা:**
> এটি দেখায় কীভাবে বিভ্রান্তিকর নাম এড়াতে হয়:
>
> 1. `userData` বিভ্রান্তিকর কারণ এতে পোস্ট রয়েছে, ব্যবহারকারীর ডেটা নয়
> 2. `isValid` খুবই অস্পষ্ট - এটি শুধু চেক করে যে কন্টেন্ট আছে কিনা
> 3. `users` বিভ্রান্তিকর কারণ এটি একক ব্যবহারকারী, অ্যারে নয়
> 4. ভালো নামগুলো সঠিকভাবে বর্ণনা করে ভেরিয়েবলে কী রয়েছে
> 5. ফাংশনের নামগুলো তারা প্রকৃতপক্ষে কী করে তার সাথে মিলতে হবে

### বড় ফাংশনের সমস্যা

```js
// ❌ একটি ফাংশন অনেক কিছু করছে
function processUser(userData) {
  // বৈধতা যাচাই
  if (!userData.email || !userData.email.includes("@")) {
    throw new Error("Invalid email");
  }
  if (!userData.name || userData.name.length < 2) {
    throw new Error("Invalid name");
  }
  if (!userData.age || userData.age < 13) {
    throw new Error("Must be at least 13");
  }

  // ডেটা রূপান্তর
  userData.email = userData.email.toLowerCase().trim();
  userData.name = userData.name.trim();
  userData.createdAt = new Date();
  userData.id = Math.random().toString(36);

  // ডেটাবেস অপারেশন
  const connection = connectToDatabase();
  const existingUser = connection.query("SELECT * FROM users WHERE email = ?", [
    userData.email,
  ]);
  if (existingUser.length > 0) {
    throw new Error("User already exists");
  }
  connection.query("INSERT INTO users SET ?", userData);

  // ইমেইল পাঠানো
  const emailTemplate = `Welcome ${userData.name}! Your account has been created.`;
  sendEmail(userData.email, "Welcome!", emailTemplate);

  // অ্যানালিটিক্স
  trackEvent("user_registered", { userId: userData.id, source: "web" });

  return userData;
}
```

> **ব্যাখ্যা:**
> এই ফাংশনটি অনেক কিছু করছে:
>
> 1. এটি ব্যবহারকারীর ডেটা যাচাই করে
> 2. এটি ডেটা রূপান্তরিত করে
> 3. এটি ডেটাবেসের সাথে ইন্টারঅ্যাক্ট করে
> 4. এটি ইমেইল পাঠায়
> 5. এটি অ্যানালিটিক্স ট্র্যাক করে
>    এটি এগুলো কঠিন করে তোলে:
>
> - ফাংশনটি কী করে তা বুঝতে
> - প্রতিটি অংশ আলাদাভাবে পরীক্ষা করতে
> - বাগ ঠিক করতে (কোন অংশ সমস্যা সৃষ্টি করছে?)
> - কোড পুনরায় ব্যবহার করতে (যদি শুধু যাচাই করতে চান?)

### ভালো পদ্ধতি: ফোকাসড ফাংশন

```js
// ✅ প্রতিটি ফাংশনের একটি একক, স্পষ্ট উদ্দেশ্য রয়েছে
function validateUserData(userData) {
  const errors = [];

  if (!userData.email || !isValidEmail(userData.email)) {
    errors.push("Invalid email address");
  }

  if (!userData.name || userData.name.trim().length < 2) {
    errors.push("Name must be at least 2 characters");
  }

  if (!userData.age || userData.age < 13) {
    errors.push("Must be at least 13 years old");
  }

  if (errors.length > 0) {
    throw new ValidationError(errors);
  }
}

function normalizeUserData(userData) {
  return {
    ...userData,
    email: userData.email.toLowerCase().trim(),
    name: userData.name.trim(),
    createdAt: new Date(),
    id: generateUserId(),
  };
}

async function checkUserExists(email) {
  const existingUser = await database.findUserByEmail(email);
  if (existingUser) {
    throw new ConflictError("User already exists");
  }
}

async function saveUser(userData) {
  return await database.createUser(userData);
}

async function sendWelcomeEmail(user) {
  const template = createWelcomeEmailTemplate(user);
  await emailService.send(user.email, "Welcome!", template);
}

function trackUserRegistration(user) {
  analytics.track("user_registered", {
    userId: user.id,
    source: "web",
    timestamp: new Date(),
  });
}

// অর্কেস্ট্রেটর ফাংশন যা প্রক্রিয়া সংগঠিত করে
async function registerUser(userData) {
  try {
    validateUserData(userData);
    const normalizedData = normalizeUserData(userData);
    await checkUserExists(normalizedData.email);
    const user = await saveUser(normalizedData);

    // অকৃত্রিম অপারেশন যা স্বাধীনভাবে ব্যর্থ হতে পারে
    await Promise.allSettled([
      sendWelcomeEmail(user),
      trackUserRegistration(user),
    ]);

    return user;
  } catch (error) {
    logger.error("User registration failed:", error);
    throw error;
  }
}
```

> **ব্যাখ্যা:**
> এটি অনেক ভালো কারণ:
>
> 1. প্রতিটি ফাংশন একটি কাজ করে এবং ভালোভাবে করে
> 2. ফাংশনগুলো ছোট এবং বুঝতে সহজ
> 3. প্রতিটি ফাংশন আলাদাভাবে পরীক্ষা করা যায়
> 4. মূল ফাংশন (`registerUser`) প্রক্রিয়া সংগঠিত করে
> 5. ত্রুটি পরিচালনা স্পষ্ট এবং নির্দিষ্ট
> 6. অকৃত্রিম অপারেশন (ইমেইল, অ্যানালিটিক্স) মূল প্রক্রিয়া ভাঙা ছাড়াই ব্যর্থ হতে পারে

### ফাংশন ডিজাইনের নীতিমালা

```js
// ✅ বিশুদ্ধ ফাংশন: একই ইনপুট = একই আউটপুট, কোনো পার্শ্ব প্রতিক্রিয়া নেই
function calculateTax(amount, rate) {
  return amount * rate;
}

function formatCurrency(amount, currency = "USD") {
  return new Intl.NumberFormat("en-US", {
    style: "currency",
    currency,
  }).format(amount);
}

// ✅ পূর্বাভাসযোগ্য রিটার্ন টাইপ
function findUserById(id) {
  const user = database.getUser(id);
  return user || null; // সবসময় User অবজেক্ট বা null রিটার্ন করে
}

function getAllUsers() {
  const users = database.getUsers();
  return users || []; // সবসময় অ্যারে রিটার্ন করে
}

// ✅ স্পষ্ট ত্রুটি পরিচালনা
function divideNumbers(a, b) {
  if (typeof a !== "number" || typeof b !== "number") {
    throw new TypeError("Both arguments must be numbers");
  }

  if (b === 0) {
    throw new Error("Division by zero is not allowed");
  }

  return a / b;
}
```

> **ব্যাখ্যা:**
> এটি ভালো ফাংশন ডিজাইনের নীতিমালা দেখায়:
>
> 1. বিশুদ্ধ ফাংশন সবসময় একই ইনপুটের জন্য একই আউটপুট রিটার্ন করে
> 2. ফাংশনের স্পষ্ট, পূর্বাভাসযোগ্য রিটার্ন টাইপ রয়েছে
> 3. ত্রুটি পরিচালনা স্পষ্ট এবং সহায়ক
> 4. ডিফল্ট প্যারামিটার ফাংশনকে আরো নমনীয় করে তোলে
> 5. ফাংশনগুলো ছোট এবং ফোকাসড

### ফাংশনের দৈর্ঘ্য নির্দেশিকা

```js
// ✅ আদর্শ ফাংশনের দৈর্ঘ্য: ৫-১৫ লাইন
function isBusinessDay(date) {
  const day = date.getDay();
  return day >= 1 && day <= 5;
}

function calculateBusinessDays(startDate, endDate) {
  let current = new Date(startDate);
  let businessDays = 0;

  while (current <= endDate) {
    if (isBusinessDay(current)) {
      businessDays++;
    }
    current.setDate(current.getDate() + 1);
  }

  return businessDays;
}

// ✅ যখন ফাংশন দীর্ঘ হয়ে যায়, সেগুলো ভেঙে ফেলুন
function processOrder(order) {
  validateOrder(order);
  const pricing = calculatePricing(order);
  const processedOrder = applyDiscounts(order, pricing);

  return finalizeOrder(processedOrder);
}
```

> **ব্যাখ্যা:**
> এটি দেখায় কিভাবে ফাংশনগুলো পরিচালনাযোগ্য রাখতে হয়:
>
> 1. ছোট ফাংশন (৫-১৫ লাইন) বুঝতে সহজ
> 2. প্রতিটি ফাংশন একটি কাজ করে
> 3. জটিল ফাংশনগুলো ছোট ভাগে ভাগ করুন
> 4. স্পষ্ট নাম ব্যবহার করুন যা প্রতিটি ফাংশন কী করে তা বর্ণনা করে
> 5. মূল ফাংশনটি উচ্চ-স্তরের ধাপগুলো দেখায়

### শিথিল সমতার বিপদ

```js
// ❌ শিথিল সমতা (==) অপ্রত্যাশিত ফলাফল দিতে পারে
console.log(0 == false); // true (!)
console.log("" == false); // true (!)
console.log(null == undefined); // true (!)
console.log("5" == 5); // true (!)
console.log([1, 2] == "1,2"); // true (!)

// এটি শর্তযুক্ত লজিকে বাগ সৃষ্টি করতে পারে
function processInput(value) {
  if (value == 0) {
    // ❌ এটি 0, false, '', [], null, undefined সবের সাথে মিলে যায়!
    return "No input provided";
  }
  return `Processing: ${value}`;
}

console.log(processInput(false)); // "No input provided" - অপ্রত্যাশিত!
console.log(processInput("")); // "No input provided" - অপ্রত্যাশিত!
console.log(processInput([])); // "No input provided" - অপ্রত্যাশিত!
```

> **ব্যাখ্যা:**
> এটি দেখায় কেন `==` বিপজ্জনক হতে পারে:
>
> 1. `==` তুলনা করার আগে টাইপ পরিবর্তন করার চেষ্টা করে, যা বিভ্রান্তিকর হতে পারে
> 2. `0 == false` সত্য কারণ `0` হল "falsy"
> 3. `"" == false` সত্য কারণ খালি স্ট্রিং "falsy"
> 4. `"5" == 5` সত্য কারণ স্ট্রিং সংখ্যায় রূপান্তরিত হয়
> 5. এটি বাগ সৃষ্টি করতে পারে যখন আপনি এই রূপান্তরগুলো আশা করেন না

### কঠোর সমতার নিরাপত্তা

```js
// ✅ কঠোর সমতা (===) মান এবং টাইপ উভয়ই চেক করে
console.log(0 === false); // false
console.log("" === false); // false
console.log(null === undefined); // false
console.log("5" === 5); // false
console.log([1, 2] === "1,2"); // false

function processInput(value) {
  if (value === 0) {
    // ✅ শুধুমাত্র ঠিক ০ এর সাথে মিলে
    return "Zero provided";
  }
  if (value === "") {
    // ✅ শুধুমাত্র খালি স্ট্রিং এর সাথে মিলে
    return "Empty string provided";
  }
  if (value === null || value === undefined) {
    // ✅ স্পষ্ট চেক
    return "No value provided";
  }
  return `Processing: ${value}`;
}
```

> **ব্যাখ্যা:**
> এটি দেখায় কেন `===` নিরাপদ:
>
> 1. `===` মান এবং টাইপ উভয়ই চেক করে
> 2. কোন টাইপ রূপান্তর হয় না, তাই ফলাফল অনুমানযোগ্য
> 3. প্রতিটি শর্ত ঠিক যা আপনি চান তা চেক করে
> 4. কোডটি যা চেক করছে সে সম্পর্কে আরও স্পষ্ট
> 5. এটি অপ্রত্যাশিত টাইপ রূপান্তর থেকে বাগ প্রতিরোধ করে

### বাস্তব-জগতের উদাহরণ

```js
// ✅ কঠোর সমতা দিয়ে ফর্ম যাচাইকরণ
function validateForm(formData) {
  const errors = [];

  // সঠিক মানের জন্য চেক করুন
  if (formData.email === "") {
    errors.push("Email is required");
  }

  if (formData.age === 0) {
    errors.push("Please enter your age");
  }

  if (formData.terms !== true) {
    // ঠিক true হতে হবে, truthy নয়
    errors.push("You must accept the terms");
  }

  return errors;
}

// ✅ API রেসপন্স হ্যান্ডলিং
function handleApiResponse(response) {
  // সঠিক স্ট্যাটাস কোডের জন্য চেক করুন
  if (response.status === 200) {
    return response.data;
  }

  if (response.status === 404) {
    throw new NotFoundError("Resource not found");
  }

  if (response.status === 401) {
    throw new AuthError("Authentication required");
  }

  throw new Error(`Unexpected status: ${response.status}`);
}

// ✅ স্টেট ম্যানেজমেন্ট
function updateUserStatus(user, newStatus) {
  const validStatuses = ["active", "inactive", "pending", "banned"];

  if (!validStatuses.includes(newStatus)) {
    throw new Error("Invalid status");
  }

  // কঠোর তুলনা দুর্ঘটনাজনিত মিল প্রতিরোধ করে
  if (user.status === newStatus) {
    return user; // কোন পরিবর্তনের প্রয়োজন নেই
  }

  return {
    ...user,
    status: newStatus,
    lastUpdated: new Date(),
  };
}
```

> **ব্যাখ্যা:**
> এই উদাহরণগুলো দেখায় কিভাবে বাস্তব কোডে `===` ব্যবহার করতে হয়:
>
> 1. ফর্म যাচাইকরণ সঠিক মানের জন্য চেক করে
> 2. API রেসপন্স হ্যান্ডলিং সঠিক স্ট্যাটাস কোডের জন্য চেক করে
> 3. স্টেট ম্যানেজমেন্ট দুর্ঘটনাজনিত আপডেট প্রতিরোধ করে
> 4. প্রতিটি চেক স্পষ্ট এবং পরিষ্কার
> 5. ত্রুটির বার্তাগুলো নির্দিষ্ট এবং সহায়ক

### কখন শিথিল সমতা গ্রহণযোগ্য হতে পারে

```js
// কখনও কখনও আপনি ইচ্ছাকৃতভাবে null অথবা undefined চেক করতে চান
function hasValue(value) {
  return value != null; // ইচ্ছাকৃতভাবে != ব্যবহার করে null এবং undefined উভয়ই ধরার জন্য
}

// কিন্তু তারপরও, স্পষ্ট চেকিং পরিষ্কার
function hasValue(value) {
  return value !== null && value !== undefined;
}

// অথবা আধুনিক বিকল্প ব্যবহার করুন
function hasValue(value) {
  return value ?? false; // Nullish coalescing পরিষ্কার
}
```

> **ব্যাখ্যা:**
> এটি দেখায় কখন আপনি `!=` ব্যবহার করতে পারেন:
>
> 1. `!=` `null` এবং `undefined` উভয়ই চেক করার জন্য উপযোগী হতে পারে
> 2. কিন্তু স্পষ্ট চেক পরিষ্কার এবং নিরাপদ
> 3. আধুনিক JavaScript-এ ভাল বিকল্প আছে (`??` অপারেটর)
> 4. সর্বদা অন্তর্নিহিতের চেয়ে স্পষ্টকে পছন্দ করুন
> 5. যদি আপনি `!=` ব্যবহার করেন তাহলে কেন করছেন তা নথিভুক্ত করুন

### অ্যারে এবং অবজেক্ট তুলনা

```js
// ❌ এমনকি কঠোর সমতা অবজেক্ট/অ্যারের জন্য কাজ করে না
const arr1 = [1, 2, 3];
const arr2 = [1, 2, 3];
console.log(arr1 === arr2); // false - ভিন্ন রেফারেন্স!

const obj1 = { name: "Alice" };
const obj2 = { name: "Alice" };
console.log(obj1 === obj2); // false - ভিন্ন রেফারেন্স!

// ✅ গভীর তুলনার জন্য, ইউটিলিটি ফাংশন ব্যবহার করুন
import { isEqual } from "lodash";

console.log(isEqual(arr1, arr2)); // true
console.log(isEqual(obj1, obj2)); // true

// ✅ অথবা সরল ক্ষেত্রে নিজের লিখুন
function arraysEqual(a, b) {
  return a.length === b.length && a.every((val, i) => val === b[i]);
}

// ✅ রেফারেন্স সমতার জন্য (একই অবজেক্ট)
const user = { name: "Alice" };
const sameUser = user;
console.log(user === sameUser); // true - একই রেফারেন্স
```

> **ব্যাখ্যা:**
> এটি দেখায় কিভাবে অবজেক্ট এবং অ্যারে তুলনা করতে হয়:
>
> 1. `===` রেফারেন্স তুলনা করে, মান (বিষয়বস্তু) নয়
> 2. একই মান থাকলেও দুটি অ্যারে আলাদা অবজেক্ট
> 3. গভীর (deep) তুলনার জন্য `lodash.isEqual` ব্যবহার করুন
> 4. সরল ক্ষেত্রে নিজের তুলনা ফাংশন লিখুন
> 5. `===` ব্যবহার করে চেক করা যায় একটি অবজেক্ট অন্যটির সঙ্গে একই কিনা

### গ্লোবাল স্কোপ সমস্যা

```js
// ❌ গ্লোবাল স্কোপ দূষিত করা
var userName = "Alice"; // গ্লোবাল ভেরিয়েবল
var userAge = 25; // গ্লোবাল ভেরিয়েবল
var isLoggedIn = false; // গ্লোবাল ভেরিয়েবল

function loginUser() {
  isLoggedIn = true; // গ্লোবাল ভেরিয়েবল পরিবর্তন করে
  currentSession = Date.now(); // দুর্ঘটনাক্রমে গ্লোবাল ভেরিয়েবল তৈরি করে!
}

function logoutUser() {
  isLoggedIn = false; // গ্লোবাল ভেরিয়েবল পরিবর্তন করে
  userName = ""; // গ্লোবাল ভেরিয়েবল পরিষ্কার করে
}

// সমস্যাসমূহ:
// 1. যেকোনো স্ক্রিপ্ট এই ভেরিয়েবলগুলো পরিবর্তন করতে পারে
// 2. অন্যান্য লাইব্রেরির সাথে নামের দ্বন্দ্ব হতে পারে
// 3. ভেরিয়েবল কোথায় পরিবর্তিত হয় তা ট্র্যাক করা কঠিন
// 4. মেমোরি কখনো পরিষ্কার হয় না
```

> **ব্যাখ্যা:**
> এটি দেখায় কেন গ্লোবাল ভেরিয়েবল খারাপ:
>
> 1. গ্লোবাল ভেরিয়েবল যেকোনো জায়গা থেকে পরিবর্তন করা যায়
> 2. এগুলো অন্যান্য কোডের সাথে সংঘর্ষ হতে পারে
> 3. এগুলো কোথায় ব্যবহৃত হয় তা ট্র্যাক করা কঠিন
> 4. এগুলো চিরকাল মেমোরিতে থেকে যায়
> 5. এগুলো কোড টেস্ট ও রক্ষণাবেক্ষণ কঠিন করে তোলে

### আধুনিক স্কোপ ব্যবস্থাপনা

```js
// ✅ মডিউল এবং সুষ্ঠু স্কোপিং ব্যবহার করা
class UserSession {
  constructor() {
    this.userName = null;
    this.userAge = null;
    this.isLoggedIn = false;
    this.sessionStart = null;
  }

  login(name, age) {
    this.userName = name;
    this.userAge = age;
    this.isLoggedIn = true;
    this.sessionStart = Date.now();

    // স্থানীয় ভেরিয়েবল এই মেথডেই থাকে
    const loginTime = new Date().toISOString();
    console.log(`User ${name} logged in at ${loginTime}`);
  }

  logout() {
    const sessionDuration = Date.now() - this.sessionStart;
    console.log(`Session lasted ${sessionDuration}ms`);

    // ইনস্ট্যান্স ভেরিয়েবল রিসেট করা
    this.userName = null;
    this.userAge = null;
    this.isLoggedIn = false;
    this.sessionStart = null;
  }
}

// ✅ মডিউল-স্কোপড ভেরিয়েবল (গ্লোবালের চেয়ে ভালো)
const userSession = new UserSession();
export { userSession };
```

> **ব্যাখ্যা:**
> এটি স্কোপ ব্যবস্থাপনার উন্নত উপায় দেখায়:
>
> 1. সংশ্লিষ্ট ডেটা এবং মেথড গ্রুপ করতে ক্লাস ব্যবহার করুন
> 2. যখন সম্ভব মেথডের ভিতরে ভেরিয়েবল রাখুন
> 3. প্রয়োজনীয় জিনিস শেয়ার করতে মডিউল এক্সপোর্ট ব্যবহার করুন
> 4. পাবলিক এবং প্রাইভেট ডেটার মধ্যে স্পষ্ট পার্থক্য
> 5. ভেরিয়েবল কোথায় ব্যবহৃত হয় তা ট্র্যাক করা সহজ

### ফাংশন স্কোপের সর্বোত্তম অনুশীলন

```js
// ✅ ভেরিয়েবলগুলো তাদের ব্যবহারের যতটা কাছে রাখুন
function processOrderItems(order) {
  // অর্ডার-লেভেল ভেরিয়েবল
  const TAX_RATE = 0.08;
  let totalAmount = 0;

  for (const item of order.items) {
    // আইটেম-লেভেল ভেরিয়েবল
    const itemTotal = item.price * item.quantity;
    const itemDiscount = calculateDiscount(item);
    const finalItemPrice = itemTotal - itemDiscount;

    totalAmount += finalItemPrice;

    // ভ্যালিডেশন লজিক যেখানে প্রয়োজন সেখানেই স্কোপড
    if (item.quantity <= 0) {
      throw new Error(`Invalid quantity for ${item.name}`);
    }
  }

  // ট্যাক্স ক্যালকুলেশন যেখানে প্রয়োজন সেখানেই স্কোপড
  const taxAmount = totalAmount * TAX_RATE;
  return totalAmount + taxAmount;
}
```

> **ব্যাখ্যা:**
> এটি দেখায় কিভাবে ফাংশনে ভেরিয়েবল সংগঠিত করতে হয়:
>
> 1. ভেরিয়েবল যেখানে ব্যবহৃত হয় তার কাছেই ডিক্লেয়ার করুন
> 2. যে মানগুলো পরিবর্তন হয় না তার জন্য কনস্ট্যান্ট ব্যবহার করুন
> 3. লুপ ভেরিয়েবল লুপের ভিতরেই রাখুন
> 4. সংশ্লিষ্ট ক্যালকুলেশনগুলো একসাথে গ্রুপ করুন
> 5. কোডের প্রবাহ অনুসরণ করা সহজ করুন

### let এবং const এর সাথে Block Scope

```js
// ✅ Block scope কার্যকরভাবে ব্যবহার করা
function handleUserInput(input) {
  if (input.type === "email") {
    const emailPattern = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
    const isValid = emailPattern.test(input.value);

    if (isValid) {
      const normalizedEmail = input.value.toLowerCase().trim();
      return { type: "email", value: normalizedEmail };
    } else {
      throw new Error("Invalid email format");
    }
  }

  if (input.type === "phone") {
    const phonePattern = /^\+?[\d\s\-\(\)]+$/;
    const isValid = phonePattern.test(input.value);

    if (isValid) {
      const normalizedPhone = input.value.replace(/\D/g, "");
      return { type: "phone", value: normalizedPhone };
    } else {
      throw new Error("Invalid phone format");
    }
  }

  throw new Error("Unsupported input type");
}
```

> **ব্যাখ্যা:**
> এটি দেখায় কিভাবে block scope ব্যবহার করতে হয়:
>
> 1. ভেরিয়েবলগুলো শুধুমাত্র তাদের ব্লকের ভিতরেই পাওয়া যায়
> 2. প্রতিটি ব্লক একটি নির্দিষ্ট ধরনের ইনপুট হ্যান্ডেল করে
> 3. ভ্যালিডেশন এবং ট্রান্সফরমেশন একসাথে গ্রুপ করা হয়
> 4. প্রতিটি ক্ষেত্রের জন্য স্পষ্ট error message
> 5. কোড ইনপুট টাইপ অনুযায়ী সংগঠিত

### Closure এবং Module Pattern

```js
// ✅ প্রাইভেট scope তৈরি করতে closure ব্যবহার করা
function createCounter(initialValue = 0) {
  let count = initialValue; // প্রাইভেট ভেরিয়েবল

  return {
    increment() {
      count++;
      return count;
    },

    decrement() {
      count--;
      return count;
    },

    getValue() {
      return count;
    },

    reset() {
      count = initialValue;
      return count;
    },
  };
}

const counter = createCounter(10);
console.log(counter.getValue()); // 10
console.log(counter.increment()); // 11
// console.log(count); // ReferenceError - count প্রাইভেট!

// ✅ বৃহত্তর কার্যকারিতার জন্য Module pattern
const UserManager = (function () {
  // প্রাইভেট ভেরিয়েবল এবং ফাংশন
  const users = new Map();
  let nextId = 1;

  function validateUser(userData) {
    if (!userData.name || !userData.email) {
      throw new Error("Name and email are required");
    }
  }

  // পাবলিক API
  return {
    addUser(userData) {
      validateUser(userData);
      const id = nextId++;
      const user = { id, ...userData, createdAt: new Date() };
      users.set(id, user);
      return user;
    },

    getUser(id) {
      return users.get(id) || null;
    },

    getAllUsers() {
      return Array.from(users.values());
    },

    removeUser(id) {
      return users.delete(id);
    },
  };
})();
```

> **ব্যাখ্যা:**
> এটি প্রাইভেট ডেটা তৈরির দুটি উপায় দেখায়:
>
> 1. Closure ভেরিয়েবলগুলোকে প্রাইভেট রাখে কিন্তু method গুলোর কাছে অ্যাক্সেসযোগ্য করে
> 2. Counter উদাহরণ একটি সাধারণ closure দেখায়
> 3. Module pattern বৃহত্তর কার্যকারিতার জন্য
> 4. প্রাইভেট ডেটা শুধুমাত্র পাবলিক method এর মাধ্যমে অ্যাক্সেস করা যায়
> 5. এটি ডেটার দুর্ঘটনাজনিত পরিবর্তন রোধ করতে সাহায্য করে

### Memory Management এবং Scope

```js
// ❌ Retained reference থেকে Memory leak
function createEventHandler() {
  const largeData = new Array(1000000).fill("data"); // বড় array

  return function handleEvent(event) {
    // এই closure largeData কে memory তে রাখে যদিও আমরা এটি ব্যবহার করি না!
    console.log("Event handled:", event.type);
  };
}

// ✅ উন্নত memory management
function createEventHandler() {
  return function handleEvent(event) {
    // Closure এ শুধু প্রয়োজনীয় জিনিস রাখুন
    console.log("Event handled:", event.type);
  };
}

// ✅ প্রয়োজনে স্পষ্ট cleanup
class DataProcessor {
  constructor() {
    this.cache = new Map();
    this.listeners = [];
  }

  addListener(element, event, handler) {
    element.addEventListener(event, handler);
    this.listeners.push({ element, event, handler });
  }

  cleanup() {
    // Event listener গুলো পরিষ্কার করুন
    this.listeners.forEach(({ element, event, handler }) => {
      element.removeEventListener(event, handler);
    });
    this.listeners = [];

    // Cache পরিষ্কার করুন
    this.cache.clear();
  }
}
```

> **ব্যাখ্যা:**
> এটি দেখায় কিভাবে memory manage করতে হয়:
>
> 1. Closure এ অপ্রয়োজনীয় ডেটা রাখবেন না
> 2. কাজ শেষ হলে event listener পরিষ্কার করুন
> 3. আর প্রয়োজন না হলে cache পরিষ্কার করুন
> 4. Resource manage করতে class ব্যবহার করুন
> 5. সঠিক resource management এর জন্য cleanup method প্রদান করুন

### এড়িয়ে চলার মতো কমেন্ট

```js
// ❌ কমেন্ট যা শুধুমাত্র কোডের পুনরাবৃত্তি করে
let count = 0; // Initialize count to 0
count++; // Increment count by 1
return count * 2; // Return count multiplied by 2

// ❌ স্পষ্ট কমেন্ট
function calculateArea(width, height) {
  // Calculate the area
  return width * height; // Return width times height
}

// ❌ পুরাতন কমেন্ট
function processUser(user) {
  // TODO: Add email validation (this was never done!)
  // Hash the password using MD5 (now uses bcrypt)
  const hashedPassword = bcrypt.hash(user.password, 10);
  return user;
}
```

> **ব্যাখ্যা:**
> এটি খারাপ কমেন্টিং অনুশীলন দেখায়:
>
> 1. কোড কী করে তা কমেন্ট করবেন না - কোড ইতিমধ্যে তা দেখাচ্ছে
> 2. স্পষ্ট কমেন্ট যোগ করবেন না যা কোনো মূল্য যোগ করে না
> 3. পুরাতন কমেন্ট রেখে দেবেন না যা কোডের সাথে মিলে না
> 4. কমেন্টকে TODO তালিকা হিসেবে ব্যবহার করবেন না
> 5. কমেন্ট কেন ব্যাখ্যা করবে, কী নয়

### মূল্য যোগ করে এমন কমেন্ট

```js
// ✅ কী নয়, কেন ব্যাখ্যা করুন
function calculateDeliveryDate(orderDate, shippingMethod) {
  // Business rule: Standard shipping takes 3-5 business days,
  // but we promise 5 days to account for potential delays
  const standardDays = 5;
  const expressDays = 2;

  // Weekend deliveries are not available, so we need to account
  // for weekends when calculating delivery dates
  const deliveryDays =
    shippingMethod === "express" ? expressDays : standardDays;

  return addBusinessDays(orderDate, deliveryDays);
}

// ✅ জটিল ব্যবসায়িক যুক্তি ব্যাখ্যা করুন
function calculateLoyaltyPoints(orderAmount, customerTier) {
  // Loyalty point calculation based on customer tier:
  // - Bronze: 1 point per dollar
  // - Silver: 1.5 points per dollar
  // - Gold: 2 points per dollar
  // - Platinum: 2.5 points per dollar + 10% bonus on orders over $100

  const baseRates = {
    bronze: 1,
    silver: 1.5,
    gold: 2,
    platinum: 2.5,
  };

  let points = orderAmount * baseRates[customerTier];

  // Platinum customers get bonus points for large orders
  if (customerTier === "platinum" && orderAmount > 100) {
    points *= 1.1; // 10% bonus
  }

  return Math.floor(points);
}

// ✅ সমাধান এবং বিশেষ ক্ষেত্রগুলি নথিভুক্ত করুন
function parseUserAgent(userAgentString) {
  // Safari on iOS reports as 'Mozilla/5.0' but includes 'Safari' later in string
  // This is different from desktop Safari which has a more standard format
  if (
    userAgentString.includes("Safari") &&
    userAgentString.includes("Mobile")
  ) {
    return "Safari Mobile";
  }

  // Edge cases: Some browsers don't follow standard format
  // Fallback to generic detection if specific patterns don't match
  return detectBrowserGeneric(userAgentString);
}
```

> **ব্যাখ্যা:**
> এটি ভালো কমেন্টিং অনুশীলন দেখায়:
>
> 1. ব্যবসায়িক নিয়ম এবং প্রয়োজনীয়তা ব্যাখ্যা করুন
> 2. জটিল গণনা নথিভুক্ত করুন
> 3. ব্রাউজার সমস্যার জন্য সমাধান ব্যাখ্যা করুন
> 4. বিশেষ ক্ষেত্র এবং বিশেষ হ্যান্ডলিং নিয়ে কমেন্ট করুন
> 5. কমেন্ট কোডের উদ্দেশ্য বুঝতে সাহায্য করবে

### ডকুমেন্টেশন কমেন্ট (JSDoc)

```js
/**
 * Calculates the monthly payment for a loan
 * @param {number} principal - The loan amount
 * @param {number} rate - Annual interest rate (as decimal, e.g., 0.05 for 5%)
 * @param {number} years - Loan term in years
 * @returns {number} Monthly payment amount
 * @throws {Error} When parameters are invalid
 *
 * @example
 * // Calculate payment for $200,000 loan at 4.5% for 30 years
 * const payment = calculateMonthlyPayment(200000, 0.045, 30);
 * console.log(payment); // 1013.37
 */
function calculateMonthlyPayment(principal, rate, years) {
  if (typeof principal !== "number" || principal <= 0) {
    throw new Error("Principal must be a positive number");
  }
  if (typeof rate !== "number" || rate < 0) {
    throw new Error("Rate must be a non-negative number");
  }
  if (typeof years !== "number" || years <= 0) {
    throw new Error("Years must be a positive number");
  }
  const monthlyRate = rate / 12;
  const n = years * 12;
  return (principal * monthlyRate) / (1 - Math.pow(1 + monthlyRate, -n));
}
```

> **ব্যাখ্যা:**
> এটি দেখায় কিভাবে ফাংশন নথিভুক্ত করতে হয়:
>
> 1. JSDoc কমেন্ট বর্ণনা করে ফাংশন কী করে
> 2. `@param` প্রতিটি প্যারামিটার বর্ণনা করে
> 3. `@returns` বর্ণনা করে ফাংশন কী রিটার্ন করে
> 4. `@throws` সম্ভাব্য ত্রুটি বর্ণনা করে
> 5. `@example` দেখায় কিভাবে ফাংশন ব্যবহার করতে হয়
> 6. ফাংশনটি নিজেই যাচাইকরণ এবং স্পষ্ট ত্রুটি বার্তা অন্তর্ভুক্ত করে

### সহজ টেস্ট লেখা

```js
// ✅ Jest ব্যবহার করে মৌলিক ইউনিট টেস্ট
function add(a, b) {
  return a + b;
}

test("adds two numbers", () => {
  expect(add(2, 3)).toBe(5);
});
```

> **ব্যাখ্যা:**
> এটি একটি মৌলিক টেস্ট দেখাচ্ছে:
>
> 1. `add` ফাংশনটি সহজ এবং এর একটি কাজ আছে
> 2. টেস্টটি বর্ণনা করে আমরা কী টেস্ট করছি
> 3. `expect(...).toBe(...)` চেক করে ফলাফল আমাদের প্রত্যাশিত কিনা
> 4. টেস্ট ব্যবহারকারীদের কাছে পৌঁছানোর আগে বাগ ধরতে সাহায্য করে
> 5. সহজ টেস্ট বুঝতে এবং রক্ষণাবেক্ষণ করতে সহজ

### কনসোল এবং ব্রেকপয়েন্ট দিয়ে ডিবাগিং

```js
function findUser(users, id) {
  console.log("Looking for user with id:", id); // ডিবাগ আউটপুট
  return users.find((user) => user.id === id);
}
```

> **ব্যাখ্যা:**
> এটি মৌলিক ডিবাগিং দেখাচ্ছে:
>
> 1. `console.log` আপনাকে দেখতে সাহায্য করে কী ঘটছে
> 2. কোড বুঝতে গুরুত্বপূর্ণ মানগুলো লগ করুন
> 3. আরও উন্নত ডিবাগিংয়ের জন্য ব্রাউজার ডেভ টুলস ব্যবহার করুন
> 4. ব্রেকপয়েন্ট আপনাকে কোড থামিয়ে পরীক্ষা করতে দেয়
> 5. ডিবাগিং বাগ খুঁজে বের করে ঠিক করতে সাহায্য করে

### `eval` এবং বিপজ্জনক প্যাটার্ন এড়িয়ে চলুন

```js
// ❌ ব্যবহারকারীর ইনপুটের সাথে কখনো eval ব্যবহার করবেন না
const userCode = getUserInput();
eval(userCode); // বিপজ্জনক!
```

> **ব্যাখ্যা:**
> এটি দেখাচ্ছে কেন `eval` বিপজ্জনক:
>
> 1. `eval` যেকোনো কোড JavaScript হিসেবে চালায়
> 2. ব্যবহারকারীর ইনপুটে ক্ষতিকর কোড থাকতে পারে
> 3. এটি একটি নিরাপত্তা ঝুঁকি
> 4. সর্বদা `eval` এবং অনুরূপ ফাংশনগুলো এড়িয়ে চলুন
> 5. নিরাপদ বিকল্প ব্যবহার করুন

### ব্যবহারকারীর ইনপুট এস্কেপ করুন

```js
// ✅ XSS প্রতিরোধের জন্য textContent ব্যবহার করুন
const userInput = "<img src=x onerror=alert(1) />";
element.textContent = userInput; // নিরাপদ
```

> **ব্যাখ্যা:**
> এটি দেখাচ্ছে কীভাবে XSS আক্রমণ প্রতিরোধ করবেন:
>
> 1. `textContent` ইনপুটকে সরল টেক্সট হিসেবে বিবেচনা করে
> 2. এটি HTML/JavaScript ইনজেকশন প্রতিরোধ করে
> 3. সর্বদা ব্যবহারকারীর ইনপুট এস্কেপ করুন
> 4. ব্যবহারকারীর ইনপুটের সাথে `innerHTML` ব্যবহার করবেন না
> 5. নিরাপত্তা গুরুত্বপূর্ণ!

### HTTPS এবং নিরাপদ স্টোরেজ ব্যবহার করুন

```js
// ✅ API কলের জন্য সর্বদা HTTPS ব্যবহার করুন
fetch("https://api.example.com/data");

// ✅ সংবেদনশীল ডেটা নিরাপদে সংরক্ষণ করুন
localStorage.setItem("token", encrypt(token));
```

> **ব্যাখ্যা:**
> এটি মৌলিক নিরাপত্তা অনুশীলন দেখাচ্ছে:
>
> 1. API কলের জন্য সর্বদা HTTPS ব্যবহার করুন
> 2. সংরক্ষণের আগে সংবেদনশীল ডেটা এনক্রিপ্ট করুন
> 3. সংবেদনশীল ডেটা সরল টেক্সটে সংরক্ষণ করবেন না
> 4. নিরাপত্তা আপনার কোডে অন্তর্ভুক্ত করা উচিত
> 5. ব্যবহারকারীর ডেটা রক্ষা করুন!

### ব্যয়বহুল অপারেশন কমিয়ে আনুন

```js
// ❌ অদক্ষ: প্রতিবার দৈর্ঘ্য পুনরায় গণনা করে
for (let i = 0; i < array.length; i++) {
  doSomething(array[i]);
}

// ✅ দক্ষ: দৈর্ঘ্য ক্যাশে করুন
for (let i = 0, len = array.length; i < len; i++) {
  doSomething(array[i]);
}
```

> **ব্যাখ্যা:**
> এটি একটি সহজ পারফরমেন্স অপ্টিমাইজেশন দেখাচ্ছে:
>
> 1. প্রথম লুপ প্রতিবার `array.length` চেক করে
> 2. দ্বিতীয় লুপ দৈর্ঘ্য একটি ভেরিয়েবলে সংরক্ষণ করে
> 3. এটি দ্রুততর, বিশেষত বড় অ্যারের জন্য
> 4. ছোট অপ্টিমাইজেশনগুলো জমা হয়
> 5. ব্যবহারকারীর অভিজ্ঞতার জন্য পারফরমেন্স গুরুত্বপূর্ণ

### ডিবাউন্সিং এবং থ্রটলিং ব্যবহার করুন

```js
// ✅ ডিবাউন্স: একটি ফাংশন কতবার চালানো হয় তা সীমিত করুন
function debounce(fn, delay) {
  let timeout;
  return (...args) => {
    clearTimeout(timeout);
    timeout = setTimeout(() => fn(...args), delay);
  };
}

window.addEventListener(
  "resize",
  debounce(() => {
    console.log("Resized!");
  }, 200)
);
```

> **ব্যাখ্যা:**
> এটি দেখাচ্ছে কীভাবে অত্যধিক ফাংশন কল প্রতিরোধ করবেন:
>
> 1. `debounce` ফাংশন কল বন্ধ না হওয়া পর্যন্ত অপেক্ষা করে
> 2. এটি ঘন ঘন ফায়ার হওয়া ইভেন্টের জন্য (যেমন resize) দরকারি
> 3. এটি পারফরমেন্স উন্নত করে
> 4. বিলম্ব (200ms) সামঞ্জস্য করা যেতে পারে
> 5. এটি ওয়েব ডেভেলপমেন্টে একটি সাধারণ প্যাটার্ন

### মডিউল ব্যবহার করুন

```js
// ✅ কোডকে মডিউলে ভাগ করুন
// user.js
export function getUser(id) {
  /* ... */
}

// order.js
export function getOrder(id) {
  /* ... */
}
```

> **ব্যাখ্যা:**
> এটি দেখাচ্ছে কীভাবে কোড সংগঠিত করবেন:
>
> 1. কোডকে পৃথক ফাইলে (মডিউল) ভাগ করুন
> 2. প্রতিটি মডিউলের একটি নির্দিষ্ট উদ্দেশ্য আছে
> 3. `export` ফাংশনগুলোকে অন্যান্য ফাইলে উপলব্ধ করে
> 4. এটি কোড রক্ষণাবেক্ষণ সহজ করে
> 5. মডিউল আধুনিক JavaScript এর একটি মূল অংশ

### সম্পর্কিত কোড গ্রুপ করুন

```js
// ✅ সম্পর্কিত ফাংশনগুলো অবজেক্ট বা ক্লাসে গ্রুপ করুন
const mathUtils = {
  add(a, b) {
    return a + b;
  },
  subtract(a, b) {
    return a - b;
  },
};
```

> **ব্যাখ্যা:**
> এটি দেখাচ্ছে কীভাবে সম্পর্কিত ফাংশন সংগঠিত করবেন:
>
> 1. অনুরূপ ফাংশনগুলো একসাথে গ্রুপ করুন
> 2. কোড সংগঠিত করতে অবজেক্ট বা ক্লাস ব্যবহার করুন
> 3. এটি কোড খুঁজে পেতে সহজ করে
> 4. এটি নেমিং দ্বন্দ্ব প্রতিরোধ করে
> 5. সংগঠন রক্ষণাবেক্ষণযোগ্যতায় সাহায্য করে

---

## আরও জানুন 📚

- [MDN JavaScript Guide](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Guide)
- [JavaScript.info](https://javascript.info/)
- [Node.js Best Practices](https://github.com/goldbergyoni/nodebestpractices)
- [ESLint Rules](https://eslint.org/docs/latest/rules/)
- [OWASP JavaScript Security Cheat Sheet](https://cheatsheetseries.owasp.org/cheatsheets/JavaScript_Security_Cheat_Sheet.html)

---

শুভ কোডিং! 🚀
