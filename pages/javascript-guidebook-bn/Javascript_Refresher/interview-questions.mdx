import { Callout } from "nextra/components";

# সম্পূর্ণ JavaScript ইন্টারভিউ প্রস্তুতি গাইড 🎯

আপনার পরবর্তী ইন্টারভিউতে সফল হতে সাহায্য করার জন্য বিস্তারিত উত্তর এবং কোড উদাহরণসহ 200+ JavaScript ইন্টারভিউ প্রশ্নের একটি বিস্তৃত সংগ্রহ!

> **💡 প্রো টিপ:** এই উদাহরণগুলো কোডিং অনুশীলন করুন এবং ধারণাগুলো নিজের ভাষায় ব্যাখ্যা করার চেষ্টা করুন। "কেন" বোঝা "কী" জানার মতোই গুরুত্বপূর্ণ!

---

## 1. মৌলিক বিষয় ও বেসিক 🌱

<div style={{ background: 'rgba(0, 100, 200, 0.10)', padding: '15px', borderRadius: '5px', marginBottom: '20px', border: '1px solid rgba(0, 100, 200, 0.15)', color: 'inherit' }}>

**প্রশ্ন ১: JavaScript কী এবং এর মূল বৈশিষ্ট্যগুলো কী?**  
**উত্তর:** JavaScript একটি উচ্চ-স্তরের, ইন্টারপ্রেটেড প্রোগ্রামিং ভাষা যা প্রাথমিকভাবে ওয়েব ডেভেলপমেন্টের জন্য ব্যবহৃত হয়। মূল বৈশিষ্ট্যগুলো হলো:

- ডায়নামিক টাইপিং
- ফার্স্ট-ক্লাস ফাংশন
- প্রোটোটাইপ-ভিত্তিক অবজেক্ট ওরিয়েন্টেশন
- ইভেন্ট-চালিত প্রোগ্রামিং
- অ্যাসিনক্রোনাস প্রোগ্রামিং সাপোর্ট

**প্রশ্ন ২: JavaScript-এ বিভিন্ন ডেটা টাইপ কী কী?**  
**উত্তর:** JavaScript-এ 8টি ডেটা টাইপ রয়েছে:

- **প্রিমিটিভ টাইপ:** Number, String, Boolean, Undefined, Null, Symbol, BigInt
- **নন-প্রিমিটিভ:** Object (অ্যারে, ফাংশন, ডেট ইত্যাদি অন্তর্ভুক্ত)

```js
let num = 42; // Number
let str = "Hello"; // String
let bool = true; // Boolean
let undef; // Undefined
let empty = null; // Null
let sym = Symbol("id"); // Symbol
let big = 123n; // BigInt
let obj = { name: "John" }; // Object
```

**প্রশ্ন ৩: `var`, `let`, এবং `const` এর মধ্যে পার্থক্য কী?**  
**উত্তর:**

- `var`: ফাংশন-স্কোপড, পুনরায় ডিক্লেয়ার এবং আপডেট করা যায়, হয়েস্টেড
- `let`: ব্লক-স্কোপড, আপডেট করা যায় কিন্তু পুনরায় ডিক্লেয়ার করা যায় না, হয়েস্টেড কিন্তু ইনিশিয়ালাইজড নয়
- `const`: ব্লক-স্কোপড, আপডেট বা পুনরায় ডিক্লেয়ার করা যায় না, হয়েস্টেড কিন্তু ইনিশিয়ালাইজড নয়

```js
// var উদাহরণ
function varExample() {
  if (true) {
    var x = 1;
  }
  console.log(x); // 1 (if ব্লকের বাইরে অ্যাক্সেসযোগ্য)
}

// let উদাহরণ
function letExample() {
  if (true) {
    let y = 1;
  }
  console.log(y); // ReferenceError: y is not defined
}

// const উদাহরণ
const z = 1;
z = 2; // TypeError: Assignment to constant variable
```

**প্রশ্ন ৪: JavaScript-এ হয়েস্টিং কী?**  
**উত্তর:** হয়েস্টিং হলো JavaScript এর আচরণ যা কম্পাইলেশনের সময় ভেরিয়েবল এবং ফাংশন ডিক্লেয়ারেশনকে তাদের স্কোপের উপরে নিয়ে যায়।

```js
console.log(x); // undefined (ReferenceError নয়)
var x = 5;

// সমতুল্য:
var x;
console.log(x); // undefined
x = 5;

// ফাংশন হয়েস্টিং
sayHello(); // "Hello!" - হয়েস্টিংয়ের কারণে কাজ করে

function sayHello() {
  console.log("Hello!");
}
```

**প্রশ্ন ৫: `==` এবং `===` এর মধ্যে পার্থক্য কী?**  
**উত্তর:**

- `==` (লুজ ইকুয়ালিটি): টাইপ কোয়ার্সনসহ ভ্যালু তুলনা করে
- `===` (স্ট্রিক্ট ইকুয়ালিটি): কোয়ার্সন ছাড়াই ভ্যালু এবং টাইপ তুলনা করে

```js
console.log(5 == "5"); // true (টাইপ কোয়ার্সন)
console.log(5 === "5"); // false (ভিন্ন টাইপ)
console.log(null == undefined); // true
console.log(null === undefined); // false
```

**প্রশ্ন ৬: JavaScript-এ টাইপ কোয়ার্সন কী?**  
**উত্তর:** টাইপ কোয়ার্সন হলো একটি ডেটা টাইপ থেকে অন্য ডেটা টাইপে ভ্যালুর স্বয়ংক্রিয় রূপান্তর।

```js
// ইমপ্লিসিট কোয়ার্সন
console.log("5" + 1); // "51" (নাম্বার থেকে স্ট্রিং)
console.log("5" - 1); // 4 (স্ট্রিং থেকে নাম্বার)
console.log(true + 1); // 2 (বুলিয়ান থেকে নাম্বার)

// এক্সপ্লিসিট কোয়ার্সন
console.log(Number("5")); // 5
console.log(String(123)); // "123"
console.log(Boolean(0)); // false
```

**প্রশ্ন ৭: `null` এবং `undefined` এর মধ্যে পার্থক্য কী?**  
**উত্তর:**

- `undefined`: ভেরিয়েবল ডিক্লেয়ার করা হয়েছে কিন্তু কোনো ভ্যালু অ্যাসাইন করা হয়নি
- `null`: কোনো অবজেক্ট ভ্যালুর ইচ্ছাকৃত অনুপস্থিতি

```js
let a;
console.log(a); // undefined

let b = null;
console.log(b); // null

console.log(typeof undefined); // "undefined"
console.log(typeof null); // "object" (পরিচিত বাগ)
```

**প্রশ্ন ৮: ট্রুথি এবং ফলসি ভ্যালু কী?**  
**উত্তর:** ফলসি ভ্যালু: `false`, `0`, `-0`, `0n`, `""`, `null`, `undefined`, `NaN`
অন্য সব ভ্যালু ট্রুথি।

```js
// ফলসি ভ্যালু
console.log(Boolean(false)); // false
console.log(Boolean(0)); // false
console.log(Boolean("")); // false
console.log(Boolean(null)); // false
console.log(Boolean(undefined)); // false
console.log(Boolean(NaN)); // false

// ট্রুথি ভ্যালু
console.log(Boolean("0")); // true
console.log(Boolean([])); // true
console.log(Boolean({})); // true
```

**প্রশ্ন ৯: `typeof` অপারেটর কী এবং এর রিটার্ন ভ্যালু কী?**  
**উত্তর:** `typeof` অপারেন্ডের টাইপ নির্দেশক একটি স্ট্রিং রিটার্ন করে।

```js
console.log(typeof 42); // "number"
console.log(typeof "hello"); // "string"
console.log(typeof true); // "boolean"
console.log(typeof undefined); // "undefined"
console.log(typeof null); // "object" (পরিচিত বাগ)
console.log(typeof {}); // "object"
console.log(typeof []); // "object"
console.log(typeof function () {}); // "function"
console.log(typeof Symbol()); // "symbol"
```

**প্রশ্ন ১০: একটি ভেরিয়েবল অ্যারে কিনা কীভাবে চেক করবেন?**  
**উত্তর:** `Array.isArray()` মেথড ব্যবহার করুন:

```js
const arr = [1, 2, 3];
const obj = { 0: 1, 1: 2, 2: 3, length: 3 };

console.log(Array.isArray(arr)); // true
console.log(Array.isArray(obj)); // false

// বিকল্প মেথড (কম নির্ভরযোগ্য)
console.log(arr instanceof Array); // true
console.log(Object.prototype.toString.call(arr)); // "[object Array]"
```

</div>

---

## 2. ফাংশন ও স্কোপ 🔧

<div style={{ background: 'rgba(230, 150, 70, 0.10)', padding: '15px', borderRadius: '5px', marginBottom: '20px', border: '1px solid rgba(230, 150, 70, 0.15)', color: 'inherit' }}>

**প্রশ্ন ১১: JavaScript-এ ফাংশন তৈরি করার বিভিন্ন উপায় কী?**  
**উত্তর:** ফাংশন তৈরি করার কয়েকটি উপায় রয়েছে:

```js
// ফাংশন ডিক্লেয়ারেশন
function add(a, b) {
  return a + b;
}

// ফাংশন এক্সপ্রেশন
const multiply = function (a, b) {
  return a * b;
};

// অ্যারো ফাংশন
const divide = (a, b) => a / b;

// অবজেক্টে মেথড
const calc = {
  subtract: function (a, b) {
    return a - b;
  },
};

// কন্সট্রাক্টর ফাংশন
function Person(name) {
  this.name = name;
}
```

**প্রশ্ন ১২: ফাংশন ডিক্লেয়ারেশন এবং ফাংশন এক্সপ্রেশনের মধ্যে পার্থক্য কী?**  
**উত্তর:**

- ফাংশন ডিক্লেয়ারেশন সম্পূর্ণভাবে হয়েস্টেড হয়
- ফাংশন এক্সপ্রেশন হয়েস্টেড হয় না

```js
// হয়েস্টিংয়ের কারণে এটি কাজ করে
console.log(declared()); // "I'm declared!"

function declared() {
  return "I'm declared!";
}

// এটি এরর দেয়
console.log(expressed()); // TypeError: expressed is not a function

var expressed = function () {
  return "I'm an expression!";
};
```

**প্রশ্ন ১৩: অ্যারো ফাংশন কী এবং সাধারণ ফাংশন থেকে এর পার্থক্য কী?**  
**উত্তর:** অ্যারো ফাংশন হলো ফাংশন লেখার একটি সংক্ষিপ্ত সিনট্যাক্স যার কিছু মূল পার্থক্য রয়েছে:

```js
// সাধারণ ফাংশন
function regular() {
  console.log(this); // কীভাবে কল করা হয় তার উপর নির্ভর করে
  console.log(arguments); // arguments অবজেক্ট আছে
}

// অ্যারো ফাংশন
const arrow = () => {
  console.log(this); // লেক্সিক্যালি বাউন্ড this
  console.log(arguments); // ReferenceError: arguments is not defined
};

// অ্যারো ফাংশনের বিভিন্নতা
const single = (x) => x * 2;
const multiple = (x, y) => x + y;
const block = (x, y) => {
  const result = x + y;
  return result;
};
```

**প্রশ্ন ১৪: JavaScript-এ ক্লোজার কী?**  
**উত্তর:** ক্লোজার হলো এমন একটি ফাংশন যা বাইরের ফাংশন রিটার্ন হওয়ার পরেও তার বাইরের স্কোপের ভেরিয়েবলগুলো অ্যাক্সেস করতে পারে।

```js
function outerFunction(x) {
  return function innerFunction(y) {
    return x + y; // ইনার ফাংশনের x এর অ্যাক্সেস আছে
  };
}

const addFive = outerFunction(5);
console.log(addFive(3)); // 8

// ব্যবহারিক উদাহরণ: মডিউল প্যাটার্ন
function createCounter() {
  let count = 0;
  return {
    increment: () => ++count,
    decrement: () => --count,
    getCount: () => count,
  };
}

const counter = createCounter();
console.log(counter.increment()); // 1
console.log(counter.getCount()); // 1
```

**প্রশ্ন ১৫: JavaScript-এ স্কোপ চেইন কী?**  
**উত্তর:** স্কোপ চেইন হলো সেই প্রক্রিয়া যার মাধ্যমে JavaScript ভেরিয়েবল খোঁজে, সবচেয়ে ভিতরের স্কোপ থেকে শুরু করে বাইরের দিকে এগিয়ে যায়।

```js
const global = "I'm global";

function outer() {
  const outerVar = "I'm outer";

  function inner() {
    const innerVar = "I'm inner";
    console.log(innerVar); // ইনার স্কোপে পাওয়া গেছে
    console.log(outerVar); // আউটার স্কোপে পাওয়া গেছে
    console.log(global); // গ্লোবাল স্কোপে পাওয়া গেছে
  }

  inner();
}

outer();
```

**প্রশ্ন ১৬: JavaScript-এ `this` কীওয়ার্ড কী?**  
**উত্তর:** `this` বর্তমান ফাংশন এক্সিকিউট করা অবজেক্টকে রেফার করে। এর ভ্যালু ফাংশন কীভাবে কল করা হয় তার উপর নির্ভর করে।

```js
// গ্লোবাল কনটেক্সট
console.log(this); // Window অবজেক্ট (ব্রাউজার) বা global (Node.js)

// অবজেক্ট মেথড
const obj = {
  name: "John",
  greet: function () {
    console.log(this.name); // "John"
  },
};

// কন্সট্রাক্টর ফাংশন
function Person(name) {
  this.name = name;
}

// অ্যারো ফাংশন (লেক্সিক্যাল this)
const arrowObj = {
  name: "Jane",
  greet: () => {
    console.log(this.name); // undefined (this গ্লোবাল রেফার করে)
  },
};
```

**প্রশ্ন ১৭: `this` এর কনটেক্সট কীভাবে পরিবর্তন করবেন?**  
**উত্তর:** `call()`, `apply()`, বা `bind()` মেথড ব্যবহার করুন:

```js
function greet(greeting, punctuation) {
  console.log(greeting + " " + this.name + punctuation);
}

const person = { name: "John" };

// call() - আর্গুমেন্ট আলাদাভাবে পাস করা হয়
greet.call(person, "Hello", "!");

// apply() - আর্গুমেন্ট অ্যারে হিসেবে পাস করা হয়
greet.apply(person, ["Hi", "."]);

// bind() - বাউন্ড কনটেক্সটসহ নতুন ফাংশন রিটার্ন করে
const boundGreet = greet.bind(person, "Hey");
boundGreet("?");
```

**প্রশ্ন ১৮: higher-order ফাংশন কী?**  
**উত্তর:** এমন ফাংশন যা অন্য ফাংশনকে আর্গুমেন্ট হিসেবে নেয় অথবা ফাংশন রিটার্ন করে।

```js
// ফাংশন যা অন্য ফাংশনকে আর্গুমেন্ট হিসেবে নেয়
function repeat(fn, times) {
  for (let i = 0; i < times; i++) {
    fn();
  }
}

repeat(() => console.log("Hello"), 3);

// ফাংশন যা অন্য ফাংশন রিটার্ন করে
function multiplyBy(factor) {
  return function (number) {
    return number * factor;
  };
}

const double = multiplyBy(2);
console.log(double(5)); // 10
```

**প্রশ্ন ১৯: ফাংশন কারিং কী?**  
**উত্তর:** কারিং হলো একাধিক আর্গুমেন্ট সহ একটি ফাংশনকে একক আর্গুমেন্ট সহ ফাংশনের ক্রমে রূপান্তরিত করা।

```js
// সাধারণ ফাংশন
function add(a, b, c) {
  return a + b + c;
}

// কারিড ভার্সন
function curriedAdd(a) {
  return function (b) {
    return function (c) {
      return a + b + c;
    };
  };
}

// অ্যারো ফাংশন কারিং
const curriedAddArrow = (a) => (b) => (c) => a + b + c;

console.log(curriedAdd(1)(2)(3)); // 6
console.log(curriedAddArrow(1)(2)(3)); // 6
```

**প্রশ্ন ২০: তাৎক্ষণিক আহ্বানকৃত ফাংশন এক্সপ্রেশন (IIFE) কী?**  
**উত্তর:** এমন ফাংশন যা তাদের সংজ্ঞায়িত হওয়ার সাথে সাথেই কার্যকর হয়।

```js
// মৌলিক IIFE
(function () {
  console.log("IIFE executed!");
})();

// প্যারামিটার সহ IIFE
(function (name) {
  console.log("Hello, " + name);
})("World");

// অ্যারো IIFE
(() => {
  console.log("Arrow IIFE!");
})();

// মডিউল প্যাটার্নের জন্য IIFE
const myModule = (function () {
  let privateVar = 0;

  return {
    increment: () => ++privateVar,
    getCount: () => privateVar,
  };
})();
```

---

## ৩. অবজেক্ট ও প্রোটোটাইপ 🏗️

**প্রশ্ন ২১: জাভাস্ক্রিপ্টে কীভাবে অবজেক্ট তৈরি করবেন?**  
**উত্তর:** অবজেক্ট তৈরি করার একাধিক উপায় রয়েছে:

```js
// অবজেক্ট লিটারেল
const obj1 = { name: "John", age: 30 };

// কনস্ট্রাক্টর ফাংশন
function Person(name, age) {
  this.name = name;
  this.age = age;
}
const obj2 = new Person("Jane", 25);

// Object.create()
const obj3 = Object.create({ name: "Bob" });

// ক্লাস (ES6)
class Animal {
  constructor(species) {
    this.species = species;
  }
}
const obj4 = new Animal("Dog");

// ফ্যাক্টরি ফাংশন
function createUser(name) {
  return {
    name,
    greet() {
      console.log(`Hello, I'm ${this.name}`);
    },
  };
}
const obj5 = createUser("Alice");
```

**প্রশ্ন ২২: প্রোটোটাইপাল ইনহেরিটেন্স কী?**  
**উত্তর:** জাভাস্ক্রিপ্ট প্রোটোটাইপ-ভিত্তিক ইনহেরিটেন্স ব্যবহার করে যেখানে অবজেক্টগুলো অন্য অবজেক্ট থেকে প্রপার্টি এবং মেথড উত্তরাধিকার সূত্রে পেতে পারে।

```js
// কনস্ট্রাক্টর ফাংশন
function Animal(name) {
  this.name = name;
}

Animal.prototype.speak = function () {
  console.log(`${this.name} makes a sound`);
};

function Dog(name, breed) {
  Animal.call(this, name);
  this.breed = breed;
}

// ইনহেরিটেন্স সেট আপ
Dog.prototype = Object.create(Animal.prototype);
Dog.prototype.constructor = Dog;

Dog.prototype.bark = function () {
  console.log(`${this.name} barks!`);
};

const myDog = new Dog("Buddy", "Golden Retriever");
myDog.speak(); // "Buddy makes a sound"
myDog.bark(); // "Buddy barks!"
```

**প্রশ্ন ২৩: প্রোটোটাইপ চেইন কী?**  
**উত্তর:** প্রোটোটাইপ চেইন হলো এমন একটি প্রক্রিয়া যার মাধ্যমে অবজেক্টগুলো একে অপরের কাছ থেকে বৈশিষ্ট্য উত্তরাধিকার সূত্রে পায়।

```js
const animal = {
  eats: true,
  walk() {
    console.log("Animal walks");
  },
};

const rabbit = {
  jumps: true,
  __proto__: animal,
};

const longEar = {
  earLength: 10,
  __proto__: rabbit,
};

console.log(longEar.eats); // true (animal থেকে উত্তরাধিকার)
console.log(longEar.jumps); // true (rabbit থেকে উত্তরাধিকার)
longEar.walk(); // "Animal walks"

// প্রোটোটাইপ চেইন যাচাই
console.log(longEar.__proto__ === rabbit); // true
console.log(rabbit.__proto__ === animal); // true
console.log(animal.__proto__ === Object.prototype); // true
```

**প্রশ্ন ২৪: ES6 ক্লাস কী এবং এগুলো কীভাবে কাজ করে?**  
**উত্তর:** ES6 ক্লাস হলো জাভাস্ক্রিপ্টের প্রোটোটাইপ-ভিত্তিক ইনহেরিটেন্সের উপর সিনট্যাক্টিক সুগার।

```js
class Person {
  constructor(name, age) {
    this.name = name;
    this.age = age;
  }

  // ইনস্ট্যান্স মেথড
  greet() {
    console.log(`Hello, I'm ${this.name}`);
  }

  // স্ট্যাটিক মেথড
  static species() {
    return "Homo sapiens";
  }

  // গেটার
  get info() {
    return `${this.name} is ${this.age} years old`;
  }

  // সেটার
  set newAge(age) {
    if (age > 0) this.age = age;
  }
}

// ইনহেরিটেন্স
class Student extends Person {
  constructor(name, age, grade) {
    super(name, age);
    this.grade = grade;
  }

  study() {
    console.log(`${this.name} is studying`);
  }
}

const student = new Student("Alice", 20, "A");
student.greet(); // "Hello, I'm Alice"
student.study(); // "Alice is studying"
```

**প্রশ্ন ২৫: `__proto__` এবং `prototype` এর মধ্যে পার্থক্য কী?**  
**উত্তর:**

- `__proto__` হলো লুকআপ চেইনে ব্যবহৃত প্রকৃত অবজেক্ট
- `prototype` হলো কনস্ট্রাক্টর ফাংশনের একটি প্রপার্টি

```js
function Person(name) {
  this.name = name;
}

Person.prototype.greet = function () {
  console.log(`Hello, I'm ${this.name}`);
};

const john = new Person("John");

console.log(john.__proto__ === Person.prototype); // true
console.log(Person.prototype.constructor === Person); // true

// প্রোটোটাইপ চেইন
console.log(john.__proto__.__proto__ === Object.prototype); // true
console.log(Object.prototype.__proto__ === null); // true
```

**প্রশ্ন ২৬: গেটার এবং সেটার কী?**  
**উত্তর:** বিশেষ মেথড যা আপনাকে প্রপার্টি অ্যাক্সেস এবং পরিবর্তনের উপায় সংজ্ঞায়িত করতে দেয়।

```js
const person = {
  firstName: "John",
  lastName: "Doe",

  // গেটার
  get fullName() {
    return `${this.firstName} ${this.lastName}`;
  },

  // সেটার
  set fullName(value) {
    const parts = value.split(" ");
    this.firstName = parts[0];
    this.lastName = parts[1];
  },
};

console.log(person.fullName); // "John Doe"
person.fullName = "Jane Smith";
console.log(person.firstName); // "Jane"

// Object.defineProperty ব্যবহার করে
const obj = {};
Object.defineProperty(obj, "name", {
  get() {
    return this._name;
  },
  set(value) {
    this._name = value.toUpperCase();
  },
});

obj.name = "john";
console.log(obj.name); // "JOHN"
```

**প্রশ্ন ২৭: জাভাস্ক্রিপ্টে প্রপার্টি ডেসক্রিপ্টর কী?**  
**উত্তর:** প্রপার্টি ডেসক্রিপ্টর অবজেক্ট প্রপার্টির আচরণ সংজ্ঞায়িত করে।

```js
const obj = {};

Object.defineProperty(obj, "name", {
  value: "John",
  writable: true, // পরিবর্তন করা যায়
  enumerable: true, // for...in লুপে দেখা যায়
  configurable: true, // মুছে ফেলা বা পুনর্কনফিগার করা যায়
});

// প্রপার্টি ডেসক্রিপ্টর পেতে
console.log(Object.getOwnPropertyDescriptor(obj, "name"));

// একাধিক প্রপার্টি সংজ্ঞায়িত করা
Object.defineProperties(obj, {
  age: {
    value: 30,
    writable: false,
  },
  city: {
    value: "New York",
    enumerable: false,
  },
});
```

**প্রশ্ন ২৮: `Object.freeze()`, `Object.seal()`, এবং `Object.preventExtensions()` এর মধ্যে পার্থক্য কী?**  
**উত্তর:**

- `Object.freeze()`: সমস্ত পরিবর্তন প্রতিরোধ করে
- `Object.seal()`: প্রপার্টি যোগ/মুছে ফেলা প্রতিরোধ করে
- `Object.preventExtensions()`: নতুন প্রপার্টি যোগ করা প্রতিরোধ করে

```js
const obj1 = { name: "John" };
Object.freeze(obj1);
obj1.name = "Jane"; // স্ট্রিক্ট মোডে উপেক্ষিত, নন-স্ট্রিক্টে ত্রুটি
obj1.age = 30; // উপেক্ষিত
delete obj1.name; // উপেক্ষিত

const obj2 = { name: "John" };
Object.seal(obj2);
obj2.name = "Jane"; // কাজ করে
obj2.age = 30; // উপেক্ষিত
delete obj2.name; // উপেক্ষিত

const obj3 = { name: "John" };
Object.preventExtensions(obj3);
obj3.name = "Jane"; // কাজ করে
obj3.age = 30; // উপেক্ষিত
delete obj3.name; // কাজ করে
```

**প্রশ্ন ২৯: কীভাবে যাচাই করবেন যে একটি অবজেক্টে কোনো প্রপার্টি আছে কিনা?**  
**উত্তর:** প্রপার্টির অস্তিত্ব যাচাই করার জন্য কয়েকটি পদ্ধতি:

```js
const obj = { name: "John", age: 30 };

// hasOwnProperty (শুধুমাত্র নিজস্ব প্রপার্টি)
console.log(obj.hasOwnProperty("name")); // true

// in অপারেটর (উত্তরাধিকারসূত্রে প্রাপ্ত সহ)
console.log("name" in obj); // true
console.log("toString" in obj); // true (উত্তরাধিকারসূত্রে প্রাপ্ত)

// Object.hasOwnProperty.call (নিরাপদ)
console.log(Object.prototype.hasOwnProperty.call(obj, "name")); // true

// hasOwnProperty মেথড (ES2022)
console.log(Object.hasOwn(obj, "name")); // true

// undefined এর জন্য যাচাই
console.log(obj.name !== undefined); // true
```

**প্রশ্ন ৩০: অবজেক্ট ডেস্ট্রাক্চারিং কী?**  
**উত্তর:** একটি সিনট্যাক্স যা অবজেক্ট থেকে প্রপার্টিগুলোকে পৃথক ভেরিয়েবলে আনপ্যাক করতে দেয়।

```js
const person = {
  name: "John",
  age: 30,
  city: "New York",
  country: "USA",
};

// মৌলিক ডেস্ট্রাক্চারিং
const { name, age } = person;

// ভেরিয়েবল নাম পরিবর্তন
const { name: fullName, age: years } = person;

// ডিফল্ট মান
const { name, occupation = "Unknown" } = person;

// নেস্টেড ডেস্ট্রাক্চারিং
const user = {
  id: 1,
  profile: {
    name: "John",
    email: "john@email.com",
  },
};

const {
  profile: { name: userName, email },
} = user;

// ফাংশন প্যারামিটার ডেস্ট্রাক্চারিং
function greet({ name, age = 0 }) {
  console.log(`Hello ${name}, you are ${age} years old`);
}

greet(person);
```

---

## ৪. অ্যারে ও অ্যারে মেথড 📊

**প্রশ্ন ৩১: অ্যারে তৈরি করার বিভিন্ন উপায় কী কী?**  
**উত্তর:** অ্যারে তৈরি করার একাধিক পদ্ধতি:

```js
// অ্যারে লিটারেল
const arr1 = [1, 2, 3, 4, 5];

// অ্যারে কনস্ট্রাক্টর
const arr2 = new Array(1, 2, 3, 4, 5);
const arr3 = new Array(5); // ৫টি খালি স্লট সহ অ্যারে তৈরি করে

// Array.from()
const arr4 = Array.from("hello"); // ['h', 'e', 'l', 'l', 'o']
const arr5 = Array.from({ length: 3 }, (_, i) => i); // [0, 1, 2]

// Array.of()
const arr6 = Array.of(1, 2, 3); // [1, 2, 3]
const arr7 = Array.of(5); // [5] (new Array(5) থেকে ভিন্ন)

// স্প্রেড অপারেটর
const arr8 = [...arr1]; // একটি কপি তৈরি করে
```

**প্রশ্ন ৩২: ইটারেশনের জন্য প্রধান অ্যারে মেথডগুলো কী কী?**  
**উত্তর:** উদাহরণ সহ সাধারণ ইটারেশন মেথড:

```js
const numbers = [1, 2, 3, 4, 5];

// forEach - প্রতিটি এলিমেন্টের জন্য ফাংশন কার্যকর করে
numbers.forEach((num, index) => {
  console.log(`${index}: ${num}`);
});

// map - রূপান্তরিত এলিমেন্ট সহ নতুন অ্যারে তৈরি করে
const doubled = numbers.map((num) => num * 2);
console.log(doubled); // [2, 4, 6, 8, 10]

// filter - টেস্ট পাস করা এলিমেন্ট সহ নতুন অ্যারে তৈরি করে
const evens = numbers.filter((num) => num % 2 === 0);
console.log(evens); // [2, 4]

// find - টেস্ট পাস করা প্রথম এলিমেন্ট রিটার্ন করে
const found = numbers.find((num) => num > 3);
console.log(found); // 4

// findIndex - টেস্ট পাস করা প্রথম এলিমেন্টের ইনডেক্স রিটার্ন করে
const foundIndex = numbers.findIndex((num) => num > 3);
console.log(foundIndex); // 3

// some - অন্তত একটি এলিমেন্ট টেস্ট পাস করে কিনা যাচাই করে
const hasEven = numbers.some((num) => num % 2 === 0);
console.log(hasEven); // true

// every - সব এলিমেন্ট টেস্ট পাস করে কিনা যাচাই করে
const allPositive = numbers.every((num) => num > 0);
console.log(allPositive); // true
```

**প্রশ্ন ৩৩: reduce এবং reduceRight মেথড কী?**  
**উত্তর:** মেথড যা একটি অ্যারেকে একক মানে কমিয়ে আনে:

```js
const numbers = [1, 2, 3, 4, 5];

// reduce - বাম থেকে ডানে
const sum = numbers.reduce((acc, curr) => acc + curr, 0);
console.log(sum); // 15

// জটিল reduce উদাহরণ
const people = [
  { name: "John", age: 30 },
  { name: "Jane", age: 25 },
  { name: "Bob", age: 35 },
];

const groupedByAge = people.reduce((acc, person) => {
  const ageGroup = person.age >= 30 ? "adult" : "young";
  if (!acc[ageGroup]) acc[ageGroup] = [];
  acc[ageGroup].push(person);
  return acc;
}, {});

// reduceRight - ডান থেকে বামে
const string = ["a", "b", "c", "d"];
const reversed = string.reduceRight((acc, curr) => acc + curr, "");
console.log(reversed); // 'dcba'
```

**প্রশ্ন ৩৪: এলিমেন্ট যোগ/মুছে ফেলার জন্য অ্যারে মেথডগুলো কী কী?**  
**উত্তর:** অ্যারে পরিবর্তনকারী মেথড:

```js
const arr = [1, 2, 3];

// push - শেষে যোগ করে, নতুন দৈর্ঘ্য রিটার্ন করে
console.log(arr.push(4, 5)); // 5
console.log(arr); // [1, 2, 3, 4, 5]

// pop - শেষ থেকে মুছে ফেলে, মুছে ফেলা এলিমেন্ট রিটার্ন করে
console.log(arr.pop()); // 5
console.log(arr); // [1, 2, 3, 4]

// unshift - শুরুতে যোগ করে, নতুন দৈর্ঘ্য রিটার্ন করে
console.log(arr.unshift(0)); // 5
console.log(arr); // [0, 1, 2, 3, 4]

// shift - শুরু থেকে মুছে ফেলে, মুছে ফেলা এলিমেন্ট রিটার্ন করে
console.log(arr.shift()); // 0
console.log(arr); // [1, 2, 3, 4]

// splice - যে কোনো স্থানে এলিমেন্ট যোগ/মুছে ফেলে
const removed = arr.splice(1, 2, "a", "b"); // start, deleteCount, ...items
console.log(removed); // [2, 3]
console.log(arr); // [1, 'a', 'b', 4]

// slice - অংশের শ্যালো কপি রিটার্ন করে (মূল পরিবর্তন করে না)
const copy = arr.slice(1, 3);
console.log(copy); // ['a', 'b']
console.log(arr); // [1, 'a', 'b', 4] (অপরিবর্তিত)
```

**প্রশ্ন ৩৫: আপনি কীভাবে একটি মান array কিনা তা পরীক্ষা করবেন?**  
**উত্তর:** Array পরীক্ষা করার একাধিক পদ্ধতি:

```js
const arr = [1, 2, 3];
const obj = { 0: 1, 1: 2, 2: 3, length: 3 };

// সর্বোত্তম পদ্ধতি
console.log(Array.isArray(arr)); // true
console.log(Array.isArray(obj)); // false

// instanceof (কিছু ক্ষেত্রে অনির্ভরযোগ্য হতে পারে)
console.log(arr instanceof Array); // true

// Object.prototype.toString
console.log(Object.prototype.toString.call(arr)); // "[object Array]"

// constructor property (পরিবর্তিত হতে পারে)
console.log(arr.constructor === Array); // true
```

**প্রশ্ন ৩৬: Array destructuring কী?**  
**উত্তর:** Array এর মানগুলোকে পৃথক variables এ unpack করার syntax:

```js
const colors = ["red", "green", "blue", "yellow"];

// মৌলিক destructuring
const [first, second] = colors;
console.log(first, second); // "red green"

// Elements skip করা
const [primary, , tertiary] = colors;
console.log(primary, tertiary); // "red blue"

// Rest operator
const [head, ...tail] = colors;
console.log(head); // "red"
console.log(tail); // ["green", "blue", "yellow"]

// Default values
const [a, b, c, d, e = "default"] = colors;
console.log(e); // "yellow" (default নয়)

// Variables swap করা
let x = 1,
  y = 2;
[x, y] = [y, x];
console.log(x, y); // 2 1

// Nested arrays
const nested = [
  [1, 2],
  [3, 4],
];
const [[a1, b1], [a2, b2]] = nested;
```

**প্রশ্ন ৩৭: অনুসন্ধানের জন্য array methods কী কী?**  
**উত্তর:** Arrays এ elements খুঁজে বের করার methods:

```js
const fruits = ["apple", "banana", "orange", "apple"];

// indexOf - প্রথম occurrence এর index
console.log(fruits.indexOf("apple")); // 0
console.log(fruits.indexOf("grape")); // -1

// lastIndexOf - শেষ occurrence এর index
console.log(fruits.lastIndexOf("apple")); // 3

// includes - boolean check
console.log(fruits.includes("banana")); // true

// find - test পাস করা প্রথম element
const people = [
  { name: "John", age: 30 },
  { name: "Jane", age: 25 },
];
const adult = people.find((person) => person.age >= 30);
console.log(adult); // { name: 'John', age: 30 }

// findIndex - test পাস করা প্রথম element এর index
const adultIndex = people.findIndex((person) => person.age >= 30);
console.log(adultIndex); // 0
```

**প্রশ্ন ৩৮: JavaScript এ arrays কীভাবে sort করবেন?**  
**উত্তর:** Custom comparators সহ sort method ব্যবহার করে:

```js
const numbers = [3, 1, 4, 1, 5, 9, 2, 6];

// Default sort (strings এ convert করে)
console.log(numbers.sort()); // [1, 1, 2, 3, 4, 5, 6, 9]

// Numeric sort
const numericSort = [...numbers].sort((a, b) => a - b);
console.log(numericSort); // [1, 1, 2, 3, 4, 5, 6, 9]

// Reverse numeric sort
const reverseSort = [...numbers].sort((a, b) => b - a);

// String array sort
const names = ["John", "jane", "Bob", "alice"];
const sortedNames = names.sort(); // ['Bob', 'John', 'alice', 'jane']

// Case-insensitive sort
const caseInsensitive = names.sort((a, b) =>
  a.toLowerCase().localeCompare(b.toLowerCase())
);

// Object sorting
const people = [
  { name: "John", age: 30 },
  { name: "Jane", age: 25 },
  { name: "Bob", age: 35 },
];

const sortedByAge = people.sort((a, b) => a.age - b.age);
const sortedByName = people.sort((a, b) => a.name.localeCompare(b.name));
```

**প্রশ্ন ৩৯: আপনি কীভাবে arrays flatten করবেন?**  
**উত্তর:** Nested arrays flatten করার একাধিক পদ্ধতি:

```js
const nested = [1, [2, 3], [4, [5, 6]], 7];

// flat() - default এ এক level flatten করে
console.log(nested.flat()); // [1, 2, 3, 4, [5, 6], 7]

// flat(depth) - depth নির্দিষ্ট করা
console.log(nested.flat(2)); // [1, 2, 3, 4, 5, 6, 7]

// flat(Infinity) - সব level flatten করা
console.log(nested.flat(Infinity)); // [1, 2, 3, 4, 5, 6, 7]

// flatMap() - map তারপর flat
const sentences = ["hello world", "how are you"];
const words = sentences.flatMap((sentence) => sentence.split(" "));
console.log(words); // ['hello', 'world', 'how', 'are', 'you']

// Recursion ব্যবহার করে manual flatten
function flattenArray(arr) {
  return arr.reduce((flat, item) => {
    return flat.concat(Array.isArray(item) ? flattenArray(item) : item);
  }, []);
}
```

**প্রশ্ন ৪০: `concat()` এবং spread operator এর মধ্যে পার্থক্য কী?**  
**উত্তর:** দুটোই arrays merge করে কিন্তু ভিন্ন আচরণ সহ:

```js
const arr1 = [1, 2];
const arr2 = [3, 4];
const arr3 = [5, 6];

// concat() - নতুন array return করে
const result1 = arr1.concat(arr2, arr3);
console.log(result1); // [1, 2, 3, 4, 5, 6]

// Spread operator - আরো নমনীয়
const result2 = [...arr1, ...arr2, ...arr3];
console.log(result2); // [1, 2, 3, 4, 5, 6]

// Spread যেকোনো position এ insertion এর সুবিধা দেয়
const result3 = [0, ...arr1, 2.5, ...arr2];
console.log(result3); // [0, 1, 2, 2.5, 3, 4]

// Performance: সাধারণ concatenation এর জন্য concat দ্রুত
// Spread আরো readable এবং flexible
```

---

## ৫। Asynchronous JavaScript 🚀

**প্রশ্ন ৪১: JavaScript এ event loop কী?**  
**উত্তর:** Event loop হল সেই mechanism যা JavaScript এর single-threaded environment এ asynchronous operations handle করে।

```js
console.log("1");

setTimeout(() => {
  console.log("2");
}, 0);

Promise.resolve().then(() => {
  console.log("3");
});

console.log("4");

// Output: 1, 4, 3, 2
// ব্যাখ্যা: Microtasks (Promises) এর priority macrotasks (setTimeout) এর চেয়ে বেশি
```

**প্রশ্ন ৪২: Promises কী এবং এগুলো কীভাবে কাজ করে?**  
**উত্তর:** Promises একটি asynchronous operation এর eventual completion বা failure প্রতিনিধিত্ব করে।

```js
// Promise তৈরি করা
const myPromise = new Promise((resolve, reject) => {
  const success = Math.random() > 0.5;

  setTimeout(() => {
    if (success) {
      resolve("Operation successful!");
    } else {
      reject(new Error("Operation failed!"));
    }
  }, 1000);
});

// Promise consume করা
myPromise
  .then((result) => {
    console.log(result);
    return "Next step";
  })
  .then((nextResult) => {
    console.log(nextResult);
  })
  .catch((error) => {
    console.error(error.message);
  })
  .finally(() => {
    console.log("Cleanup operations");
  });

// Promise states: pending, fulfilled, rejected
```

**প্রশ্ন ৪৩: async/await কী এবং এটি কীভাবে কাজ করে?**  
**উত্তর:** Async/await হল Promises এর উপর syntactic sugar যা asynchronous code কে synchronous এর মত দেখায়।

```js
// Promise return করে এমন function
function fetchData() {
  return new Promise((resolve) => {
    setTimeout(() => resolve("Data fetched"), 1000);
  });
}

// async/await ব্যবহার করা
async function getData() {
  try {
    console.log("Fetching data...");
    const data = await fetchData();
    console.log(data);
    return data;
  } catch (error) {
    console.error("Error:", error);
  }
}

// Async function call করা
getData().then((result) => console.log("Final result:", result));

// একাধিক awaits
async function fetchMultipleData() {
  const data1 = await fetchData();
  const data2 = await fetchData();
  return [data1, data2];
}

// Parallel execution
async function fetchParallelData() {
  const [data1, data2] = await Promise.all([fetchData(), fetchData()]);
  return [data1, data2];
}
```

**প্রশ্ন ৪৪: Promise static methods গুলো কী কী?**  
**উত্তর:** Promise একাধিক promises handle করার জন্য বেশ কিছু static methods প্রদান করে:

```js
const promise1 = Promise.resolve(3);
const promise2 = new Promise((resolve) =>
  setTimeout(() => resolve("foo"), 1000)
);
const promise3 = Promise.resolve(42);

// Promise.all - সবগুলো resolve হওয়ার জন্য অপেক্ষা করে, কোনো reject হলে fail করে
Promise.all([promise1, promise2, promise3]).then((values) =>
  console.log(values)
); // [3, 'foo', 42]

// Promise.allSettled - সবগুলো settle (resolve বা reject) হওয়ার জন্য অপেক্ষা করে
Promise.allSettled([promise1, Promise.reject("error"), promise3]).then(
  (results) => console.log(results)
);
// [{status: 'fulfilled', value: 3}, {status: 'rejected', reason: 'error'}, ...]

// Promise.race - প্রথম settled promise দিয়ে resolves/rejects করে
Promise.race([promise1, promise2, promise3]).then((value) =>
  console.log(value)
); // 3 (প্রথম resolve হওয়া)

// Promise.any - প্রথম fulfilled promise দিয়ে resolve করে
Promise.any([Promise.reject("error"), promise2, promise3]).then((value) =>
  console.log(value)
); // 'foo' অথবা 42

// Promise.resolve/reject - resolved/rejected promises তৈরি করে
const resolved = Promise.resolve("immediate");
const rejected = Promise.reject(new Error("immediate error"));
```

**প্রশ্ন ৪৫: Callback hell কী এবং এটি কীভাবে এড়ানো যায়?**  
**উত্তর:** Callback hell হল একাধিক callbacks এর nesting যা code পড়তে এবং maintain করতে কঠিন করে তোলে।

```js
// Callback hell উদাহরণ
getData((data) => {
  processData(data, (processedData) => {
    saveData(processedData, (result) => {
      sendNotification(result, (notification) => {
        console.log("All done!");
      });
    });
  });
});

// সমাধান ১: Named functions
function handleData(data) {
  processData(data, handleProcessedData);
}

function handleProcessedData(processedData) {
  saveData(processedData, handleSavedData);
}

// সমাধান ২: Promises
getData()
  .then((data) => processData(data))
  .then((processedData) => saveData(processedData))
  .then((result) => sendNotification(result))
  .then(() => console.log("All done!"))
  .catch((error) => console.error(error));

// সমাধান ৩: Async/await
async function handleDataFlow() {
  try {
    const data = await getData();
    const processedData = await processData(data);
    const result = await saveData(processedData);
    await sendNotification(result);
    console.log("All done!");
  } catch (error) {
    console.error(error);
  }
}
```

**প্রশ্ন ৪৬: Microtasks এবং macrotasks কী?**  
**উত্তর:** Event loop এ বিভিন্ন priority সহ asynchronous tasks এর বিভিন্ন ধরন।

```js
console.log("1"); // Synchronous

setTimeout(() => console.log("2"), 0); // Macrotask

Promise.resolve().then(() => console.log("3")); // Microtask

queueMicrotask(() => console.log("4")); // Microtask

setTimeout(() => console.log("5"), 0); // Macrotask

Promise.resolve()
  .then(() => {
    console.log("6");
    return Promise.resolve();
  })
  .then(() => console.log("7")); // Chained microtasks

console.log("8"); // Synchronous

// Output: 1, 8, 3, 4, 6, 7, 2, 5
// Microtasks সবসময় macrotasks এর আগে execute হয়
```

**প্রশ্ন ৪৭: Async operations এ errors কীভাবে handle করবেন?**  
**উত্তর:** Asynchronous code এ errors handle করার একাধিক উপায়:

```js
// Promise error handling
fetch("/api/data")
  .then((response) => {
    if (!response.ok) {
      throw new Error(`HTTP error! status: ${response.status}`);
    }
    return response.json();
  })
  .then((data) => console.log(data))
  .catch((error) => console.error("Fetch error:", error));

// Async/await error handling
async function fetchData() {
  try {
    const response = await fetch("/api/data");
    if (!response.ok) {
      throw new Error(`HTTP error! status: ${response.status}`);
    }
    const data = await response.json();
    return data;
  } catch (error) {
    console.error("Error fetching data:", error);
    throw error; // প্রয়োজনে re-throw করুন
  }
}

// Global error handlers
window.addEventListener("unhandledrejection", (event) => {
  console.error("Unhandled promise rejection:", event.reason);
  event.preventDefault(); // Default browser behavior prevent করুন
});

process.on("unhandledRejection", (reason, promise) => {
  console.error("Unhandled Rejection at:", promise, "reason:", reason);
});
```

**প্রশ্ন ৪৮: setTimeout এবং setInterval এর মধ্যে পার্থক্য কী?**  
**উত্তর:** দুটোই timing operations এর জন্য ব্যবহৃত হয় কিন্তু ভিন্নভাবে আচরণ করে:

```js
// setTimeout - delay এর পর একবার execute করে
const timeoutId = setTimeout(() => {
  console.log("Executed once after 1 second");
}, 1000);

// Timeout clear করা
clearTimeout(timeoutId);

// setInterval - বারবার execute করে
const intervalId = setInterval(() => {
  console.log("Executed every second");
}, 1000);

// Interval clear করা
setTimeout(() => {
  clearInterval(intervalId);
  console.log("Interval cleared");
}, 5000);

// Recursive setTimeout (setInterval এর চেয়ে ভাল বিকল্প)
function recursiveTimeout() {
  console.log("Recursive execution");
  setTimeout(recursiveTimeout, 1000);
}

// Dynamic delay সহ
function dynamicTimeout() {
  console.log("Dynamic execution");
  const delay = Math.random() * 2000 + 500; // 500-2500ms
  setTimeout(dynamicTimeout, delay);
}
```

**প্রশ্ন ৪৯: Debouncing এবং throttling কী?**  
**উত্তর:** Function execution এর frequency নিয়ন্ত্রণ করার কৌশল:

```js
// Debouncing - delay এর পর execute করে, নতুন call এ delay reset করে
function debounce(func, delay) {
  let timeoutId;
  return function (...args) {
    clearTimeout(timeoutId);
    timeoutId = setTimeout(() => func.apply(this, args), delay);
  };
}

// ব্যবহার: Search input
const debouncedSearch = debounce((query) => {
  console.log("Searching for:", query);
  // এখানে API call
}, 300);

document.getElementById("search").addEventListener("input", (e) => {
  debouncedSearch(e.target.value);
});

// Throttling - interval অনুযায়ী সর্বোচ্চ একবার execute করে
function throttle(func, interval) {
  let lastTime = 0;
  return function (...args) {
    const now = Date.now();
    if (now - lastTime >= interval) {
      lastTime = now;
      func.apply(this, args);
    }
  };
}

// ব্যবহার: Scroll event
const throttledScroll = throttle(() => {
  console.log("Scroll position:", window.scrollY);
}, 100);

window.addEventListener("scroll", throttledScroll);
```

**প্রশ্ন ৫০: JavaScript এ API calls কীভাবে করবেন?**  
**উত্তর:** HTTP requests করার একাধিক পদ্ধতি:

```js
// Fetch API (আধুনিক approach)
async function fetchUserData(userId) {
  try {
    const response = await fetch(`/api/users/${userId}`, {
      method: "GET",
      headers: {
        "Content-Type": "application/json",
        Authorization: "Bearer token",
      },
    });

    if (!response.ok) {
      throw new Error(`Error: ${response.status}`);
    }

    const userData = await response.json();
    return userData;
  } catch (error) {
    console.error("Failed to fetch user data:", error);
    throw error;
  }
}

// Fetch দিয়ে POST request
async function createUser(userData) {
  try {
    const response = await fetch("/api/users", {
      method: "POST",
      headers: {
        "Content-Type": "application/json",
      },
      body: JSON.stringify(userData),
    });

    const result = await response.json();
    return result;
  } catch (error) {
    console.error("Failed to create user:", error);
    throw error;
  }
}

// XMLHttpRequest (পুরাতন approach)
function makeXHRRequest(url, method = "GET") {
  return new Promise((resolve, reject) => {
    const xhr = new XMLHttpRequest();
    xhr.open(method, url);

    xhr.onload = () => {
      if (xhr.status >= 200 && xhr.status < 300) {
        resolve(JSON.parse(xhr.responseText));
      } else {
        reject(new Error(`Request failed: ${xhr.status}`));
      }
    };

    xhr.onerror = () => reject(new Error("Network error"));
    xhr.send();
  });
}
```

---

## ৬। DOM Manipulation & Events 🌐

**প্রশ্ন ৫১: আপনি কীভাবে DOM elements select করবেন?**  
**উত্তর:** DOM থেকে elements select করার একাধিক পদ্ধতি:

```js
// ID দিয়ে
const elementById = document.getElementById("myId");

// Class name দিয়ে
const elementsByClass = document.getElementsByClassName("myClass");
const firstByClass = elementsByClass[0];

// Tag name দিয়ে
const elementsByTag = document.getElementsByTagName("div");

// Query selector (CSS-style selectors)
const firstMatch = document.querySelector(".myClass");
const allMatches = document.querySelectorAll(".myClass");

// Advanced selectors
const nested = document.querySelector("div.container > p.highlight");
const multiple = document.querySelectorAll(
  'input[type="text"], input[type="email"]'
);

// Traversing
const parent = element.parentElement;
const children = element.children;
const siblings = element.nextElementSibling;
const previous = element.previousElementSibling;
```

**প্রশ্ন ৫২: আপনি কীভাবে DOM elements manipulate করবেন?**  
**উত্তর:** DOM elements modify করার বিভিন্ন উপায়:

```js
const element = document.getElementById("myElement");

// Text content
element.textContent = "New text content";
element.innerHTML = "<strong>HTML content</strong>";

// Attributes
element.setAttribute("data-id", "123");
element.getAttribute("data-id");
element.removeAttribute("data-id");

// Properties
element.id = "newId";
element.className = "newClass";
element.classList.add("active");
element.classList.remove("inactive");
element.classList.toggle("highlight");
element.classList.contains("active");

// Styles
element.style.color = "red";
element.style.backgroundColor = "blue";
element.style.cssText = "color: red; background: blue;";

// Elements তৈরি এবং insert করা
const newElement = document.createElement("div");
newElement.textContent = "New element";
newElement.className = "new-item";

// Insertion methods
parent.appendChild(newElement);
parent.insertBefore(newElement, existingChild);
element.insertAdjacentHTML("beforeend", "<p>New paragraph</p>");
element.insertAdjacentElement("afterbegin", newElement);

// Elements remove করা
element.remove();
parent.removeChild(element);
```

**প্রশ্ন ৫৩: ইভেন্ট লিসেনার কী এবং কীভাবে সেগুলো ব্যবহার করবেন?**  
**উত্তর:** ইভেন্ট লিসেনার আপনাকে ব্যবহারকারীর ইন্টারঅ্যাকশনে সাড়া দিতে সাহায্য করে:

```js
const button = document.getElementById("myButton");

// ইভেন্ট লিসেনার যোগ করা
button.addEventListener("click", function (event) {
  console.log("Button clicked!");
  console.log("Event type:", event.type);
  console.log("Target:", event.target);
});

// অ্যারো ফাংশন লিসেনার
button.addEventListener("click", (e) => {
  e.preventDefault(); // ডিফল্ট আচরণ প্রতিরোধ করা
  e.stopPropagation(); // ইভেন্ট বাবলিং বন্ধ করা
});

// একাধিক লিসেনার
button.addEventListener("mouseenter", handleMouseEnter);
button.addEventListener("mouseleave", handleMouseLeave);

function handleMouseEnter(e) {
  e.target.style.backgroundColor = "lightblue";
}

function handleMouseLeave(e) {
  e.target.style.backgroundColor = "";
}

// ইভেন্ট লিসেনার সরানো
button.removeEventListener("click", handleClick);

// ইভেন্ট ডেলিগেশন (ডায়নামিক কনটেন্টের জন্য)
document.addEventListener("click", function (e) {
  if (e.target.matches(".dynamic-button")) {
    console.log("Dynamic button clicked");
  }
});
```

**প্রশ্ন ৫৪: ইভেন্ট বাবলিং এবং ক্যাপচারিং কী?**  
**উত্তর:** DOM এর মাধ্যমে ইভেন্টগুলো কীভাবে প্রচারিত হয় তার প্রক্রিয়া:

```js
// HTML: <div id="outer"><div id="inner"><button>Click</button></div></div>

const outer = document.getElementById("outer");
const inner = document.getElementById("inner");
const button = document.querySelector("button");

// ইভেন্ট বাবলিং (ডিফল্ট) - টার্গেট থেকে রুট পর্যন্ত
button.addEventListener("click", () => console.log("Button"));
inner.addEventListener("click", () => console.log("Inner"));
outer.addEventListener("click", () => console.log("Outer"));

// বাটন ক্লিক করলে আউটপুট: Button, Inner, Outer

// ইভেন্ট ক্যাপচারিং - রুট থেকে টার্গেট পর্যন্ত
button.addEventListener("click", () => console.log("Button capture"), true);
inner.addEventListener("click", () => console.log("Inner capture"), true);
outer.addEventListener("click", () => console.log("Outer capture"), true);

// আউটপুট: Outer capture, Inner capture, Button capture

// প্রচারণা বন্ধ করা
inner.addEventListener("click", (e) => {
  e.stopPropagation();
  console.log("Stopped at inner");
});

// তাৎক্ষণিক প্রচারণা বন্ধ
inner.addEventListener("click", (e) => {
  e.stopImmediatePropagation();
  console.log("No other listeners will run");
});
```

**প্রশ্ন ৫৫: ফর্ম ইভেন্টগুলো কীভাবে হ্যান্ডেল করবেন?**  
**উত্তর:** ফর্ম এলিমেন্ট এবং তাদের ইভেন্টগুলোর সাথে কাজ করা:

```js
const form = document.getElementById("myForm");
const input = document.getElementById("myInput");

// ফর্ম সাবমিশন
form.addEventListener("submit", function (e) {
  e.preventDefault(); // ডিফল্ট ফর্ম সাবমিশন প্রতিরোধ করা

  const formData = new FormData(form);
  const data = Object.fromEntries(formData);
  console.log("Form data:", data);

  // ফর্ম ভ্যালিডেট এবং প্রক্রিয়া করা
  if (validateForm(data)) {
    submitForm(data);
  }
});

// ইনপুট ইভেন্ট
input.addEventListener("input", function (e) {
  console.log("Input value changed:", e.target.value);
  validateField(e.target);
});

input.addEventListener("focus", function (e) {
  e.target.classList.add("focused");
});

input.addEventListener("blur", function (e) {
  e.target.classList.remove("focused");
  validateField(e.target);
});

// কাস্টম ভ্যালিডেশন
function validateField(field) {
  const value = field.value.trim();
  const errorElement = document.getElementById(field.id + "-error");

  if (value === "") {
    field.classList.add("error");
    errorElement.textContent = "This field is required";
  } else {
    field.classList.remove("error");
    errorElement.textContent = "";
  }
}

// ফাইল ইনপুট হ্যান্ডলিং
const fileInput = document.getElementById("fileInput");
fileInput.addEventListener("change", function (e) {
  const files = e.target.files;
  for (let file of files) {
    console.log("File:", file.name, file.size, file.type);
  }
});
```

**প্রশ্ন ৫৬: `innerHTML`, `textContent`, এবং `innerText` এর মধ্যে পার্থক্য কী?**  
**উত্তর:** এলিমেন্ট কনটেন্ট পেতে/সেট করার বিভিন্ন উপায়:

```js
const element = document.getElementById("content");

// innerHTML - HTML কনটেন্ট পায়/সেট করে
element.innerHTML = "<strong>Bold text</strong>";
console.log(element.innerHTML); // "<strong>Bold text</strong>"

// textContent - টেক্সট কনটেন্ট পায়/সেট করে, HTML উপেক্ষা করে
element.textContent = "<strong>Bold text</strong>";
console.log(element.textContent); // "<strong>Bold text</strong>"

// innerText - দৃশ্যমান টেক্সট পায়/সেট করে, স্টাইলিং মেনে চলে
const hiddenElement = document.createElement("div");
hiddenElement.innerHTML = '<span style="display: none;">Hidden</span>Visible';
console.log(hiddenElement.textContent); // "HiddenVisible"
console.log(hiddenElement.innerText); // "Visible"

// পারফরম্যান্স এবং নিরাপত্তা
// innerHTML: স্ক্রিপ্ট চালাতে পারে (XSS ঝুঁকি), ধীর
// textContent: XSS থেকে নিরাপদ, দ্রুত
// innerText: CSS বিবেচনা করে, textContent এর চেয়ে ধীর
```

**প্রশ্ন ৫৭: এলিমেন্টের মাত্রা এবং অবস্থান নিয়ে কীভাবে কাজ করবেন?**  
**উত্তর:** এলিমেন্টের আকার এবং অবস্থান পাওয়া এবং সেট করা:

```js
const element = document.getElementById("myElement");

// মাত্রা পাওয়া
console.log("Width:", element.offsetWidth); // প্যাডিং, বর্ডার সহ
console.log("Height:", element.offsetHeight); // প্যাডিং, বর্ডার সহ
console.log("Client width:", element.clientWidth); // বর্ডার বাদে
console.log("Client height:", element.clientHeight); // বর্ডার বাদে
console.log("Scroll width:", element.scrollWidth); // সম্পূর্ণ কনটেন্ট প্রস্থ
console.log("Scroll height:", element.scrollHeight); // সম্পূর্ণ কনটেন্ট উচ্চতা

// অবস্থান পাওয়া
console.log("Offset left:", element.offsetLeft);
console.log("Offset top:", element.offsetTop);

// getBoundingClientRect - ভিউপোর্টের সাপেক্ষে
const rect = element.getBoundingClientRect();
console.log("Rect:", {
  top: rect.top,
  left: rect.left,
  width: rect.width,
  height: rect.height,
  right: rect.right,
  bottom: rect.bottom,
});

// স্ক্রল অবস্থান
console.log("Scroll left:", element.scrollLeft);
console.log("Scroll top:", element.scrollTop);

// উইন্ডো মাত্রা
console.log("Window width:", window.innerWidth);
console.log("Window height:", window.innerHeight);
console.log("Document width:", document.documentElement.scrollWidth);
```

**প্রশ্ন ৫৮: কাস্টম ইভেন্ট কীভাবে তৈরি এবং পাঠাবেন?**  
**উত্তর:** কাস্টম ইভেন্ট তৈরি এবং হ্যান্ডল করা:

```js
// কাস্টম ইভেন্ট তৈরি করা
const customEvent = new CustomEvent("myCustomEvent", {
  detail: {
    message: "Hello from custom event!",
    timestamp: Date.now(),
  },
  bubbles: true,
  cancelable: true,
});

// কাস্টম ইভেন্ট পাঠানো
const element = document.getElementById("myElement");
element.dispatchEvent(customEvent);

// কাস্টম ইভেন্ট শোনা
element.addEventListener("myCustomEvent", function (e) {
  console.log("Custom event received:", e.detail);
});

// ইভেন্ট কনস্ট্রাক্টর (পুরাতন পদ্ধতি)
const oldEvent = new Event("oldCustomEvent", {
  bubbles: true,
  cancelable: true,
});

// createEvent দিয়ে ইভেন্ট তৈরি (লিগেসি)
const legacyEvent = document.createEvent("CustomEvent");
legacyEvent.initCustomEvent("legacyEvent", true, true, { data: "legacy" });

// ব্যবহারিক উদাহরণ: কম্পোনেন্ট যোগাযোগ
class EventBus {
  constructor() {
    this.element = document.createElement("div");
  }

  on(event, callback) {
    this.element.addEventListener(event, callback);
  }

  emit(event, data) {
    this.element.dispatchEvent(new CustomEvent(event, { detail: data }));
  }

  off(event, callback) {
    this.element.removeEventListener(event, callback);
  }
}

const eventBus = new EventBus();
eventBus.on("userLogin", (e) => console.log("User logged in:", e.detail));
eventBus.emit("userLogin", { username: "john" });
```

**প্রশ্ন ৫৯: `addEventListener` এবং `onclick` এর মধ্যে পার্থক্য কী?**  
**উত্তর:** ইভেন্ট হ্যান্ডেল করার বিভিন্ন পদ্ধতি:

```js
const button = document.getElementById("myButton");

// onclick প্রপার্টি - শুধুমাত্র একটি হ্যান্ডলার থাকতে পারে
button.onclick = function () {
  console.log("First handler");
};

button.onclick = function () {
  console.log("Second handler"); // প্রথমটি প্রতিস্থাপন করে
};

// addEventListener - একাধিক হ্যান্ডলার থাকতে পারে
button.addEventListener("click", function () {
  console.log("First listener");
});

button.addEventListener("click", function () {
  console.log("Second listener"); // উভয়ই চালু হবে
});

// addEventListener এর সুবিধা:
// ১. একই ইভেন্টের জন্য একাধিক লিসেনার
// ২. ভাল নিয়ন্ত্রণ (ক্যাপচার/বাবল ফেজ)
// ৩. নির্দিষ্ট লিসেনার সরানো যায়
// ৪. আধুনিক জাভাস্ক্রিপ্ট অনুশীলনের জন্য ভাল

const handler = function () {
  console.log("Removable handler");
};

button.addEventListener("click", handler);
button.removeEventListener("click", handler); // সরানো যায়

// onclick নির্বাচনীভাবে সরানো যায় না
button.onclick = null; // সব onclick হ্যান্ডলার সরিয়ে দেয়
```

**প্রশ্ন ৬০: জাভাস্ক্রিপ্টে CSS ক্লাসের সাথে কীভাবে কাজ করবেন?**  
**উত্তর:** গতিশীলভাবে CSS ক্লাস ম্যানিপুলেট করা:

```js
const element = document.getElementById("myElement");

// classList মেথড
element.classList.add("active"); // ক্লাস যোগ করা
element.classList.remove("inactive"); // ক্লাস সরানো
element.classList.toggle("highlight"); // ক্লাস টগল করা
element.classList.contains("active"); // ক্লাস আছে কিনা চেক করা
element.classList.replace("old", "new"); // ক্লাস প্রতিস্থাপন করা

// একাধিক ক্লাস
element.classList.add("class1", "class2", "class3");
element.classList.remove("class1", "class2");

// className প্রপার্টি (পুরাতন পদ্ধতি)
element.className = "new-class another-class";
element.className += " additional-class";

// শর্তাধীন ক্লাস ব্যবস্থাপনা
function updateStatus(isActive) {
  if (isActive) {
    element.classList.add("active");
    element.classList.remove("inactive");
  } else {
    element.classList.add("inactive");
    element.classList.remove("active");
  }
}

// ইউটিলিটি ফাংশন
function hasClass(element, className) {
  return element.classList.contains(className);
}

function toggleClass(element, className, force) {
  return element.classList.toggle(className, force);
}

// CSS ক্লাস অ্যানিমেশন উদাহরণ
function animateElement(element) {
  element.classList.add("animate");

  element.addEventListener(
    "animationend",
    function () {
      element.classList.remove("animate");
    },
    { once: true }
  );
}
```

---

## ৭. ES6+ ফিচারসমূহ 🆕

**প্রশ্ন ৬১: টেমপ্লেট লিটারেল কী এবং কীভাবে সেগুলো ব্যবহার করবেন?**  
**উত্তর:** টেমপ্লেট লিটারেল এমবেডেড এক্সপ্রেশন সহ স্ট্রিং তৈরির সহজ উপায় প্রদান করে:

```js
const name = "John";
const age = 30;

// টেমপ্লেট লিটারেল সিনট্যাক্স
const greeting = `Hello, my name is ${name} and I am ${age} years old.`;

// মাল্টি-লাইন স্ট্রিং
const multiLine = `
  This is a multi-line string.
  No need for \n or string concatenation.
  It preserves formatting and indentation.
`;

// এক্সপ্রেশন মূল্যায়ন
const calculation = `The result is: ${5 + 3 * 2}`;

// টেমপ্লেটে ফাংশন কল
function formatCurrency(amount) {
  return `${amount.toFixed(2)}`;
}

const price = `The price is ${formatCurrency(19.99)}`;

// ট্যাগড টেমপ্লেট
function highlight(strings, ...values) {
  return strings.reduce((result, string, i) => {
    const value = values[i] ? `<mark>${values[i]}</mark>` : "";
    return result + string + value;
  }, "");
}

const highlighted = highlight`Hello ${name}, you are ${age} years old!`;
```

**প্রশ্ন ৬২: ডিস্ট্রাকচারিং অ্যাসাইনমেন্ট কী?**  
**উত্তর:** ডিস্ট্রাকচারিং অ্যারে থেকে ভ্যালু বা অবজেক্ট থেকে প্রপার্টি আনপ্যাক করতে দেয়:

```js
// অ্যারে ডিস্ট্রাকচারিং
const colors = ["red", "green", "blue"];
const [primary, secondary, tertiary] = colors;

// এলিমেন্ট স্কিপ করা
const [first, , third] = colors;

// রেস্ট অপারেটর
const [head, ...tail] = colors;

// ডিফল্ট ভ্যালু
const [a, b, c, d = "yellow"] = colors;

// অবজেক্ট ডিস্ট্রাকচারিং
const person = { name: "John", age: 30, city: "New York" };
const { name, age } = person;

// ভেরিয়েবল নাম পরিবর্তন
const { name: fullName, age: years } = person;

// ডিফল্ট ভ্যালু
const { name, occupation = "Unknown" } = person;

// নেস্টেড ডিস্ট্রাকচারিং
const user = {
  id: 1,
  profile: {
    name: "John",
    contact: {
      email: "john@email.com",
    },
  },
};

const {
  profile: {
    name: userName,
    contact: { email },
  },
} = user;

// ফাংশন প্যারামিটার ডিস্ট্রাকচারিং
function greet({ name, age = 0 }) {
  return `Hello ${name}, you are ${age} years old`;
}

// ভেরিয়েবল স্বাপ করা
let x = 1,
  y = 2;
[x, y] = [y, x];
```

**প্রশ্ন ৬৩: স্প্রেড অপারেটর এবং রেস্ট প্যারামিটার কী?**  
**উত্তর:** স্প্রেড ইটারেবল বিস্তার করে, রেস্ট একাধিক এলিমেন্ট সংগ্রহ করে:

```js
// স্প্রেড
const arr = [1, 2, 3];
const copy = [...arr];

// রেস্ট
function logAll(...args) {
  console.log(args);
}
```

**প্রশ্ন ৬৪: ফাংশন আর্গুমেন্টে রেস্ট প্যারামিটার কীভাবে ব্যবহার করবেন?**  
**উত্তর:** রেস্ট প্যারামিটার (`...args`) সব অবশিষ্ট আর্গুমেন্ট একটি অ্যারেতে সংগ্রহ করে।

```js
function sum(...numbers) {
  return numbers.reduce((acc, n) => acc + n, 0);
}
console.log(sum(1, 2, 3)); // 6
```

**প্রশ্ন ৬৫: স্প্রেড অপারেটর এবং রেস্ট প্যারামিটারের মধ্যে পার্থক্য কী?**  
**উত্তর:** স্প্রেড এলিমেন্টগুলো বিস্তার করে (যেমন, অ্যারে/অবজেক্টে), রেস্ট একাধিক এলিমেন্ট একটি অ্যারেতে সংগ্রহ করে (ফাংশন প্যারামিটার বা ডিস্ট্রাকচারিং-এ)।

```js
// স্প্রেড
const arr = [1, 2, 3];
const copy = [...arr];

// রেস্ট
function logAll(...args) {
  console.log(args);
}
```

**প্রশ্ন ৬৬: রেস্ট প্রপার্টি সহ অবজেক্ট ডিস্ট্রাকচারিং কী?**  
**উত্তর:** আপনি অবশিষ্ট প্রপার্টিগুলো একটি নতুন অবজেক্টে সংগ্রহ করতে পারেন।

```js
const { a, ...rest } = { a: 1, b: 2, c: 3 };
console.log(rest); // { b: 2, c: 3 }
```

**প্রশ্ন ৬৭: অপশনাল চেইনিং (`?.`) কী এবং কীভাবে উপকারী?**  
**উত্তর:** এটি গভীর নেস্টেড প্রপার্টিতে নিরাপদ প্রবেশের সুবিধা দেয় যদি কোনো প্রপার্টি undefined হয় তাহলে এরর ছাড়াই।

```js
const user = {};
console.log(user.profile?.name); // undefined (কোনো এরর নেই)
```

**প্রশ্ন ৬৮: নালিশ কোয়ালেসিং (`??`) কী এবং `||` থেকে কীভাবে আলাদা?**  
**উত্তর:** `??` শুধুমাত্র বাম দিকের মান `null` বা `undefined` হলে ডান দিকের মান রিটার্ন করে, যখন `||` বাম দিকের মান যেকোনো falsy হলে ডান দিকের মান রিটার্ন করে।

```js
const value = 0;
console.log(value || 10); // 10
console.log(value ?? 10); // 0
```

**প্রশ্ন ৬৯: ফাংশনে ডিফল্ট প্যারামিটার কী?**  
**উত্তর:** একটি প্যারামিটার যার ডিফল্ট মান আছে যদি প্রদান করা না হয়।

```js
function greet(name = "Guest") {
  return `Hello, ${name}!`;
}
console.log(greet()); // Hello, Guest!
```

**প্রশ্ন ৭০: ট্যাগড টেমপ্লেট লিটারেল কী?**  
**উত্তর:** একটি ফাংশন যা টেমপ্লেট লিটারেল প্রক্রিয়া করে।

```js
function tag(strings, ...values) {
  return strings[0] + values.map((v, i) => v + strings[i + 1]).join("");
}
console.log(tag`Hello ${"World"}!`); // Hello World!
```

**প্রশ্ন ৭১: সিম্বল কী এবং কীভাবে ব্যবহার করা হয়?**  
**উত্তর:** সিম্বল একটি অনন্য এবং অপরিবর্তনীয় প্রিমিটিভ মান, প্রায়ই অবজেক্ট প্রপার্টি কী হিসেবে নাম সংঘর্ষ এড়াতে ব্যবহৃত হয়।

```js
const sym = Symbol("id");
const obj = { [sym]: 123 };
```

**প্রশ্ন ৭২: সেট কী এবং অ্যারে থেকে কীভাবে আলাদা?**  
**উত্তর:** সেট অনন্য মানের সংগ্রহ; অ্যারেতে ডুপ্লিকেট থাকতে পারে।

```js
const set = new Set([1, 2, 2, 3]);
console.log(set); // Set {1, 2, 3}
```

**প্রশ্ন ৭৩: ম্যাপ কী এবং অবজেক্ট থেকে কীভাবে আলাদা?**  
**উত্তর:** ম্যাপ যেকোনো ধরনের কী রাখতে পারে এবং ইনসার্শন অর্ডার বজায় রাখে; অবজেক্টে শুধুমাত্র স্ট্রিং/সিম্বল কী।

```js
const map = new Map();
map.set("a", 1);
map.set(2, "b");
console.log(map.get(2)); // 'b'
```

**প্রশ্ন ৭৪: WeakMap এবং WeakSet কী?**  
**উত্তর:** কালেকশন যা অবজেক্টকে কী হিসেবে রাখে এবং গার্বেজ কালেকশন প্রতিরোধ করে না।

**প্রশ্ন ৭৫: জেনারেটর ফাংশন কী?**  
**উত্তর:** একটি ফাংশন যা তার এক্সিকিউশন বিরতি এবং পুনরায় শুরু করতে পারে, মানের একটি সিকোয়েন্স উৎপাদন করে।

```js
function* gen() {
  yield 1;
  yield 2;
  yield 3;
}
const g = gen();
console.log(g.next().value); // 1
```

**প্রশ্ন ৭৬: জাভাস্ক্রিপ্টে প্রক্সি কী?**  
**উত্তর:** একটি অবজেক্ট যা অন্য অবজেক্ট মোড়ানো এবং অপারেশন (get, set, ইত্যাদি) ইন্টারসেপ্ট করে।

```js
const target = { message: "hello" };
const proxy = new Proxy(target, {
  get(obj, prop) {
    return prop in obj ? obj[prop] : "not found";
  },
});
console.log(proxy.message); // 'hello'
console.log(proxy.foo); // 'not found'
```

**প্রশ্ন ৭৭: `Object.assign()` এবং অবজেক্টের জন্য spread operator এর মধ্যে পার্থক্য কী?**  
**উত্তর:** উভয়ই প্রপার্টি কপি করে, কিন্তু spread আরো সংক্ষিপ্ত এবং literals এ ব্যবহার করা যায়।

**প্রশ্ন ৭৮: dynamic import কী?**  
**উত্তর:** `import()` ব্যবহার করে asynchronously modules লোড করা।

```js
import("./module.js").then((m) => m.doSomething());
```

**প্রশ্ন ৭৯: tree shaking কী?**  
**উত্তর:** বিল্ড প্রসেসের সময় অব্যবহৃত কোড অপসারণ করা (ES modules এবং bundlers দ্বারা সমর্থিত)।

**প্রশ্ন ৮০: polyfill কী?**  
**উত্তর:** এমন কোড যা ওয়েব ব্রাউজারে একটি ফিচার ইমপ্লিমেন্ট করে যা নেটিভভাবে সাপোর্ট করে না।

**প্রশ্ন ৮১: transpiler কী?**  
**উত্তর:** একটি টুল যা জাভাস্ক্রিপ্টের একটি ভার্সন থেকে অন্য ভার্সনে কোড রূপান্তর করে (যেমন, Babel)।

**প্রশ্ন ৮২: callback function কী?**  
**উত্তর:** একটি ফাংশন যা অন্য একটি ফাংশনে আর্গুমেন্ট হিসেবে পাস করা হয়, পরে execute করার জন্য।

**প্রশ্ন ৮৩: promise কী?**  
**উত্তর:** একটি অবজেক্ট যা asynchronous operation এর সম্ভাব্য সম্পূর্ণতা বা ব্যর্থতার প্রতিনিধিত্ব করে।

**প্রশ্ন ৮৪: promises এ কীভাবে errors handle করবেন?**  
**উত্তর:** rejected promises handle করতে `.catch()` ব্যবহার করুন।

**প্রশ্ন ৮৫: async/await কী?**  
**উত্তর:** promises এর উপর syntactic sugar, যা asynchronous কোডকে synchronous কোডের মতো লিখতে দেয়।

**প্রশ্ন ৮৬: async/await এ কীভাবে errors handle করবেন?**  
**উত্তর:** await expressions এর চারপাশে try/catch blocks ব্যবহার করুন।

**প্রশ্ন ৮৭: event loop কী?**  
**উত্তর:** জাভাস্ক্রিপ্টকে asynchronous operations handle করতে দেয় callbacks একটি queue এ রেখে এবং বর্তমান call stack খালি হওয়ার পর সেগুলো execute করে।

**প্রশ্ন ৮৮: microtasks এবং macrotasks কী?**  
**উত্তর:** Microtasks (যেমন, promise callbacks) পরবর্তী rendering এর আগে execute হয়, আর macrotasks (যেমন, setTimeout) rendering এর পরে execute হয়। Microtasks এর উচ্চ অগ্রাধিকার রয়েছে।

**প্রশ্ন ৮৯: debouncing কী?**  
**উত্তর:** events এ pause এর পর একটি function call delay করা।

**প্রশ্ন ৯০: throttling কী?**  
**উত্তর:** নির্দিষ্ট সময়ের মধ্যে একটি function কতবার call করা যাবে তা সীমিত করা।

**প্রশ্ন ৯১: callback hell কী?**  
**উত্তর:** গভীরভাবে nested callbacks যা কোড পড়তে এবং maintain করতে কঠিন করে তোলে।

**প্রশ্ন ৯২: কীভাবে callback hell এড়ানো যায়?**  
**উত্তর:** promises, async/await ব্যবহার করুন, অথবা callbacks modularize করুন।

**প্রশ্ন ৯৩: race condition কী?**  
**উত্তর:** যখন ফলাফল asynchronous operations এর timing বা ক্রমের উপর নির্ভর করে।

**প্রশ্ন ৯৪: async/await ব্যবহারের একটি বাস্তব উদাহরণ কী?**  
**উত্তর:** API থেকে ডেটা fetch করা এবং ডেটা load হওয়ার পর UI আপডেট করা।

```js
async function loadData() {
  const res = await fetch("/api/data");
  const data = await res.json();
  console.log(data);
}
```

**প্রশ্ন ৯৫: Promise.all() কী?**  
**উত্তর:** একটি method যা promises এর একটি array নেয় এবং একটি single promise return করে যা সবগুলো resolve হলে resolve হয়।

**প্রশ্ন ৯৬: Promise.race() কী?**  
**উত্তর:** একটি method যা একটি promise return করে যা promises এর যেকোনো একটি resolve বা reject হওয়ার সাথে সাথেই resolve বা reject হয়।

**প্রশ্ন ৯৭: DOM কী?**  
**উত্তর:** Document Object Model হলো HTML এবং XML documents এর জন্য একটি programming interface।

**প্রশ্ন ৯৮: DOM এ কীভাবে elements select করবেন?**  
**উত্তর:** `getElementById`, `querySelector`, এবং `getElementsByClassName` এর মতো methods ব্যবহার করুন।

**প্রশ্ন ৯৯: DOM element এ কীভাবে event listener যোগ করবেন?**  
**উত্তর:** `element.addEventListener('click', handler)` ব্যবহার করুন।

**প্রশ্ন ১০০: event delegation কী?**  
**উত্তর:** parent element এ একটি single event listener attach করা যা children থেকে events handle করে।

**প্রশ্ন ১০১: DOM events এ bubbling এবং capturing কী?**  
**উত্তর:** Bubbling: event child থেকে parent এ propagate হয়; capturing: parent থেকে child এ।

**প্রশ্ন ১০২: কীভাবে event এর default action prevent করবেন?**  
**উত্তর:** event handler এ `event.preventDefault()` ব্যবহার করুন।

**প্রশ্ন ১০৩: কোন scenario তে আপনি event delegation ব্যবহার করবেন?**  
**উত্তর:** যখন আপনার কাছে items এর একটি list থাকে (যেমন, একটি todo list) এবং আপনি যেকোনো item এ clicks efficiently handle করতে চান, এমনকি items dynamically add হলেও।

```js
document.getElementById("list").addEventListener("click", (e) => {
  if (e.target.tagName === "LI") {
    console.log("Clicked:", e.target.textContent);
  }
});
```

---

## ৮. Scenario-Based প্রশ্ন 🎯

**প্রশ্ন ১০৪: জাভাস্ক্রিপ্টে কীভাবে একটি debounce function implement করবেন?**  
**উত্তর:** একটি debounce function events এ pause এর পর একটি function এর execution delay করে।

```js
function debounce(func, delay) {
  let timeoutId;
  return function (...args) {
    clearTimeout(timeoutId);
    timeoutId = setTimeout(() => func.apply(this, args), delay);
  };
}

// ব্যবহার: Search input
const debouncedSearch = debounce((query) => {
  console.log("Searching for:", query);
  // API call here
}, 300);

document.getElementById("search").addEventListener("input", (e) => {
  debouncedSearch(e.target.value);
});
```

**প্রশ্ন ১০৫: জাভাস্ক্রিপ্টে কীভাবে একটি throttle function implement করবেন?**  
**উত্তর:** একটি throttle function নির্দিষ্ট interval এ একটি function এর execution সীমিত করে।

```js
function throttle(func, interval) {
  let lastTime = 0;
  return function (...args) {
    const now = Date.now();
    if (now - lastTime >= interval) {
      lastTime = now;
      func.apply(this, args);
    }
  };
}

// ব্যবহার: Scroll event
const throttledScroll = throttle(() => {
  console.log("Scroll position:", window.scrollY);
}, 100);

window.addEventListener("scroll", throttledScroll);
```

---

## ৯. Browser APIs 🌐

**প্রশ্ন ১১২: জাভাস্ক্রিপ্টে localStorage API কীভাবে ব্যবহার করবেন?**  
**উত্তর:** localStorage API আপনাকে expiration date ছাড়াই browser এ data store করতে দেয়।

```js
// Data storing
localStorage.setItem("username", "JohnDoe");

// Data retrieving
const username = localStorage.getItem("username");
console.log(username);

// Data removing
localStorage.removeItem("username");
```

**প্রশ্ন ১১৩: জাভাস্ক্রিপ্টে sessionStorage API কীভাবে ব্যবহার করবেন?**  
**উত্তর:** sessionStorage API আপনাকে একটি session এর জন্য browser এ data store করতে দেয়।

```js
// Data storing
sessionStorage.setItem("username", "JohnDoe");

// Data retrieving
const username = sessionStorage.getItem("username");
console.log(username);

// Data removing
sessionStorage.removeItem("username");
```

**প্রশ্ন ১১৪: জাভাস্ক্রিপ্টে history API কীভাবে ব্যবহার করবেন?**  
**উত্তর:** history API আপনাকে browser history manipulate করতে দেয়।

```js
// পিছনে navigate করা
history.back();

// সামনে navigate করা
history.forward();

// history stack এ নতুন state push করা
history.pushState(null, "", "new-page.html");
```

**প্রশ্ন ১১৫: জাভাস্ক্রিপ্টে navigator API কীভাবে ব্যবহার করবেন?**  
**উত্তর:** navigator API browser সম্পর্কে তথ্য প্রদান করে।

```js
// Browser name
console.log(navigator.userAgent);

// Platform
console.log(navigator.platform);

// Online status
console.log(navigator.onLine);
```

**প্রশ্ন ১১৬: জাভাস্ক্রিপ্টে screen API কীভাবে ব্যবহার করবেন?**  
**উত্তর:** screen API screen সম্পর্কে তথ্য প্রদান করে।

```js
// Screen width
console.log(screen.width);

// Screen height
console.log(screen.height);

// Screen color depth
console.log(screen.colorDepth);
```

**প্রশ্ন ১১৭: জাভাস্ক্রিপ্টে location API কীভাবে ব্যবহার করবেন?**  
**উত্তর:** location API URL সম্পর্কে তথ্য প্রদান করে।

```js
// Current URL
console.log(location.href);

// Protocol
console.log(location.protocol);

// Hostname
console.log(location.hostname);
```

**প্রশ্ন ১১৮: জাভাস্ক্রিপ্টে document API কীভাবে ব্যবহার করবেন?**  
**উত্তর:** document API DOM manipulate করার methods প্রদান করে।

```js
// ID দিয়ে element পেতে
const element = document.getElementById("myElement");

// Class name দিয়ে elements পেতে
const elements = document.getElementsByClassName("myClass");

// Tag name দিয়ে elements পেতে
const tags = document.getElementsByTagName("div");

// CSS selector দিয়ে elements পেতে
const matches = document.querySelectorAll(".myClass");
```

**প্রশ্ন ১১৯: জাভাস্ক্রিপ্টে console API কীভাবে ব্যবহার করবেন?**  
**উত্তর:** console API console এ তথ্য log করার methods প্রদান করে।

```js
// Basic console logging
console.log("Hello, world!");

// Console logs grouping করা
console.group("User Information");
console.log("Name:", "John Doe");
console.log("Age:", 30);
console.log("City:", "New York");
console.groupEnd();

// Console logs counting
console.count("Button clicks");
```

**প্রশ্ন ১২০: জাভাস্ক্রিপ্টে URL API কীভাবে ব্যবহার করবেন?**  
**উত্তর:** URL API URLs manipulate করার methods প্রদান করে।

```js
// URL parsing
const url = new URL("https://example.com/path?query=value");
console.log(url.href);

// URL তৈরি করা
const newUrl = new URL("https://example.com");
newUrl.pathname = "new-path";
console.log(newUrl.href);
```

**প্রশ্ন ১২১: জাভাস্ক্রিপ্টে Web APIs কীভাবে ব্যবহার করবেন?**  
**উত্তর:** Web APIs জাভাস্ক্রিপ্টে অতিরিক্ত functionality প্রদান করে।

```js
// Fetch API
async function fetchData() {
  const response = await fetch("/api/data");
  const data = await response.json();
  console.log(data);
}

// Geolocation API
navigator.geolocation.getCurrentPosition((position) => {
  console.log("Latitude:", position.coords.latitude);
  console.log("Longitude:", position.coords.longitude);
});

// Web Workers
const worker = new Worker("worker.js");
worker.postMessage("Hello, worker!");
```

---

## ১০. Senior Devs এর জন্য উন্নত জাভাস্ক্রিপ্ট প্রশ্ন 🚀

**প্রশ্ন ১৩১: জাভাস্ক্রিপ্টে memory leak কী এবং কীভাবে এটি প্রতিরোধ করবেন?**

**উত্তর:** Memory leak হয় যখন আর প্রয়োজন নেই এমন memory release করা হয় না। সাধারণ কারণ: global variables, ভুলে যাওয়া timers, detached DOM nodes, references holding করা closures। Listeners, timers পরিষ্কার করে এবং অপ্রয়োজনীয় references এড়িয়ে প্রতিরোধ করুন।

```js
// উদাহরণ: Timer এবং event listener পরিষ্কার করা
let timer = setInterval(() => doSomething(), 1000);
window.addEventListener("resize", handler);
// পরে, যখন আর প্রয়োজন নেই:
clearInterval(timer);
window.removeEventListener("resize", handler);
```

**প্রশ্ন ১৩২: জাভাস্ক্রিপ্টে event loop কীভাবে কাজ করে?**

**উত্তর:** Event loop জাভাস্ক্রিপ্টকে async operations handle করতে দেয় call stack এবং তারপর task/microtask queues প্রসেস করে। এটি single-threaded environment এ non-blocking behavior নিশ্চিত করে।

```js
console.log("A");
setTimeout(() => console.log("B"), 0);
Promise.resolve().then(() => console.log("C"));
console.log("D");
// আউটপুট: A, D, C, B
```

**প্রশ্ন ১৩৩: Tail call optimization কী?**

**উত্তর:** এটি JS engine optimization যেখানে function এর শেষ function call নতুন stack frame যোগ করে না। এখনো browsers এ ব্যাপকভাবে supported নয়।

```js
// উদাহরণ: Tail-recursive factorial
function factorial(n, acc = 1) {
  if (n <= 1) return acc;
  return factorial(n - 1, n * acc); // Tail call
}
console.log(factorial(5)); // 120
```

**প্রশ্ন ১৩৪: `Object.freeze()` এবং `const` এর মধ্যে পার্থক্য কী?**

**উত্তর:** `const` variable এর reassignment প্রতিরোধ করে, কিন্তু object এখনো mutate করা যায়। `Object.freeze()` object কে immutable করে তোলে।

```js
const obj = { a: 1 };
obj.a = 2; // অনুমোদিত
Object.freeze(obj);
obj.a = 3; // strict mode এ ignore
```

**প্রশ্ন ১৩৫: জাভাস্ক্রিপ্টে কীভাবে একটি object deep clone করবেন?**

**উত্তর:** `structuredClone(obj)` (আধুনিক), অথবা `JSON.parse(JSON.stringify(obj))` (সীমাবদ্ধতা: কোন functions, dates, ইত্যাদি নেই), অথবা Lodash এর `cloneDeep` এর মতো libraries ব্যবহার করুন।

```js
const original = { a: 1, b: { c: 2 } };
const copy = structuredClone(original);
copy.b.c = 42;
console.log(original.b.c); // 2
```

**প্রশ্ন ১৩৬: WeakMap কী এবং কখন এটি ব্যবহার করবেন?**

**উত্তর:** WeakMap keys এ weak references রাখে (শুধুমাত্র objects), garbage collection এর অনুমতি দেয়। Objects এর জন্য metadata store করতে তাদের cleanup প্রতিরোধ না করে দরকারী।

```js
const wm = new WeakMap();
let obj = {};
wm.set(obj, "metadata");
obj = null; // এখন GC এর জন্য eligible, WeakMap entry অপসারিত
```

**প্রশ্ন ১৩৭: জাভাস্ক্রিপ্ট apps এ কীভাবে XSS (Cross-Site Scripting) প্রতিরোধ করবেন?**

**উত্তর:** `innerHTML` ব্যবহার করে DOM এ untrusted data কখনো inject করবেন না। সর্বদা user input sanitize করুন এবং `textContent` এর মতো safe APIs ব্যবহার করুন।

```js
// অনিরাপদ:
div.innerHTML = userInput;
// নিরাপদ:
div.textContent = userInput;
```

**প্রশ্ন ১৩৮: Synchronous এবং asynchronous code এর মধ্যে পার্থক্য কী?**

**উত্তর:** Synchronous code main thread কে block করে; async code (callbacks, promises, async/await) tasks complete হওয়ার জন্য অপেক্ষা করার সময় অন্য operations চালাতে দেয়।

```js
// Synchronous
console.log("Start");
for (let i = 0; i < 1e8; i++) {} // UI block করে
console.log("End");
// Asynchronous
console.log("Start");
setTimeout(() => console.log("End"), 0);
```

**প্রশ্ন ১৩৯: async/await code এ কীভাবে errors handle করবেন?**

**উত্তর:** `await` calls এর চারপাশে `try/catch` blocks ব্যবহার করে errors catch করুন।

```js
async function fetchData() {
  try {
    const res = await fetch("/api/data");
    const data = await res.json();
    return data;
  } catch (err) {
    console.error("Error:", err);
  }
}
```

**প্রশ্ন ১৪০: `map`, `forEach`, এবং `reduce` এর মধ্যে পার্থক্য কী?**

**উত্তর:** `map` নতুন array return করে, `forEach` শুধু iterate করে, `reduce` array থেকে একটি single value accumulate করে।

```js
const arr = [1, 2, 3];
const doubled = arr.map((x) => x * 2); // [2, 4, 6]
arr.forEach((x) => console.log(x)); // 1, 2, 3
const sum = arr.reduce((acc, x) => acc + x, 0); // 6
```

**প্রশ্ন ১৪১: Closure কী এবং কেন এটি উপযোগী?**

**উত্তর:** Closure হলো একটি function যা তার outer scope মনে রাখে। Data privacy এবং function factories এর জন্য উপযোগী।

```js
function makeCounter() {
  let count = 0;
  return function () {
    return ++count;
  };
}
const counter = makeCounter();
console.log(counter()); // 1
console.log(counter()); // 2
```

**প্রশ্ন ১৪২: কীভাবে একটি function debounce এবং throttle করবেন?**

**উত্তর:** Debounce: pause এর পর execution delay করে। Throttle: execution কে interval অনুযায়ী একবার সীমিত করে। Scroll বা resize এর মতো events optimize করতে ব্যবহৃত।

```js
// Debounce
function debounce(fn, delay) {
  let timer;
  return (...args) => {
    clearTimeout(timer);
    timer = setTimeout(() => fn(...args), delay);
  };
}
// Throttle
function throttle(fn, interval) {
  let last = 0;
  return (...args) => {
    const now = Date.now();
    if (now - last >= interval) {
      last = now;
      fn(...args);
    }
  };
}
```

**প্রশ্ন ১৪৩: `==` এবং `===` এর মধ্যে পার্থক্য কী?**

**উত্তর:** `==` type coercion সহ values compare করে; `===` value এবং type উভয় compare করে (strict equality)।

```js
console.log(5 == "5"); // true
console.log(5 === "5"); // false
```

**প্রশ্ন ১৪৪: JS code এ কীভাবে performance bottlenecks detect এবং fix করবেন?**

**উত্তর:** Browser dev tools (Performance tab, Memory tab, Network tab) ব্যবহার করুন, code profile করুন, এবং slow parts optimize করুন (যেমন, অপ্রয়োজনীয় reflows এড়ানো, efficient algorithms ব্যবহার)।

```js
// উদাহরণ: Profile করতে console.time ব্যবহার
console.time("loop");
for (let i = 0; i < 1e6; i++) {}
console.timeEnd("loop");
```

**প্রশ্ন ১৪৫: `Symbol` type এর উদ্দেশ্য কী?**

**উত্তর:** Symbols হলো unique এবং immutable values, প্রায়ই object property keys হিসেবে name collisions এড়াতে ব্যবহৃত।

```js
const sym = Symbol("id");
const obj = { [sym]: 123 };
console.log(obj[sym]); // 123
```

**প্রশ্ন ১৪৬: কীভাবে একটি object কে iterable বানাবেন?**

**উত্তর:** `[Symbol.iterator]` method implement করুন, যা `next()` method সহ একটি iterator object return করে।

```js
const myIterable = {
  *[Symbol.iterator]() {
    yield 1;
    yield 2;
    yield 3;
  },
};
for (const val of myIterable) {
  console.log(val); // 1, 2, 3
}
```

**প্রশ্ন ১৪৭: `call`, `apply`, এবং `bind` এর মধ্যে পার্থক্য কী?**

**উত্তর:** সবগুলো `this` context set করে। `call` এবং `apply` function তৎক্ষণাৎ invoke করে (`call` comma args ব্যবহার করে, `apply` array ব্যবহার করে)। `bind` bound context সহ নতুন function return করে।

```js
function greet(msg) {
  console.log(msg + ", " + this.name);
}
const person = { name: "Alice" };
greet.call(person, "Hi"); // Hi, Alice
greet.apply(person, ["Hello"]); // Hello, Alice
const bound = greet.bind(person, "Hey");
bound(); // Hey, Alice
```

**প্রশ্ন ১৪৮: Proxy কী এবং এটি কীসের জন্য ব্যবহার করতে পারেন?**

**উত্তর:** Proxy একটি object wrap করে এবং operations (get, set, ইত্যাদি) intercept করে। Validation, logging, বা reactive programming এর জন্য উপযোগী।

```js
const target = { a: 1 };
const proxy = new Proxy(target, {
  get(obj, prop) {
    console.log("Accessed", prop);
    return obj[prop];
  },
});
console.log(proxy.a); // Accessed a, 1
```

**প্রশ্ন ১৪৯: JSON serialization এ কীভাবে circular references handle করবেন?**

**উত্তর:** `JSON.stringify` এ custom replacer function ব্যবহার করুন, অথবা circular structures support করে এমন `flatted` এর মতো libraries।

```js
const obj = {};
obj.self = obj;
function replacer(key, value) {
  const seen = new WeakSet();
  return function (key, value) {
    if (typeof value === "object" && value !== null) {
      if (seen.has(value)) return;
      seen.add(value);
    }
    return value;
  };
}
JSON.stringify(obj, replacer()); // Circular error এড়ায়
```

**প্রশ্ন ১৫০: Microtasks এবং macrotasks এর মধ্যে পার্থক্য কী?**

**উত্তর:** Microtasks (Promises, MutationObserver) বর্তমান script এর পরে, rendering এর আগে চলে। Macrotasks (setTimeout, setInterval) rendering এর পরে চলে। Microtasks এর উচ্চ অগ্রাধিকার আছে।

```js
console.log("A");
Promise.resolve().then(() => console.log("B"));
setTimeout(() => console.log("C"), 0);
console.log("D");
// আউটপুট: A, D, B, C
```

**প্রশ্ন ১৫১: যখন একটি তৃতীয় পক্ষের API ধীর বা অনির্ভরযোগ্য হয়, কিন্তু আপনার অ্যাপকে প্রতিক্রিয়াশীল থাকতে হয়, আপনি কীভাবে এই পরিস্থিতি সামলাবেন?**

**উত্তর:** টাইমআউট, পুনঃচেষ্টা এবং ফলব্যাক ডেটা ব্যবহার করুন। উদাহরণ:

```js
async function fetchWithTimeout(url, timeout = 3000) {
  return Promise.race([
    fetch(url),
    new Promise((_, reject) =>
      setTimeout(() => reject(new Error("Timeout")), timeout)
    ),
  ]);
}
```

**প্রশ্ন ১৫২: ওয়েব অ্যাপে শেয়ার্ড স্টেট আপডেট করার সময় রেস কন্ডিশন কীভাবে প্রতিরোধ করবেন?**

**উত্তর:** লক, অ্যাটমিক আপডেট, বা স্টেট ম্যানেজমেন্ট লাইব্রেরি ব্যবহার করুন। উদাহরণ:

```js
let updating = false;
async function safeUpdate(newValue) {
  if (updating) return;
  updating = true;
  await updateState(newValue);
  updating = false;
}
```

**প্রশ্ন ১৫৩: একটি বড় সিঙ্গেল-পেজ অ্যাপ্লিকেশনে মেমরি লিক কীভাবে ডিবাগ করবেন?**

**উত্তর:** ব্রাউজার ডেভ টুলস (মেমরি ট্যাব, হিপ স্ন্যাপশট) ব্যবহার করুন, বিচ্ছিন্ন DOM নোড খুঁজুন এবং অপসারিত না হওয়া ইভেন্ট লিসেনার চেক করুন। উদাহরণ:

```js
// পরিষ্কার করার সময় ইভেন্ট লিসেনার সরান
function cleanup() {
  element.removeEventListener("click", handler);
}
```

**প্রশ্ন ১৫৪: রিয়েল-টাইম সহযোগিতামূলক অ্যাপে পুরানো ডেটা কীভাবে হ্যান্ডেল করবেন?**

**উত্তর:** ভার্সনিং, টাইমস্ট্যাম্প, বা দ্বন্দ্ব সমাধানের কৌশল ব্যবহার করুন। উদাহরণ:

```js
if (incoming.version > local.version) {
  updateLocalData(incoming);
}
```

**প্রশ্ন ১৫৫: ফর্ম সাবমিশনের জন্য অপটিমিস্টিক UI আপডেট কীভাবে বাস্তবায়ন করবেন?**

**উত্তর:** তৎক্ষণাৎ UI আপডেট করুন, তারপর সার্ভার ব্যর্থ হলে ফিরিয়ে নিন। উদাহরণ:

```js
function submitForm(data) {
  showSuccess(); // অপটিমিস্টিক
  api.save(data).catch(() => showError());
}
```

**প্রশ্ন ১৫৬: দীর্ঘমেয়াদী গণনা যাতে UI ব্লক না করে তা কীভাবে নিশ্চিত করবেন?**

**উত্তর:** ওয়েব ওয়ার্কার ব্যবহার করুন বা setTimeout/setImmediate দিয়ে কাজকে ভাগ করুন। উদাহরণ:

```js
// worker.js ফাইলে
self.onmessage = function (e) {
  const result = heavyComputation(e.data);
  self.postMessage(result);
};
```

**প্রশ্ন ১৫৭: ব্রাউজারে সংবেদনশীল ডেটা নিরাপদে কীভাবে সংরক্ষণ করবেন?**

**উত্তর:** সংবেদনশীল ডেটার জন্য localStorage/sessionStorage এড়িয়ে চলুন। HttpOnly/Secure ফ্ল্যাগসহ কুকি ব্যবহার করুন, বা সংরক্ষণের আগে ডেটা এনক্রিপ্ট করুন। উদাহরণ:

```js
// সংরক্ষণের আগে এনক্রিপ্ট করুন
localStorage.setItem("token", encrypt(token));
```

**প্রশ্ন ১৫৮: একটি বড় কোডবেসে সার্কুলার ডিপেন্ডেন্সি কীভাবে হ্যান্ডেল করবেন?**

**উত্তর:** মডিউলগুলোকে আলাদা করার জন্য রিফ্যাক্টর করুন, ডিপেন্ডেন্সি ইনজেকশন ব্যবহার করুন, বা সাধারণ লজিক একটি কমন মডিউলে সরান। উদাহরণ:

```js
// সাধারণ লজিক utils.js এ সরান
import { shared } from "./utils.js";
```

**প্রশ্ন ১৫৯: জাভাস্ক্রিপ্ট অ্যাপ্লিকেশনে ফিচার ফ্ল্যাগ কীভাবে বাস্তবায়ন করবেন?**

**উত্তর:** ফিচার টগল করার জন্য একটি কনফিগ অবজেক্ট বা রিমোট সার্ভিস ব্যবহার করুন। উদাহরণ:

```js
const features = { newUI: true };
if (features.newUI) {
  renderNewUI();
} else {
  renderOldUI();
}
```

**প্রশ্ন ১৬০: DOM এ বড় তালিকা দক্ষতার সাথে কীভাবে হ্যান্ডেল করবেন?**

**উত্তর:** উইন্ডোইং/ভার্চুয়ালাইজেশন (যেমন, react-window) ব্যবহার করুন, বা শুধুমাত্র দৃশ্যমান আইটেম রেন্ডার করুন। উদাহরণ:

```js
const visibleItems = items.slice(start, end);
visibleItems.forEach(renderItem);
```

</div>
