# ➕ জাভাস্ক্রিপ্ট অপারেটর: ম্যাজিক টুলস নিয়ে সারাহর অ্যাডভেঞ্চার 🛠️

আবারও স্বাগতম! চলুন সারাহর সাথে যোগ দিই যখন সে জাভাস্ক্রিপ্টে বিভিন্ন কাজ করার জন্য ম্যাজিক টুলস (অপারেটর) আবিষ্কার করে। একটি মজার, ইমোজি-ভরা যাত্রার জন্য প্রস্তুত হোন! 🌈

---

## গল্প: সারাহর টুলবক্স 🧰

সারাহ পাজল সমাধান করতে পছন্দ করে। একদিন, সে একটি ম্যাজিক টুলবক্স পায়। এর ভিতরের প্রতিটি টুল কিছু বিশেষ কাজ করতে পারে—যোগ করা, তুলনা করা, এমনকি সিদ্ধান্ত নেওয়া! জাভাস্ক্রিপ্টে, এই টুলগুলোকে **অপারেটর** বলা হয়।

---

## ১. অ্যারিথমেটিক অপারেটর ➕➖✖️➗

সারাহর প্রথম টুলগুলো তাকে গণিতে সাহায্য করে:

- `+` (যোগ): সংখ্যা যোগ করে বা শব্দ যুক্ত করে
- `-` (বিয়োগ): বাদ দেয়
- `*` (গুণ): গুণন!
- `/` (ভাগ): সমান ভাগ করে
- `%` (ভাগশেষ): ভাগ করার পর যা বাকি থাকে
- `**` (সূচক): পাওয়ার আপ!

```js
let apples = 5 + 3; // 8 🍎
let candies = 10 - 2; // 8 🍬
let stars = 2 * 4; // 8 ⭐
let slices = 16 / 2; // 8 🍕
let leftovers = 17 % 3; // 2 🍪
let bigNumber = 2 ** 3; // 8 🚀
```

---

## ২. অ্যাসাইনমেন্ট অপারেটর 📦

সারাহ এইগুলো ব্যবহার করে তার বক্সগুলোতে (ভেরিয়েবল) জিনিস রাখে:

- `=` (অ্যাসাইন): বক্সে কিছু রাখা
- `+=` (যোগ করে অ্যাসাইন): বক্সে আরও যোগ করা
- `-=` (বিয়োগ করে অ্যাসাইন): কিছু বাদ দেওয়া
- `*=` (গুণ করে অ্যাসাইন): কন্টেন্ট দ্বিগুণ বা ত্রিগুণ করা
- `/=` (ভাগ করে অ্যাসাইন): কন্টেন্ট ভাগ করা

```js
let score = 10; // =
score += 5; // 15 🎯
score -= 3; // 12 🎯
score *= 2; // 24 🎯
score /= 4; // 6 🎯
```

---

## ৩. কম্প্যারিজন অপারেটর 🤔

সারাহ জিনিস তুলনা করতে পছন্দ করে:

- `==` (সমান): এরা কি একই? (টাইপ উপেক্ষা করে)
- `===` (স্ট্রিক্ট সমান): এরা কি সত্যিই একই? (টাইপও চেক করে)
- `!=` (সমান নয়): এরা কি আলাদা?
- `!==` (স্ট্রিক্ট সমান নয়): সত্যিই আলাদা?
- `>` (বড়): একটি কি বড়?
- `<` (ছোট): একটি কি ছোট?
- `>=` (বড় বা সমান): যথেষ্ট বড়?
- `<=` (ছোট বা সমান): যথেষ্ট ছোট?

```js
let age = 18;
console.log(age == "18"); // true 😮
console.log(age === "18"); // false 😲
console.log(age > 16); // true 👍
console.log(age < 10); // false 👎
```

---

## ৪. লজিক্যাল অপারেটর 🧠

সারাহ সিদ্ধান্ত নেওয়ার জন্য এইগুলো ব্যবহার করে:

- `&&` (AND): দুটোই সত্য হতে হবে 🤝
- `||` (OR): অন্তত একটি সত্য হতে হবে 🙌
- `!` (NOT): উল্টে দাও! 🔄

```js
let hasTicket = true;
let isAdult = false;

console.log(hasTicket && isAdult); // false 🚫
console.log(hasTicket || isAdult); // true ✅
console.log(!isAdult); // true 🙃
```

---

## ৫. বিশেষ অপারেটর 🕵️‍♂️

- `typeof`: বক্সে কী আছে? 🧐
- `instanceof`: এটি কি বিশেষ ধরনের অবজেক্ট? 🏷️

```js
console.log(typeof 42); // 'number' 🔢
console.log([1, 2, 3] instanceof Array); // true 📦
```

---

## সারাহর অপারেটর টিপস 💡

- নিরাপদ তুলনার জন্য `==` এর পরিবর্তে `===` ব্যবহার করুন! 🛡️
- মনে রাখবেন: `+` সংখ্যা যোগ করতে বা স্ট্রিং (শব্দ) যুক্ত করতে পারে।
- লজিক্যাল অপারেটর আপনার কোডে স্মার্ট সিদ্ধান্ত নিতে সাহায্য করে।

---

## কুইজ টাইম! 🎲

১. `5 % 2` কী রিটার্ন করে?
২. `==` এবং `===` এর মধ্যে পার্থক্য কী?
৩. এটি কী প্রিন্ট করবে?

```js
let a = 10;
a += 5;
console.log(a);
```

৪. `typeof "hello"` কী রিটার্ন করে?
৫. `!false || false` এর মান কত?

<details>
  <summary>উত্তর দেখুন</summary>
  <ol>
    <li>
      <code>1</code> (৫ কে ২ দিয়ে ভাগ করার পর যা বাকি থাকে)
    </li>
    <li>
      <code>==</code> শুধু মান চেক করে, <code>===</code> মান এবং টাইপ উভয়ই চেক
      করে
    </li>
    <li>
      <code>15</code>
    </li>
    <li>
      <code>"string"</code>
    </li>
    <li>
      <code>true</code>
    </li>
  </ol>
</details>

---

## সারাংশ 📝

- অপারেটর হল সারাহর ম্যাজিক টুলস যা গণিত, তুলনা, এবং লজিকের জন্য ব্যবহৃত হয়! 🛠️
- আপনার কোডে বিল্ড, তুলনা, এবং সিদ্ধান্ত নেওয়ার জন্য এগুলো ব্যবহার করুন।
- জাভাস্ক্রিপ্ট উইজার্ড হওয়ার জন্য এই টুলগুলো ব্যবহার করে অনুশীলন করুন! 🧙‍♂️

হ্যাপি কোডিং! 🚀
