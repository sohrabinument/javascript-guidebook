# 🔄 কন্ট্রোল ফ্লো এবং লুপ: কোড ল্যান্ডে সারাহর অ্যাডভেঞ্চার 🗺️

আবারও স্বাগতম! চলুন সারাহর সাথে যোগ দিই যখন সে কন্ট্রোল ফ্লো এবং লুপ ব্যবহার করে জাভাস্ক্রিপ্টে সিদ্ধান্ত নেওয়া এবং কাজ পুনরাবৃত্তি করা শিখে। পছন্দ, পথ, এবং মজার একটি যাত্রার জন্য প্রস্তুত হোন 🎉

---

## গল্প: অ্যামিউজমেন্ট পার্কে সারাহর দিন 🎡

সারাহ তার প্রিয় অ্যামিউজমেন্ট পার্কে একটি দিন কাটাচ্ছে। প্রতিটি রাইড, স্ন্যাক স্ট্যান্ড, এবং গেম বুথে সে পছন্দের মুখোমুখি হয়। জাভাস্ক্রিপ্টে, **কন্ট্রোল ফ্লো** হল আমরা কিভাবে আমাদের কোডকে কোন পথ নিতে হবে তা বলি—ঠিক যেমন সারাহ পরবর্তীতে কী করবে তা সিদ্ধান্ত নেয়! 🎟️

---

## ১. If/Else: সারাহ কি রোলার কোস্টারে চড়বে? 🎢

সারাহ রোলার কোস্টারে চড়তে চায়, কিন্তু শুধুমাত্র যদি সে যথেষ্ট লম্বা হয়!

```js
// সারাহ চেক করে সে রোলার কোস্টারের জন্য যথেষ্ট লম্বা কিনা।
let isTallEnough = true;

if (isTallEnough) {
  console.log("সারাহ রোলার কোস্টারে চড়ে! 🎢");
} else {
  console.log("সারাহ অন্য একটি রাইড বেছে নেয়। 🎠");
}
```

_আপনি যদি এই কোডটি চালান, আপনি দেখবেন: সারাহ রোলার কোস্টারে চড়ে! 🎢_

- `if` একটি কন্ডিশন চেক করে।
- `else` চলে যদি কন্ডিশন মিথ্যা হয়।
- আরও পছন্দের জন্য আপনি `else if` যোগ করতে পারেন!

---

## ২. Switch: সারাহ কোন স্ন্যাক বেছে নেবে? 🍿

সারাহ ক্ষুধার্ত এবং স্ন্যাক স্ট্যান্ডে যায়। সে পপকর্ন, আইসক্রিম, বা কটন ক্যান্ডি বেছে নিতে পারে।

```js
// সারাহ একটি স্ন্যাক বেছে নেয়।
let snack = "popcorn";

switch (snack) {
  case "popcorn":
    console.log("সারাহ পপকর্ন উপভোগ করে! 🍿");
    break;
  case "ice cream":
    console.log("সারাহ আইসক্রিম খায়! 🍦");
    break;
  case "cotton candy":
    console.log("সারাহ কটন ক্যান্ডি চিবায়! 🍭");
    break;
  default:
    console.log("সারাহ এক বোতল পানি নেয়। 💧");
}
```

_আপনি যদি এই কোডটি চালান, আপনি দেখবেন: সারাহ পপকর্ন উপভোগ করে! 🍿_

- `switch` একটি মান অনেকগুলো কেসের সাথে তুলনা করে।
- `break` ম্যাচ পাওয়ার পর চেক করা বন্ধ করে।
- `default` চলে যদি কোন কেস ম্যাচ না করে।

---

## ৩. লুপ: রিং টস গেম খেলা 🎯

সারাহ রিং টস বুথে ৩টি পুরস্কার জিততে চায়। সে যথেষ্ট জিতা পর্যন্ত খেলা চালিয়ে যায়!

### For লুপ: সারাহ রিং ছোঁড়ে 🏅

```js
// সারাহ বুথে ৩টি রিং ছোঁড়ে।
for (let i = 1; i <= 3; i++) {
  console.log(`সারাহ রিং #${i} ছোঁড়ে! 🎯`);
}
```

_এটি প্রিন্ট করবে: সারাহ রিং #1 ছোঁড়ে! 🎯 ... #3 পর্যন্ত।_

- `for` লুপ একটি কোড ব্লক নির্দিষ্ট সংখ্যক বার পুনরাবৃত্তি করে।

### While লুপ: লাইনে দাঁড়িয়ে থাকা ⏳

সারাহ ফেরিস হুইলের জন্য লাইনে দাঁড়িয়ে থাকে। সে তার পালা না আসা পর্যন্ত অপেক্ষা করে।

```js
// সারাহ তার পালা না আসা পর্যন্ত লাইনে অপেক্ষা করে।
let inLine = true;
let turns = 0;

while (inLine && turns < 5) {
  turns++;
  console.log(`সারাহ অপেক্ষা করে... (${turns}) ⏳`);
  if (turns === 3) {
    inLine = false;
    console.log("ফেরিস হুইলে সারাহর পালা! 🎡");
  }
}
```

_এটি সারাহর অপেক্ষা প্রিন্ট করবে, এবং ৩য় পালায়: ফেরিস হুইলে সারাহর পালা! 🎡_

- `while` লুপ চলতে থাকে যতক্ষণ কন্ডিশন সত্য থাকে।

### Do-While লুপ: ক্ল মেশিন চেষ্টা করা 🕹️

সারাহ অন্তত একবার ক্ল মেশিন চেষ্টা করে, এমনকি যদি সে নিশ্চিত না হয় যে সে জিতবে।

```js
// সারাহ অন্তত একবার ক্ল মেশিন চেষ্টা করে।
let prizeWon = false;
do {
  console.log("সারাহ ক্ল মেশিন চেষ্টা করে! 🕹️");
  // ধরা যাক সে এবার জিতল না।
} while (prizeWon);
```

_এটি সবসময় প্রিন্ট করে: সারাহ ক্ল মেশিন চেষ্টা করে! 🕹️_

- `do-while` সবসময় কোডটি অন্তত একবার চালায়।

---

## ৪. Break এবং Continue: পার্ক এক্সপ্লোর করা 🗺️

সারাহ ৫টি আকর্ষণ এক্সপ্লোর করে, কিন্তু হন্টেড হাউস এড়িয়ে যায় এবং ক্লান্ত হলে আগেই চলে যায়।

```js
// সারাহ আকর্ষণগুলো এক্সপ্লোর করে, হন্টেড হাউস এড়িয়ে যায়, এবং ক্লান্ত হলে চলে যায়।
for (let i = 1; i <= 5; i++) {
  if (i === 3) {
    // আকর্ষণ ৩-এ, এটি হন্টেড হাউস—সারাহ এড়িয়ে যায়!
    console.log("সারাহ হন্টেড হাউস এড়িয়ে যায়! 👻");
    continue;
  }
  if (i === 5) {
    // আকর্ষণ ৫-এ, সারাহ ক্লান্ত হয়ে পার্ক ছেড়ে চলে যায়।
    console.log("সারাহ ক্লান্ত হয়ে পার্ক ছেড়ে চলে যায়! 🏡");
    break;
  }
  // অন্য আকর্ষণগুলোর জন্য, সারাহ সেগুলো উপভোগ করে।
  console.log(`সারাহ আকর্ষণ #${i} উপভোগ করে! 🎢`);
}
```

_এটি আকর্ষণ ১, ২ উপভোগ, ৩ এড়িয়ে যাওয়া, ৪ উপভোগ, তারপর ৫-এ চলে যাওয়া প্রিন্ট করবে।_

---

## সারাহর কন্ট্রোল ফ্লো টিপস 💡

- সাধারণ পছন্দের জন্য `if/else`, অনেক অপশনের জন্য `switch` ব্যবহার করুন।
- লুপ আপনাকে কাজ পুনরাবৃত্তি করতে সাহায্য করে—এগুলো ব্যবহার করতে ভয় পাবেন না!
- `break` এবং `continue` আপনার যাত্রার উপর আরও নিয়ন্ত্রণ দেয়।

---

## কুইজ টাইম! 🎲

১. এটি কী প্রিন্ট করে?

```js
let hungry = true;
if (hungry) {
  console.log("স্ন্যাক খাও! 🍎");
} else {
  console.log("খেলা চালিয়ে যাও! 🎠");
}
```

২. এই লুপটি কতবার চলে?

```js
for (let i = 0; i < 4; i++) {
  console.log(i);
}
```

৩. লুপে `break` কী করে?
৪. `while` এবং `do-while` এর মধ্যে পার্থক্য কী?

<details>
  <summary>উত্তর দেখুন</summary>
  <ol>
    <li>
      <code>স্ন্যাক খাও! 🍎</code>
    </li>
    <li>৪ বার (০, ১, ২, ৩ প্রিন্ট করে)</li>
    <li>এটি লুপ আগেই বন্ধ করে দেয়।</li>
    <li>
      <code>while</code> প্রথমে কন্ডিশন চেক করে, <code>do-while</code> অন্তত
      একবার চলে।
    </li>
  </ol>
</details>

---

## সারাংশ 📝

- কন্ট্রোল ফ্লো সারাহকে (এবং আপনাকে!) কোডে সিদ্ধান্ত নিতে এবং কাজ পুনরাবৃত্তি করতে সাহায্য করে। 🗺️
- আপনার প্রোগ্রামের যাত্রা পরিচালনা করতে `if/else`, `switch`, এবং লুপ ব্যবহার করুন।
- জাভাস্ক্রিপ্ট ল্যান্ডে একজন মাস্টার এক্সপ্লোরার হওয়ার জন্য এই টুলগুলো অনুশীলন করুন! 🧭

হ্যাপি কোডিং! 🚀
