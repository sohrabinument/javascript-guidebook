# Git & GitHub: সম্পূর্ণ গাইড 🚀

import { Callout } from "nextra/components";
import { Tabs } from "nextra/components";

![Git Workflow](../../../public/git-workflow.gif)

> **Git এ নতুন?** 🌟 এই গাইড আপনাকে শূন্য থেকে হিরো বানাবে! আমরা সহজ উদাহরণ এবং বাস্তব নমুনা ব্যবহার করব যাতে Git আপনার কাছে স্বাভাবিক মনে হয়।

---

## 🎯 আপনি কী শিখবেন

এই গাইডের শেষে, আপনি পারবেন:

✅ একজন পেশাদারের মতো আপনার প্রজেক্টের পরিবর্তনগুলি ট্র্যাক করতে  
✅ টিম সদস্যদের সাথে নির্বিঘ্নে সহযোগিতা করতে  
✅ আর কখনো আপনার কাজ হারাবেন না  
✅ আধুনিক ডেভেলপমেন্টে ব্যবহৃত "Git workflow" বুঝতে  
✅ আত্মবিশ্বাসের সাথে সাধারণ Git ভুলগুলি ঠিক করতে  
✅ একজন বিশেষজ্ঞের মতো merge conflicts সামলাতে  
✅ rebase, merge, এবং squash কৌশলগুলিতে দক্ষতা অর্জন করতে  
✅ একটি কম্পিউটারে একাধিক GitHub অ্যাকাউন্ট পরিচালনা করতে

---

## 🤔 Git কী? কেন Version Control ব্যবহার করবেন?

Git হলো একটি **version control system** - এটিকে আপনার কোডের জন্য টাইম মেশিন হিসেবে ভাবুন! ⏰

### Git যে সমস্যার সমাধান করে 🧩

কল্পনা করুন আপনি একটি গল্প লিখছেন:

📝 **দিন ১:** আপনি অধ্যায় ১ লিখলেন  
✏️ **দিন ২:** আপনি অধ্যায় ২ যোগ করলেন এবং অধ্যায় ১ সম্পাদনা করলেন  
😱 **দিন ৩:** আপনি বুঝতে পারলেন অধ্যায় ১ সম্পাদনার আগে ভালো ছিল

**Git ছাড়া:** আপনি আটকে গেছেন! আপনার আসল অধ্যায় ১ চিরকালের জন্য হারিয়ে গেছে। 💔

**Git সহ:** আপনি যেকোনো সংস্করণে ফিরে যেতে পারেন, ঠিক কী পরিবর্তন হয়েছে দেখতে পারেন, এবং এমনকি একসাথে একাধিক গল্পের সংস্করণে কাজ করতে পারেন। ✨

<Callout type="info">
  **বাস্তব জীবনের উদাহরণ:** Git হলো Google Docs এর version history এর মতো,
  কিন্তু ডেভেলপারদের জন্য সুপারচার্জড। এটি প্রতিটি পরিবর্তন ট্র্যাক করে, একাধিক
  মানুষকে একসাথে কাজ করতে দেয়, এবং নিশ্চিত করে যে কিছুই সত্যিকার অর্থে হারিয়ে
  যায় না।
</Callout>

---

## 🏗️ মূল ধারণাগুলি সহজভাবে

### Repository (Repo) 📁

একটি **repository** হলো আপনার প্রজেক্ট ফোল্ডার যাতে Git এর সুপার পাওয়ার চালু করা আছে। এটি একটি সাধারণ ফোল্ডারের মতো, কিন্তু Git এর ভিতরের সবকিছু দেখে এবং আপনার করা প্রতিটি পরিবর্তন মনে রাখে!

### Commits 📸

একটি **commit** হলো একটি নির্দিষ্ট মুহূর্তে আপনার প্রজেক্টের স্ন্যাপশট। এটিকে আপনার কাজের ছবি তোলার মতো ভাবুন - আপনি সবসময় সেই সঠিক মুহূর্তটি দেখতে পারেন। এটি ভিডিও গেমের সেভ পয়েন্টের মতো!

### Branches 🌳

**Branches** আপনাকে একসাথে বিভিন্ন ফিচারে কাজ করতে দেয়। কল্পনা করুন আপনি এমন একটি গল্প লিখছেন যেখানে আপনি বিভিন্ন সমাপনী চেষ্টা করতে চান - প্রতিটি সমাপনী একটি branch! আপনি আপনার মূল কাজ না ভেঙে আপনার প্রজেক্টের সমান্তরাল সংস্করণে কাজ করতে পারেন।

<Callout type="info">
  **ভিজুয়ালাইজেশন:** আপনার প্রজেক্টকে একটি গাছ হিসেবে ভাবুন: 🌳 **কাণ্ড** আপনার
  মূল branch (সাধারণত `main` বা `master` বলা হয়) 🌿 **Branches** হলো যেখানে
  আপনি নতুন ফিচার নিয়ে পরীক্ষা করেন 🔄 প্রস্তুত হলে আপনি সবসময় branches গুলিকে
  কাণ্ডে merge করতে পারেন
</Callout>

---

## 🛠️ Git ইনস্টল করা

### ধাপে ধাপে ইনস্টলেশন 📥

**প্রথম ধাপ:**  
[git-scm.com/downloads](https://git-scm.com/downloads) এ যান এবং আপনার কম্পিউটারের জন্য Git ডাউনলোড করুন।

**দ্বিতীয় ধাপ:**  
আপনার ডাউনলোড করা ফাইলটি খুলুন এবং Git ইনস্টল করার জন্য নির্দেশাবলী অনুসরণ করুন।

### প্ল্যাটফর্ম-নির্দিষ্ট নির্দেশাবলী 💻

<Tabs items={["Windows", "macOS", "Linux"]}>
  <Tabs.Tab>
    **Windows ইনস্টলেশন:**  
    1. [git-scm.com](https://git-scm.com/downloads) থেকে Git ডাউনলোড করুন 📥  
    2. ডিফল্ট সেটিংস দিয়ে ইনস্টলার চালান ⚙️  
    3. যাচাই করতে Command Prompt বা PowerShell খুলুন: 🔍  
    ```bash
    git --version
    ```
    💡 **টিপ:** আরও ভালো টার্মিনাল অভিজ্ঞতার জন্য ইনস্টলেশনের সময় "Git Bash" বেছে নিন!
  </Tabs.Tab>

{" "}

<Tabs.Tab>
  **macOS ইনস্টলেশন:** **বিকল্প ১: Homebrew (প্রস্তাবিত)** 🍺 ```bash brew
  install git ``` **বিকল্প ২: ডাউনলোড** 📥 1.
  [git-scm.com](https://git-scm.com/downloads) থেকে ডাউনলোড করুন 2. ডিফল্ট
  সেটিংস দিয়ে ইনস্টল করুন 💡 **টিপ:** যদি আপনার Homebrew না থাকে, প্রথমে
  [brew.sh](https://brew.sh) থেকে এটি ইনস্টল করুন
</Tabs.Tab>

  <Tabs.Tab>
    **Linux ইনস্টলেশন:**  
    **Ubuntu/Debian:** 🐧  
    ```bash
    sudo apt update
    sudo apt install git
    ```
    **CentOS/RHEL:** 🔴  
    ```bash
    sudo yum install git
    ```
    💡 **টিপ:** বেশিরভাগ Linux ডিস্ট্রিবিউশনে Git আগে থেকেই ইনস্টল করা থাকে!
  </Tabs.Tab>
</Tabs>

### প্রথমবার সেটআপ 🎯

**আপনার পরিচয় কনফিগার করুন** 👤

Git কে বলুন আপনি কে (এটি আপনার commits এ দেখা যায়):

```bash
git config --global user.name "Your Name"
git config --global user.email "your.email@example.com"
```

**আপনার ডিফল্ট এডিটর সেট করুন (ঐচ্ছিক)** ✏️

```bash
git config --global core.editor "code --wait"  # VS Code
git config --global core.editor "nano"         # Nano (সহজ)
```

**আপনার সেটআপ যাচাই করুন** ✅

```bash
git config --list
```

💡 **প্রো টিপ:** সহজ সম্পাদনার জন্য আপনার Git কনফিগারেশন আপনার ডিফল্ট এডিটরে খুলতে `git config --global --edit` ব্যবহার করুন!

---

## 🚀 প্রয়োজনীয় Git কমান্ড (শিক্ষানবিস থেকে উন্নত)

### Git শুরু করা 🎬

<Callout type="info">
  **এটি কী করে?** এই কমান্ডটি Git কে বলে আপনার ফোল্ডার দেখা শুরু করতে। এটি আপনার
  প্রজেক্টের জন্য "টাইম মেশিন" চালু করার মতো! ⏰
</Callout>

```bash
git init
```

**কী ঘটে:** Git একটি লুকানো `.git` ফোল্ডার তৈরি করে যা সমস্ত version history সংরক্ষণ করে। এখন আপনার ফোল্ডার একটি Git repository যেখানে আপনি পরিবর্তন ট্র্যাক করা, সংস্করণ সংরক্ষণ করা, এবং অন্যদের সাথে সহযোগিতা করা শুরু করতে পারেন।

💡 **টিপ:** আপনি `ls -la` (Linux/Mac) বা `ls --all` (Windows) দিয়ে লুকানো `.git` ফোল্ডারটি দেখতে পারেন। এটি মুছে ফেলবেন না - এতে আপনার সম্পূর্ণ প্রজেক্ট ইতিহাস রয়েছে!

---

### Git স্ট্যাটাস চেক করা 📊

<Callout type="info">
  **কেন এটি ব্যবহার করবেন?** এই কমান্ডটি আপনাকে দেখায় কোন ফাইলগুলি পরিবর্তিত
  হয়েছে, কোনগুলি staged আছে, এবং কোনগুলি নেই। এটি জমা দেওয়ার আগে আপনার
  হোমওয়ার্ক তালিকা চেক করার মতো! আপনার Git ড্যাশবোর্ড! 📋
</Callout>

```bash
git status
```

**উদাহরণ আউটপুট:**

```
On branch main
Changes not staged for commit:
  modified:   README.md

Untracked files:
  new-feature.js
```

💡 **টিপ:** আপনার স্ট্যাটাসের একটি ছোট, আরও কমপ্যাক্ট দৃশ্যের জন্য `git status -s` ব্যবহার করুন!

---

### Git ট্র্যাকিংয়ের জন্য ফাইল Stage করা 📦

<Callout type="info">
  **staging মানে কী?** Staging হলো জমা দেওয়ার আগে আপনার হোমওয়ার্ক ব্যাগে রাখার
  মতো। শুধুমাত্র staged ফাইলগুলি আপনার পরবর্তী commit এ অন্তর্ভুক্ত হবে! এটি
  শিপিংয়ের আগে বাক্সে আইটেম প্যাক করার মতো। 📬
</Callout>

```bash
git add <filename>      # একটি নির্দিষ্ট ফাইল stage করা
git add --all           # সব ফাইল stage করা
git add .               # বর্তমান ডিরেক্টরিতে সব ফাইল stage করা
```

**আরও staging অপশন:**

```bash
git add README.md
git add src/index.js
git add "*.js"          # সব JavaScript ফাইল
git add src/            # সম্পূর্ণ src ডিরেক্টরি
```

💡 **প্রো টিপ:** ইন্টারঅ্যাক্টিভভাবে ফাইলের অংশ stage করতে `git add -p` ব্যবহার করুন - যখন আপনি শুধুমাত্র নির্দিষ্ট পরিবর্তনগুলি commit করতে চান তখন নিখুঁত!

---

### পরিবর্তন Commit করা 💾

<Callout type="info">
  **commit কী?** একটি commit ভিডিও গেমে চেকপয়েন্ট সংরক্ষণের মতো। আপনি সবসময়
  এতে ফিরে যেতে পারেন! এটি একটি বর্ণনামূলক বার্তা সহ আপনার প্রজেক্টের স্ন্যাপশট।
  🎮
</Callout>

```bash
git commit -m "Your message"
```

একটি বার্তা লিখুন যা বর্ণনা করে আপনি কী পরিবর্তন করেছেন, যেমন "নতুন অধ্যায় যোগ করা" বা "ভূমিকায় টাইপো ঠিক করা"।

**ভালো Commit বার্তা লেখা:** ✍️

✅ `Add user login functionality`  
✅ `Fix navbar responsive design`  
✅ `Update API documentation`  
❌ `stuff`  
❌ `fixed it`  
❌ `asdf`

💡 **প্রো টিপ:** একটি কমান্ডে ট্র্যাক করা ফাইলগুলি add এবং commit করতে `git commit -am "message"` ব্যবহার করুন!

---

### Git Log চেক করা 📜

<Callout type="info">
  **কেন log চেক করবেন?** Log আপনাকে আপনার প্রজেক্টের সমস্ত সেভ পয়েন্ট (commits)
  দেখায়। এটি প্রতিদিন আপনি কী করেছেন দেখতে আপনার ডায়েরি উল্টানোর মতো! আপনার
  প্রজেক্টের সব স্ন্যাপশট, কে পরিবর্তন করেছে, এবং কখন দেখুন। 📖
</Callout>

```bash
git log
# অথবা একটি ছোট দৃশ্যের জন্য:
git log --oneline
```

**আরও log অপশন:**

```bash
git log                           # বিস্তারিত log
git log --oneline                # কমপ্যাক্ট দৃশ্য
git log --graph --oneline --all  # branches এর ভিজুয়াল উপস্থাপনা
```

💡 **প্রো টিপ:** শুধুমাত্র আপনার commits দেখতে `git log --author="Your Name"` ব্যবহার করুন, বা সাম্প্রতিক commits এর জন্য `git log --since="2 weeks ago"`!

---

### এক Commit অবস্থা থেকে অন্যটিতে ফিরে যাওয়া ⏪

<Callout type="warning">
  **সাবধান!** ⚠️ এটি আপনার প্রজেক্টকে আগের অবস্থায় পরিবর্তন করতে পারে। নিশ্চিত
  হন যে আপনি সত্যিই ফিরে যেতে চান! এই কমান্ডগুলির কিছু স্থায়ীভাবে কাজ মুছে
  ফেলতে পারে।
</Callout>

```bash
git reset --hard <commit id>
```

আপনি `git log` থেকে commit id খুঁজে পেতে পারেন।

**অন্যান্য reset অপশন:**

| পরিস্থিতি                              | কমান্ড                          | প্রভাব                                    |
| -------------------------------------- | ------------------------------- | ----------------------------------------- |
| একটি ফাইল unstage করা                  | `git reset HEAD file.txt`       | staging area থেকে সরানো হয়               |
| শেষ commit বাতিল করা (পরিবর্তন রাখা)   | `git reset --soft HEAD~1`       | Commit সরানো হয়েছে, ফাইলগুলি এখনও staged |
| শেষ commit বাতিল করা (পরিবর্তন হারানো) | `git reset --hard HEAD~1`       | ⚠️ Commit এবং পরিবর্তন মুছে ফেলা হয়েছে   |
| মুছে ফেলা ফাইল পুনরুদ্ধার করা          | `git checkout HEAD -- file.txt` | শেষ commit থেকে পুনরুদ্ধার                |

💡 **নিরাপত্তা টিপ:** `--hard` reset ব্যবহার করার আগে সবসময় একটি ব্যাকআপ branch তৈরি করুন: `git branch backup-branch`

---

### প্রতিটি পরিবর্তনের জন্য রেফারেন্স সহ Git Log দেখা 🔍

<Callout type="info">
  **reflog কী?** Reflog আপনাকে আপনার প্রজেক্টে করা সমস্ত চালনা দেখায়, এমনকি
  যেগুলি আপনি বাতিল করেছেন! এটি আপনার Git নিরাপত্তা জাল। 🥅
</Callout>

```bash
git reflog
```

💡 **পুনরুদ্ধার টিপ:** যদি আপনি ভুলবশত অনেক দুর reset করেন, commit hash খুঁজতে `git reflog` ব্যবহার করুন এবং ফিরে যেতে `git reset --hard <hash>`!

---

### Git ট্র্যাকিং থেকে একটি ফাইল সরানো 🗑️

<Callout type="info">
  **কেন একটি ফাইল সরাবেন?** যদি আপনি ভুলবশত কিছু যোগ করেন, আপনি Git কে বলতে
  পারেন এটি ট্র্যাক করা বন্ধ করতে। হয়তো আপনি দুর্ঘটনাক্রমে পাসওয়ার্ড বা বড়
  ফাইল যোগ করেছেন! 🙈
</Callout>

```bash
git rm <filename>
```

💡 **টিপ:** Git থেকে সরাতে কিন্তু আপনার কম্পিউটারে ফাইল রাখতে `git rm --cached <filename>` ব্যবহার করুন!

---

## 🌿 Branch নিয়ে কাজ করা

### কেন Branch ব্যবহার করবেন? 🤔

Branch আপনাকে দেয়:

🛡️ মূল কোড না ভেঙে ফিচার নিয়ে কাজ করার সুবিধা  
🧪 নিরাপদে পরীক্ষা-নিরীক্ষার সুবিধা  
👥 দ্বন্দ্ব ছাড়াই সহযোগিতার সুবিধা  
📚 পরিষ্কার প্রজেক্ট ইতিহাস রাখার সুবিধা

<Callout type="info">
  **উদাহরণ:** Branch গুলো একটা রচনার একাধিক খসড়ার মতো। আপনি আপনার মূল কাজ
  হারানো ছাড়াই বিভিন্ন পদ্ধতি চেষ্টা করতে পারেন। 📝
</Callout>

### একটি Branch তৈরি করুন 🌱

<Callout type="info">
  **Branch কি?** Branch হলো আপনার প্রজেক্টের একটি সমান্তরাল মহাবিশ্বের মতো।
  আপনি আপনার মূল কাজ পরিবর্তন না করেই নতুন জিনিস চেষ্টা করতে পারেন! 🌌
</Callout>
  
  ```bash
git branch <branch name>
```

💡 **নামকরণের টিপ:** `feature/user-login` অথবা `fix/navbar-bug` এর মতো বর্ণনামূলক নাম ব্যবহার করুন যাতে branch টি কিসের জন্য তা স্পষ্ট হয়!

---

### Branch তালিকা দেখুন 📋

```bash
git branch --list
```

বর্তমান branch এর পাশে একটি তারকা চিহ্ন (\*) থাকবে।

💡 **রঙের টিপ:** সুন্দর রঙের সাথে স্থানীয় এবং দূরবর্তী উভয় branch দেখতে `git branch -a` ব্যবহার করুন!

---

### একটি Branch থেকে অন্য Branch এ যান 🔄

```bash
git switch <branch name>
```

**অথবা একই কমান্ডে তৈরি করুন এবং সুইচ করুন:**

```bash
git switch -c <branch name>  # Create and switch to new branch
```

💡 **দ্রুত টিপ:** পূর্ববর্তী branch এ ফিরে যেতে `git switch -` ব্যবহার করুন!

---

### একটি Branch থেকে অন্য Branch এ কোড মার্জ করুন 🔗

<Callout type="warning" emoji="💡">
	**গুরুত্বপূর্ণ:** আপনি যে branch এ অন্য branch থেকে কোড আনতে চান, প্রথমে `git switch <branchname>` ব্যবহার করে সেই branch এ যান। তারপর নিচের কমান্ড দিন।
</Callout>

```bash
git merge <branch name>
```

**সম্পূর্ণ মার্জ প্রক্রিয়া:**

```bash
git switch main                    # Switch to main branch
git merge feature/user-login      # Merge feature branch
```

💡 **পরিষ্কার মার্জের টিপ:** মার্জ করার পর সবসময় আপনার কোড টেস্ট করুন এবং branch ইতিহাস সংরক্ষণের জন্য `git merge --no-ff` ব্যবহার বিবেচনা করুন!

---

### Git থেকে একটি Branch মুছে ফেলুন (সব পদ্ধতি, কখন এবং কেন) 🗑️

একটি branch মুছে ফেলার কয়েকটি উপায় আছে, এটি নির্ভর করে এটি স্থানীয় (আপনার কম্পিউটারে) নাকি দূরবর্তী (GitHub এ) কিনা।

#### স্থানীয় Branch মুছুন 🏠

```bash
git branch -d branch-name
```

🔹 `-d` ফ্ল্যাগ মানে "delete"। এটি শুধুমাত্র তখনই branch মুছবে যখন এটি সম্পূর্ণভাবে মার্জ হয়ে গেছে (নিরাপদ মুছে ফেলা)।

**জোর করে মুছুন (যদি মার্জ না হয়ে থাকে):**

```bash
git branch -D branch-name
```

🔹 `-D` ফ্ল্যাগ মানে "যেভাবেই হোক মুছে ফেল"। আপনি যদি নিশ্চিত হন যে আপনার branch এর পরিবর্তনগুলোর প্রয়োজন নেই তাহলে এটি ব্যবহার করুন।

**কখন এবং কেন?** 🤔

✅ আপনার মূল branch এ (যেমন `main` অথবা `master`) পরিবর্তনগুলো মার্জ করার পর branch মুছুন  
🧹 আপনার প্রজেক্ট পরিষ্কার রাখতে পুরানো branch গুলো পরিষ্কার করুন  
⚠️ শুধুমাত্র তখনই জোর করে মুছুন যখন আপনি নিশ্চিত যে আপনার সেই branch এর কাজের প্রয়োজন নেই

#### দূরবর্তী Branch মুছুন (GitHub এ) ☁️

```bash
git push origin --delete branch-name
```

🔹 এটি GitHub কে বলে দূরবর্তী রিপোজিটরি থেকে branch টি সরিয়ে ফেলতে।

**কখন এবং কেন?** 🤔

✅ pull request মার্জ করার পর, আপনার GitHub রেপো পরিষ্কার রাখতে দূরবর্তী branch মুছে ফেলুন  
🚫 বিভ্রান্তি প্রতিরোধ করে এবং পুরানো branch গুলোতে দুর্ঘটনাবশত কাজ করা থেকে বিরত রাখে

💡 **অটোমেশনের টিপ:** অনেক টিম pull request মার্জ করার পর স্বয়ংক্রিয়ভাবে branch মুছে ফেলার জন্য GitHub সেট আপ করে!

---

### Branch এর নাম পরিবর্তন করুন ✏️

<Callout type="warning" emoji="💡">
	**গুরুত্বপূর্ণ:** একটি branch এর নাম পরিবর্তন করতে, প্রথমে `git switch <branchname>` ব্যবহার করে সেই branch এ যান। তারপর নিচের কমান্ড দিন।
</Callout>

```bash
git branch -m <updated branch name>
```

### Branch নামকরণের নিয়মাবলী 📝

| ধরন       | ফরম্যাট               | উদাহরণ                  |
| --------- | --------------------- | ----------------------- |
| ফিচার     | `feature/description` | `feature/user-login`    |
| বাগ ফিক্স | `fix/description`     | `fix/navbar-responsive` |
| হটফিক্স   | `hotfix/description`  | `hotfix/security-patch` |
| রিলিজ     | `release/version`     | `release/v1.2.0`        |

💡 **টিমের টিপ:** প্রজেক্ট শুরু করার আগে আপনার টিমের সাথে নামকরণের নিয়মাবলী নিয়ে একমত হন!

---

## 💾 Git Stashing

### Git পরিবর্তনগুলো Stash করুন (Git Draft এ অসম্পূর্ণ কাজ সংরক্ষণ করুন) 📋

<Callout type="info">
  **Stashing কি?** Stashing হলো আপনার কাজ পরে করার জন্য একটি ড্রয়ারে রেখে
  দেওয়ার মতো, যাতে আপনি অন্য কিছু নিয়ে কাজ করতে পারেন! commit না করেই
  অস্থায়ীভাবে আপনার কাজ সংরক্ষণ করুন। 🗄️
</Callout>

```bash
git stash
```

**বার্তা সহ নামযুক্ত stash:**

```bash
git stash push -m "WIP: user login form"
```

💡 **সংগঠনের টিপ:** আপনার stash গুলোর সাথে সবসময় বর্ণনামূলক বার্তা ব্যবহার করুন - পরে আপনি নিজেকে ধন্যবাদ দেবেন!

---

### Stash তালিকা চেক করুন 📜

```bash
git stash list
```

💡 **দ্রুত দেখার টিপ:** আপনার stash গুলোর একটি পরিষ্কার দৃশ্যের জন্য `git stash list --oneline` ব্যবহার করুন!

---

### Git Stash এ কি সংরক্ষিত হয়েছে তা দেখুন 👀

```bash
git stash show -p
```

**নির্দিষ্ট stash দেখান:**

```bash
git stash show stash@{0}  # Show stash contents
```

💡 **বিস্তারিত দেখার টিপ:** শুধু ফাইলের নাম নয়, আসল পরিবর্তনগুলো দেখতে `-p` যোগ করুন!

---

### Git Stash প্রয়োগ করুন 🔄

```bash
git stash apply <stash id>
# or to apply and remove from stash:
git stash pop
```

**নির্দিষ্ট stash প্রয়োগ করুন:**

```bash
git stash apply stash@{0}  # Apply specific stash
git stash drop stash@{0}   # Delete specific stash
```

💡 **নিরাপত্তার টিপ:** যদি আপনি backup হিসেবে stash রাখতে চান তাহলে `pop` এর পরিবর্তে `apply` ব্যবহার করুন!

---

## 🔧 ফাইল ব্যবস্থাপনা এবং .gitignore

### যদি আপনি দুর্ঘটনাবশত একটি ফাইল Git এ যোগ করে ফেলেন 🙈

কখনো কখনো commit করার পর আপনি বুঝতে পারেন যে আপনি একটি ফাইল Git থেকে ignore করতে চান:

**প্রথম ধাপ:**  
একটি .gitignore ফাইল তৈরি করুন এবং সেই ফাইলের নাম এতে যোগ করুন।

**দ্বিতীয় ধাপ:**

```bash
git rm --cached <filename>
```

### .gitignore উদাহরণ 📝

<Tabs items={["Node.js", "Python", "General"]}>
  <Tabs.Tab>
    **Node.js প্রজেক্ট:**
    ```gitignore
    # Dependencies
    node_modules/
    npm-debug.log*
    yarn-debug.log*
    yarn-error.log*
    
    # Production builds
    /dist
    /build
    
    # Environment variables
    .env
    .env.local
    .env.production
    
    # IDE files
    .vscode/
    .idea/
    ```
  </Tabs.Tab>

{" "}

<Tabs.Tab>
  **Python প্রজেক্ট:** ```gitignore # Python __pycache__/ *.py[cod] *$py.class
  *.so .Python # Virtual environments venv/ env/ ENV/ # IDE .vscode/ .idea/
  *.swp *.swo ```
</Tabs.Tab>

  <Tabs.Tab>
    **সাধারণ ফাইল:**
    ```gitignore
    # OS generated files
    .DS_Store
    .DS_Store?
    ._*
    .Spotlight-V100
    .Trashes
    ehthumbs.db
    Thumbs.db
    
    # Logs
    *.log
    
    # Temporary files
    *.tmp
    *.temp
    ```
  </Tabs.Tab>
</Tabs>

💡 **টেমপ্লেটের টিপ:** যেকোনো প্রজেক্ট ধরনের জন্য তৈরি .gitignore টেমপ্লেট পেতে [gitignore.io](https://gitignore.io) চেক করুন!

---

## 🌐 GitHub এর সাথে কাজ করা

### GitHub কি? 🐙

GitHub হল কোডের জন্য Google Drive এর মতো - এটি আপনার Git repositories অনলাইনে সংরক্ষণ করে এবং সহযোগিতার বৈশিষ্ট্য যোগ করে যেমন:

📋 Issue tracking  
🔄 Pull requests  
👥 Team management  
🚀 Automated deployments

GitHub হল একটি বিশাল লাইব্রেরির মতো যেখানে আপনি আপনার Git প্রজেক্টগুলি অনলাইনে রাখতে পারেন, বন্ধুদের সাথে শেয়ার করতে পারেন এবং একসাথে কাজ করতে পারেন। এটি আপনার কাজ প্রদর্শন করার এবং অন্যদের কাছ থেকে সাহায্য পাওয়ার একটি জায়গাও!

### GitHub বা Remote Repository তে ফাইল যোগ করা বা Push করা 📤

**প্রথম ধাপ:**

```bash
git remote add <origin name> <github repository link>
```

উদাহরণ:  
`git remote add origin git@github.com:YourUsername/your-repo.git`

**দ্বিতীয় ধাপ:**

```bash
git push -u <origin name> <branch name>
```

উদাহরণ:  
`git push -u origin main`

💡 **প্রথম Push টিপ:** `-u` ফ্ল্যাগটি tracking সেট আপ করে, তাই পরের বার আপনি শুধু `git push` ব্যবহার করতে পারবেন!

### SSH সেট আপ করা (প্রস্তাবিত) 🔐

**SSH Key তৈরি করুন** 🗝️

```bash
ssh-keygen -t ed25519 -C "your.email@example.com"
```

ডিফল্ট লোকেশনের জন্য Enter চাপুন, একটি passphrase সেট করুন (ঐচ্ছিক)।

**GitHub এ SSH Key যোগ করুন** ➕

1. আপনার public key কপি করুন:
   ```bash
   cat ~/.ssh/id_ed25519.pub
   ```
2. GitHub → Settings → SSH and GPG keys এ যান
3. "New SSH key" ক্লিক করুন এবং আপনার key পেস্ট করুন

**সংযোগ পরীক্ষা করুন** 🧪

```bash
ssh -T git@github.com
```

আপনার দেখা উচিত: "Hi username! You've successfully authenticated..."

💡 **নিরাপত্তা টিপ:** ভাল নিরাপত্তা এবং সুবিধার জন্য সবসময় পাসওয়ার্ডের পরিবর্তে SSH keys ব্যবহার করুন!

---

### Remote Repository থেকে Pull করা ⬇️

```bash
git pull
```

💡 **ভাল অনুশীলনের টিপ:** সর্বশেষ পরিবর্তনগুলি পেতে নতুন কাজ শুরু করার আগে সবসময় pull করুন!

---

### Remote Repository থেকে Fetch করা (Merge না করে পরিবর্তন দেখা) 👁️

```bash
git fetch
```

### Push, Pull, Fetch বোঝা 🔄

📤 **Push:** আপনার commits GitHub এ আপলোড করুন। শিক্ষকের কাছে আপনার হোমওয়ার্ক পাঠানোর মতো।  
📥 **Pull:** GitHub থেকে পরিবর্তনগুলি ডাউনলোড এবং merge করুন। আপনার গ্রুপ প্রজেক্টের সর্বশেষ সংস্করণ পাওয়ার মতো।  
👀 **Fetch:** Merge না করে পরিবর্তনগুলি ডাউনলোড করুন। আপনার বন্ধুরা কি করেছে তা পরীক্ষা করার মতো, কিন্তু এখনও এটি আপনার কাজে যোগ না করা।

| Command     | এটি কি করে                           |
| ----------- | ------------------------------------ |
| `git push`  | আপনার commits আপলোড করে              |
| `git pull`  | Fetches এবং remote changes merge করে |
| `git fetch` | শুধুমাত্র fetches, merge করে না      |

<Callout type="info">
  **গুরুত্বপূর্ণ:** Push করার আগে সবসময় pull করুন! এটি আপনার কাজকে আপনার
  বন্ধুদের সাথে আপ টু ডেট রাখে। 🤝
</Callout>

---

## 🔄 Pull Requests Workflow

**Feature Branch তৈরি করুন** 🌿

```bash
git switch -c feature/awesome-feature
# আপনার পরিবর্তনগুলি করুন
git add .
git commit -m "Add awesome feature"
```

**Branch টি GitHub এ Push করুন** 📤

```bash
git push -u origin feature/awesome-feature
```

**Pull Request তৈরি করুন** 📋

1. GitHub এ আপনার repository তে যান
2. "Compare & pull request" ক্লিক করুন
3. বিবরণ যোগ করুন এবং reviewers চাইন
4. "Create pull request" ক্লিক করুন

**Merge এবং Clean Up** 🧹

অনুমোদনের পর:

```bash
git switch main
git pull                                    # সর্বশেষ পরিবর্তনগুলি পান
git branch -d feature/awesome-feature       # Local branch মুছুন
git push origin --delete feature/awesome-feature  # Remote branch মুছুন
```

💡 **Review টিপ:** অন্যদের review করতে বলার আগে সবসময় নিজের pull request প্রথমে review করুন!

---

## 🚨 Merge Conflicts পরিচালনা

### Merge Conflict কি? 💥

একটি merge conflict তখন ঘটে যখন Git বিভিন্ন branch থেকে পরিবর্তনগুলি স্বয়ংক্রিয়ভাবে একত্রিত করতে পারে না। এটি দুইজন ব্যক্তির একটি নথিতে একই বাক্য সম্পাদনা করার মতো - Git এর আপনার সিদ্ধান্ত নেওয়া প্রয়োজন কোন সংস্করণটি রাখতে হবে।

যখন আপনি অন্যদের সাথে কাজ করেন বা এমনকি বিভিন্ন branch এ কাজ করেন, কখনও কখনও দুইজন (বা দুটি branch) একই ফাইলের একই অংশ পরিবর্তন করে। যখন আপনি সেই পরিবর্তনগুলি merge করার চেষ্টা করেন, Git জানে না কোন সংস্করণটি রাখতে হবে।

### আপনার Merge Conflict আছে কিভাবে জানবেন 🚨

⚠️ যখন আপনি `git merge branch-name` চালান, Git থামবে এবং এইরকম একটি বার্তা দেখাবে:

> CONFLICT (content): Merge conflict in filename.txt

📱 আপনার terminal বলবে: "Automatic merge failed; fix conflicts and then commit the result."

📊 যদি আপনি `git status` চালান, আপনি "Unmerged" হিসেবে চিহ্নিত ফাইলগুলি দেখবেন

### ধাপে ধাপে Conflicts সমাধান করা 🛠️

**Merge করার চেষ্টা করুন** 🔄

```bash
git merge branch-name
```

যদি conflict থাকে, Git আপনাকে বলবে কোন ফাইলগুলি প্রভাবিত।

**Conflicted ফাইল(গুলি) খুলুন** 📝

আপনার code editor এ ফাইলটি খুলুন। আপনি এরকম বিশেষ চিহ্নগুলি দেখবেন:

```
<<<<<<< HEAD
আপনার পরিবর্তনগুলি (যে branch এ আপনি আছেন)
=======
তাদের পরিবর্তনগুলি (যে branch থেকে আপনি merge করছেন)
>>>>>>> branch-name
```

🔹 `<<<<<<< HEAD` এবং `=======` এর মধ্যে সবকিছু আপনার সংস্করণ  
🔹 `=======` এবং `>>>>>>> branch-name` এর মধ্যে সবকিছু অন্য branch এর সংস্করণ

**কি রাখবেন সিদ্ধান্ত নিন** 🤔

উভয় সংস্করণ পড়ুন। সিদ্ধান্ত নিন আপনি আপনার পরিবর্তন, তাদের পরিবর্তন, বা উভয়ের মিশ্রণ রাখতে চান কিনা।
ফাইলটি সম্পাদনা করুন যাতে এটি আপনার চাওয়া মতো দেখায়।
**সমস্ত conflict চিহ্নগুলি সরান** (`<<<<<<<`, `=======`, `>>>>>>>`).

**উদাহরণ:**

আগে:

```
<<<<<<< HEAD
Hello, world!
=======
Hello, Git!
>>>>>>> feature-branch
```

পরে (যদি আপনি উভয়টি চান):

```
Hello, world!
Hello, Git!
```

অথবা (যদি আপনি শুধু একটি চান):

```
Hello, world!
```

**Conflict সমাধান হিসেবে চিহ্নিত করুন** ✅

ফাইলটি সেভ করুন এবং Git কে বলুন আপনি conflict ঠিক করেছেন:

```bash
git add filename.txt
```

**Merge সম্পূর্ণ করুন** 🏁

যদি সমস্ত conflicts সমাধান হয়, merge শেষ করুন:

```bash
git commit
```

Git আপনার জন্য একটি commit message স্বয়ংক্রিয়ভাবে তৈরি করতে পারে।

### নতুনদের জন্য টিপস 💡

⏰ **আপনার সময় নিন:** উভয় সংস্করণ সাবধানে পড়ুন। যদি আপনি নিশ্চিত না হন, একজন সহকর্মীকে জিজ্ঞেস করুন বা প্রজেক্টের ইতিহাস পরীক্ষা করুন  
🖥️ **আপনার code editor ব্যবহার করুন:** বেশিরভাগ editor (যেমন VS Code) conflicts হাইলাইট করে এবং কোন পরিবর্তনগুলি রাখতে হবে তা বেছে নেওয়া সহজ করে তোলে  
🧪 **আপনার কোড পরীক্ষা করুন:** Conflicts সমাধানের পর, আপনার প্রজেক্ট চালান যাতে সবকিছু প্রত্যাশিত ভাবে কাজ করে  
😌 **ঘাবড়াবেন না!** Merge conflicts স্বাভাবিক এবং সবার সাথে ঘটে। যত বেশি অনুশীলন করবেন, তত সহজ হয়ে যাবে

<Callout type="info">
  **মনে রাখবেন:** Merge conflicts দুইজন ব্যক্তির একই পৃষ্ঠায় লেখার মতো। আপনার
  শুধু সিদ্ধান্ত নিতে হবে চূড়ান্ত গল্পটি কি বলবে! 📖
</Callout>

---

## 🔄 Merge, Rebase & Squash সহজভাবে ব্যাখ্যা করা

এই তিনটি ধারণা বোঝা Git দক্ষতার জন্য অত্যন্ত গুরুত্বপূর্ণ। আসুন সহজ উপমা এবং স্পষ্ট উদাহরণ দিয়ে এগুলি ভেঙে ফেলি।

### Merge কি? 🔗

**Merge** দুটি branch একসাথে একত্রিত করে, উভয় branch এর ইতিহাস সংরক্ষণ করে।

#### লাইব্রেরি উপমা 📚

কল্পনা করুন আপনি এবং আপনার বন্ধু উভয়ে একই বইয়ের জন্য অধ্যায় লিখছেন:

- আপনি আপনার নোটবুকে অধ্যায় 1 এবং 2 লিখেন
- আপনার বন্ধু তাদের নোটবুকে অধ্যায় 3 এবং 4 লিখে
- **Merging** হল উভয় নোটবুক একটি চূড়ান্ত বইয়ে একত্রিত করার মতো, কে কি এবং কখন লিখেছে তার ট্র্যাক রাখা

#### ভিজ্যুয়াল উদাহরণ:

**Merge এর আগে:**

```
main:     A---B---C
               \
feature:        D---E
```

**Merge এর পরে:**

```
main:     A---B---C---F (merge commit)
               \     /
feature:        D---E
```

#### কখন Merge ব্যবহার করবেন? 🤔

✅ যখন আপনি আপনার feature branch এর সম্পূর্ণ ইতিহাস সংরক্ষণ করতে চান  
✅ যখন একটি টিমে কাজ করছেন এবং দেখতে চান কে কি অবদান রেখেছে  
✅ যখন আপনি সহজে একটি সম্পূর্ণ feature revert করতে চান

**Command:**

```bash
git switch main
git merge feature/user-login
```

---

### Rebase কি? 🔀

**Rebase** আপনার branch কে অন্য একটি branch এর সর্বশেষ বিন্দু থেকে শুরু করার জন্য সরিয়ে দেয়, আপনার পরিবর্তনগুলি পুনরায় চালনা করে যেন সেগুলি সর্বশেষ পরিবর্তনের পরে ঘটেছে।

#### বাড়ি স্থানান্তরের উপমা 🏠

কল্পনা করুন আপনি একটি বাড়ির সম্প্রসারণ তৈরি করছেন:

- আপনি বর্তমান ভিত্তির উপর নির্মাণ শুরু করেন
- আপনি নির্মাণ করার সময়, কেউ মূল বাড়িটি সংস্কার করে
- **Rebasing** হল আপনার সম্প্রসারণটি নতুন সংস্কারকৃত বাড়ির সাথে সংযুক্ত করার জন্য সরানোর মতো, এটি দেখতে যেন আপনি সংস্কারটি সম্পূর্ণ হওয়ার পরে সম্প্রসারণটি তৈরি করেছেন

#### ভিজ্যুয়াল উদাহরণ:

**Rebase এর আগে:**

```
main:     A---B---C---F
               \
feature:        D---E
```

**Rebase এর পরে:**

```
main:     A---B---C---F
                       \
feature:                D'---E'
```

লক্ষ্য করুন কিভাবে D এবং E হয়ে যায় D' এবং E' - এগুলি একই পরিবর্তন কিন্তু এখন F এর উপরে প্রয়োগ করা।

#### কখন Rebase ব্যবহার করবেন? 🤔

✅ যখন আপনি একটি পরিষ্কার, রৈখিক প্রজেক্ট ইতিহাস চান  
✅ আপনার feature branch merge করার আগে ইতিহাস পরিষ্কার করতে  
✅ যখন আপনি আপনার feature branch কে সর্বশেষ main branch পরিবর্তনের সাথে আপডেট করতে চান

**Commands:**

```bash
git switch feature/user-login
git rebase main
```

<Callout type="warning">
  **গুরুত্বপূর্ণ:** অন্যরা কাজ করছে এমন branch গুলি কখনও rebase করবেন না!
  শুধুমাত্র আপনার নিজস্ব feature branches rebase করুন। Rebasing ইতিহাস পুনর্লিখন
  করে, যা আপনার সহকর্মীদের বিভ্রান্ত করতে পারে। ⚠️
</Callout>

---

### Squash কি? 🤏

**Squash** একাধিক commit কে একটি একক commit এ একত্রিত করে, আপনার commit history পরিষ্কার করে।

#### মোটামুটি খসড়ার উপমা 📝

একটি প্রবন্ধ লেখার কথা কল্পনা করুন:

- খসড়া ১: "ভূমিকা লিখুন"
- খসড়া ২: "ভূমিকায় টাইপো ঠিক করুন"
- খসড়া ৩: "উপসংহার যোগ করুন"
- খসড়া ৪: "উপসংহারে টাইপো ঠিক করুন"
- খসড়া ৫: "চূড়ান্ত পর্যালোচনা"

**Squashing** হলো এই সব খসড়াগুলিকে একটি চূড়ান্ত commit এ একত্রিত করার মতো: "ভূমিকা এবং উপসংহার সহ সম্পূর্ণ প্রবন্ধ লিখুন"

#### চাক্ষুষ উদাহরণ:

**Squash এর আগে:**

```
feature: A---B---C---D---E
         |   |   |   |   |
         |   |   |   |   └── চূড়ান্ত টাইপো ঠিক করুন
         |   |   |   └────── Error handling যোগ করুন
         |   |   └────────── Login এ bug ঠিক করুন
         |   └────────────── Login form যোগ করুন
         └────────────────── Login feature শুরু করুন
```

**Squash এর পরে:**

```
feature: A'
         |
         └── সম্পূর্ণ user login কার্যকারিতা যোগ করুন
```

#### কখন Squash ব্যবহার করবেন? 🤔

✅ "কাজ-চলমান" commit গুলি পরিষ্কার করতে feature branch merge করার আগে  
✅ যখন আপনার অনেক ছোট commit আছে যা প্রকল্পের ইতিহাসে মূল্য যোগ করে না  
✅ যখন আপনি একটি পরিষ্কার, পেশাদার commit history উপস্থাপন করতে চান

#### কিভাবে Squash করবেন (ধাপে ধাপে) 👣

**Interactive Rebase পদ্ধতি:**

1. **সিদ্ধান্ত নিন** কতগুলি commit আপনি squash করতে চান (ধরা যাক শেষ ৩টি):

   ```bash
   git rebase -i HEAD~3
   ```

2. **একটি এডিটর খুলবে** আপনার commit গুলি দেখিয়ে:

   ```
   pick a1b2c3d Start login feature
   pick b2c3d4e Add login form
   pick c3d4e5f Fix bug in login
   ```

3. **পরিবর্তন করুন** `pick` কে `squash` (বা `s`) এ যে commit গুলি আপনি একত্রিত করতে চান:

   ```
   pick a1b2c3d Start login feature
   squash b2c3d4e Add login form
   squash c3d4e5f Fix bug in login
   ```

4. **সেভ করুন এবং বন্ধ করুন** এডিটর। Git আপনাকে একটি নতুন commit message লিখতে বলবে।

5. **স্পষ্ট বার্তা লিখুন** একত্রিত commit এর জন্য:

   ```
   সম্পূর্ণ user login কার্যকারিতা যোগ করুন

   - validation সহ login form তৈরি করুন
   - authentication logic যোগ করুন
   - login error গুলি যথাযথভাবে পরিচালনা করুন
   ```

6. **আবার সেভ করুন এবং বন্ধ করুন**। আপনার commit গুলি এখন squash হয়ে গেছে! 🎉

**`HEAD~3` এর মানে কি?** 🤔

🎯 `HEAD` = আপনার বর্তমান branch এর সর্বশেষ commit  
🔢 `~3` = HEAD থেকে ৩টি commit পিছনে যান  
📋 তাই `HEAD~3` = সম্পাদনার জন্য আমাকে শেষ ৩টি commit দেখান

---

### Merge, Rebase, এবং Squash এর মধ্যে নির্বাচন 🤔

এখানে একটি সহজ সিদ্ধান্ত বৃক্ষ:

#### **Merge** ব্যবহার করুন যখন:

✅ আপনি feature development এর সম্পূর্ণ ইতিহাস সংরক্ষণ করতে চান  
✅ একাধিক লোক feature branch এ কাজ করেছে  
✅ আপনি পরে সহজেই একটি সম্পূর্ণ feature revert করতে চান  
✅ আপনি "Git Flow" workflow অনুসরণ করছেন  
✅ আপনার দল পরিষ্কার ইতিহাসের চেয়ে স্বচ্ছতা পছন্দ করে

**উদাহরণ পরিস্থিতি:** "user authentication" এর মতো একটি বড় feature যা সপ্তাহ নিয়েছে এবং একাধিক developer জড়িত ছিল।

#### **Rebase** ব্যবহার করুন যখন:

✅ আপনি একটি পরিষ্কার, রৈখিক প্রকল্প ইতিহাস চান  
✅ আপনি একা feature branch এ কাজ করছেন  
✅ আপনি latest main changes দিয়ে আপনার feature branch আপডেট করতে চান  
✅ আপনার দল বিস্তারিত ট্র্যাকিংয়ের চেয়ে পরিষ্কার ইতিহাস পছন্দ করে  
✅ আপনি merge করার জন্য প্রস্তুতি নিচ্ছেন এবং merge commit এড়াতে চান

**উদাহরণ পরিস্থিতি:** একটি ছোট bug fix বা feature যা আপনি একা develop করেছেন এবং পরিষ্কারভাবে integrate করতে চান।

#### **Squash** ব্যবহার করুন যখন:

✅ আপনার অনেক "work in progress" commit আছে  
✅ আপনার commit message গুলি অগোছালো বা অর্থহীন  
✅ আপনি প্রতি feature এর জন্য একটি পরিষ্কার commit চান  
✅ আপনি একটি pull request তৈরি করতে যাচ্ছেন  
✅ আপনার commit গুলিতে "fix typo", "oops forgot semicolon" এর মতো জিনিস রয়েছে

**উদাহরণ পরিস্থিতি:** আপনি একটি login form develop করার সময় ১৫টি commit করেছেন, যার মধ্যে fix, typo, এবং experiment রয়েছে।

---

### Workflow সমন্বয় 🔄

**সাধারণ পেশাদার Workflow:**

1. **Feature branch তৈরি করুন** main থেকে
2. **কাজ করুন এবং commit করুন** ঘন ঘন (অগোছালো commit ঠিক আছে)
3. **Rebase করুন** আপনার branch কে latest main এ আপডেট রাখতে
4. **Squash করুন** সম্পর্কিত commit গুলি ইতিহাস পরিষ্কার করতে
5. **Merge করুন** (বা squash merge) main এ pull request এর মাধ্যমে

```bash
# ধাপ ১: Feature তৈরি করুন এবং কাজ করুন
git switch -c feature/user-profile
# ... commit করুন ...

# ধাপ ২: Latest main দিয়ে আপডেট করুন
git switch main
git pull
git switch feature/user-profile
git rebase main

# ধাপ ৩: অগোছালো commit গুলি squash করুন
git rebase -i HEAD~5  # শেষ ৫টি commit squash করুন

# ধাপ ৪: Push করুন এবং PR তৈরি করুন
git push -u origin feature/user-profile
```

---

## 🏢 দলগত Workflow এবং সর্বোত্তম অনুশীলন

### Git Flow বনাম GitHub Flow 🌊

#### Git Flow (ঐতিহ্যবাহী)

```
main ────────────────────────────────
     \                    /
develop ──────────────────
        \        /
feature/  ──────
```

**কখন ব্যবহার করবেন:** বড় দল, নির্ধারিত রিলিজ, জটিল প্রকল্প

#### GitHub Flow (আধুনিক)

```
main ──────────────────────────────
     \              /
feature ──────────
```

**কখন ব্যবহার করবেন:** ক্রমাগত deployment, ছোট দল, web অ্যাপ্লিকেশন

### দলগত নিয়মাবলী 📋

#### Commit Message স্ট্যান্ডার্ড

```
type(scope): description

feat(auth): OAuth login কার্যকারিতা যোগ করুন
fix(navbar): mobile responsive সমস্যা সমাধান করুন
docs(readme): ইনস্টলেশন নির্দেশাবলী আপডেট করুন
style(css): main.css এ indentation ঠিক করুন
refactor(api): user data fetching সরল করুন
test(login): authentication এর জন্য unit test যোগ করুন
```

#### Branch নামকরণ নিয়মাবলী

| ধরন        | ফরম্যাট                  | উদাহরণ                   |
| ---------- | ------------------------ | ------------------------ |
| Feature    | `feature/description`    | `feature/user-dashboard` |
| Bug Fix    | `fix/description`        | `fix/login-validation`   |
| Hotfix     | `hotfix/description`     | `hotfix/security-patch`  |
| Release    | `release/version`        | `release/v2.1.0`         |
| Experiment | `experiment/description` | `experiment/new-ui`      |

---

## 🔧 উন্নত Git কৌশল

### Cherry Pick (নির্দিষ্ট Commit কপি করুন) 🍒

**এটি কি করে:** একটি নির্দিষ্ট commit কে একটি branch থেকে অন্য branch এ কপি করে।

```bash
# abc123 commit কে বর্তমান branch এ কপি করুন
git cherry-pick abc123

# একাধিক commit কপি করুন
git cherry-pick abc123 def456 ghi789

# একটি রেঞ্জের commit কপি করুন
git cherry-pick abc123..def456
```

**কখন ব্যবহার করবেন:**
✅ আপনার অন্য branch থেকে একটি bug fix প্রয়োজন  
✅ আপনি সবকিছু merge না করে একটি নির্দিষ্ট feature প্রয়োগ করতে চান  
✅ আপনি ভুল branch এ commit করেছেন

### Interactive Rebase (উন্নত ইতিহাস সম্পাদনা) ✏️

```bash
git rebase -i HEAD~5  # শেষ ৫টি commit সম্পাদনা করুন
```

**Interactive rebase এর অপশন:**

- `pick` - Commit টি যেমন আছে তেমনই রাখুন
- `reword` - Commit message পরিবর্তন করুন
- `edit` - থামুন এবং commit পরিবর্তন করুন
- `squash` - পূর্ববর্তী commit এর সাথে একত্রিত করুন
- `drop` - Commit টি সম্পূর্ণভাবে সরিয়ে দিন

**উদাহরণ workflow:**

```
pick a1b2c3d Add user registration
reword b2c3d4e Fix typo in email validation
squash c3d4e5f Update email validation tests
drop d4e5f6g Debug logging (remove this)
edit e5f6g7h Add password strength checker
```

### Bisect (Bug-পরিচয়কারী Commit খুঁজুন) 🔍

**এটি কি করে:** একটি bug কখন চালু হয়েছিল তা খুঁজে বের করতে commit গুলিতে binary search করে।

```bash
git bisect start
git bisect bad                    # বর্তমান commit এ bug আছে
git bisect good v1.0             # এই version কাজ করছিল
# Git আপনার জন্য commit গুলি check out করবে test করার জন্য
git bisect good                  # যদি বর্তমান commit কাজ করে
git bisect bad                   # যদি বর্তমান commit এ bug আছে
# Git সমস্যাযুক্ত commit খুঁজে না পাওয়া পর্যন্ত পুনরাবৃত্তি করুন
git bisect reset                 # মূল অবস্থায় ফিরে যান
```

### Worktrees (একাধিক কার্যকারী ডিরেক্টরি) 🌳

**এটি কি করে:** বিভিন্ন folder এ একসাথে একাধিক branch এ কাজ করুন।

```bash
git worktree add ../feature-branch feature/new-ui
git worktree add ../hotfix-branch hotfix/critical-bug

# সব worktree তালিকা করুন
git worktree list

# Worktree সরান
git worktree remove ../feature-branch
```

**কখন ব্যবহার করবেন:**
✅ আপনাকে ঘন ঘন branch গুলির মধ্যে switch করতে হয়  
✅ আপনি একটি branch এ কাজ করার সময় অন্য branch এ test চালাতে চান  
✅ আপনি branch গুলিতে implementation তুলনা করছেন

---

## 🐛 সাধারণ ভুল এবং সেগুলি কিভাবে ঠিক করবেন

### "আমি ভুল branch এ commit করেছি!" 😱

**সমাধান ১: শেষ commit কে অন্য branch এ সরান**

```bash
# বর্তমান অবস্থান থেকে নতুন branch তৈরি করুন
git branch feature/correct-branch

# বর্তমান branch কে পূর্ববর্তী commit এ reset করুন
git reset --hard HEAD~1

# নতুন branch এ switch করুন (আপনার commit সেখানে নিরাপদ)
git switch feature/correct-branch
```

**সমাধান ২: সঠিক branch এ cherry-pick করুন**

```bash
# Commit hash টি note করুন
git log --oneline -1  # abc123 Your commit message

# সঠিক branch এ switch করুন
git switch correct-branch

# Commit টি কপি করুন
git cherry-pick abc123

# ফিরে যান এবং ভুল branch থেকে সরান
git switch wrong-branch
git reset --hard HEAD~1
```

### "আমি আমার শেষ commit undo করতে চাই কিন্তু changes গুলি রাখতে চাই" 🔄

```bash
git reset --soft HEAD~1
```

আপনার file গুলি modified থাকবে, কিন্তু commit undo হয়ে যাবে।

### "আমি ভুলে একটি file delete করেছি" 📂

**যদি আপনি এখনো commit না করে থাকেন:**

```bash
git checkout HEAD -- filename.txt
```

**যদি আপনি deletion commit করেছেন এবং এটি ফিরে চান:**

```bash
git show HEAD~1:filename.txt > filename.txt
```

### "আমার শেষ commit message এ টাইপো আছে" ✏️

```bash
git commit --amend -m "সঠিক commit message"
```

### "আমি শেষ commit সম্পূর্ণভাবে undo করতে চাই" ⚠️

```bash
git reset --hard HEAD~1
```

**সতর্কতা:** এটি স্থায়ীভাবে commit এবং সব changes মুছে দেয়!

### "আমি এমন কিছু push করেছি যা করা উচিত ছিল না" 🚨

**যদি অন্য কেউ এখনো pull না করে থাকে:**

```bash
git push --force-with-lease origin branch-name
```

**যদি অন্যরা pull করে থাকতে পারে:**

```bash
git revert abc123  # abc123 কে undo করে একটি নতুন commit তৈরি করে
git push origin branch-name
```

---

## 🔐 Git নিরাপত্তা এবং সর্বোত্তম অনুশীলন

### সংবেদনশীল তথ্য সুরক্ষা 🛡️

#### কখনো এগুলি commit করবেন না:

❌ পাসওয়ার্ড, API key, token  
❌ ডাটাবেস credentials  
❌ Private key (SSH, SSL certificates)  
❌ ব্যক্তিগত তথ্য  
❌ বড় binary file

#### নিরাপত্তার জন্য .gitignore

```gitignore
# Environment variables
.env
.env.local
.env.production
.env.staging

# গোপন তথ্য সহ configuration file
config/secrets.yml
config/database.yml

# Log file যাতে সংবেদনশীল তথ্য থাকতে পারে
*.log
logs/

# IDE file যাতে প্রকল্পের তথ্য থাকতে পারে
.vscode/settings.json
.idea/
```

#### যদি আপনি ভুলে গোপন তথ্য commit করেন 🚨

**ধাপ ১: ভবিষ্যতের commit থেকে সরান**

```bash
echo "config/secrets.yml" >> .gitignore
git rm --cached config/secrets.yml
git commit -m "ট্র্যাকিং থেকে secrets file সরান"
```

**ধাপ ২: ইতিহাস পরিষ্কার করুন (প্রয়োজনে)**

```bash
# সাম্প্রতিক commit এর জন্য
git filter-branch --force --index-filter \
  'git rm --cached --ignore-unmatch config/secrets.yml' \
  --prune-empty --tag-name-filter cat -- --all
```

**ধাপ ৩: আপসকৃত গোপন তথ্য অবিলম্বে পরিবর্তন করুন**

### Commit স্বাক্ষর করা 🔏

```bash
# GPG signing সেট আপ করুন
git config --global user.signingkey YOUR_KEY_ID
git config --global commit.gpgsign true

# পৃথক commit স্বাক্ষর করুন
git commit -S -m "স্বাক্ষরিত commit message"
```

---

## 🚀 Git পারফরম্যান্স এবং বড় প্রজেক্ট

### বড় ফাইল পরিচালনা 📦

#### Git LFS (Large File Storage)

```bash
# Git LFS ইনস্টল করুন
git lfs install

# বড় ফাইল ট্র্যাক করুন
git lfs track "*.psd"
git lfs track "*.zip"
git lfs track "videos/*.mp4"

# আপনার রেপোতে .gitattributes যোগ করুন
git add .gitattributes
git commit -m "Add Git LFS tracking"
```

#### শ্যালো ক্লোন (দ্রুত ডাউনলোড)

```bash
# শুধুমাত্র সাম্প্রতিক ইতিহাস ক্লোন করুন
git clone --depth 1 https://github.com/user/repo.git

# শুধুমাত্র নির্দিষ্ট ব্রাঞ্চ ক্লোন করুন
git clone --single-branch --branch main https://github.com/user/repo.git
```

### Git পারফরম্যান্স অপ্টিমাইজ করা ⚡

```bash
# অপ্রয়োজনীয় ফাইল পরিষ্কার করুন
git gc --aggressive --prune=now

# আনট্র্যাকড ফাইল মুছে ফেলুন
git clean -fd

# রিপোজিটরি কম্প্রেস করুন
git repack -Ad

# রিপোজিটরির আকার চেক করুন
git count-objects -vH
```

---

## 🎯 Git হুক এবং অটোমেশন

### ক্লায়েন্ট-সাইড হুক 🎣

#### প্রি-কমিট হুক (কমিটের আগে টেস্ট চালান)

`.git/hooks/pre-commit` তৈরি করুন:

```bash
#!/bin/sh
# কমিট করার আগে টেস্ট চালান
npm test
if [ $? -ne 0 ]; then
  echo "Tests failed! কমিট বাতিল হয়েছে।"
  exit 1
fi
```

#### কমিট মেসেজ হুক (স্ট্যান্ডার্ড প্রয়োগ করুন)

`.git/hooks/commit-msg` তৈরি করুন:

```bash
#!/bin/sh
# কমিট মেসেজ ফরম্যাট চেক করুন
if ! grep -qE "^(feat|fix|docs|style|refactor|test|chore)(\(.+\))?: .{1,50}" "$1"; then
  echo "অবৈধ কমিট মেসেজ ফরম্যাট!"
  echo "ব্যবহার করুন: type(scope): description"
  echo "উদাহরণ: feat(auth): add login functionality"
  exit 1
fi
```

### সার্ভার-সাইড হুক 🖧

#### প্রি-রিসিভ হুক (মেইনে সরাসরি পুশ প্রত্যাখ্যান)

```bash
#!/bin/sh
while read oldrev newrev refname; do
  if [ "$refname" = "refs/heads/main" ]; then
    echo "মেইনে সরাসরি পুশের অনুমতি নেই!"
    echo "দয়া করে পুল রিকোয়েস্ট ব্যবহার করুন।"
    exit 1
  fi
done
```

---

## 🔧 Git কনফিগারেশন এবং অ্যালিয়াসেস

### উপযোগী Git অ্যালিয়াসেস ⚡

```bash
# ~/.gitconfig-এ যোগ করুন বা এই কমান্ডগুলি চালান:

# সংক্ষিপ্ত স্ট্যাটাস
git config --global alias.s "status --short"

# সুন্দর লগ
git config --global alias.lg "log --oneline --decorate --graph --all"

# দ্রুত কমিট
git config --global alias.c "commit -m"

# দ্রুত অ্যাড এবং কমিট
git config --global alias.ac "!git add -A && git commit -m"

# শেষ কমিট আন্ডু করুন (পরিবর্তন রাখুন)
git config --global alias.undo "reset --soft HEAD~1"

# শেষ কমিট সহ ব্রাঞ্চ দেখান
git config --global alias.br "branch -v"

# দ্রুত পুশ
git config --global alias.p "push"

# দ্রুত পুল
git config --global alias.pl "pull"

# স্টেজড পরিবর্তনের diff দেখান
git config --global alias.ds "diff --staged"
```

### উন্নত কনফিগারেশন 🎛️

```bash
# ডিফল্ট ব্রাঞ্চের নাম সেট করুন
git config --global init.defaultBranch main

# অটো-সেটআপ রিমোট ট্র্যাকিং
git config --global push.autoSetupRemote true

# রেকর্ডকৃত কনফ্লিক্ট রেজোলিউশন পুনরায় ব্যবহার করুন
git config --global rerere.enabled true

# আরো রঙ ব্যবহার করুন
git config --global color.ui auto

# উন্নত diff অ্যালগরিদম
git config --global diff.algorithm patience

# টাইপো অটো-কারেক্ট
git config --global help.autocorrect 1
```

---

## একটি কম্পিউটারে একাধিক GitHub অ্যাকাউন্ট পরিচালনা 🔄

> **সাধারণ পরিস্থিতি:** আপনার একটি ব্যক্তিগত GitHub অ্যাকাউন্ট এবং একটি কাজের GitHub অ্যাকাউন্ট রয়েছে, এবং আপনাকে একই কম্পিউটারে উভটি দ্বন্দ্ব ছাড়াই ব্যবহার করতে হবে! 💼🏠

---

### 🤔 কেন আপনার একাধিক GitHub অ্যাকাউন্ট প্রয়োজন

**সাধারণ পরিস্থিতি:**

- 👔 **কাজের অ্যাকাউন্ট** কোম্পানির প্রজেক্টের জন্য
- 🏠 **ব্যক্তিগত অ্যাকাউন্ট** সাইড প্রজেক্ট এবং ওপেন সোর্সের জন্য
- 🎓 **স্কুলের অ্যাকাউন্ট** একাডেমিক কাজের জন্য
- 💼 **ক্লায়েন্ট অ্যাকাউন্ট** ফ্রিল্যান্স কাজের জন্য

**সমস্যা:** Git ডিফল্টভাবে একবারে শুধুমাত্র একটি GitHub অ্যাকাউন্ট ব্যবহার করতে পারে, যার ফলে:

- ভুল নামে কমিট দেখানো
- রিপোজিটরিতে পুশ করার সময় অনুমতির ত্রুটি
- আপনি কোন অ্যাকাউন্ট ব্যবহার করছেন তা নিয়ে বিভ্রান্তি

---

### 🛠️ সমাধানের রূপরেখা

আমরা **SSH কী** (সুপারিশকৃত পদ্ধতি) ব্যবহার করে একাধিক অ্যাকাউন্ট সেট আপ করব। আমরা যা করব:

1. 🗝️ প্রতিটি অ্যাকাউন্টের জন্য আলাদা SSH কী তৈরি করব
2. ⚙️ প্রতিটি অ্যাকাউন্টের জন্য সঠিক কী ব্যবহার করতে SSH কনফিগার করব
3. 📁 বিভিন্ন প্রজেক্টের জন্য বিভিন্ন Git কনফিগারেশন সেট আপ করব
4. 🚀 অ্যাকাউন্টের মধ্যে পরিবর্তনের জন্য সহজ কমান্ড ব্যবহার করব

---

### 📋 ধাপে ধাপে সেটআপ

### ধাপ ১: প্রতিটি অ্যাকাউন্টের জন্য SSH কী তৈরি করুন 🔑

**আপনার ব্যক্তিগত অ্যাকাউন্টের জন্য:**

```bash
ssh-keygen -t ed25519 -C "your-personal-email@gmail.com" -f ~/.ssh/id_ed25519_personal
```

**আপনার কাজের অ্যাকাউন্টের জন্য:**

```bash
ssh-keygen -t ed25519 -C "your-work-email@company.com" -f ~/.ssh/id_ed25519_work
```

**কী হচ্ছে:**

- `-t ed25519` - আধুনিক, নিরাপদ Ed25519 অ্যালগরিদম ব্যবহার করে
- `-C "email"` - কী শনাক্ত করতে একটি মন্তব্য যোগ করে
- `-f ~/.ssh/filename` - নির্দিষ্ট নামে কী সংরক্ষণ করে

**এখন আপনার এই ফাইলগুলি আছে:**

```
~/.ssh/
├── id_ed25519_personal      (প্রাইভেট কী)
├── id_ed25519_personal.pub  (পাবলিক কী)
├── id_ed25519_work          (প্রাইভেট কী)
└── id_ed25519_work.pub      (পাবলিক কী)
```

💡 **নিরাপত্তা টিপ:** যখন পাসফ্রেজের জন্য প্রম্পট আসে, একটি ব্যবহার করুন! এটি অতিরিক্ত নিরাপত্তার স্তর যোগ করে।

---

### ধাপ ২: GitHub অ্যাকাউন্টে SSH কী যোগ করুন 🔗

**প্রতিটি অ্যাকাউন্টের জন্য, আপনাকে পাবলিক কী (.pub ফাইল) যোগ করতে হবে:**

#### ব্যক্তিগত অ্যাকাউন্ট:

1. আপনার ব্যক্তিগত পাবলিক কী কপি করুন:
   ```bash
   cat ~/.ssh/id_ed25519_personal.pub
   ```
2. GitHub.com → Settings → SSH and GPG keys-এ যান
3. "New SSH key" ক্লিক করুন
4. টাইটেল: "Personal Computer"
5. কী পেস্ট করুন এবং সেভ করুন

#### কাজের অ্যাকাউন্ট:

1. আপনার কাজের পাবলিক কী কপি করুন:
   ```bash
   cat ~/.ssh/id_ed25519_work.pub
   ```
2. GitHub.com (কাজের অ্যাকাউন্টে পরিবর্তন করুন) → Settings → SSH and GPG keys-এ যান
3. "New SSH key" ক্লিক করুন
4. টাইটেল: "Work Computer"
5. কী পেস্ট করুন এবং সেভ করুন

---

### ধাপ ৩: বিভিন্ন কী ব্যবহার করতে SSH কনফিগার করুন 🎛️

SSH কনফিগ ফাইল তৈরি বা সম্পাদনা করুন:

```bash
nano ~/.ssh/config
```

এই কনফিগারেশন যোগ করুন:

```
# ব্যক্তিগত GitHub অ্যাকাউন্ট
Host github.com-personal
    HostName github.com
    User git
    IdentityFile ~/.ssh/id_ed25519_personal
    IdentitiesOnly yes

# কাজের GitHub অ্যাকাউন্ট
Host github.com-work
    HostName github.com
    User git
    IdentityFile ~/.ssh/id_ed25519_work
    IdentitiesOnly yes
```

**এটি কী করে:**

- অ্যালিয়াস তৈরি করে (`github.com-personal`, `github.com-work`)
- প্রতিটি অ্যালিয়াসকে একটি নির্দিষ্ট SSH কী ব্যবহার করতে ম্যাপ করে
- `IdentitiesOnly yes` SSH কে অন্যান্য কী চেষ্টা করা থেকে বিরত রাখে

💡 **গুরুত্বপূর্ণ:** `Host` নামগুলি আপনি যা চান তাই হতে পারে! আপনি `personal-github` বা `work-gh` এর পরিবর্তে ব্যবহার করতে পারেন।

---

### ধাপ ৪: আপনার SSH সংযোগ পরীক্ষা করুন ✅

**ব্যক্তিগত অ্যাকাউন্ট পরীক্ষা করুন:**

```bash
ssh -T git@github.com-personal
```

**কাজের অ্যাকাউন্ট পরীক্ষা করুন:**

```bash
ssh -T git@github.com-work
```

**প্রত্যাশিত প্রতিক্রিয়া:**

```
Hi YourPersonalUsername! আপনি সফলভাবে প্রমাণীকৃত হয়েছেন...
Hi YourWorkUsername! আপনি সফলভাবে প্রমাণীকৃত হয়েছেন...
```

যদি আপনি ত্রুটি পান, আপনার SSH কী সেটআপ এবং কনফিগ ফাইল পুনরায় চেক করুন।

---

## 🚀 একাধিক অ্যাকাউন্ট ব্যবহার করা

### পদ্ধতি ১: রিপোজিটরি-নির্দিষ্ট কনফিগারেশন (সুপারিশকৃত) 📁

**প্রতিটি রিপোজিটরির জন্য, নির্দিষ্ট ব্যবহারকারী সেট করুন:**

```bash
# আপনার ব্যক্তিগত প্রজেক্ট ফোল্ডারে
git config user.name "Your Personal Name"
git config user.email "personal@gmail.com"

# আপনার কাজের প্রজেক্ট ফোল্ডারে
git config user.name "Your Work Name"
git config user.email "work@company.com"
```

### পদ্ধতি ২: ডিরেক্টরি-ভিত্তিক কনফিগারেশন (উন্নত) 📂

**ফোল্ডারের অবস্থানের ভিত্তিতে স্বয়ংক্রিয় পরিবর্তন সেট আপ করুন:**

আপনার গ্লোবাল Git কনফিগ সম্পাদনা করুন:

```bash
git config --global --edit
```

শর্তাধীন অন্তর্ভুক্তি যোগ করুন:

```ini
[user]
    name = Your Personal Name
    email = personal@gmail.com

[includeIf "gitdir:~/work/"]
    path = ~/.gitconfig-work

[includeIf "gitdir:~/company-projects/"]
    path = ~/.gitconfig-work
```

`~/.gitconfig-work` তৈরি করুন:

```ini
[user]
    name = Your Work Name
    email = work@company.com
```

**এটি কী করে:**

- ব্যক্তিগত কনফিগ ডিফল্ট
- কাজের কনফিগ স্বয়ংক্রিয়ভাবে `~/work/` এবং `~/company-projects/` ফোল্ডারে প্রয়োগ হয়

---

## 🔄 একাধিক অ্যাকাউন্ট দিয়ে ক্লোনিং এবং পুশিং

### রিপোজিটরি ক্লোনিং 📥

**ব্যক্তিগত রিপোজিটরি:**

```bash
git clone git@github.com-personal:YourPersonalUsername/repo-name.git
```

**কাজের রিপোজিটরি:**

```bash
git clone git@github.com-work:YourWorkUsername/repo-name.git
```

**পার্থক্য লক্ষ্য করুন:** `git@github.com` এর পরিবর্তে, আমরা আমাদের SSH অ্যালিয়াস ব্যবহার করি!

### বিদ্যমান রিপোজিটরিতে রিমোট যোগ করা 🔗

**বিদ্যমান রিপোজিটরি পরিবর্তন করে ব্যক্তিগত অ্যাকাউন্ট ব্যবহার করুন:**

```bash
git remote set-url origin git@github.com-personal:YourPersonalUsername/repo-name.git
```

**বিদ্যমান রিপোজিটরি পরিবর্তন করে কাজের অ্যাকাউন্ট ব্যবহার করুন:**

```bash
git remote set-url origin git@github.com-work:YourWorkUsername/repo-name.git
```

### পরিবর্তন পুশ করা 📤

রিমোট সঠিকভাবে সেট আপ করার পরে, পুশ করা স্বাভাবিকভাবে কাজ করে:

```bash
git push origin main
```

Git স্বয়ংক্রিয়ভাবে রিমোট URL এর ভিত্তিতে সঠিক SSH কী ব্যবহার করবে!

---

## 🎯 দ্রুত সেটআপ স্ক্রিপ্ট

### স্ক্রিপ্ট ১: অ্যাকাউন্ট চেকার 🔍

`check-git-account.sh` তৈরি করুন:

```bash
#!/bin/bash
echo "=== বর্তমান Git কনফিগারেশন ==="
echo "নাম: $(git config user.name)"
echo "ইমেইল: $(git config user.email)"
echo "রিমোট URL: $(git remote get-url origin 2>/dev/null || echo 'কোনো রিমোট সেট নেই')"
echo "================================="
```

ব্যবহার:

```bash
chmod +x check-git-account.sh
./check-git-account.sh
```

### স্ক্রিপ্ট ২: অ্যাকাউন্ট সুইচার 🔄

`switch-git-account.sh` তৈরি করুন:

```bash
#!/bin/bash

if [ "$1" = "personal" ]; then
    git config user.name "Your Personal Name"
    git config user.email "personal@gmail.com"
    echo "✅ ব্যক্তিগত অ্যাকাউন্টে পরিবর্তিত হয়েছে"
elif [ "$1" = "work" ]; then
    git config user.name "Your Work Name"
    git config user.email "work@company.com"
    echo "✅ কাজের অ্যাকাউন্টে পরিবর্তিত হয়েছে"
else
    echo "ব্যবহার: ./switch-git-account.sh [personal|work]"
fi
```

ব্যবহার:

```bash
chmod +x switch-git-account.sh
./switch-git-account.sh personal
./switch-git-account.sh work
```

---

## 🚨 সাধারণ সমস্যা এবং সমাধান

### সমস্যা ১: "Permission denied (publickey)" ❌

**সম্ভাব্য কারণ:**

- SSH কী GitHub অ্যাকাউন্টে যোগ করা হয়নি
- ভুল SSH কী ব্যবহার হচ্ছে
- SSH এজেন্ট চলছে না

**সমাধান:**

```bash
# SSH এজেন্ট চলছে কিনা চেক করুন
ssh-add -l

# চলছে না হলে, শুরু করুন
eval "$(ssh-agent -s)"

# আপনার কী যোগ করুন
ssh-add ~/.ssh/id_ed25519_personal
ssh-add ~/.ssh/id_ed25519_work

# আবার সংযোগ পরীক্ষা করুন
ssh -T git@github.com-personal
ssh -T git@github.com-work
```

### সমস্যা ২: কমিট ভুল লেখক দেখাচ্ছে 👤

**সমস্যা:** আপনার কমিট ভুল GitHub অ্যাকাউন্টের অধীনে প্রদর্শিত হচ্ছে।

**সমাধান:** রিপোজিটরির জন্য সঠিক ব্যবহারকারী সেট করুন:

```bash
# বর্তমান কনফিগ চেক করুন
git config user.name
git config user.email

# সঠিক কনফিগ সেট করুন
git config user.name "Correct Name"
git config user.email "correct@email.com"

# ইতিমধ্যে কমিট করা পরিবর্তনের জন্য
git rebase -i HEAD~3  # শেষ ৩টি কমিট পরিবর্তন করুন
# 'edit' অপশন ব্যবহার করুন এবং git commit --amend --author="Name <email>" চালান
```

### সমস্যা ৩: SSH কনফিগ কাজ করছে না 🔧

**আপনার SSH কনফিগ সিনট্যাক্স চেক করুন:**

```bash
ssh -F ~/.ssh/config -T git@github.com-personal
```

**সাধারণ ভুল:**

- ইন্ডেন্টেশন (ট্যাবের পরিবর্তে স্পেস ব্যবহার করুন)
- হোস্টনামে টাইপো
- অনুপস্থিত `IdentitiesOnly yes`

### সমস্যা ৪: একাধিক SSH কী দ্বন্দ্ব করছে 🔀

**সমস্যা:** SSH সব কী চেষ্টা করে এবং রেট লিমিট হয়ে যায়।

**সমাধান:** SSH কনফিগে `IdentitiesOnly yes` ব্যবহার করুন এবং সঠিক কী নির্দিষ্ট করুন:

```
Host github.com-personal
    HostName github.com
    User git
    IdentityFile ~/.ssh/id_ed25519_personal
    IdentitiesOnly yes
    PreferredAuthentications publickey
```

---

## 🔧 উন্নত কনফিগারেশন

### বিভিন্ন Git অ্যালিয়াস ব্যবহার করা 📝

**আপনার শেল প্রোফাইলে যোগ করুন (.bashrc, .zshrc):**

```bash
# বিভিন্ন অ্যাকাউন্টের জন্য Git অ্যালিয়াস
alias gp-personal='git config user.name "Personal Name" && git config user.email "personal@gmail.com"'
alias gp-work='git config user.name "Work Name" && git config user.email "work@company.com"'

# ক্লোন অ্যালিয়াস
alias clone-personal='git clone git@github.com-personal:'
alias clone-work='git clone git@github.com-work:'
```

**ব্যবহার:**

```bash
# বর্তমান রেপোর জন্য অ্যাকাউন্ট সেট করুন
gp-personal
gp-work

# রিপোজিটরি ক্লোন করুন
clone-personal YourUsername/repo-name
clone-work CompanyUsername/repo-name
```

### গ্লোবাল বনাম লোকাল Git কনফিগারেশন 🌐

**শ্রেণীবিন্যাস বোঝা:**

1. **সিস্টেম কনফিগ** (`/etc/gitconfig`) - সব ব্যবহারকারীকে প্রভাবিত করে
2. **গ্লোবাল কনফিগ** (`~/.gitconfig`) - আপনার সব রেপোকে প্রভাবিত করে
3. **লোকাল কনফিগ** (`.git/config`) - শুধুমাত্র বর্তমান রেপোকে প্রভাবিত করে

**একাধিক অ্যাকাউন্টের জন্য সেরা অনুশীলন:**

- **ব্যক্তিগত** গ্লোবাল ডিফল্ট হিসেবে সেট করুন
- রিপোজিটরি বা ডিরেক্টরি অনুযায়ী **কাজের** কনফিগ ওভাররাইড করুন

---

## 📋 দ্রুত রেফারেন্স চিট শিট

### SSH কী জেনারেশন 🗝️

```bash
# ব্যক্তিগত
ssh-keygen -t ed25519 -C "personal@email.com" -f ~/.ssh/id_ed25519_personal

# কাজের
ssh-keygen -t ed25519 -C "work@email.com" -f ~/.ssh/id_ed25519_work
```

### SSH কনফিগ টেমপ্লেট 📄

```
Host github.com-personal
    HostName github.com
    User git
    IdentityFile ~/.ssh/id_ed25519_personal
    IdentitiesOnly yes

Host github.com-work
    HostName github.com
    User git
    IdentityFile ~/.ssh/id_ed25519_work
    IdentitiesOnly yes
```

### Git Commands 💻

```bash
# Set account for current repo
git config user.name "Name"
git config user.email "email@domain.com"

# Clone with specific account
git clone git@github.com-personal:username/repo.git
git clone git@github.com-work:username/repo.git

# Change remote URL
git remote set-url origin git@github.com-personal:username/repo.git
```

### টেস্টিং 🧪

```bash
# Test SSH connections
ssh -T git@github.com-personal
ssh -T git@github.com-work

# Check current Git config
git config user.name
git config user.email
git remote get-url origin
```

---

## 🎉 সফল! আপনি সব সেটআপ সম্পন্ন করেছেন!

এখন আপনার কাছে রয়েছে:
✅ বিভিন্ন অ্যাকাউন্টের জন্য একাধিক SSH কী  
✅ সঠিক SSH কনফিগারেশন  
✅ অ্যাকাউন্টের মধ্যে পরিবর্তনের পদ্ধতি  
✅ আপনার জীবনকে সহজ করার স্ক্রিপ্ট  
✅ সাধারণ সমস্যা সমাধানের জ্ঞান

### 💡 দৈনন্দিন ব্যবহারের জন্য প্রো টিপস:

1. **সর্বদা যাচাই করুন** কমিট করার আগে আপনার Git কনফিগ:

   ```bash
   git config user.email
   ```

2. **বর্ণনামূলক ফোল্ডার নাম ব্যবহার করুন** বিভ্রান্তি এড়াতে:

   ```
   ~/projects/
   ├── personal/
   │   ├── my-blog/
   │   └── side-project/
   └── work/
       ├── company-website/
       └── internal-tool/
   ```

3. **আপনার IDE সেটআপ করুন** স্ট্যাটাস বারে বর্তমান Git ব্যবহারকারী দেখানোর জন্য

4. **টেমপ্লেট তৈরি করুন** সঠিক রিমোট URL সহ নতুন রিপোজিটরির জন্য

5. **কমিট সাইনিং ব্যবহার করুন** অতিরিক্ত নিরাপত্তার জন্য যদি আপনার কোম্পানি প্রয়োজন হয়

**একাধিক অ্যাকাউন্ট নিয়ে খুশি কোডিং! 🚀**

## 🏆 Git সেরা অনুশীলনের সারসংক্ষেপ

### দৈনিক কর্মপ্রবাহ ☀️

1. **প্রতিদিন শুরু:**

   ```bash
   git switch main
   git pull
   ```

2. **ফিচার ব্রাঞ্চ তৈরি:**

   ```bash
   git switch -c feature/task-name
   ```

3. **কাজ করুন এবং ঘন ঘন কমিট করুন:**

   ```bash
   git add .
   git commit -m "WIP: working on task"
   ```

4. **পুশ করার আগে:**

   ```bash
   git rebase -i HEAD~3  # Clean up commits
   git push -u origin feature/task-name
   ```

5. **মার্জের পর, পরিষ্কার করুন:**
   ```bash
   git switch main
   git pull
   git branch -d feature/task-name
   ```

### কোড রিভিউ সেরা অনুশীলন 👥

#### PR তৈরির আগে:

✅ আপনার কোড ভালভাবে পরীক্ষা করুন  
✅ স্পষ্ট কমিট বার্তা লিখুন  
✅ প্রয়োজনে ডকুমেন্টেশন আপডেট করুন  
✅ কমিট হিস্ট্রি স্কোয়াশ বা পরিষ্কার করুন  
✅ নিশ্চিত করুন CI পাস হয়েছে

#### PR বর্ণনা লেখা:

```markdown
## What does this PR do?

Brief description of the changes

## How to test

Step-by-step testing instructions

## Screenshots (if applicable)

Before/after images

## Checklist

- [ ] Tests added
- [ ] Documentation updated
- [ ] No console errors
```

### দলগত সহযোগিতা 🤝

#### যোগাযোগ:

✅ বর্ণনামূলক ব্রাঞ্চ নাম ব্যবহার করুন  
✅ স্পষ্ট কমিট বার্তা লিখুন  
✅ জটিল কোডে মন্তব্য করুন  
✅ ব্রেকিং চেঞ্জ সম্পর্কে দলকে জানান  
✅ আপনার সিদ্ধান্ত ডকুমেন্ট করুন

#### রিপোজিটরি ব্যবস্থাপনা:

✅ প্রধান ব্রাঞ্চ স্থিতিশীল রাখুন  
✅ সব পরিবর্তনের জন্য পুল রিকোয়েস্ট ব্যবহার করুন  
✅ মার্জ করা ব্রাঞ্চ মুছে ফেলুন  
✅ নিয়মিত রিপোজিটরি পরিষ্কার করুন  
✅ সামঞ্জস্যপূর্ণ কোডিং মান

---

## 🆘 জরুরি Git কমান্ড

### "সাহায্য! আমি সব ভেঙে ফেলেছি!" 🚨

```bash
# See what you did recently
git reflog

# Go back to where things worked
git reset --hard HEAD@{5}  # or whatever number from reflog

# If you pushed and broke remote
git push --force-with-lease origin branch-name
```

### "আমি মনে করতে পারছি না কি করছিলাম" 🤔

```bash
# See your recent activity
git reflog --all

# See what branches you have
git branch -a

# See what you were working on
git status
git stash list
```

### "অন্য কেউ রিপোজিটরি ভেঙে ফেলেছে" 👥

```bash
# Get a fresh copy
git fetch --all
git reset --hard origin/main

# Or start completely fresh
cd ..
rm -rf project-folder
git clone https://github.com/user/repo.git
```

---

## 🎓 শেখার রিসোর্স এবং পরবর্তী ধাপ

### অনুশীলন রিপোজিটরি 🏋️

1. **[Learn Git Branching](https://learngitbranching.js.org/)** - ইন্টারঅ্যাক্টিভ Git টিউটোরিয়াল
2. **[Git Exercises](https://gitexercises.fracz.com/)** - Git পরিস্থিতি অনুশীলন
3. **[Oh My Git!](https://ohmygit.org/)** - Git শেখার গেম

### অন্বেষণ করার জন্য উন্নত বিষয় 🚀

- **Git Submodules** - অন্যান্য রিপোজিটরি অন্তর্ভুক্ত করুন
- **Git Subtrees** - সাবমডিউলের বিকল্প
- **Git Workflows** - GitLab Flow, OneFlow, ইত্যাদি
- **CI/CD Integration** - GitHub Actions, GitLab CI
- **Git Server Setup** - স্ব-হোস্ট করা Git রিপোজিটরি

### উপযোগী টুলস 🛠️

- **GitKraken** - ভিজুয়াল Git ক্লায়েন্ট
- **Sourcetree** - বিনামূল্যে Git GUI
- **VS Code Git** - বিল্ট-ইন Git ইন্টিগ্রেশন
- **GitHub Desktop** - সহজ GitHub ক্লায়েন্ট
- **git-flow** - Git ওয়ার্কফ্লো কমান্ড

---

## 🎉 অভিনন্দন!

আপনি বিস্তৃত Git গাইড সম্পন্ন করেছেন! এখন আপনার কাছে রয়েছে জ্ঞান:

✅ ব্যক্তিগত প্রজেক্টে আত্মবিশ্বাসের সাথে Git ব্যবহার করার  
✅ দলের সাথে কার্যকরভাবে সহযোগিতার  
✅ জটিল Git পরিস্থিতি সামলানোর  
✅ Git ভুল থেকে পুনরুদ্ধারের  
✅ ইন্ডাস্ট্রির সেরা অনুশীলন অনুসরণের

মনে রাখবেন: Git দক্ষতা অনুশীলনের সাথে আসে। সবকিছু মুখস্থ করার বিষয়ে চিন্তা করবেন না - এমনকি অভিজ্ঞ ডেভেলপাররাও নিয়মিত Git কমান্ড খুঁজে দেখেন। গুরুত্বপূর্ণ বিষয় হল ধারণা বোঝা এবং প্রয়োজনের সময় সাহায্য কোথায় পাবেন তা জানা।

Happy কোডিং! 🚀
