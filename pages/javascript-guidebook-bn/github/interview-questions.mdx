import { Callout } from "nextra/components";

# গিট ইন্টারভিউ প্রশ্ন ও উত্তর 🎯

এই প্রশ্নগুলো ব্যবহার করুন ইন্টারভিউ প্রস্তুতির জন্য বা গিট সম্পর্কে আপনার বোঝাপড়া যাচাই করতে। প্রশ্নগুলো বিগিনার থেকে অ্যাডভান্সড এবং বাস্তব পরিস্থিতি ভিত্তিক। নিজে নিজে উত্তর দেয়ার চেষ্টা করুন, তারপর মিলিয়ে দেখুন!

> **💡 প্রো টিপ:** উত্তরগুলো ঢেকে রেখে নিজে নিজে উত্তর দিন। এতে ধারণাগুলো আরও ভালোভাবে মনে থাকবে।

---

## ১.১ – গিটের বেসিক 🧩

<div style={{ background: 'rgba(0, 100, 200, 0.10)', padding: '15px', borderRadius: '5px', marginBottom: '20px', border: '1px solid rgba(0, 100, 200, 0.15)', color: 'inherit' }}>

**প্রশ্ন ১: গিট কী এবং কেন ব্যবহার করা হয়?**  
উত্তর: গিট হলো একটি ডিস্ট্রিবিউটেড ভার্সন কন্ট্রোল সিস্টেম, যা কোডের পরিবর্তন ট্র্যাক করতে, টিমে একসাথে কাজ করতে এবং দরকার হলে আগের ভার্সনে ফিরে যেতে সাহায্য করে। টিমওয়ার্ক ও নিরাপদ কোড ম্যানেজমেন্টের জন্য অপরিহার্য।

**প্রশ্ন ২: কীভাবে নতুন গিট রিপোজিটরি তৈরি করবেন?**  
উত্তর: প্রজেক্ট ফোল্ডারে নিচের কমান্ড দিন:

```bash
git init
```

**প্রশ্ন ৩: গিটে ফাইলের স্ট্যাটাস কীভাবে দেখবেন?**  
উত্তর:
```bash
git status
```
এতে কোন ফাইল স্টেজড, আনস্টেজড বা আনট্র্যাকড তা দেখা যায়।

**প্রশ্ন ৪: কীভাবে ফাইল স্টেজ ও কমিট করবেন?**  
উত্তর: ফাইল স্টেজ করতে:
```bash
git add <filename>
```
কমিট করতে:
```bash
git commit -m "তোমার বার্তা"
```

</div>

---

## ১.২ – ইন্টারমিডিয়েট প্রশ্ন ও পরিস্থিতি 🏆

<div style={{ background: 'rgba(230, 150, 70, 0.10)', padding: '15px', borderRadius: '5px', marginBottom: '20px', border: '1px solid rgba(230, 150, 70, 0.15)', color: 'inherit' }}>

**প্রশ্ন ৫: কীভাবে কমিট হিস্টোরি দেখবেন?**  
উত্তর:
```bash
git log
```
সংক্ষেপে দেখতে:
```bash
git log --oneline
```

**প্রশ্ন ৬: পরিস্থিতি: ভুল করে সেনসিটিভ ডেটা কমিট করেছেন। কীভাবে ইতিহাস থেকে মুছবেন?**  
উত্তর: ফাইল রিমুভ করে ইতিহাস রিরাইট করুন:
```bash
git rm --cached <filename>
git commit --amend
# অথবা গভীরভাবে মুছতে filter-branch বা BFG ব্যবহার করুন
```
<Callout type="warning">সতর্ক থাকুন: ইতিহাস রিরাইট করলে টিমমেটদের সমস্যা হতে পারে!</Callout>

**প্রশ্ন ৭: কীভাবে নতুন ব্রাঞ্চ তৈরি ও সুইচ করবেন?**  
উত্তর: তৈরি করতে:
```bash
git branch feature-branch
```
সুইচ করতে:
```bash
git switch feature-branch
```
একসাথে করতে:
```bash
git checkout -b feature-branch
```

**প্রশ্ন ৮: পরিস্থিতি: মূল ব্রাঞ্চে প্রভাব না ফেলে ঝুঁকিপূর্ণ কিছু ট্রাই করতে চান। কী করবেন?**  
উত্তর: নতুন ব্রাঞ্চ তৈরি করুন:
```bash
git checkout -b experiment
```
সেখানে কাজ করুন, সফল হলে মার্জ করুন।

**প্রশ্ন ৯: কীভাবে অন্য ব্রাঞ্চ মার্জ করবেন?**  
উত্তর:
```bash
git merge branch-name
```

**প্রশ্ন ১০: মার্জ কনফ্লিক্ট কী এবং কীভাবে সমাধান করবেন?**  
উত্তর: দুই ব্রাঞ্চের পরিবর্তন একসাথে মেলাতে সমস্যা হলে গিট ফাইলে কনফ্লিক্ট মার্ক করে। সমাধান:
১. ফাইল খুলে `<<<<<<<`, `=======`, `>>>>>>>` মার্কার খুঁজুন।
২. কোন অংশ রাখবেন ঠিক করুন।
৩. মার্কার মুছে ফেলুন।
৪. স্টেজ ও কমিট করুন:
```bash
git add <filename>
git commit
```
<Callout type="info">VS Code-এর মতো এডিটর ব্যবহার করলে কনফ্লিক্ট সহজে সমাধান করা যায়।</Callout>

</div>

---

## ১.৩ – অ্যাডভান্সড ও পরিস্থিতি ভিত্তিক প্রশ্ন 🚀

<div style={{ background: 'rgba(20, 120, 220, 0.10)', padding: '15px', borderRadius: '5px', marginBottom: '20px', border: '1px solid rgba(20, 120, 220, 0.15)', color: 'inherit' }}>

**প্রশ্ন ১১: পরিস্থিতি: মার্জের আগে ছোট ছোট কমিট একসাথে করতে চান। কীভাবে করবেন?**  
উত্তর: ইন্টার‍্যাকটিভ রিবেস ও স্কোয়াশ ব্যবহার করুন:
```bash
git rebase -i HEAD~3
```
যে কমিটগুলো একসাথে করতে চান, তাদের "pick" এর জায়গায় "squash" লিখুন, তারপর সেভ ও রিবেস শেষ করুন।

**প্রশ্ন ১২: শেষ কমিট বাতিল করে পরিবর্তন রেখে দিতে চান?**  
উত্তর:
```bash
git reset --soft HEAD~1
```

**প্রশ্ন ১৩: পরিস্থিতি: কাজ অস্থায়ীভাবে রেখে ব্রাঞ্চ বদলাতে চান। কী করবেন?**  
উত্তর: স্ট্যাশ ব্যবহার করুন:
```bash
git stash
```
ব্রাঞ্চ বদলান, পরে কাজ ফেরত আনতে:
```bash
git stash apply
```

**প্রশ্ন ১৪: কীভাবে লোকাল ও রিমোট ব্রাঞ্চ ডিলিট করবেন?**  
উত্তর: লোকাল:
```bash
git branch -d branch-name
```
রিমোট:
```bash
git push origin --delete branch-name
```

**প্রশ্ন ১৫: পরিস্থিতি: এক কম্পিউটারে দুইটি গিটহাব অ্যাকাউন্ট ব্যবহার করতে চান। কীভাবে করবেন?**  
উত্তর: আলাদা SSH কী তৈরি করে `~/.ssh/config` ফাইলে কনফিগার করুন। উদাহরণ:
```bash
# Personal
Host github-personal
  HostName github.com
  User git
  IdentityFile ~/.ssh/id_ed25519_personal

# Work
Host github-work
  HostName github.com
  User git
  IdentityFile ~/.ssh/id_ed25519_work
```
তারপর ক্লোন/পুশ করার সময় সঠিক রিমোট URL ব্যবহার করুন।

</div>

---

## ১.৪ – আরও গিট ইন্টারভিউ প্রশ্ন ও পরিস্থিতি 🌟

<div style={{ background: 'rgba(100, 200, 100, 0.10)', padding: '15px', borderRadius: '5px', marginBottom: '20px', border: '1px solid rgba(100, 200, 100, 0.15)', color: 'inherit' }}>

**প্রশ্ন ১৬: নির্দিষ্ট কমিটে কী পরিবর্তন হয়েছে দেখবেন কীভাবে?**  
উত্তর:
```bash
git show <commit-hash>
```
এতে ঐ কমিটে কী পরিবর্তন হয়েছে দেখা যায়।

**প্রশ্ন ১৭: পরিস্থিতি: কোনো ফাইলে পরিবর্তন বাতিল করে শেষ কমিটের অবস্থায় ফেরত আনতে চান। কী করবেন?**  
উত্তর:
```bash
git checkout -- <filename>
```
এতে ফাইলের লোকাল পরিবর্তন বাতিল হবে।

**প্রশ্ন ১৮: বর্তমান ও শেষ কমিটের মধ্যে পার্থক্য কীভাবে দেখবেন?**  
উত্তর:
```bash
git diff
```
এতে আনস্টেজড পরিবর্তন দেখা যায়। স্টেজড পরিবর্তন দেখতে:
```bash
git diff --cached
```

**প্রশ্ন ১৯: পরিস্থিতি: ভুল করে ফাইল ডিলিট করেছেন, শেষ কমিট থেকে ফেরত আনতে চান। কী করবেন?**  
উত্তর:
```bash
git checkout HEAD -- <filename>
```
এতে ফাইলটি শেষ কমিট থেকে ফেরত আসবে।

**প্রশ্ন ২০: গিটে ফাইলের নাম পরিবর্তন ও রেকর্ড করবেন কীভাবে?**  
উত্তর:
```bash
git mv oldname.txt newname.txt
git commit -m "Rename file"
```

**প্রশ্ন ২১: পরিস্থিতি: কোন ফাইলের কোন লাইন কে কখন পরিবর্তন করেছে দেখতে চান। কী করবেন?**  
উত্তর:
```bash
git blame <filename>
```
এতে লাইন-বাই-লাইন লেখক ও কমিট তথ্য দেখা যায়।

**প্রশ্ন ২২: কীভাবে রিমোট রিপোজিটরি ক্লোন করবেন?**  
উত্তর:
```bash
git clone <repository-url>
```

**প্রশ্ন ২৩: পরিস্থিতি: কোনো ফাইল রিপো থেকে মুছতে চান কিন্তু লোকাল রাখতে চান। কী করবেন?**  
উত্তর:
```bash
git rm --cached <filename>
git commit -m "Remove file from repo but keep locally"
```

**প্রশ্ন ২৪: প্রজেক্টে কনফিগার করা সব রিমোট রিপোজিটরি কীভাবে দেখবেন?**  
উত্তর:
```bash
git remote -v
```

**প্রশ্ন ২৫: পরিস্থিতি: রিমোট থেকে পরিবর্তন আনতে চান, কিন্তু অটো-মার্জ নয়। কী করবেন?**  
উত্তর:
```bash
git fetch
```
এতে শুধু পরিবর্তন ডাউনলোড হবে, মার্জ হবে না।

**প্রশ্ন ২৬: ব্রাঞ্চ হিস্টোরির গ্রাফিকাল রিপ্রেজেন্টেশন কীভাবে দেখবেন?**  
উত্তর:
```bash
git log --oneline --graph --all
```

**প্রশ্ন ২৭: পরিস্থিতি: শেয়ার্ড রিপোতে পুশ করা কমিট রিভার্ট করতে চান। কী করবেন?**  
উত্তর:
```bash
git revert <commit-hash>
```
এতে নতুন কমিট তৈরি হবে যা আগের পরিবর্তন বাতিল করবে, ইতিহাস ঠিক থাকবে।

**প্রশ্ন ২৮: নির্দিষ্ট কমিটে ট্যাগ (যেমন রিলিজ) কীভাবে করবেন?**  
উত্তর:
```bash
git tag v1.0.0 <commit-hash>
git push origin v1.0.0
```

**প্রশ্ন ২৯: পরিস্থিতি: সব স্ট্যাশ লিস্ট দেখতে চান। কী করবেন?**  
উত্তর:
```bash
git stash list
```

**প্রশ্ন ৩০: নির্দিষ্ট স্ট্যাশ অ্যাপ্লাই করে লিস্ট থেকে মুছতে চান। কী করবেন?**  
উত্তর:
```bash
git stash pop stash@{n}
```
এখানে `n` হলো স্ট্যাশ নম্বর।

</div>

---

<Callout type="info">
  এই প্রশ্নগুলো প্র্যাকটিস করুন এবং বাস্তবে গিট রিপোজিটরিতে কমান্ডগুলো ব্যবহার করুন—তাহলে আরও ভালোভাবে শিখতে পারবেন!
</Callout>
